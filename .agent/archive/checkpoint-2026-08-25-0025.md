# Checkpoint

## Project

benchmark

## Created

2026-08-25 00:25

## Git

Repository detected: False

Git repository not detected. Checkpoint will continue without Git information.

### Modified Files

```text
No modified files detected.
```

### Recent Commits

```text
No recent commits available.
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark（D:\claudework\benchmark）

## Current Phase

Phase 15 完成（Agent-Assisted Visual Diagnosis）。**这是诊断工具阶段：不改算法/模型/GT/指标/runtime，Agent 多模态只用于解释"为何失败"。**建了 `src/diagnostics/`（frame_sampler/contact_sheet/case_builder/report_generator）+ 每 GT 案例包（contact sheet + case.json）+ 本会话视觉审查写 diagnosis.json + `benchmark_report_phase15.md`。**诊断结论**：闭环内唯一真正失败是 **s3 = GT 边界/不确定(E)**；s4/s2 = **特征判别失败(C)**（整帧 CLS 把暗色军事/人物场景混同）；s0/s1 = 相似场景混淆(B)（正确区深 rank5/6）；s6 = 证据不足(F)但 **14C 已解决**；s5 = 干净命中。**"相似外观掩盖语义身份"是贯穿性主因**。**完成后停止，未进 Phase 16。**

## Current Task

- ✅ Phase 15：搭建 `src/diagnostics/` 视觉诊断管线，复用 14A/14A.1/14C 结果 + 2fps 缓存 + 2.mkv 极小窗口抽帧，自动生成 7 个案例包（contact sheet + case JSON + AGENT_REVIEW_PROMPT.md），Agent 逐段看图并写 `diagnosis.json`，装配 `benchmark_report_phase15.md`。**结论：s3 是 GT 边界问题（非特征），s2/s4 是特征判别失败，s0/s1 相似场景混淆，s6 已解决，s5 干净。** 产物 `diagnostics/{s0..s6}/`、`diagnostics/AGENT_REVIEW_PROMPT.md`、`benchmark_report_phase15.md`。**完成后停止，未进 Phase 16。**
- 关键：接触 sheet 只显示 top3 候选，正确区在深位时（s0 rank6/s1 rank5）该 contact sheet 看不到正确候选，需以 case.json（cover 字段）为准；诊断主要依据 case.json 权威数值 + 视觉语义。

## Completed

- Phase 1~11 全部完成（见 PROJECT_HANDOFF.md）
- Phase 12：双基线 real 首跑 recall=0；三重证据确认原 GT 6/7 段错位；重建 corrected Segment GT；B recall 0.143 / A recall 0。
- Phase 13A/B/C：Fine Segment Localization。13A 连续轨迹达成（s6 0.75/s1 0.632）；13B 证原片 1fps 非瓶颈；13C 证 gap tolerance 近无效、expanded query 有害。
- Phase 14A：全局 Candidate Retrieval。s0 被召回（补回 TransVCL 漏掉）、868s 巨窗消除、7/7 正确区浮现；strict cover 3/7。
- **Phase 14A.1**：Candidate Clustering 连续性修正。gap10/15/20 → recall_B 恒 3/7、无巨窗。s2 正确区已融合[1300,1344](44s, cov0.462)；s4 未被桥接（两个碎片，连续性拒 12s 桥）；s6 仅[1580,1584](4s)。gap20 引入 s4 假桥[6614,6644]。产物 `work/phase14a1_clustering/phase14a1_results.json`。
- **Phase 14C**：Edited Query Density。pad=±4s 固定、只查 {2,4,8}fps。**n_recall_B 恒 6/7（s0/s1/s2/s4/s5/s6=Y, s3=N）在每档 fps 完全相同**；s4 0.818/s6 1.0/s2 0.538（对比 14A.1 pad0 的 0.273/0.167/0.462，提升全来自 pad）。s4 由两碎片融合为 [1374,1404]（bridged=[]，基础时间聚类即可）；s6 单选 [1550,1610]；s2 rank1=[1212,1234] 错误场景压过正确区 rank2=[1296,1346]。**更高 fps 反略伤 fine localization**（s2 iou .368→.226、s6 .75→.5、s5 .455→.353）。s3 恒 0.375（唯一 miss，特征难例）。产物 `work/phase14c_density/phase14c_results.json`；脚本 `src/experiments/query_density_14c.py`。
- **Phase 15（本次）**：Agent-Assisted Visual Diagnosis。新建 `src/diagnostics/{frame_sampler,contact_sheet,case_builder,visual_diagnostics,report_generator}.py`；每一 GT 生成 contact sheet(sX_comparison.jpg 5行×3列 + sX_edited.jpg) + case.json + `AGENT_REVIEW_PROMPT.md`；Agent 逐段看图写 `diagnosis.json`；装配 `benchmark_report_phase15.md`。**诊断汇总（7 段）**：s2=C 特征判别失败(sim0.726 暗室内男子反超正确区0.558)、s4=C(sim0.649 但 fine loc 仅 iou0.273, 暗色军事画面不可分)、s3=E GT 边界/不确定(蒙太奇拉向 s2/s4 两区, GT 尾段混入异构镜头)、s0/s1=B 相似场景混淆(正确区 rank6/5)、s6=F 证据不足(已解决)、s5=B(干净命中, cover1.0)。**贯穿主因：整帧 CLS 把"暗色+军人/人物+纹理"的高外观相似场景混同，掩盖语义身份。**

## Current Problem

**已解决（Phase 14C）**：fps 非杠杆；真正的杠杆是编辑查询窗口 span（pad±4s 把 recall 3/7→6/7）。
**已定性（Phase 15 视觉，替换 14C 对 s3 的不精确归因）**：闭环内唯一真正的 miss **s3** 不是"特征无法判别"，而是 **GT 边界/不确定（E）**——GT[1340,1356] 是区间估计、跨多个异构镜头且与 s2 重叠/尾段混入非匹配画面；edited 是蒙太奇（直升机→s2 共享区、士兵走开→s4 区），故无单一候选能 cover≥0.5。**真正需要换特征的是 s2/s4（C）**：整帧 CLS 把"暗色+军人/人物+纹理"的相似外观混同（s2 暗室内男子 sim0.726 反超正确区 0.558；s4 暗色军事画面不可分，fine loc 仅 iou0.273）。s0/s1 为相似场景混淆（B，正确区深 rank6/5），s6 证据不足（F，已解决），s5 干净命中。

## Current Implementation

- 特征链路：DINOv2 384-d（L2，全局）+ 全库 Top-K 检索 + 连续性感知候选窗聚类 + temporal consistency 打分。
- 方法（14C）：edit 查询窗口 = GT±4s（pad 固定），fps∈{2,4,8}（唯一变量）；聚类固定 base gap10 + bridge15 + max window60；每段先候选召回（recall_A/B/rank/coverage），再在 best-cover 候选窗上跑 13A 式 fine localization（thresh 0.4，±1 帧平滑，longest run → IoU/start/end err）。检索/特征/GT 与 14A/14A.1 逐值一致。
- 输出：`phase14c_results.json`（per_fps{per_gt + summary} + comparison{global + per_gt cross-fps} + 候选机制 candidates(含 bridged_gaps)）。

## Current Decision

- **GT**：Segment 结构 corrected，原 GT 不动。本阶段未改 GT/特征/检索/聚类参数。
- **查询采样率(fps)不是杠杆**（已证）。**编辑查询窗口 span（pad）是杠杆**（14C 证）。
- **诊断是开发期工具，不进 runtime**：Agent 多模态只用于解释"为何失败"，DINOv2/Localization 才是最终引擎；两者不混淆。Agent 不自动改算法，只输出 diagnosis.json 供人工决定。
- **闭环内唯一 miss s3 = GT 边界/不确定（E）为主**：需复核/收紧 corrected GT（非换特征）。**真正换特征的方向是 s2/s4（C）**：整帧 CLS 对暗色军人/人物场景可判别性差，方向=局部 patch/区域特征或场景一致性排序（惩罚 s2 的暗室内男子 0.726 这类高分异场景）。
- **Phase 15 结束即停**，不进 Phase 16。后续（复核 s3 GT、换判别特征、s2/s0/s1 排序、重建 benchmark_results.json、收尾）由用户决定。

## Next Actions

1. **（需用户拍板，建议优先）复核/收紧 s3 的 corrected GT**：视觉诊断显示 GT[1340,1356] 是区间估计、与 s2 重叠、尾段混入异构镜头（老年女性肖像）——需人工复核 s3 的真实对应区间；若 GT 收缩到[约1340,1346]则 s3 可 cover≥0.5（recall 6/7→7/7）。这是闭环内唯一 miss 的最省力解法，无需换特征。
2. **（需用户拍板，换特征方向）s2/s4 判别失败**：整帧 CLS 对"I'm 暗色军人/人物"可判别性差。方向=局部 patch/区域特征、或候选阶段场景一致性排序（惩罚 s2 暗室内男子 sim0.726 这类高分异场景）。
3. **（需用户拍板）s0/s1 相似场景混淆**：正确区深 rank6/5。可加时序/场景一致性排序或更高层场景模型。
4. （可选，需授权）校正 Segment GT 下 `--engine all` 重建 `benchmark_results.json`（当前空/单引擎态；会覆盖历史）。
5. 若无后续：Phase 12/13/14A/14A.1/14C/15 以 report §6 + corrected Segment GT + 各阶段结果 + Phase15 诊断报告收尾。

## Last Updated

2026-08-25 00:25
## Important Constraints

- **`datasets/real/ground_truth.json` 不可修改**（已遵守）。
- 不为好看伪造/修改结果；失败如实标注。
- **正式 GT 以 Segment 为单位**；不把 original_end 固定为 start+1；boundary_uncertain 时如实标记。
- 不做 GUI、不做最终产品、不研究音频指纹、不淘汰 TransVCL。
- Python 绝对路径 `D:\claudework\video-dedup-tool\.venv\Scripts\python.exe`。
- `benchmark.py` 每次运行覆盖 `benchmark_results.json`（本阶段未跑）。

## Known Issues

- **s3 特征难例（闭环内唯一 miss）**：2s 剪辑与 s2 同内容，映射共享区 [1296,1346]，任何 gap/fps/pad 都 cov≤0.375；需判别特征或边界建模。
- **s2 排名/判别**：错误场景 [1212,1234]（mean 0.726）压过正确区 rank2=[1296,1346]；fine localization iou 仅 0.226~0.368（traj 常外扩到 1296）。
- **更高 fps 略伤 fine localization**：8fps 下 s6 iou .75→.5、s5 .455→.353、s2 .368→.226（更多帧拉长但拉偏 trajectory）。
- **fine localization 上限受候选窗宽+特征区分度约束**：s2/s4 finloc iou 0.226/0.273，即使候选窗已覆盖正确区。
- `benchmark_results.json` 空/单引擎态（每次运行覆盖）。`--engine all` 重建暂缓。
- `src/experiments/*.py` 视运行方式需 `PYTHONPATH=src`。
- DINOv2 整帧 CLS 对相似军旅场景区分度有限；DINOv2+TA 成段仍 total_pred=0。
- s0 校正值 weak；s0–s5 为 boundary_uncertain。

## Last Updated

2026-08-25 00:25

## TODO Snapshot

# TODO

## P0 — Current（Phase 12 收尾）

- [x] 三重证据确认原 real ground_truth.json 6/7 段错位（真实对应 1261–1742s）
- [x] 用户拍板：正式 GT 以 **Segment** 为单位（保留 edited/original 起止，不把 original_end 固定为 start+1；只能确认场景→标 boundary_uncertain；锚点仅辅助）
- [x] 逐段连续对应终核（`vision_qa.cli case` 生成编辑片段帧 vs 候选原片区间帧 contact sheet），确认 s6=1578–1602 连续、s0 最弱
- [x] 重建 `datasets/real/ground_truth_corrected.json`（Segment + boundary_uncertain；原 GT md5 不变）
- [x] runner 加 `--ground-truth`/`--gt` 开关（benchmark.py + report.py），正控通过
- [x] 校正 Segment GT 下重测：**B recall=0.143（匹配 s5）/ A recall=0**；更新报告 §5/§6、STATE、终核产物、记忆

## P1 — Next（Phase 13/14 + 可选，需用户授权/决策）

- [x] **Phase 14A：Candidate Retrieval 全局粗定位**（2026-08-24）。per-GT edited 2fps 查询 × 全片 3830 帧 Top-K=20，按原片时间近邻聚成候选窗（强制 ≤60s）。**成果：s0 被召回 [1130,1140]（曾被 TransVCL 窗口 [1261,2130] 完全漏掉）；869s 巨窗消除（max 44s/avg 4.6s）；7/7 GT 正确区域都出现在候选窗。** strict cover≥0.5 召回 3/7（s0/s1/s5）。敏感性 gap 10/15/20/30：s4=聚类伪影（gap≥15 翻 0.27→0.82），s2 恒 0.46、s6 恒 0.17、s3 0.12→0.38。见 `work/phase14a_candidate_retrieval/phase14a_results.json`。**完成后停止，未进下一阶段。**
  - [x] **Phase 14A.1：Candidate Clustering 连续性修正**（2026-08-24）。仅改聚类/排序：gap 10/15/20 + >10s 桥接需过 单调+mean_sim+局部速度 判据 + 60s 硬上限。**recall_A=7/7、recall_B 恒 3/7、无巨窗（≤44s）**。关键：s4 正确区在 10/15/20 都未桥接（两碎片，连续性拒 12s 桥，因 4 帧查询→20s 区 edit↔orig 非单调）；Phase 14A pure-time gap15 能让 s4 翻 0.82 但无时序验证。**结论：聚类参数非杠杆，剩 miss 是查询密度(s2尾/s4/s6)+特征难例(s3)；gap→20 引入 s4 假桥[6614,6644]。** 修复 `_hits` sims reshape bug。见 `work/phase14a1_clustering/phase14a1_results.json`。
  - [x] **Phase 14C：Edited Query Density**（2026-08-24）。pad=±4s 固定，只改查询采样率 {2,4,8}fps，其余全不动。**n_recall_B 恒 6/7 所有 fps 相同——fps 非杠杆；提升来自窗口 span（pad 把 recall 3/7→6/7）**：s4 0.273→0.818（两碎片融合[1374,1404] bridged=[]）、s6 0.167→1.0、s2 0.462→0.538（正确区 rank2 被错误场景 rank1[1212,1234] sim0.726 压过）。更高 fps 反略伤 finloc。产物 `work/phase14c_density/phase14c_results.json`；脚本 `src/experiments/query_density_14c.py`。（Phase 15 视觉修正：s3 "特征难例"实为 **GT 边界/不确定**，见下。）
  - [x] **Phase 15：Agent-Assisted Visual Diagnosis**（2026-08-25）。新建 `src/diagnostics/` 管线；每 GT 生成 contact sheet(sX_comparison.jpg + sX_edited.jpg) + case.json + `diagnostics/AGENT_REVIEW_PROMPT.md`；Agent 逐段视觉审查写 `diagnosis.json`；装配 `benchmark_report_phase15.md`。**诊断**：s2=C 判别失败(暗室内男子0.726反超正确区0.558)、s4=C(暗色军事画面不可分, finloc iou0.273)、s3=E GT 边界/不确定(蒙太奇拉向 s2/s4 两区+GT 尾段异构)、s0/s1=B 相似场景混淆(正确区 rank6/5)、s6=F 证据不足(已解决)、s5=B 干净命中。**贯穿主因：整帧 CLS 混同'暗色+军人/人物+纹理'外观。**
- [ ] **Phase 14B（需用户决定）**：把全局检索得到的候选窗交给 Fine Localization（14C 已内嵌 finloc；s4 现已有单候选 [1374,1404]、s6 单选 [1550,1610]、s2 正确区 [1296,1346]。注意 finloc 上限受窗宽+特征区分度约束：s2/s4 iou 仅 0.226/0.273）。
- [ ] （需用户决定）**复核/收紧 s3 的 corrected GT**：诊断示 GT[1340,1356] 为区间估计、与 s2 重叠、尾段混入异构镜头；若收缩到[约1340,1346]可让 s3 cover≥0.5（recall→7/7），最省力、无需换特征。
- [ ] （需用户决定）**s2/s4 换判别特征**：整帧 CLS 对暗色军人/人物可判别性差；方向=局部 patch/区域特征或候选阶段场景一致性排序。
- [ ] （需用户决定）**s0/s1 相似场景混淆**：正确区深 rank6/5；可加时序/场景一致性排序。
- [ ] （可选，需授权）校正 Segment GT 下 `--engine all` 重建 `benchmark_results.json`（当前空/单引擎态；会重跑 vdf/vcsl/transvcl 并覆盖历史）。
- [ ] （可选）DINOv2+TA 调优：放宽 DP 单调对齐阈值以成段（当前 total_pred=0）。

## P2 — Later

- [ ] 若 Phase 13 跑通：用校正 Segment GT 更新 ARCHITECTURE_ANALYSIS.md + README 选型结论
- [ ] 评估 s0 弱置信度项（可选：更精细视觉定位）

## Blocked

- **`--engine all` 重建汇总**：被"不覆盖 results/vdf|vcsl|transvcl 历史结果"约束阻断，需用户授权允许覆盖。
- **DINOv2+TA 成段**：DP 单调对齐阈值过严，需调优特征或阈值（在正确 GT 下仍 total_pred=0）。
- **boundary_uncertain 的 6/7 段**：s0–s5 仅确认场景/连续区域，完整边界待 Phase 13。
