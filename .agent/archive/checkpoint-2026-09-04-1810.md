# Checkpoint

## Project

benchmark

## Created

2026-09-04 18:10

## Git

Repository detected: True

Git information collected.

### Modified Files

```text
M .agent/CHANGELOG.md
 M .agent/DECISIONS.md
 M .agent/INDEX.md
 M .agent/STATE.md
 M .agent/TODO.md
 M .gitignore
 M AGENTS.md
 M PROJECT_HANDOFF.md
 M mvp/docs/MVP_ARCHITECTURE.md
 M mvp/docs/MVP_ROADMAP.md
 M mvp/docs/THIRD_PARTY_NOTICES.md
 M mvp/src/app/locator_service.py
 M mvp/src/device/__init__.py
 M mvp/src/device/dinov2_model.py
 M mvp/src/domain/__init__.py
 M mvp/src/domain/models.py
 M mvp/src/engine/clustering/clustering.py
 M mvp/src/engine/confidence/confidence.py
 M mvp/src/engine/feature_store/feature_store.py
 M mvp/src/engine/feature_store/index_bundle.py
 M mvp/src/engine/localization/__init__.py
 M mvp/src/engine/localization/finloc.py
 M mvp/src/engine/localization/pipeline.py
 M mvp/src/engine/segment/__init__.py
 M mvp/src/engine/segment/segment.py
 M mvp/src/infrastructure/__init__.py
 M mvp/src/infrastructure/config.py
 M mvp/src/infrastructure/logging.py
 M mvp/tests/test_domain_infrastructure.py
 M mvp/tests/test_localization_confidence.py
 M mvp/tests/test_locator_service.py
 M mvp/tests/test_segment.py
?? .agent/archive/checkpoint-2026-08-26-0146.md
?? .agent/archive/checkpoint-2026-08-26-0150.md
?? .agent/archive/checkpoint-2026-08-26-0159.md
?? .agent/archive/checkpoint-2026-08-26-1945.md
?? .agent/archive/checkpoint-2026-08-26-2054.md
?? .agent/archive/checkpoint-2026-08-26-2227.md
?? .agent/archive/checkpoint-2026-08-27-0127.md
?? .agent/archive/checkpoint-2026-08-27-0336.md
?? .agent/archive/checkpoint-2026-08-27-2042.md
?? .agent/archive/checkpoint-2026-08-28-0028.md
?? .agent/archive/checkpoint-2026-08-28-2042.md
?? .agent/archive/checkpoint-2026-08-30-0045.md
?? .agent/archive/checkpoint-2026-08-31-1829.md
?? .agent/archive/checkpoint-2026-08-31-2356.md
?? .agent/archive/checkpoint-2026-09-01-GT-v4-handoff.md
?? .agent/archive/checkpoint-2026-09-02-0317.md
?? .agent/archive/checkpoint-2026-09-02-GT-test1-3-handoff.md
?? .agent/archive/checkpoint-2026-09-04-0033.md
?? .agent/archive/checkpoint-2026-09-04-0207.md
?? .claude/
?? bundle_optimization_report.md
?? bundle_report.md
?? cur
?? docs/
?? mvp/api/
?? mvp/benchmark/
?? mvp/docs/LOCAL_FEATURE_SETUP.md
?? mvp/logs/
?? mvp/poc/amdgpu_onnx/bench_throughput_only.py
?? mvp/scripts/.gitignore
?? mvp/scripts/backend.spec
?? mvp/scripts/backfill_scene_tables.py
?? mvp/scripts/build_backend.py
?? mvp/scripts/build_fineseg_review_sheets.py
?? mvp/scripts/build_gt_A_candidates.py
?? mvp/scripts/build_gt_rebuild_candidates.py
?? mvp/scripts/build_gt_review.py
?? mvp/scripts/build_gt_v3_candidates.py
?? mvp/scripts/build_p36_review_sheet.py
?? mvp/scripts/compare_8fps_vs_doc.py
?? mvp/scripts/compare_fps_vs_doc.py
?? mvp/scripts/diag_recall2_contacts.py
?? mvp/scripts/diag_user_contacts.py
?? mvp/scripts/diag_user_mismatch.py
?? mvp/scripts/diag_user_signals.py
?? mvp/scripts/diff_results.py
?? mvp/scripts/eval_cases.py
?? mvp/scripts/eval_timeline_prior.py
?? mvp/scripts/flash_guard.py
?? mvp/scripts/gen_case_sheets.py
?? mvp/scripts/harden_gt_v3.py
?? mvp/scripts/measure_baseline.py
?? mvp/scripts/measure_lenient_v4.py
?? mvp/scripts/measure_recall.py
?? mvp/scripts/measure_shot_recall.py
?? mvp/scripts/pr/
?? mvp/scripts/rerun_8fps.py
?? mvp/scripts/rerun_fps.py
?? mvp/scripts/rerun_timeline_prior.py
?? mvp/scripts/rerun_twopass.py
?? mvp/scripts/rerun_twopass_flash.py
?? mvp/scripts/research_feature_upgrade.py
?? mvp/scripts/research_feature_upgrade2.py
?? mvp/scripts/research_fineseg_8fps_scan_test.py
?? mvp/scripts/research_fineseg_scan_test.py
?? mvp/scripts/research_flash_detect.py
?? mvp/scripts/research_gt_validate.py
?? mvp/scripts/research_local_feature.py
?? mvp/scripts/research_montage_localize.py
?? mvp/scripts/research_p36_8fps_fineseg.py
?? mvp/scripts/research_p36_fineseg_experiment.py
?? mvp/scripts/research_p36_fps_compare.py
?? mvp/scripts/research_patch_recall.py
?? mvp/scripts/research_patch_recall_gt.py
?? mvp/scripts/research_phase21.py
?? mvp/scripts/research_phase24_1.py
?? mvp/scripts/research_phase24_1_data.py
?? mvp/scripts/research_phase24_1_multimodal.py
?? mvp/scripts/research_phase24_1_vlm.py
?? mvp/scripts/research_provenance_neighbor.py
?? mvp/scripts/research_provenance_neighbor_v4.py
?? mvp/scripts/research_query_axis.py
?? mvp/scripts/research_recall_sweep.py
?? mvp/scripts/research_run_module.py
?? mvp/scripts/research_scene_retrieval.py
?? mvp/scripts/research_second_signal.py
?? mvp/scripts/research_semantic_signal_M1_multimodal.py
?? mvp/scripts/research_semantic_signal_M2_subtitle_recall.py
?? mvp/scripts/research_semantic_signal_M3_clip_recall.py
?? mvp/scripts/research_semantic_signal_M4_density.py
?? mvp/scripts/research_semantic_signal_M4_density_v4.py
?? mvp/scripts/research_semantic_signal_P1.py
?? mvp/scripts/research_semantic_signal_P2.py
?? mvp/scripts/research_semantic_signal_P3_seq_context.py
?? mvp/scripts/research_timeline_v4_quant.py
?? mvp/scripts/research_timeline_v4_rollback.py
?? mvp/scripts/research_twopass_prototype.py
?? mvp/scripts/research_twopass_prototype_v2.py
?? mvp/scripts/research_verify_montage.py
?? mvp/scripts/review_fineseg_vlm.py
?? mvp/scripts/review_gt_A_vlm.py
?? mvp/scripts/review_gt_partial_vlm.py
?? mvp/scripts/review_gt_rebuild_vlm.py
?? mvp/scripts/review_gt_remaining_vlm.py
?? mvp/scripts/review_gt_vlm.py
?? mvp/scripts/review_p36_vlm.py
?? mvp/scripts/run_backend.py
?? mvp/scripts/verify_evidence_recall.py
?? mvp/scripts/verify_export_frametruth.py
?? mvp/scripts/verify_frame_precision.py
?? mvp/scripts/verify_greedy_top1.py
?? mvp/scripts/verify_moment_recall.py
?? mvp/scripts/verify_pr_timeline.py
?? mvp/scripts/verify_recall_sample.py
?? mvp/src/app/exporters.py
?? mvp/src/engine/localization/conflict_rerank.py
?? mvp/src/engine/localization/evidence_localize.py
?? mvp/src/engine/localization/montage_localize.py
?? mvp/src/engine/localization/patch_rerank.py
?? mvp/src/engine/localization/seq_align.py
?? mvp/src/engine/localization/sequence_rerank.py
?? mvp/src/engine/localization/temporal_repair.py
?? mvp/src/engine/localization/text_anchor.py
?? mvp/src/engine/segment/card_guard.py
?? mvp/src/infrastructure/settings_repo.py
?? mvp/tests/test_card_guard.py
?? mvp/tests/test_conflict_rerank.py
?? mvp/tests/test_exporters.py
?? mvp/tests/test_logging.py
?? mvp/tests/test_scene_recall.py
?? mvp/tests/test_seq_align.py
?? mvp/tests/test_sequence_rerank.py
?? mvp/tests/test_temporal_ambiguity.py
?? mvp/tests/test_text_anchor.py
?? mvp/tests/test_timeline_prior.py
?? mvp/ui/
```

### Recent Commits

```text
3b7a031 H3: finalize H3_MPS_GO; revert CI to workflow_dispatch-only
a9d7ce4 H3: lower MPS batch (4) to sidestep 5.37GiB buffer limit; publish results to ci-results
d2e528e H3 POC: capture any MPS runtime/op error into results.json instead of crashing
09be13b H3: add push trigger on master so a git push auto-runs the POC
00fa0e7 Fix .gitignore: anchor src/ to repo root (was ignoring mvp/src)
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark → **Source Video Locator MVP**（D:\claudework\benchmark）

> **项目状态：ALGORITHM_UNFROZEN / MVP_ITERATION_ACTIVE（2026-08-28 用户解除全部算法与模型限制，含 CLS forward；原 RESEARCH_FROZEN 表述失效，仅存档）**
> 算法研究已冻结收尾；进入 MVP 产品化（Source Video Locator）。研究结论见 `ARCHITECTURE_DECISION_PHASE20.md`；MVP 设计文档见 `mvp/docs/`。
>
> **硬件/开发状态（2026-08-26 统一）**：H1 Windows CPU = **IMPLEMENTED**；H2 Windows AMD / DirectML = **IMPLEMENTED**；H3 macOS Apple Silicon / MPS = **GO / POC completed**；H4 Windows NVIDIA / CUDA = **PENDING**（H4-0 环境检查已完成 = **`H4_GPU_RUNNER_UNAVAILABLE`**，无可用 Windows GPU runner，POC 无法推进）；UI（第 8 项 PySide6）= **PAUSED**。

> **硬件路线（2026-08-25 用户拍板锁定）**：H1 Windows CPU → H2 **Windows AMD GPU(DirectML)** → H3 macOS Apple Silicon(MPS) → H4 Windows NVIDIA(CUDA)。**不支持 macOS Intel**。`DeviceBackend` 必须保持可扩展——未来加 `CUDABackend` 不改上层 FeatureStore/Retrieval/Ranking/Localization/Confidence/UI。CUDA 不进当前实施阶段。
>
> **AMD GPU POC（`mvp/poc/amdgpu_onnx/`，独立、未接入 MVP）= `AMD_BACKEND_GO`**：RX 6750 GRE + ONNX Runtime DirectML + 冻结 DINOv2 ViT-S/14 CLS-384。15.15 fps，10.39× 加速，Top-1 邻居一致、embedding 数值稳定、500 帧无 NaN/norm 异常。
>
> **H2-Preflight（`h2_preflight.py`，2026-08-25）= `MEMORY_STABLE`**：2.mkv@0.5fps 建索引 3834/3834 帧全跑完，内存 278.5→336.8MB（+58.3MB，全部为首批~250帧一次性 warmup；之后 328–337MB 窄带波动，非线性增长），all_finite / norm=1.0，无 DML allocator 错误，全片 323.9s、11.84 fps。
>
> **H2 = `DirectMLBackend` 已正式接入并验证（2026-08-25）→ `H2_AMD_DIRECTML = IMPLEMENTED`**：
> `mvp/src/device/directml_backend.py`（`DeviceBackend` 实现，复用冻结 `_imagenet_preprocess`，ONNX+DML provider，numpy L2）+ `device/resolve_backend(preferred="auto")` 统一 resolver（能力探测 + 自动 CPU fallback + 明确日志 fallback_reason）+ `device/resolve_dml_model`/`asset_meta`（模型资产 resolver）+ `infrastructure.DeviceConfig`（preferred/onnx_model/dml_device_id/dml_batch_size）+ `mvp/scripts/export_dml_model.py`（从冻结模型重导出产品 ONNX 资产到 `<app_data>/models/dinov2_cls_384/` 含 `asset.json`）。
> 验证：全套 72 项测试过（含新增 `test_directml_backend.py` 10 项：A 类无 GPU→fallback/probe 结构/B 类 AMD 实机推理+CPU 正确性 cos>0.999+FeatureStore 完整兼容 create/load/validate/invalidate）；真实 2.mkv 生产索引构建 `smoke_directml_backend.py`：381.3s / 3834 帧 / 10.06 fps / `[3834,384] float32` / 内存 +53.4MB 稳定 / `IndexMeta.backend="directml" feature_version=...@0.5_l2`（与 CPU 同 schema，不复制 store）/ reload 0.002s。相比 H1 CPU ~3793.6s ≈ **9.95× 墙钟加速**（vs POC 324s 略高 +18%，因生产路径含文件哈希/持久化/探测开销）。

---

## Current Phase

**MVP 产品化 — Stage 1（编码）进行中**：已完成第 1~7 项 + **H1/H2 已接入并验证（H1_CPU / H2_AMD_DIRECTML = IMPLEMENTED）** + **H3（macOS MPS）POC = GO / 已完成（2026-08-26，不重复运行）**。**UI（第 8 项）已转向 = Vue3+TS+Electron 桌面工作台，Stage 1 初始代码已交付（2026-08-26）**（见 Current Task §2）。算法研究 Phase 1~19 已冻结收尾。

> **2026-08-26 — H3（macOS Apple Silicon / MPS）POC 完成 → `H3_MPS_GO`**（真实 Apple Silicon 验证通过，GitHub Actions 云端跑成功）。**repo**：`bsaizne/source-video-locator`（private→public；push 走 SSH——本机网络 HTTPS 443 被阻断、SSH 22/443 可达；DNS=小米路由 192.168.31.1 解析到 GitHub 20.205.243.x 段但 443 握手超时）。已 `git init` + 完整 `.gitignore`（排视频/权重/特征/第三方引擎/工具二进制/研究代码 `src/`/phase 报告/生成物/凭据/IDE）+ 入仓 `mvp/`、`.github/workflows/h3-macos-mps.yml`、`.agent/`、设计/研究结论文档；**含修复：`.gitignore` 无锚 `src/` 曾误忽略整个 `mvp/src`（致 CI checkout 缺 `device/dinov2_model.py`），已改 `/src/`**。**H3 POC = GO**：macOS 15.7.7 / arm64 / torch 2.13.0，`device_mps_actual=mps`（确认非 CPU fallback）、权重下载+sha256 通过、正确性 cos_mean=1.0 / max_abs_diff=6.3e-7 / mps norm deviation 1.19e-7、7.875× 加速（0.87→6.84 fps；batch=4 最佳 6.67；batch≥8 触发 `Invalid buffer size 5.37GiB` + 暴跌 2.83fps）、500 帧 all_finite 无异常无 fallback、峰值内存 ~1.07GB。**关键约束：MPS batch_size ≤4（推荐 4）**——未来正式 `MPSBackend` 必须遵守。CI workflow 已改 **workflow_dispatch-only**（手动；不 push 自动触发，避免每次 push 烧 macOS runner）。`mvp/src/device` 零改动；无 MPSBackend / UI / H4。POC 见 `mvp/poc/macos_mps/`。

> **2026-08-26 — H4-0（Windows NVIDIA / CUDA 环境与 runner 可用性检查）= `H4_GPU_RUNNER_UNAVAILABLE`**：目标环境为 GitHub Actions → Windows GPU larger runner → NVIDIA → CUDA。核实结论：① `bsaizne/source-video-locator` 为**个人（User）账户的公共仓库**（api 确认 `owner.type="User"`、`private:false`）；② 官方 GPU hosted runners **不对开源/公共仓库开放**，且需 **organization/enterprise 级计费配置**；③ 官方 GPU hosted runner **无 Windows 变体**（H3 所用 `macos-15` 为标准 runner，非 GPU）；④ 该 repo Actions 历史仅 4 次 `H3 macOS MPS POC`（macos-15），从未使用 Windows/GPU 较大 runner；⑤ 本机无 GitHub 认证（无 gh CLI / token / credential.helper，`actions/runners` endpoint 返 401）；⑥ 本机 GPU = **AMD Radeon RX 6750 GRE 10GB**（非 NVIDIA），`torch 2.13.0+cpu`、`torch.cuda.is_available()=False`、`device_count=0` → 本机也无法做 CUDA POC。**未创建 `mvp/poc/nvidia_cuda/` / workflow，未改 `mvp/src`，未实现 NVIDIA 支持，未进 UI。** 结论（不伪造）：当前无可用 Windows GPU runner，H4 POC 无法推进；需用户提供 NVIDIA 环境（自托管 Linux runner / 云或本地 NVIDIA 机器）+ 计费权限。

---

## Current Task

> **▶ 2026-09-05 — C 项验证完成：两级分层切分 = 最佳方案（严格 +8 / 场景 +18 / 负例持平）+ 白闪守卫生效 + GPU 优先约定固化**:
> 按 TODO 顶部 C 项执行：① 先验证 8fps 查询侧细切分收益（p36 单段取证 = 假设成立，8fps 整段即命中 GT）→
> ② 全局采样 4/6/8fps 四片全量对照 = 严格全部净负（104→100/99/103）+ t2r02b 假阳性兑现 + 2mkv 负例 +1 →
> ③ **用户提出两级分层切分架构修正（粗采样 5-8 帧找候选切点 + 局部 ±20 帧密帧精修边界 + 最短镜头保护 0.5s +
> fps 换算间隔）→ 实现并四片全量验证 = 严格 112/139 (+8)、场景级 136/139 (+18)、负例 4/9 (持平)**——
> 唯一严格净正收益方案（p36 目标救回，2mkv/test3 场景级满分；全局采样方案全被两级切分取代）;
> ④ **白闪守卫（用户四层方案：特征前置过滤/切点后处理/相似度衰减/业务兜底）实现并验证**：真实白闪定位
> 1.mp4 ed 18.35-18.46（唯一白闪区，恰被粗网格误报切点 18.345）→ 守卫删除该虚假切点（段数 70→69），
> 三指标零回退；判据校准（白闪 mean 219-251 非纯白 → mean≥200 且亮像素占比≥0.5；尖峰判据相邻差≥80 且
> 峰值≥180，20.07 真实切换不误伤）; ⑤ **诚实边界：n03（66.1-67）非白闪**——帧为正常画面，误配 HIGH [116-118]
> 是 CLS 内容相似机制（4 帧特征稳定指向源片 117），白闪守卫救不了它（doc 2fps 1 帧被 min_frames=2 门掉才
> 碰巧正确拒绝），属 t2r02b 同类「内容相似误配」需另立方案，负例 3/4 维持。
> **GPU 优先约定固化（用户拍板）**：算法/推理类任务一律优先 GPU（DirectML，本机 RX 6750 GRE），启动打印
> BACKEND_SELECTED（=DirectMLBackend/amd 生效，实测 3D 引擎 88-92%），DML 不可用才显式 fallback CPU 留痕——
> 已写入 DECISIONS.md 2026-09-05 + AGENTS.md。
> **产物**：research_twopass_prototype(_v2).py + rerun_twopass.py + flash_guard.py + rerun_twopass_flash.py +
> work/rerun_*_twopass(_flash).results.json + FINDINGS_C_ITEM_8FPS.md（六、七章）。
> **待用户拍板**：两级切分 + 白闪守卫是否进 runtime（替换 analyze_edited_video 切分，跑全套单测 + 四片回归；
> 编辑侧特征不落索引，无需 bump feature_version；段数增多影响 UI 结果数/导出需回归验收）。

> **▶ 2026-09-04 — 研究侧收尾归档（M6 v4 重算 + M4/M8 v4 重跑 + ⑩ p08b 复核）+ 三指标基线固化（measure_baseline.py）**:
> 研究侧收尾: **M6 v4 重算(RESCUE 0/39, CLS 池内 39/39, 2026-09-01)** + **M4 v4 重跑(仅 p26, DML 664s)**: v4 真值下 1fps 稀疏索引即
> best_rank=1、margin=+0.2802 → 旧「p26 12→43 恶化」是错误 GT 假象(p26 非特征上限, 与 M6 v4 CLS best=1 双向闭合);
> **M8 v4 重跑(纯 numpy 秒级)**: p26 真值/干扰互换后仍 AMBIGUOUS(uniq 差 0.027)、p08 维持 AMBIGUOUS、t3r12 维持;
> **⑩ p08b 复核 = 作废(污染残留)**: M5「patch 救回 32→2」真值窗(1048-1050)=p38 旧错误 GT 区, v4 正确位置=1108.15-1109.1,
> M6 v4 证 CLS rank=6(池内)/patch 6 零增量 → E21/M5 正面结论作废, patch 召回方向维持关闭(0/39)。
> **FAILURE_TAXONOMY 收尾归档**: 失败族最终 = p08(2.mkv 唯一兄弟机位) + t3r12(test 域); p26 移除(GT 错)、test4 逻辑剔除;
> A 类在 2.mkv 为空(CLS 39/39 进池) → 研究侧全维度闭环, 不再立项新探针。
> 三指标基线固化: **measure_baseline.py**(默认=文档基线批: 2.mkv→user_results.json, test1-3→cases/*_results.json;
> --use-rerun 切换 2026-09-02 重跑批) + **measure_shot_recall.py 重构抽 evaluate()**(CLI 输出不变) →
> 输出 work/baseline_v4.json。验证与 GT_BASELINE 文档完全一致: 2.mkv 32/39 场景 36/39 负例 2/4 支撑 79/137;
> test1 32/43 场景 38/43 负例 0/1 支撑 103/158; test2 9/20 场景 10/20 负例 0/1 支撑 13/40;
> test3 31/37 场景 34/37 负例 2/3 支撑 102/149。(注: 2.mkv 支撑 79/137 vs STATE 旧记录 80/137 差 1 = 历史快照差异;
> test1 rerun 批 97/152 vs 文档批 103/158 = 子 span 枚举差异, 严格/场景/负例两批完全一致。)
> 产物: semantic_signal/FINDINGS_M4_V4.md + FINDINGS_M8_V4.md + FINDINGS_P08B_REVIEW_V4.md + FAILURE_TAXONOMY.md(收尾段);
> mvp/scripts/measure_baseline.py + measure_shot_recall.py(重构) + work/baseline_v4.json / baseline_v4_doc.json。
> **同日追加 — ① p28/p36 召回层深漏验证 + ② 评测口径澄清(宽松口径)**: p28 在 rerun 批已 HIT(旧「深漏 800s」基于旧批, 已修复);
> p36 正确帧在 CLS top-20(rank 2/3)但被「帧级 argmax 分散→单例簇 min_frames=2 门掉 + scene 回退 top-5 未含正确 scene 232」
> 两级淘汰 → 定位选择问题非召回层(2.mkv 无召回层缺口, 39/39 进池已证); 剩余真漏 = p08(兄弟机位)+p36(定位选择)。
> ② 宽松口径对照: 严格 32/39 → 宽松①(±6s/覆盖≥30%) 35/39(+p20/p34/p41 边界偏差/GT前移) → 宽松②(±15s) 37/39(+p05/p35);
> 真实失败仅 p08+p36(与①双向闭合); 汇报口径建议宽松② 37/39(94.9%), 严格 32/39 为回归上限并标注 5 条口径低估;
> 不触碰 ConfidenceConfig(⑦ 冻结维持)。产物 FINDINGS_P28P36_RECALL_VERIFY.md + FINDINGS_LENIENT_V4_METRICS.md +
> mvp/scripts/measure_lenient_v4.py。
> **⑦ 保守化标定 = 冻结跳过**(护栏「Confidence 公式冻结+不标定占位+不用 GT 字段」为保留项, 2026-09-01 拍板权威; 不实施)。
> 本条目为 2026-09-04 同日工作(研究侧收尾+基线固化), 与下方历史条目独立; 全部零 runtime 改动。

> **▶ 2026-09-02 — test1 GT 人工复核闭环（43 正例 + 1 负例）**:
> 用户逐段人工复核 test1 全部段（分:秒标注），已回填 `gt_review/ground_truth_test1_draft.json`：
> 40 条按用户标注拆条（r07/r08/r10/r12/r13/r14/r30 多子镜头拆细），**r14 补缺失段 ed46-49→51:59~52:02**
> （原算法定位 3121-3123 实为正确！）+ r14 拆 5 镜头（a/c/d/e/b，连续覆盖 ed46-62.5）。
> 对照原算法定位: 12 段大位移（🔴 位移>5s），8 段微调，r08 三镜头跨度 460s 用户确认。
> **待办**: test2/test3 人工复核（用户逐个发定位中）→ 回填后统一跑 v4 口径三指标。
> 已确认关键: r14 缺失段=GT 漏标非算法错; 拆条后 test1 正例 43 条。

> **▶ 2026-09-02 — test2 GT 人工复核闭环（20 正例 + 1 负例）**:
> 用户逐段复核 test2（编辑片仅 69.4s / 9 段，段覆盖完整），已回填 `gt_review/ground_truth_test2_draft.json`：
> 按编辑时间轴拆条 20 条（r5/r6/r7 用户给的点跨越结果段边界，已按 ed54/55.2/56.5/57 归位）。
> **关键发现: test2 解说切跳极频繁**——r05a(ed43.8→72:25:24) 与 r05b(ed53.5→26:59:02) 同段内跳跃 -2726s（真实跳切）。
> 用户确认: r00 窗口=50:29:00~50:29:10(帧)、r01c 归 r1、r05a 窗口 72:25±1s。

> **▶ 2026-09-02 — test3 GT 人工复核闭环（37 正例 + 3 负例）**:
> 用户逐段复核 test3（编辑片 155.6s / 34 段），按编辑时间锚点重建，已回填 `gt_review/ground_truth_test3_draft.json`。
> **重大修正: r14/r15 = 加长版内容（原片正常版没有）→ 负例**——r15 之前被标 HIGH 且 multi_case 裁决算对，
> 实际是负例，直接影响 test3 HIGH 精度统计（此前 23/24 需重估）。
> **r10 = 7:22-7:24 与 conflict_rerank 修复一致**（此前记录的"差 1.5s 待修"实际已修好）。
> r13 倒叙（7:46→4:06 时间轴真实回溯）。宽窗口段（r16/r21/r24）用户确认保持。

> **▶ 2026-09-02 — ⑤' 全部闭环 + GT 正式化 + test3 HIGH 精度重估**:
> test1-3 三份 GT 正式化到 `datasets/real/ground_truth_test1/2/3.json`（100 正例 + 5 负例，tier=verified）。
> 三指标: test1 严格 32/43/场景 38/43; test2 严格 9/20/场景 10/20（拆条窄窗低估）;
> test3 严格 31/37/场景 34/37、负例误报 2/3。
> **test3 HIGH 精度重估: 24→23 段（r15 加长版负例剔除），r10 已修(7:22-7:24) → 23/23=100%**
> （原 23/24 中 r15 算对实为负例 + r10 算错实已修，双向修正）。

> **▶ 2026-09-02 — ②③ 单调弱先验进候选生成已编码，真实数据实测零触发（有据收窄）**:
> 实现 `_apply_timeline_prior`（Ambiguous 型段用前序段定位中点做锚点，带外 primary + 带内竞争候选
> 且 cover 落差<=0.10 才切；逃生门=唯一强候选/首段/前段未定位/带内/全带外不触发）+ 配置
> (timeline_prior_enabled/ta_band_s=45/ta_max_cover_drop=0.10) + 单测 8 项；**全套 222 全绿**。
> **重跑验证（新代码含先验，复用索引）**: 2.mkv/test1-3 四片新旧三指标**完全一致**（32/39、
> 32/43、9/20、31/37 零变化）→ **先验零触发零影响**。
> 原因: ①多数 montage 段 primary 带外但带内无 cover 接近的竞争候选；②p08 型兄弟机位 primary
> 1048 恰在带内(离前段 33s)、真值 1108 带外——先验结构性无效（呼应 M8 邻接证伪）；③真实跳切
> 段全带外, 逃生门生效不误伤。
> **结论**: 候选生成级弱先验零增量（时间轴先验价值已由事后 temporal_repair(+1 p16) + 时间轴→
> Ambiguity 兑现）；实现保留为护栏（零回归默认开）；**不建议再调参投入**（零信号）。
> 产物 `semantic_signal/FINDINGS_TIMELINE_PRIOR.md` + `work/rerun_*_timelineprior.results.json`。

> **▶ 2026-09-02 — ⑤' 材料交付(用户逐段人工复核中) + ② 时间轴→Ambiguity 已编码 + ④ 时序重排 v4 量化已完成**:
> ⑤' 数据层材料: 三份 GT 草案 `gt_review/ground_truth_test1/2/3_draft.json`(全部段, tier=pending, 定位占位待毫秒复核)
> + 复核工作表 `GT_REVIEW_WORKSHEET_test1-3.md` + 时间轴清单 `TIMELINE_CROSSCHECK_test1-3.md`;
> 对照图 76 张按最新定位全部重生成(含 test2 补齐 3→9 张)。**用户正在逐段人工复核, 待回填后 tier→verified/loose**。
> ② 时间轴→Ambiguity = `_apply_temporal_ambiguity`(复用 find_temporal_outliers, 修复后仍离群 HIGH→MEDIUM + reason
> temporal_outlier_ambiguous, 不改定位; 配置 temporal_ambiguity_enabled/ta_max_downgrade; 2.mkv 零触发零回归; 全套 214 全绿)。
> ④ v4 量化 = temporal_outlier_repair 净纠正 +1(p16), 产物 `semantic_signal/FINDINGS_TIMELINE_V4_QUANT.md`。
> ③ r10 = current 442-444 vs 真值 444.5(差 1.5s, 留 ⑤' 精修)。

> **▶ 用户拍板推进顺序(2026-09-01 深夜)= 数据层 test1-3 全量毫秒级重标 + 时间轴→Ambiguity + r10 单点**:
> 执行序: ① test1-3 全部 GT 毫秒级人工重标(数据层主线, 吸收 B 段; test4 剔除) → ② 时间轴→Ambiguity → ③ test3 r10 单点 → ④ 时序重排 v4 量化 → 之后单调弱先验→保守化标定→M1-M8 低成本重跑→p08b。
> ⑤'(test1-3 全量毫秒级) 是 ⑨ B 段的超集+升级, ⑨ 并入 ⑤'。完整见 `gt_review/NEXT_STEPS.md` 顶部。

> **▶ GT v4 重建完成(2026-09-01 专会话)= 41 条全定案写入 ground_truth_v4.json, v4 三指标 31/39, p26 实证 GT 错非特征上限**:
> 用户逐条裁决 41 条(NOT_REVIEWED p36-p41 补审 + WRONG 重定位 + PARTIAL 收窄 + WRONG_GT 按线索改)——**删除 p37(p10 ED 重合)/p38(p08 ED 重复), 29 条 relocate + 2 条 delete = 31 条 corrections 留痕**(v3 原文件保留未覆盖)。
> **辅助通道**: 三批 VLM(Volcengine Ark)补审 = ① 14 条(NOT_REVIEWED+WRONG) ② 13 条 PARTIAL 逐帧 ③ 9 条 top-6 候选窗判定; 用户看候选图逐条拍板(容差±几 ms)。
> **关键实证**: **p26 MISS→HIT**(GT 2809-2810→1768.2-1770.05 用户精修 00:29:28.20-00:29:30.05, runtime 1762-1769 HIGH 命中)= GT 标错而非特征上限 → M1-M8 中把 p26 当「不可辨识/特征上限」的结论作废; 补充线索: ed 1:17.4 闪帧子镜头=原片 1783.1(仅一瞬间)。
> **v4 重测**(同 results/脚本): 严格 **31/39**(verified 30/36, loose 1/3) | 场景级(±15s) **36/39** | 负例 **2/4** | 支撑 **80/137**。
> v3 对照: 严格 39/41 / 场景级 39/41 / 负例 2/4 / 支撑 91/137 —— **下降为修正 GT 后的真实口径, 非算法回退**(旧 GT 大量标错, 算法命中错误窗口被误计对)。
> A 段数据层补 GT 后 v4 复测(2026-09-01): p08/p05/p20 精修毫秒 + p28 修正(2810-2812→**1833.15-1836.00** 用户画面确认)→ **严格 32/39**(p28 MISS→HIT, runtime 1828-1840 命中; 再次实证旧 GT 标错致假 MISS)。剩余 v4 MISS: p08(1108.15-1109.1, 兄弟机位特征上限)/p36(2042-2043.4 runtime 未命中); part: p05/p20/p34/p35/p41(场景级命中未盖窄窗)。
> **下一步**: 重审 M1-M8 依赖错误 GT 的结论(p26 已证; p38 兄弟机位=重复已删; 三指标以 v4 为基线)。决策见 DECISIONS.md 2026-09-01。

> **▶ test4 数据错误确认并逻辑剔除(2026-09-01)= ed/om 是两部不同电影, test4 全部 GT/探针结论作废**:
> ffprobe 铁证: test4-ed.mp4(81s 竖屏 576×832) vs test4-om.mkv(71min 横屏 1920×804) 时长差50倍/横竖屏/帧率/音频全不同。
> **影响**: test4/t4r01 相关 GT 与 M2/M3/M4/M5/M7/M8/P1/P2「同质场景」失败族结论全部作废; FAILURE_TAXONOMY test4 案例失效;
> 数据层 B 段改 test2+test3; 三指标 v4 基于 2.mkv 不受影响。**逻辑剔除不物理删文件**(保留证据, 详见 datasets/real/test4-INVALID.md)。

> **▶ Ambiguity Detection 原型实验完成(2026-09-01)= 现有置信信号无法分离「正确 HIGH」与「错配 HIGH」, AMBIGUOUS 检测无内部信号**:
> 重跑 29 段 EvidenceLocalizer 提取内部 multi-evidence 信号: **HIGH 档精度仅 5/13(38%)**, 但正确/错配在
> mode/n_clusters/qcov/dispersion/best_sim/margin 上完全同构(primary best_sim 0.49-0.69 重叠)。
> 机制: 错配 HIGH 多为 clean 单证据簇(secondary=None → margin 饱和 1.0), `low_candidate_margin`/`multiple_similar_candidates`
> 都要求 n_strong_clusters>=2, clean 段结构上不触发降险 flag → 无论对错都 HIGH。
> 结论: AMBIGUOUS 检测在该特征架构下无内部分歧信号可用(与 M1-M8 兄弟机位混叠 + 2026-08-27 置信标定研究双向闭合);
> 校准转向 = 保守化 HIGH 门槛标定(用 v4 数据降「自信错答」precision), 不立项 AMBIGUOUS。产物 `semantic_signal/FINDINGS_AMBIGUITY_PROTOTYPE.md`。

> **▶ M6 v4 重算完成(2026-09-01)= patch 召回 RESCUE 1/41→0/39, v3 的 10 条「CLS 池外」全是 GT 标错假象, 方向彻底关闭**:
> 用户拍板重算 M6(v4 GT): **RESCUE 0/39、CLS 池外 0、CLS 池内 39/39**——v3 里 p05/p10/p20/p23/p24/p26/p32/p41 的
> 「CLS 池外未救回」修正后全部 best_rank 1-8 直命中(池内); **p13「patch 唯一救回」也是 GT 标错假象**(v4 CLS best=1)。
> 结论: patch 召回 runtime 化零增量(0/39), 方向关闭(有据); M6 原「RESCUE 1/41」作废。产物 `semantic_signal/FINDINGS_M6_REVISED.md`。

> **▶ M1-M8 结论重审完成(2026-09-01 GT v4 修正后)= p26/p38 从「不可辨识样本」移除, 失败族收窄**:
> 以 v4 GT 重判全部 M1-M8 存档数据: **p26=GT 标错实证**(旧 2809 错→正确 1766-1770, v4 runtime 直接 HIT 1762-1769 HIGH)——
> M1b「VLM 反向选错」实为判对(1766 SAME 3/3=真值), M2「p26 字幕正面信号」反转(3/3 命中的是错误窗 2808), M4/M5/M6/M7/M8 的 p26 行全部作废或需重算;
> **p38=与 p08 重复已删**且旧真值 1048 本身错(正确=1108-1110=p08), M5 p08b 救回存疑/M6 p38 行作废/M7-M8 p38 半边作废;
> **失败族收窄为**: p08(兄弟机位, 2.mkv 唯一合法案例) + t3r12/t4r01/test4(test 域不受影响);
> **M6 全量「RESCUE 1/41」统计作废需 v4 重算**(方向性收益低预判不变)。产物 `semantic_signal/FINDINGS_REVIEW_M1M8.md`, 三指标基线=v4。

> **▶ GT v3 人工审查发现大量标错(2026-09-01)= 三指标 39/41 基线作废, M1-M8 结论需重审, 进入 GT 重建阶段**
> 用户逐条审核 ground_truth_v3.json 的 original 窗口(35/41 条已审): **仅 6 条 OK**(p06/p17/p22/p25/p29/p30), 29 条有误
> (PARTIAL 13 / WRONG 8 / WRONG_GT 7 / p26 确认错), 6 条未审(p36-p41)。
> **后果**: 三指标 39/41 + M1-M8 依赖 p 系列 GT 的结论全部作废/需重审(p26 已证=GT 错非特征上限)。
> **资产**: `gt_review/GT_REVIEW_RECORD.md` + 41 张对照图 `gt_review/pXX_sheet.jpg`。
> **下一步**: 修正 GT(按用户逐条判定)→ 补审 p36-p41 → 重测三指标 + 重审 M1-M8。决策见 DECISIONS.md 2026-09-01。

> **▶ 校准阶段立项(2026-09-01 用户拍板 A+B)= 目标从「让 Ambiguous 变对」转为「提高可识别样本召回/精度 + 正确识别 Ambiguous」**:
> 研究侧全维度闭环(外观三层/序列P3/邻接M8/语义M1-3/密度M4)后, 失败族定性「不可辨识样本」→ 进入**校准阶段**(非继续找新模型)。
> **四层状态**: ①算法层=能力边界已知(五类信号不足以消歧 Ambiguous); ②产品层=**Confidence Calibration + Ambiguity Detection**(HIGH 自动/MEDIUM 通过或提示/LOW 提示/AMBIGUOUS 明确转人工——用 margin/similar_band/multiple_similar_candidates 等现有信号);
> ③数据层=**补真实 GT(范围 A+B)**: A=2.mkv 失败族周边+Ambiguous 候选段(已补: p08/p05/p20 精修 + p28 待终点 + 其余正确); **B=test2+test3 LOW/MEDIUM 完整 GT**(test4 数据错误已剔除, 见下); GT 逐帧画面确认纪律不变;
> ④研究层=额外来源信号(provenance)=Research/Future 非阻塞。决策见 DECISIONS.md 2026-09-01。三指标 39/41 仍为回归基线。

> **▶ M8 原片邻接唯一性探针已完成(2026-09-01,研究侧)= 来源身份信息方向证伪, 失败族正式定性「不可辨识样本」**:
> 用户正确指出「来源身份信息」是外观之外唯一未被否定维度, 并划界 = 不是重包 P3(编辑上下文), 而是**原片侧 Shot Graph 邻接唯一性**。
> **M8 结果**(纯 numpy, 秒级): 用 scenes 表(2.mkv 699 场景)对 5 个失败案例测「真值 vs 干扰 邻接唯一性」——**全部 AMBIGUOUS**:
> p38/p08 兄弟机位真-干扰邻接余弦 0.613(同一场对话戏邻接同样貌); p26 唯一性差仅 0.027; t3r12 干扰反而更唯一; t4r01 几乎相等。
> **归因**: 兄弟机位邻接本身相似(场景集中在同一时间窗, 呼应 P3 根因); P2 过度归并担忧被证实。
> **结论**: 来源身份信息清单全部落空(前后镜头P3/剪辑点/镜头图邻接M8/字幕对白M1-3/音频不同源/OCR字牌单例) →
> 失败族(p38/p26/t3r12/t4r01)**正式定性为「不可辨识样本」**——不是算法没做好, 是这些样本在可获得证据下无可区分线索。
> 研究侧全维度闭环: 外观(CLS/patch/局部) + 结构(序列P3/事件P2/邻接M8) + 语义(M1-3) + 密度(M4)。零 runtime, 三指标 39/41 未动。FINDINGS `semantic_signal/FINDINGS_M8.md`, 脚本 `mvp/scripts/research_provenance_neighbor.py`, 数据 `work/provenance_neighbor_results.json`。

> **▶ M7 局部特征探针已完成(2026-09-01,研究侧)= ALIKED 局部描述子对核心难例(p38/p26)无解, 外观三层(CLS/patch/局部)全部关闭**:
> 用户拍板「先1再2」: 1=FAILURE_TAXONOMY.md 固化 A/B/C/D 分类; 2=现代局部特征验证。网络受限下用户手动装 kornia 0.8.3 + aliked-n32.pth(本地加载验证 cos 通过)。
> **M7 结果**(CPU ALIKED, 633s): 池=均匀采样∪真值/干扰窗(~260-318帧, 不借 CLS 池, 测独立判别力)——**p38(兄弟机位士兵特写) rank 61 + 干扰反超(margin −24)**;
> **p26(夜读) rank 125 + 干扰反超(margin −19)**——两个 M6 显示 CLS/patch 双失败的案例, ALIKED 局部结构同样失败。
> p08 rank 21(margin +34)/t3r12 rank 16(+48) 弱正向但方向不一致(同对兄弟机位 p08 +34 vs p38 −24, 局部匹配对查询机位敏感);
> p01/t4r01 无退化无增量。**归因**: 兄弟机位拍同一刚性场景, 局部 patch 本身相似(单应成立), 局部证据天然同貌——M7 与 Phase 24-1 几何结论呼应。
> **结论**: 局部特征方向关闭(有据); 失败族(兄弟机位/夜读/同质/重复)在**外观三层全部无解**, 定性为「身份级区分」局限。零 runtime, 三指标 39/41 未动。FINDINGS `semantic_signal/FINDINGS_M7.md`, 脚本 `mvp/scripts/research_local_feature.py`, 数据 `work/local_feature_results.json`。

> **▶ M6 GT 级 patch 召回覆盖探针已完成(2026-09-01,研究侧)= patch 召回 runtime 化不值(数据): 仅 +1/41, 方向关闭**:
> 用户拍板「跑完 41 条 GT 再决定 patch 召回 runtime 化值不值」。M6(DML 双输出 ONNX, 6633s)对全部 41 条 GT 正例算
> CLS 全索引 best_rank + patch 混合池 best_rank——**RESCUE 1/41 = 仅 p13**(峡湾直升机航拍: CLS 188 → patch 1);
> CLS 池外 10 条中 patch 只救回 1 条, 其余 9 条未救回(p05/p10/p20/p23/p24/p26/p32/p38/p41); 31 条 CLS 本就在池内。
> **关键**: M5 的 p08b 32→2 在 M6 p38(同一目标区域 1048-1050, CLS 340→patch 225)不复现——查询帧选取不同致矛盾,
> patch「救回」对查询帧高度敏感, M5 高估了稳定性。p26/p24/p41/p38 双通道均无解, 失败族维持特征上限。
> **结论**: patch 召回 runtime 化 = 仅 +1/41 且信号不稳, 低于「≥2-3 条才值得」门槛 → **方向关闭(有据)**, 与方向 A/C 归档并列;
> p13 留档为唯一真实救回案例。零 runtime, 三指标 39/41 未动。FINDINGS `semantic_signal/FINDINGS_M6.md`, 脚本 `mvp/scripts/research_patch_recall_gt.py`, 数据 `work/patch_recall_gt_results.json`。

> **▶ M5 patch 级召回探针已完成(2026-09-01,研究侧)= patch 对兄弟机位选对实例有效, p26 仍特征上限**:
> **M5 结果**(DML 双输出 ONNX, 918s):混合池(CLS top-200 ∪ 全索引采样 ∪ 真值/干扰窗)内 patch V3 打分——**p08b 32→2**(兄弟机位选对实例, 与 E21 一致, 含采样+干扰更真实池内复现); p08 6→5 微改进; **p26 22→24 无解**(干扰 sim 0.951 反超正确 0.900, margin −0.05, patch 层同样特征上限); 易例 p01/t3r12/t4r01 全保持 top-1 零回退。**产品相关缺口**: runtime 检索 top-20, p08b(CLS 32)/p26(CLS 22) 均超池, patch 可救 p08b 型、救不了 p26。工程情报:DML patch 双输出 ONNX 18fps 数值一致, patch runtime 化可行。FINDINGS `semantic_signal/FINDINGS_M5.md`。

> **▶ 索引密度探针 M4 已完成(2026-09-01,研究侧零 runtime)= 8fps 密帧对判别零增益,索引密度方向关闭,证据链 M1-M4 全貌闭环**:
> 起因:用户质疑「帧数不能再提升吗?GPU 加速还能往上吗?」(对标同类软件 30fps 全片解析)。澄清:Phase 14C「2→4→8fps 零增益」是查询侧结论,索引侧从未测过 → M4 首次实测。
> **M4 结果**(DML batch=1,717s):同一编辑段查询,8fps 密帧 vs 1fps 稀疏索引候选池对比——
> p08 best_rank 都 1(top5 2→5 略改善)/margin 0.393→0.365;p26 best_rank **12→43(密帧反而恶化,候选池稀释)**/margin −0.280→−0.247 仍负;t3r12/t4r01 完全持平(margin 略降)。**8fps 未把任何负 margin 变正**。
> **归因**:索引密度解决「正确帧数量」不解决「正确 vs 干扰可分性」;p26 干扰 dist_max_sim 0.876 反超正确 0.596 是 CLS 特征混叠,任何帧率救不了。**30fps 全片解析成本 ×30 换不来判别增益 → 方向关闭**。
> **吞吐实测**(RX 6750 GRE + ViT-S @518):DML batch=1 **20.5fps** 最佳,大 batch 并行收益≈0(plateau ~15fps),达不到 30fps;**工程情报:未来 DML 批量推理用 batch=1 而非默认 8**。
> **难例多模态辅助**(用户指示):M4 难例 p26 正是 M1/M2/M3 靶心——M1b VLM 事件级反向选错(真值 0/3)、M2 字幕召回唯一弱正(3/3,不可索引化)、M3 CLIP 无判别力。三形态已测,无可靠分离。
> **证据链 M1-M4 全貌**:判定(M1)/召回(M2)/索引(M3)/密度(M4) 四路全无解 → 失败族维持特征上限=已知局限。零 runtime,三指标 39/41 不受影响。FINDINGS `semantic_signal/FINDINGS_M4.md`。

> **▶ 方向 C 已关闭(2026-09-01)**:多模态(字幕/VLM)语义经 M1 判定证伪 + M2 VLM 召回单例弱 + M3 可规模 CLIP 索引证伪(无判别力 Δ<0.003 + 易例回归)→ 无可靠可规模多模态通道。剩余失败族(兄弟机位/同质场景/夜读/重复镜头)维持「特征上限=已知局限」;未来如需重开,证据在 `semantic_signal/FINDINGS_M1/M2/M3.md`。
> 起因:用户拍板立项方向 C 完整验证(A)= 引入文本嵌入 → 建「字幕→原片场景」向量索引 → 多案例量化。前置已通:pip 装 sentence-transformers 6.0.1 + clip-ViT-B-32-multilingual-v1(hf-mirror 可达, 512 维, 支持法语),不碰 onnxruntime/rapidocr 依赖。
> **M3 结果(38s 纯本地)**:CLIP 跨模态字幕检索**无判别力**——所有候选场景相似度挤成平台(p26 Δ0.002、p08 Δ0.001、test4 Δ0.002),排序≈噪声;真值 rank p26 2/8、p08 3/7、test4 2/8;**易例 p01 被字幕推到 5/7(比随机差)**。
> **机制归因**:解说字幕是**故事级叙事文案**不是画面级描述,与画面松散/解耦(test4 事件无对应、p01 抽象文案带偏);CLIP ViT-B-32 文本-图像对齐对「外观近同」候选无区分力。
> **方向 C 全貌**:M1 判定证伪 + M2 VLM 召回仅 p26 单例弱(3/3 vs 1/3, 昂贵不可规模) + M3 可规模索引证伪 → **多模态语义无可靠可规模通道**。
> **对用户「早期纯视觉是否错过多模态红利」的最终回答**:方向 C 是唯一被遗漏通道,现已完整验证 = 无可靠红利可回收,「纯视觉+特征上限」经得起多模态验证。
> **已拍板(2026-09-01):方向 C 关闭**。多模态(字幕/VLM)语义经 M1 判定证伪 + M2 VLM 召回单例弱 + M3 可规模 CLIP 索引证伪(无判别力 Δ<0.003 + 易例回归)→ 无可靠可规模多模态通道。剩余失败族(兄弟机位/同质场景/夜读/重复镜头)维持「特征上限=已知局限」;未来如需重开,证据在 `semantic_signal/FINDINGS_M1/M2/M3.md`。

> **▶ 字幕语义召回探针 M2 已完成(2026-09-01,研究侧零 runtime)= 方向 C 首次正面信号(p26 3/3 vs 1/3),但有清晰边界**:
> 起因:用户拍板「结合多模态判定」后进一步拍板测**字幕语义召回索引**(方向 C 的正确用法,此前只列远期从未实测)。探针 `research_semantic_signal_M2_subtitle_recall.py`(VLM 调用 24 次,数据 `work/semantic_signal_M2_results.json`)。
> **M2 结果**:
> - **p26 夜读 = ✅ 字幕语义召回首次正面信号**:字幕「她用望远镜看 Levi 在做什么」真值区 [2808-2811] **3/3 EVENT YES**(VLM 认出圆形暗角=望远镜视角看 Levi),干扰夜阳台 1/3——视觉 CLS 分不清的夜读/夜阳台,字幕能分(p26 上第一个正向信号)。
> - **t3r12 = ⚠️ 弱正向**:真值 2/3 vs 干扰 1/3(精灵王站着看,重复镜头语义同貌区分度有限)。
> - **test4 = ❌ 0/3 全 NO**:字幕「二十多人被困滑梯管道」在原片画面无对应——解说字幕是叙事文案,与画面解耦。
> - **净结论**:方向 C **不是全盘证伪**——字幕精确描述原片画面事件时(p26)字幕召回有效,修正此前「结构性不可行」的悲观判断;但适用边界 = 字幕-画面对应性(有则有效,解耦则无效)。**环境约束:无文本嵌入模型**(sentence-transformers 全 MISS)→ 只能 VLM 逐窗匹配,未建向量索引。
> **已拍板(2026-09-01):立项方向 C 完整验证(A)**——引入文本嵌入模型 → 建「字幕语义→原片场景」向量索引 → 更多案例量化召回增益(重点收集 p26 型「字幕精确描述画面事件」案例)。**前置可行性**:本机无文本嵌入模型(sentence-transformers 全 MISS),需先确认 pip 安装/模型下载可达性;若网络不可达则退回受限形态(VLM caption 化)并如实标记。

> **▶ 多模态判定探针 M1 已完成(2026-09-01,研究侧零 runtime)= 事件级语义证伪/字幕召回弱信号,特征上限获语义层独立确认**:
> 起因:用户拍板分层检索方向「结合多模态进行判定」(A: 召回层+判定层两层都测)。探针 `research_semantic_signal_M1_multimodal.py`(VLM 调用 36 次,数据 `work/semantic_signal_M1_results.json`)。
> **M1a 字幕语义召回 = 弱方向性信号**:p08 正确 1/3 vs 兄弟 0/3、t3r12 正确 2/3 vs 干扰 1/3(弱偏好正确), 但 p26 两窗都 2-3/3 → 单靠字幕无法精确分离失败族。
> **M1b VLM 事件级判定 = 证伪(甚至反向)**:p08 兄弟 3/3 SAME(语义同貌无法区分);**p26 编辑段被判与干扰夜阳台 3/3 SAME、与真值夜读 0/3 SAME(反向选错, VLM 视觉混淆比 CLS 更严重)**;t3r12 正确/干扰都判 SAME(重复实例语义同貌)。
> **净结论**:失败族本质=「语义相同的实例」(兄弟/重复/夜读夜阳台), 事件级语义层与外观层**共享同一天花板**, VLM 事件级判定无判别力(p26 甚至反向); 字幕语义存在弱方向性但非独立判别器, 可留作未来两阶段检索的召回侧弱加权候选。
> **证据链闭环**:外观/结构(五路)+ 事件聚类(方向 A)+ 序列上下文(P3)+ 语义/多模态(M1) 五层全部无解 → **特征上限为多维独立确认的已知局限**。零 runtime, 三指标 39/41 不受影响。FINDINGS `semantic_signal/FINDINGS_M1.md`。

> **▶ 第六路候选信号·镜头序列上下文探针 P3 已完成(2026-09-01,研究侧零 runtime)= 证伪,特征上限正式封顶**:
> 起因:用户指出五路+方向 A 之后仍有一路未证伪=镜头序列上下文(用目标镜头前后镜头内容联合匹配,独立证据源)。已按用户要求做离线最小验证:`research_semantic_signal_P3_seq_context.py`(纯现有 CLS+GT 编辑段边界,零 runtime)。
> **P3 结果(全部证伪)**:
> - **P3a 编辑序列与原片时间轴非 1:1**:p07→[1027,1041]、p08→[1160,1166]、p09→[1131,1147]、p26→[1766,1770](真值 2809 偏 1000s+)、p40/p41→[1894,1900]/[346,355]——快剪解说编辑序列≠原片序列。
> - **P3b p08 序列匹配=不升反降**:单镜头真值134 rank 44 vs 序列匹配 rank 61,**兄弟场景128 被推到 top-1**。top3 全为同一对话戏场景(128/135/138)。
> - **P3c p26 对照=证伪**:真值 rank 17→35,答案被拉向编辑上下文区域(1772-1907),远离 2809。
> - **根因**:①p08 编辑段后相邻镜头 p09 内容恰来自原片 1048-1082=兄弟机位区域,编辑上下文自身就混着兄弟内容,非独立证据;②同一场对话戏镜头在原片集中在 1010-1154 同一时间窗,不存在"只在 1108 出现的唯一上下文";③p26 编辑重排致上下文把答案拉向错误方向。
> **回答用户三问**:①p08 查询片段**不含完整镜头边界**(编辑段仅覆盖场景134 内部 2/15s),但 GT 边界提供完整查询镜头,失败不在此;②编辑侧 scenes.npy 仅 2 粗场景无细边界,但 GT 编辑段可当查询镜头;③p26 上下文序列差异更极端(相邻 1772-1780 vs 真值 2809)。
> **结论**:外观层/事件聚类(P1/P2)/序列上下文(P3)全部证伪或边界受限→ **剩余失败族(兄弟机位/同质场景/夜读)正式封顶为特征上限=已知局限**。唯一未探索空间=方向 C 多模态语义(远期,工程量大且语义层可能同样分不清兄弟机位)。零 runtime,三指标 39/41 不受影响。FINDINGS `semantic_signal/FINDINGS_P3.md`。

> **▶ 剪映通道验收闭环(2026-09-01 用户确认)**:`D:\JianyingPro Drafts\` 下 test1-ed.loc.v6 / test3-ed.loc.v7 草稿已由用户双击确认完成,剪映通道最终验收通过,**不再列入待用户动作**。导出验收主力维持 PR CEP 通道(119/119 PASS)。剪映相关历史待办均视为已闭环。

> **▶ Phase 24-2 语义级第二信号·方向 A 探针 P2 已完成(2026-09-01,研究侧零 runtime)= 兄弟机位事件级 top-1/重复镜头保持/同质场景证伪 = 已拍板【方向 A 归档】**:
> 拍板:用户选 **A1 继续 P2**。P2 设计 = 事件归并规则(时序近邻+指纹联合聚类:场景中心时间差≤T_gap AND 指纹余弦≥S_sim 连通分量;网格 30/60/120s × 0.55/0.60/0.65/0.70)+ 编辑段→事件单元端到端排序(对照帧级/场景级/事件级三档)。脚本 `research_semantic_signal_P2.py`,数据 `work/semantic_signal_P2_results.json`,FINDINGS `semantic_signal/FINDINGS_P2.md`。
> **P2 结果**:
> - **P2a 归并规则**:兄弟机位(134↔128)**12/12 参数全同单元**(归并稳健捕获同一事件多机位,单元含 946-1166s 连续对话戏 19 场景);重复镜头正确[43,44,45] 12/12 同单元但**干扰 42/46 被时序并入**(过度归并);同质场景 test4 正确实例多数参数**不同单元**且干扰混入(证伪确认)。
> - **P2b 端到端**:p08+p38 兄弟机位 场景级 8/699 → **事件级 top-1**(T_gap=120 三档稳健)——**正面回答 P1a「rank 6/11 不唯一」:归并规则=时序+指纹联合而非纯指纹聚类**;t3-r12 事件级 top-1(11/12 参数)保持;test4 无效;易例 p01 全档 top-1 零回退;p04/p17 场景级排名差为「场景指纹均值稀释」代理伪影(帧级 148/10 正常,非 runtime 回退);p26 事件级意外到 2/129(谨慎解读,不宣称特征上限已解决)。
> **净结论**:方向 A 与用户拍板预期(重复镜头族+部分兄弟机位族)一致,兄弟机位族超预期(事件级 top-1)。零 runtime 改动;三指标 39/41 不受影响。
> **已拍板(2026-09-01):方向 A 归档**。P1+P2 证据链完整(兄弟机位事件级 top-1 / 重复镜头保持 / 同质场景证伪),按立项约定「P2 结束无论成败都归档」→ 剩余失败族(同质场景)维持「已知局限」。未来如需重开,证据在 `semantic_signal/FINDINGS_P1.md` + `FINDINGS_P2.md`。

> **▶ Phase 24-2 语义级第二信号·方向 A 探针 P1 已完成(2026-09-01,研究侧零 runtime)= 部分成立/部分证伪,待拍板 P2 或关闭**:
> 起因:Phase 24-1 五路信号(ViT-B 升规模/视觉几何/运动签名/投影头/OCR 文字)全部证伪 + VLM 两轮判读确认失败族为特征上限后,用户拍板写「语义级第二信号」立项材料 → `semantic_signal/RESEARCH_PROPOSAL.md`(方向 A=场景实例身份建模[推荐] / B=剪辑叙事序列对齐 / C=多模态语义[远期])→ 用户选 **A**,写探针 `research_semantic_signal_P1.py` 并跑完。
> **P1 结果(静态度量,未训身份嵌入)**:
> - **P1a 归并能力 = 信号存在但不唯一**:p08 场景134↔p38 场景128(GT 双真兄弟机位)指纹 mutual_sim=0.704、基线 0.366±0.137、**z=2.47**(显著近),但 rank=6/11(非彼此最相似,前有 5~10 个其它场景)→ 聚合信号存在,单靠指纹聚类无法精确归并。
> - **P1b t3-r12(重复镜头)= ✅ 可分离**:正确实例场景[43,44,45] vs 干扰[42,46,47,48],within=0.587 vs cross=0.399 → **sep=+0.188**,正确场景平均指纹排名 **2.0/7(top-5 达标)**——比帧级(用户判错/特征上限)更有判别力,**方向 A 对重复镜头族有效**。
> - **P1b test4(同质滑梯)= ❌ 不可分**:within=0.482 vs cross=0.516 → **sep=−0.035**(跨簇反高),确认特征上限,方向 A 不覆盖同质场景族。
> **净结论**:方向 A 对**重复镜头族**有真实价值(t3-r12 场景指纹可分离);对**兄弟机位族**信号存在但不唯一(需时序上下文/人物连续性等更强事件身份特征,当前指纹粒度不足);对**同质场景族**证伪。**P2(身份嵌入端到端排序)未跑**——P1a 的不唯一意味着仅基于场景指纹均值的身份嵌入排序提升有限,P2 需先解决"哪些场景构成同一事件"的归并规则。
> **待用户拍板**:A1 继续 P2(先设计事件归并规则[时序近邻+指纹联合聚类],再验证编辑段→事件单元端到端排序;预期收益仅限重复镜头族+部分兄弟机位族) / A2 关闭方向 A(保留 t3-r12 可分离证据,剩余失败族正式关闭为已知局限)。零 runtime;三指标 39/41 基线未动。

> **▶ Phase 24-1 非外观第二信号三探针预研已完成 = 全部证伪(2026-08-31 晚,研究侧零 runtime 改动)**:
> 起因:用户提案立 Phase 24-1(研究侧),三探针打已知失败族(p08/p08b/p26/t3-r12)+ 易例回归 + 三指标。提案的事实核对全数成立(patch-max V3 在 runtime、视觉空间几何从未试过、PRNU/加速失真不适用、时序几何已证伪)。**实测结果:三探针全部证伪,且核心预期被否定**——
> ①**视觉几何一致性 = 证伪(核心)**:patch 互近邻 + `estimateAffinePartial2D`/`findHomography` RANSAC 内点率。p08 真值帧 1108 HOMOG=0.33 vs 兄弟 1048=0.51;p08b 兄弟 1109=0.75/n_match=329 压倒真值 1048=0.49/78。**归因(理论层)**:同一刚性场景从不同机位拍摄,patch 对应仍满足单应约束(平面近似),"不同机位→违反单一仿射"前提在多视图几何上不成立;叠加 1fps 候选粒度帧对齐敏感(真值窗内 0.33→0.85 跳变)。p26 的 inlier rank 2 为假阳性(干扰 1692 0.591 仍压真值 0.576,无 margin)。
> ②**时序运动签名 = 证伪(方法学不成立)**:编辑片快剪(压缩比>1),窗口帧差签名时间轴与原片非 1:1,未先对齐连易例 p01 都 corr=-0.87。不重试。
> ③**难例投影头微调 = 数据不缺,无可分信号**:分块余弦盘点——每索引跨场景高相似对(sim≥0.60,Δt≥2s)= 2.mkv 49,572 / test1 51,289 / test2 26,018 / test3 91,708 / test4 31,737,**「数据瓶颈」论点不成立**;但 CLS 特征级混叠 + ①②证伪 → 无可注入第二信号,不立项。
> **回归**:`measure_shot_recall.py`(GT v3,41/4)现行 = 严格 39/41(verified 35/37, loose 4/4)、场景级 39/41、FP 2/4、支撑 91/137 —— 与 Phase 24 基线**完全一致,零回退**(pre24 快照 38/41 为旧态)。
> **结论**:剩余失败族(p08/p08b 同场戏兄弟机位、p26 夜读、t3-r12 重复镜头)= 特征上限,**维持「接受为已知局限」既有拍板**;未来唯一有价值方向 = 语义级第二信号(场景身份/多模态字幕),当前无证据不立项。不改 runtime/不打包/不 bump feature_version。产物:`mvp/scripts/research_phase24_1.py` + `research_phase24_1_data.py`、`work/phase24_1_probe_results.json`、`work/phase24_1_data.json`、FINDINGS `mvp/benchmark/user_case/phase24_1/FINDINGS.md`。

> **▶ 交接执行单(2026-08-31)已完成并闭环 = NLE 自动验证通道定案:PR CEP 建成并验收 PASS(119/119),Resolve 免费版路死**:
> 起因:剪映(CEF)桌面自动化不可用 → 寻找"能程序化读时间轴"的 NLE 做导出自动验收。两条路实测:
> **① DaVinci Resolve 21.0.4 免费版 = 死路**:安装于 `D:\DaVinciResolve_setup`(用户自装),外部脚本 API 连接全部失败(fuscript Lua `Resolve()`=nil / Python fusionscript.dll PyInit 失败[3.11/3.13 均试] / 脚本服务从未监听);UI 无"外部脚本使用"选项;网络确认 **Windows 免费版禁用外部脚本,Studio($295)独占**。排障遗产:`python27.dll` 已补进安装目录(py2.7.18 MSI 解包),config.dat 曾写 `System.Scripting.Mode`(无关根因,无害保留)。Resolve 保留作 XML 手动目检工具。
> **② Premiere Pro 2026 CEP 面板 = 建成验收 PASS**:自研扩展 `com.svl.timelineexport`(`%APPDATA%\Roaming\Adobe\CEP\extensions\`,manifest 在 `CSXS\` 子目录[放根目录不加载,照 Film Impact 实测结构],PlayerDebugMode 已设 HKCU CSXS.9-13)。面板按钮 → `host.jsx`(ExtendScript)自动导入 SVL FCP7 XML → 打开序列 → 时间轴全量导出 JSON(`work/svl_pr_timeline.json`)。**踩坑三条**:ExtendScript=ES3 无 JSON(手写序列化器);PR 26.0 的 Time 对象 `.toString()` 崩(undefined,改走 `.ticks`/254016000000 数值通道);面板改动需关开面板或重启 PR 生效。
> **E2E 验收数字**:test1 XML(119 clipitem/6 轨/30fps)→ PR 实读 119 clips;逐项四元组(start/end/in/out)比对 **119/119 匹配,最大偏差 0.0000s**。"PR 读轨道→自动验收"通道正式闭环,以后任何导出批可复用:面板点一下 → JSON → 比对脚本。
> **本会话收尾(2026-08-31)**:①比对逻辑已固化为 `mvp/scripts/verify_pr_timeline.py` 并复验(默认 XML=`export_smoke/test1-ed.loc.xml` JSON=`work/svl_pr_timeline.json`,`--xml/--json/--tol` 可覆盖,退出码 0=全匹配;本次跑=119/119、最大偏差 0.0000s、EXIT=0)。②`host.jsx` XML/输出路径已参数化:`svlRunAll(xmlPath,outPath)` 由面板 evalScript 字符串传参、空值回退 `SVL_XML_DEFAULT/SVL_OUT_DEFAULT`(旧 `svlRunAll()` 兼容);`index.html` 加两个输入框(FCP7 XML 路径/输出 JSON 路径);`q()` 双写反斜杠防 ExtendScript 误把 `\b`/`\t`/`\n` 当转义(Node 往返测试含易错字符通过;host.jsx Node `--check` 语法 0)。③FCP7 多轨 XML 导出已支持(现有通道),无需改。**扩展源码已归档**`mvp/scripts/pr/cep_extensions/com.svl.timelineexport/`(仓库为源;安装=复制到 %APPDATA%),用法文档 `mvp/scripts/pr/README.md`。**待用户动作**:重启 PR 开面板确认输入框、可选填任意导出批 XML 验证多批复用。

> **用户四条反馈落地(2026-08-30 晚 II,历史)**:①手动排除——Result.excluded+POST /api/results/exclude+结果页「不导出此段/恢复导出」+表格徽标+导出过滤(r16 实证);②导出对话框「片段宽度」可选(完整镜头/仅核心窗口),material_width 透传;③表格标签挤压修复(旗标列定宽 170px+换行);④文件夹选择器=旧程序壳无新 IPC,维持手填。test3 草稿重导 v7(用户修正基线+r16 排除,单轨 9 段/全原速/242s)。

> **▶ 交接执行单(2026-08-30 深夜 III,上一阶段)= Phase 24 冲突扩池重排已实施并验收,闭环**:
> **test3 全 34 段全量审查完成(2026-08-30 晚,用户逐段对比后要求全量复查+点名 11 段)**:
> 基线=用户手动修正后的 results(Desktop/1/,含 r0/r8/r11/r27 四处手动替换),34/34 段对照图全量审查(无抽查),记录 `cases/test3_gt_adjudication_full.json`。**结论:29 个非 LOW/NIS 段中 26 对/2 部分/1 错**——r10 错(正确≈7:24.5,在前后段 7:22/7:27.5 空隙内,待结果页手动替换);r16 部分(索林对,编辑关键镜头『精灵王拒绝』不在 8:04.5-8:06.5 窗口);r12 用户判错(精灵王重复镜头互混,人眼难分=特征上限)。**用户三类反馈解码**:①"r16/19/20/24/26 对不上"=并排播放失同步(卷轴 217s vs 解说 155s+原速 vs 快剪→系统性错位;真问题仅 r16 窗口偏移;r19/r24 为 LOW 不进默认导出;r20 实际正确)+②"r22/23/25 缺前后镜头"=结果页预览是核心定位窗口(2s)而草稿是镜头表扩展完整镜头(口径差非错误)+③"r27/28/32 前后多余"=完整镜头取材固有特性(所在完整镜头 13-18s,编辑只用 2s;如需可加『仅核心窗口』导出选项)。**文件夹选择器**:设置页『选择目录』走 Electron IPC(app:openDirectory),旧程序壳无此 IPC→按钮无效;手动填路径可用;重打包后生效(待允许)。

> **▶ 交接执行单(2026-08-30 晚更新,下一会话从此开始)= Phase 23-0 特征升级预研完成,结论待拍板**:
> **交接拍板(AskUserQuestion 留痕)**:A 特征升级立项 ✅ / B 暂不打包 ✅ / C test2-4 LOW/MEDIUM GT 暂不补 ✅。
> 用户"继续 benchmark"→ Phase 24-0 预研(音频/纯几何时序均否定;t3r10 真值区有被压制证据峰 0.73 vs 0.87)→ 用户 AskUserQuestion 拍板 **A 立项** → 已实施验收:**`engine/localization/conflict_rerank.py`**(几何触发:编辑序相邻重叠+低分 mover+宽度门;落位 `best_free_span`:空隙窗扣全部 claim 的无主张子区间 relocate 取最优——实机实证整窗 relocate 会被非相邻合法 claim 整单否决;接受:证据门槛 sim≥0.60/落差≤0.25)+ `_apply_conflict_rerank` 接线(时序修复之后,mover 置信分升序处理+窗口刷新)+ PipelineConfig 5 旋钮(`conflict_*`)。**验收**:后端 206(+19)+api 58 全绿;2.mkv GT v3 零回退(39/41、FP 2/4、支撑 91/137,零触发);test1/2/4 零触发零漂移;**test3 r10 自动修复 447-449→442-444**(人工真值≈444.5,与 r11 重叠消除),其余段零扰动,对照图 `second_signal/r10_fix_verify.jpg`。**r10 不再需要结果页手动替换。** 未打包(长期规则)。详见 `mvp/benchmark/user_case/second_signal/FINDINGS.md`。
> **▶▶ 交接定稿补充(2026-08-30 深夜,Phase 22 执行会话收尾)**:Phase 22 六项 + 用户全部反馈轮(导出六轮迭代/UI 15 条/精度修复/test1-4 GT 全量裁决/时序修复/标定结论/手动排除机制/片段宽度选项/表格布局)**全部闭环**,测试基线 后端 187+api 58+前端 67 全绿。**剪映草稿最新版**:test1-ed.loc.v6 与 test3-ed.loc.v7(用户修正基线+r16 排除,单轨 9 段/全原速/242s)均在 `D:\JianyingPro Drafts\` 待用户重开确认。**Phase 23-0 拍板确认**:特征升级不立项(证据不利)、暂不打包、LOW/MEDIUM GT 不补——剩余失败族(同场景重复实例/同质场景)留待"非外观第二信号"新研究阶段(场景身份/时序先验/音频),何时启动由用户定。下一会话无未竟必做项;新需求或新反馈直接提。
> **Phase 23-0 预研已完成(2026-08-30 晚,~0.5 天),结论 = 不建议 bump feature_version**:ViT-B/14(官方 dinov2_vitb14_pretrain.pth,与冻结模型类参数化实例化,missing/unexpected=[] 加载)在实锤失败探针上零增益、p08b 明确恶化(CLS margin −0.138→−0.281)。两条通路均测:CLS 全局(`research_feature_upgrade.py`)与 patch-max top100(`research_feature_upgrade2.py`,V3 定义);探针 = p26/p08/p08b/p27 + t3r10 + t4r01 观察 + p01/p04/p17 易例回归。patch ViT-S 在 p08b 险胜(+0.006,argmax@1049 正确)= 与 Phase 21 V3 一致且已在 runtime,无新增空间。详见 `mvp/benchmark/user_case/feature_upgrade/FINDINGS.md` + `work/feature_upgrade_probe*_results.json`。**升规模的成本收益不成立**(5 部片 1-2h GPU 重建 + 特征存储翻倍 + ONNX 资产 346MB + 全套回归 vs 探针零改善),剩余失败族(同场景重复实例/同质场景)需非外观第二信号(场景实例身份/时序先验/音频)= 新研究阶段。
> **待用户拍板**:A. 接受剩余失败为已知局限,Phase 23 关闭(推荐,证据不利);B. 立非外观第二信号研究;C. 坚持全量重建验证(不建议)。
> **仍待用户动作(与上并行)**:①重启剪映双击「test1-e...y draft」(v6 草稿已在 `D:\JianyingPro Drafts\`)最终验收剪映通道;②test3 r10 结果页手动替换(真值 ≈7:24.5,当前错位 7:27-7:29)。
> **交接基线已复验(2026-08-30 晚)**:后端 185 + api 56 + 前端 67 全绿;DML 正常(onnxruntime-directml 1.24.4,DmlExecutionProvider 在列,%LOCALAPPDATA% 模型资产在位);v6/v4/test1 草稿均在剪映根目录;8765 端口旧实例仍在(PID 24764,冒烟需换端口)。api 测试须带 PYTHONPATH=`mvp/src;mvp` 运行(loader 才找得到 domain 模块,裸 discover 会 1 error)。
> **环境必读**:本机 DML 默认可用(模型资产已补至 %LOCALAPPDATA%);索引缓存两处——Roaming `Video Locator AI/data/index`(test1-4)与 LOCALAPPDATA `SourceVideoLocator/index`(2.mkv/src120);8765 端口常驻一个旧后端实例(PID 可能变化,冒烟换端口);桌面自动化对剪映(CEF)不可用(身份绑定失败),剪映内操作需人工。**守卫不变**:不打包需明确允许;GT 新增条目须逐帧确认;后端 185+api 56+前端 67 全绿为交接基线。

> **▶ 交接执行单(2026-08-30 下午定稿)= Phase 22 全部六项完成,收尾验收与下阶段决策**:
> **今日全览(全部已留痕,详见下方各条)**:①②③ 导出三通道(EDL/FCP7 XML/剪映 v6)+切点吸附+导出策略 ✓;④ card_guard 0.85 + 密帧重试+0.62 门槛 ✓;⑤ test1-4 镜头级 GT 全量裁决(58 HIGH 段 96.6%)+时序离群修复(r18)+Confidence 标定结论(门槛维持 MEDIUM) ✓;⑥ EDL/XML 帧比对 PASS ✓;⑦ UI 15 条反馈轮 ✓;剪映导出器 v2→v6 六轮迭代(pyJianYingDraft+镜头表定界取材扩展+时间码素材名)。
> **待用户动作(按优先级)**:
> 1. **重启剪映 → 打开「test1-ed.loc.v6 draft」**(草稿根目录 `D:\JianyingPro Drafts\`,10 clips/227s/全原速/单轨)——确认时间线渲染、可播放、可编辑。这是剪映通道的最终验收;失败截图反馈(v2 时代列表解析已通过,v6 结构由 pyJianYingDraft 生成把握较大)。
> 2. **test3 r10 手动替换**:结果页找到 r10(ed 0:55.5-0:57,当前错位 7:27-7:29)→候选/预览定位到 **原片 7:24.5 附近**(逃亡奔跑镜头)→"设为主定位"→重新导出。(test3 r12 与 test4 全片=特征上限,见下)
> **下阶段决策(需用户拍板)**:
> A. **特征升级立项**:已实锤的特征上限案例=p08/p26/test3-r12(同场景重复镜头)、test4 全片(同质彩色滑梯,检索最强 sim 仅 0.53-0.60)。方向=更强 backbone 或 patch 级检索,**需 bump feature_version 全量重建索引**(5 部片 GPU 约 1-2h)+回归验收,工作量一个大阶段。
> B. **打包发布**(需明确允许):代码签名/图标等后置项。
> C. **test2-4 LOW/MEDIUM 段完整 GT**(可选,当前仅 HIGH 段全量裁决)。
> **环境必读**:本机 DML 默认可用(模型资产已补至 %LOCALAPPDATA%);索引缓存两处——Roaming `Video Locator AI/data/index`(test1-4)与 LOCALAPPDATA `SourceVideoLocator/index`(2.mkv/src120);8765 端口常驻一个旧后端实例(PID 可能变化,冒烟换端口);桌面自动化对剪映(CEF)不可用(身份绑定失败),剪映内操作需人工。**守卫不变**:不打包需明确允许;GT 新增条目须逐帧确认;后端 185+api 56+前端 67 全绿为交接基线。
> **Phase 22 ⑥ 验收第一轮(2026-08-30 晚)**:① **EDL/XML 自动帧比对验收 PASS**——新脚本 `mvp/scripts/verify_export_frametruth.py`(无 NLE:解析 EDL→实测 fps 反解时间码→抽帧 DINOv2 相似度 + 错位对照)。test1 28 事件:roundtrip 28/28、对齐 13/28、margin +0.130;120s E2E 6 事件:roundtrip 6/6、对齐 4/6、margin +0.091;XML 帧号反解与 EDL 一致;DML 跑通。② **剪映通道半验收**——草稿拷入 `D:\JianyingPro Drafts\` 后,**剪映 11.3.0 草稿列表成功识别解析**(165.3K|02:11=我们的时间轴时长,格式被正确读取);双击打开验证被桌面自动化环境阻断(ZCode 自身窗口持前台+动态壁纸→CUA 帧绑定/焦点全拒),**待用户手动双击「test1-e...y draft」卡片**看时间轴(5 轨)。剪映列表卡片顺序=test1 草稿排第一,可直接点。
> **环境事实(本机)**:DirectML 正常(RX 6750 GRE + onnxruntime-directml 1.24.4),运行时模型资产已补齐(%LOCALAPPDATA% models/dinov2_cls_384 复制自打包产物),`pick_best_available` 默认选中 directml/amd,无需 SVL_DML_MODEL;DML 全管线 E2E 65.8s(索引复用)且产物与 CPU 逐字节一致。桌面自动化注意:本机动态壁纸+ZCode 前台导致全屏帧秒过期、窗口帧对 CEF 应用(剪映)身份绑定失败、剪映窗口几乎整体被 ZCode UI 覆盖(SetWindowPos 右移可解覆盖,但点击仍被拒)——剪映内 GUI 操作需人工。
> **Phase 22 ①②③(导出 MVP + 场景切点吸附 + 导出策略)已完成并验收(2026-08-30)**:
> ① **导出器**:`mvp/src/app/exporters.py`(纯函数,无 IO)——`ExportClip` 计划单元 + `build_export_plan`(门槛/不导规则/主+子 span 展平/±0.5s 近重合去重/子 span 记录槽封顶父段编辑时长) + `snap_clips_to_scenes` + `render_edl`/`render_fcp7_xml`/`build_jianying_files`。`SourceLocatorService.export_project(fmt=edl|fcp7_xml|jianying)` 落盘(剪映=草稿文件夹 `*.loc.jy_draft/`),非法 fmt/缺 original_video → ApplicationError。时间换算:管线秒=相对首帧媒体时间,NDF timecode 按名义 fps 拆分,源片 TC 原点=首帧(MKV start_time 与 NLE 打开素材行为一致锚定首帧,无需修正),fps/分辨率 ffprobe 实测(失败回退 25 + 警告)。轨道布局:EDL=单轨只放 main/low 主 clip;XML/V1 main、V2 low 备用、V3+ 第 k 子 span 一条轨;剪映同构 + speed 素材承载源/记录时长比。
> ② **切点吸附**:源片侧 in/out 吸附最近 scenes 边界(±`snap_tolerance_s`=1s),记录侧不动;塌缩(out≤in+0.05s)回退该侧;索引不可用静默降级(警告)。
> ③ **策略**:`ExportConfig`(min_confidence=MEDIUM / low_policy=exclude|backup / snap_scenes / snap_tolerance_s,config JSON `export.*`);not_in_source、failure_reason、零宽 original|edited、门槛之下主 span(backup 时 LOW 例外进备用轨)及其子 span 不导。API `POST /api/export` 增 format/min_confidence/low_policy/snap_scenes(format 默认 json 走原通道,后端兼容)。
> **测试**:后端 171(mvp/tests,+27 test_exporters:timecode/计划门槛/吸附塌缩回退/EDL 结构/XML ElementTree 解析/剪映 JSON 结构/服务接线与降级)+ api 53(+2 format 路由)全绿;前端零改动(format 默认 json 兼容)。
> **真机 E2E**:① test1 结果批(33 段)三格式导出冒烟→ EDL 31 事件(23.976fps 实测换算)/XML 6 轨 119 clipitem/剪映 5 轨 14 段,产物 `mvp/benchmark/user_case/export_smoke/`;② 本机无既有索引→新建 120s 真索引(2150s 起 src120.mp4 + ed30.mp4 前 30s,`work/loc_smoke/`)跑完整 locate(6 HIGH+4 no_evidence,CPU 回退=本机无 DML onnx 资产,如实降级)→ 导出 **7/14 clipitem 真实吸附**(scenes.npy 真实生效,EDL/XML snap=in/out 注释留痕)。
> **诚实边界**:剪映草稿为 beta(自研锁 app_version=5.9.0),导入验收=TODO 第 6 项用户真机过;EDL 源/记录时长不匹配(压缩比>1)按 NLE 隐含变速处理,XML/剪映显式 speed 承载;本机 `%LOCALAPPDATA%` 无历史索引缓存,test1 冒烟吸附走降级路径(警告留痕),吸附主验证靠 120s E2E + 单测。

> **Phase 21 场景指纹召回扩展层已实施完成并验收(2026-08-30)**,执行单五项全部闭环:
> ① 索引侧场景表:`FeatureStore.create_index` 落盘 `scenes.npy`([S,2] 起止秒)+ `scene_feats.npy`([S,384] L2 场景指纹),`detect_shots(cut0.15/z1.5/min3s)`(2.mkv 699 场景),`feature_version` bump `+scn1` 自动重建,validate 纳入场景表存在性;`IndexBundle` 增 scenes/scene_feats;既有 5 个真实索引经 `backfill_scene_tables.py` 从缓存特征确定性回填(等价重建,免 50min GPU)。
> ② 证据扩池:`EvidenceLocalizer` 场景单元(查询均值 vs 场景指纹 top-5,预算 120 帧,每场景独立过门)+ `_gate_scene_spans`(严格门+IoU 去重,无保底);**帧级为主/场景级只扩池**——场景 span 不进任何重排器(DP/patch/text anchor 不消费),仅作 `original_segments` 附加子 span(`OriginalSegment.from_scene_pool` 标记,UI types.ts 已镜像)+ 帧级零证据时救回主定位(诚实 LOW)。config 3 旋钮 `scene_recall_enabled(True)/scene_top_k(5)/scene_max_expand_frames(120)`。
> ③ 回归门槛达标(同环境同口径):**场景关 38/41、FP 2/4、支撑 52/64(与 08-29 基线一致,可复现)→ 场景开 39/41、FP 2/4、支撑 90/136**;+1 = p33 被场景子 span 2043-2053 真救回;MISS(p08/p26)不变;p27 主定位保住;2.mkv+test1-4 主定位/置信逐段零漂移(scene-off 对照 0 差异);no_evidence 段获救回(test3 s1/test4 s0/s6/s9)。**默认开**。
> ④ 测试:mvp 144(+10:场景表生命周期 2 + 扩池语义 7 + text anchor 护栏 1)+ api 51 全绿。
> ⑤ 收尾:FINDINGS(`mvp/benchmark/user_case/scene_recall/FINDINGS.md`)/STATE/DECISIONS 完成;**未打包(用户长期规则)**。
> **遗留**:p26 类(帧级强错误簇)非扩池可解,需更强特征;无支撑子 span 增多(81%→66%,带标记 LOW 附加候选);场景 span 的 UI 展示标注 = 后续 UI 轮。
> **环境事件(交接必读)**:venv 内 `onnxruntime` 1.29.0(纯 CPU)曾覆盖 `onnxruntime-directml` 1.24.4 同名包(疑 08-29 装 rapidocr 引入),DirectML 静默失效;已恢复 `onnxruntime-directml==1.24.4`(DML 回归+rapidocr 兼容验证)。**跨日基线对比必须核对 backend selected=directml**(本次 test1-4 相对 08-29 文件的 34 处主定位漂移全由此起,scene-off 对照复现、与算法无关)。评测器 `measure_shot_recall.py` mid_in 规则已修复(原漏下界,p08 假 HIT;基线须同口径重测=38/41)。


> **2026-08-30 — Phase 21 路径1 可行性实验:场景级检索有条件成立(p26 被救回)**:`research_scene_retrieval.py` 索引侧 detect_shots(cut0.15/z1.5/min3s)→ 349 场景(均值22s)+ 场景指纹匹配。5 探针:**p26 帧级 12 → 场景指纹 3**(唯一帧级硬失败被场景级聚合救回);p27 字牌场景指纹 17(长对话场景指纹稀释,帧级已对)——**场景级不可替代帧级,须混合**。**实施方向定案:场景指纹作候选召回扩展层**(帧级 top-K ∪ 场景 top 场景内帧 → 既有管线),索引 build 时生成场景表缓存,与 sequence DP/patch rerank/text anchor 兼容。脚本 `research_scene_retrieval.py`,数据 `scene_retrieval_results.json`。**


> **2026-08-29(晨 VIII)— runtime patch 重排修复上线并验收**:首跑 29 段全失败(`_patch_rerank_span` 误用实验脚本作用域变量 `times`+异常未兜底)→ 修复(bundle.times + 整体 try/except 非致命)。**验收:严格召回 39/41=95% 零回退、负例 2/4、支撑 43/51→52/64、子 span 23→36(patch 重排留痕+多段输出生效)**。至此 Phase 21 实施完成:OCR 文字锚点 + patch 最大匹配重排 + 时序 DP + 场景级多段输出 + not_in_source 全部在 runtime。**未做打包**(用户规则)。剩余:p26 类特征上限(需更强特征)、UI 晋级标注(可选)、多案例 Confidence 标定(待更多样本)。


> **2026-08-29(晨 VII)— Phase 21 E21 特征变体实验完成:V3 patch最大匹配胜出**:`research_phase21.py` 6 探针 × 5 变体(重排实验:CLS top-50 内真值名次)。**V3 patch-max(每查询 patch 对候选帧 patch 最大余弦,top100 均值)= top3 4/6,p08b 32→2、p08 6→5、易例零回退**——解"同场景兄弟机位"混淆(p08/p08b/p43 三真值场景)。V1 时序上下文负结果(恶化),V2/V4 中性。p26 最好 12(未解,疑 GT 帧错位或需更强特征)。**下一步 = runtime 二阶段 patch 重排**(低 margin 段触发,CPU torch forward_features,DirectML 不受影响;query patches 可在 embed 时缓存)。实验数据 `phase21_probe_results.json`,脚本 `research_phase21.py`。


> **2026-08-29(晨 VI)— 时序 DP 重排上线 + 天花板确认**:新模块 `sequence_rerank.py`(Viterbi DP:编辑顺序 × 每段候选池,时间倒退罚 λ/s + SKIP 逃生门)+ `_apply_sequence_rerank` 接线(旧主定位挪入 original_segments 留痕)+ 4 配置旋钮(`seq_dp_*`)+ 4 单测。**真机:GT 案例零回退(39/41=95%);test4 时间倒退 7→6——幅度有限的根因 = 候选池受限**(相似实例占满候选,正确实例可能不在候选里,与 p26 同机理)。DP 的天花板 = 候选生成质量 → Phase 21 检索层研究方向(场景实例枚举/更细粒度索引)。修复过程:locator_service 接线曾因 seq_items 初始化打到 __init__ 处导致 13 个测试错,已修。测试 134+51 全绿。**本会话算法侧 cheap 改进至此全部收割;下一步 = Phase 21 正式研究或更多案例积累。**


> **2026-08-29(晨 V)— 4 个新真实案例泛化验证完成(ProjectXIXI test1-4)**:影片分别为 The Northman(2.3h HEVC 1920×960)/Dodgeball(1.4h)/霍比特人(2.8h H.264)/水上乐园怪谈(1.2h AC3)。批跑 4/4 完成(总索引 ~27700 帧 @1fps,DirectML,含分析/OCR 锚点)。**结果:test1 33 段(HIGH28/LOW5/NIS2)、test2 9 段(H3/L6/NIS1)、test3 34 段(H24/M3/L7/NIS1)、test4 18 段(H3/M1/L14/空未定位4)**。多模态抽检 15+ 段:test1 7/8 对(r10 夜间暗景被卡守卫误杀=已知夜间形态复现)、test2 4/4 对、test3 3/3 对+密集条带证实"窗盖场景中段,快剪压缩两端溢出"(已修:场景级多段输出)、test4 2/3 对(0.5s 超短切 no_evidence 未定位)。**泛化结论**:定位能力跨 5 部片源/4 种画幅成立;复现的通用局限=①≤0.5s 超短切(2fps 单帧,no_evidence);②夜间暗景卡守卫误杀;③快剪压缩比>1 的窗宽截断(已修多段输出)。test2 文件名 typo(tset2-ed.mp4)按实际名处理。汇总:`cases/multi_case_summary.json`。


> **2026-08-29(晨 IV-b)— 用户审计质疑确认 + 场景级多段输出修复**:用户指出定位验证帧是"前后帧"——密集条带(2.5s/帧)实证:**r21 编辑 8s 对应原片 46:46-47:26 阿索格大战约 40s**,报告窗只盖中段 46:59-47:14,两端被截(快剪压缩比>1,单窗数学上不可能全覆盖)。**修复:clean 段覆盖区间超过窗宽 1.5× 时,按窗宽切块全部输出到 original_segments(主 original=最优块),不再丢弃两端**。真机验证:多段输出 9/29 结果,三指标零回退(39/41、FP 2/4、支撑 43/51)。评测器同口径补全(编辑侧+原片侧双并集)。剩余 2 MISS 不变(p08 场景内偏移/p26 特征混淆)。


> **2026-08-29(晨 IV)— 改进项 1/2 实验闭环:窗宽-细分参数矩阵,default 胜出保留**:评估器补全联合规则(编辑侧并集计入复合 GT)后发现审计的"宽度膨胀"是复合段现象(结果级 0 个短段配大窗)。公平矩阵:default 严格 **39/41=95%**(只剩 p08 场景内偏移/p26 特征混淆 2 个真 MISS)vs C1b 90%(窗宽 3.9 vs 5.7)vs C1 80%。**决策:保留 default 细分/窗宽参数**——召回优先,C1b 的紧凑收益不值 -2 命中;C1 的 0.5s 单帧段是检索噪声源。剩余 2 MISS 与切分参数无关(内容已切出,错在检索相似度)= Phase 21 第二信号范围。评估器联合规则补全已入 `measure_shot_recall.py`。


> **2026-08-29(晨 III)— 打包修复+打包后端 E2E 通过 + 逐镜头全量审计(32 镜头)**:
**打包 3 修复**:① `build-release.ps1` 仓库根错位(mvp/ui 布局下路径翻倍);② `build_backend.py` 剪枝误删 PIL(rapidocr 依赖,冻结环境 OCR 静默失效);③ `backend.spec` hiddenimports 元组语法 + excludes 含 PIL 冲突 → 全部修复。**打包后端 E2E(真实数据)**:backend.exe health 200 → /api/index 复用 7668 帧 1fps 索引(directml/amd)→ analyze completed → 日志实锤 `text anchor promoted ed=81.0-85.5 -> 1809-1813 sim=0.78 (main 0.00)`(OCR 冻结环境工作)、片尾 not_in_source=true。注意:打包后端直启需 SVL_DINOV2_WEIGHTS 指向内置 pth。产物 `release/win-unpacked/Video Locator.exe`(08-29 02:22)。
**逐镜头审计**(`gt_v3/shot_audit.md|json`,32 镜头 × 时长 × 覆盖 × 宽度):编辑侧联合覆盖 **32/32 无真漏检**(5 个复合长镜头由多结果联合覆盖 0.80-0.92,单结果≥50% 口径下 27/32);原片侧唯一未定位内容 = p26 夜读(2809,CLS 硬混淆 0.87错/0.31真)。**新改进项**:① 12 个 ≤2s 短镜头拿 15s 定位窗(宽度膨胀最大 19×,_bound_span 需按镜头时长收紧);② 复合长镜头(12.6s/20.3s)二级细分边界仍偏粗;③ p26 类需场景身份第二信号。


> **2026-08-29(晨)— Phase 21 实施第 1 步:OCR 文字锚点重排已接入 runtime 并真机验证**:
新模块 `engine/localization/text_anchor.py`(OcrEngine 懒加载 rapidocr/依赖缺失自动禁用 + 归一化 + 字符 3-gram 对称包含率 + TikTok 水印过滤)+ `locator_service._apply_text_anchor`(查询段帧有内容文字时,对主 span/子 span 窗 ±2s 各抽 4 帧OCR,文字显著更优的子 span 晋级为主 original;非致命 try/except)。**三个实现教训**:①候选窗 2 帧采样会整窗错过 1-2s 文字镜头 → 4 帧;②文字相似度聚合用 **max(最佳锚点行)** 而非均值——编辑侧解说字幕行会稀释唯一强字牌锚点;③OriginalSegment 字段名是 start/end 非candidate_*。配置 3 旋钮 `text_anchor_*`;requirements + backend.spec(hiddenimports+datas 模型)已接。**真机验证:OCR 锚点成功晋级 p27 主定位 1788-1797→1809-1813(问句字牌真实位置,GT [1808,1815] 内),全套 171 测试绿,召回/负例/支撑三指标零回退**。UI 展示 not_in_source 与晋级标注 = 后续 UI 轮。


> **2026-08-29 — OCR 实验强阳性 + OCR 修正 GT 自身错误;严格召回升至 90%**:rapidocr-onnxruntime 实测 4 帧全可读(0.8+)——ed85.5 读出问句字牌文字,**og1811 也读出 "WHAT is YOUR NAME?"**,证明问题字牌在 30:11 真实出现 → **GT v3 的 p27 精修 [1859,1869] 证伪修正为 [1808,1815]**(sign_timeline.jpg 全景裁决),管线原定位 1788-1813 本来正确。**最终基线:严格召回 37/41=90%(verified 92%)、场景级 93%、负例误报 2/4、支撑 43/51、HIGH 精确率 100%**。**Phase 21 方向定案:OCR 文字锚点 = 首选第二信号**(方向 2 patch-token 负结果留档;方向 1 降级)。实现注意:编辑侧有三文字层(原片字牌/解说字幕/TikTok 水印),匹配须集合式含容错;OCR 仅在重排阶段对候选窗少量帧执行,成本可控。依赖已装:rapidocr-onnxruntime。


> **2026-08-28(深夜 III)— Phase 21 预研诊断完成:最后 3 个 MISS 全部定性为"同场景内部分辨率上限"**:逐帧检索归因(脚本见会话)——p08 场景已找到(1006-1035)仅机位偏移 55s,且 moment 候选只有 1 个(1033-1035),多 moment 输出无效;p26 真值 sim 0.31 vs 错误区 0.87(CLS 硬混淆);p27 编辑块跨两块相似绿牌无边界(84.5-86 的 WHAT 字牌 85.5 帧 sim 0.84 但 1811 帧 0.91 更高)。**方向排序:场景内时序结构先验(零依赖)→ patch-token 稠密 → OCR 文字锚点**;诊断已写入 DIAGNOSIS_RECALL2.md,Phase 21 立项材料齐备。长期决定(护栏取消等 5 条)已记入 DECISIONS.md。


> **2026-08-28(深夜 II)— 多模态裁决 4 个争议 HIGH 全对 + GT v3 扩到 41 条;HIGH 档精确率 100%(标定结论)**:对 8 个"无 GT 支撑 HIGH"逐一出图裁决(nonehigh_*/s4_cmp)——**全部画面确认为真匹配**(s14 夜间狙击镜特写 140-142、s15/s17 夜间阳台设备瞄准/走位 1774-1780、s4 对话场景士兵特写机位 1048-1050,与 p08 的 1108-1110 同场景不同切镜)。根因 = 细切分新找回的子镜头 GT 未枚举,非误配。**GT v3 扩至 41 条(全 verified 增补)**。**最终基线(GT v3@41)**:严格召回 36/41=88%(verified 33/37=89%)、场景级 37/41=90%、负例误报 2/4(n03/n04 正确拒绝)、有支撑 span 42/51、**HIGH 精确率 14/14=100%**。**置信度标定结论(TODO #11 单案例证据)**:分数分离良好——错/漏的 3 条其覆盖结果分数均 ≤0.69 未进 HIGH,所有 HIGH 均真;ConfidenceConfig 现阈值在真实案例上 HIGH precision=100%,正式标定待多视频样本。剩余 MISS=p08(同场景相邻机位,兄弟条目 p43 已命中)/p26(夜读,特征上限)/p27(同场景不同字牌,特征上限)/p33 部分/p34(复合 GT vs 细分粒度,子镜头各自命中)。**卡片守卫回归零复发**(白闪/片尾两负例持续 OK)。


> **2026-08-28(深夜)— 算法迭代第 1 轮(护栏解除):自信答错消灭 + 子span门控 + 长块二级细分**:
**改动 A** 新增 `engine/segment/card_guard.py` 黑底文字卡/logo 像素守卫(三判据:近黑占比≥0.6 + 亮像素占比 1-30% + 亮斑低扩散(<48)且最大连通域≥0.4% 面积;`card_*` 6 旋钮)——卡片段跳检索直接 `not_in_source`。**改动 B** `evidence_localize._gate_subspans`:子 span 严格门(cover<0.45 且 sim<0.45 双低丢)+ IoU>0.6 去重 + top-4 上限(`subspan_*` 4 旋钮)。**改动 C** `detect_shots_two_level`:>8s 长块块内放宽参数(cut×0.75/z×0.85/min_shot 0.6s)二级细分(`seg_max_shot_s` 等 4 旋钮)。**改动 D** `Result.not_in_source: bool`(to_dict/from_dict 对称,API 透传)。**修复历程**:卡检测两轮误杀夜间场景(橙爆/暖灯扩散、铁丝网散点高光)→ 白色度+连通域判据修复;子span门 sim 0.50→0.45(夜间场景 sim~0.5-0.66)。**验收(GT v3 加固版,37 正例/4 负例)**:严格召回 32/37(verified 29/33)、**n04 TikTok 片尾=OK(自信答错消灭,原 HIGH 0.95→正确拒绝)**、n03 OK、负例误报 2/4(仅剩 n01/n02 未确认区间软负例)、有支撑 span 35/51。**剩余 MISS=p08(同场景相邻镜头 1048 vs 1108)/p26(夜读 2809,特征上限)/p27(同场景不同字牌 1788 vs 1859)**=第二信号/场景身份类,**p34=复合 GT vs 细分粒度口径问题(子镜头各自精确定位,GT 未枚举)**。**旧 p17(跨段边界)修复 ✓(1397-1399 HIGH 精确)**。测试 120(mvp/tests)+ 51(mvp/api)全绿(新增 test_card_guard 10 项 + two-level 3 + gate 5 + card 路径 3)。


> **2026-08-28(夜)— GT v3 镜头级标注完成 + 镜头级基线(护栏解除后首个产物)**:scdet(0.18)=31 切点 → 32 镜头 → DINOv2 检索 top-3 候选窗 → 32 张逐镜头对照图(`mvp/benchmark/user_case/gt_v3/`)逐张多模态裁决 → **`datasets/real/ground_truth_v3.json`(39 正例 verified30/loose9 + 4 负例,镜头级 ±1s)**。评估器 `mvp/scripts/measure_shot_recall.py`。**当前参数基线:严格召回 36/39=92%(verified 30/32=94%)、负例误报 4/4(n04=TikTok 片尾→7660 HIGH 为硬负例,n01/n02 未确认区间偏保守)、有支撑 span 29/39(无支撑含已判假阳性 s4-sub1303/s9-sub3654/s13-main7660)**。已核实真 MISS:p27(夜战 2231-2270)、p28(夜读 2809-2810,检索 top-1 曾命中但管线聚类丢弃)、p17 部分(跨段边界)。新增脚本 `build_gt_v3_candidates.py` / `measure_shot_recall.py`。v3 用途:per-shot 召回/负例误报/支撑率三指标直接驱动 F1(logo 降档)/F2(子span门控)/F3(切分细化)的量化验收。

> **2026-08-28(晚)— 全流程复检 + 多模态裁决:datasets v1 GT 7/7 证伪;真实失败模式清单出炉(护栏已解除)**:缓存 1fps 索引复跑生产管线(14 段/25 子 span/HIGH3,复现一致)+ 28 张逐段对照图人工判读(`mvp/benchmark/user_case/contact2/`)。**① v1 GT 全错**:7 条 GT 全部指向字卡("LEVI KANE 2025")/望远镜远景/暗色外观巧合场景(`contact2/gtdatasets_*`),系早期引擎外观误配被固化为 GT;**可靠 GT = 蒙太奇研究验证 GT,已逐条画面确认并固化 `datasets/real/ground_truth_v2.json`**(新增不覆盖)。**② 对 v2 GT 真实召回 ≈5.5-6.5/7**,STATE 原"GT 6/7"口径对 v2 成立。**③ 管线质量**:r1/r2/r5/r7/r10 真匹配(r2 望远镜水塔=跨呈现形式命中),r4/r9 蒙太奇子 span 基本正确;**残留失败 = r13 TikTok 片尾 logo↔电影片尾版权卡 HIGH 0.95(自信答错)+ 2 个外观巧合假阳性子 span(s4 sub1303、r9 sub3654)+ 编辑侧长块切分粗(s3/s8/s9 13-24s 多镜头)**。**④ 迭代路线(小→大)**:logo/字卡 hard_flag 降档 + 子 span 二级门控(小)→ 编辑侧二次切分 + v2 GT 重标 ConfidenceConfig(中)→ 中心重嵌入/patch-token/场景身份第二信号(大,Phase 21 立项证据=contact2)。报告:`mvp/benchmark/user_case/DIAGNOSIS_RECALL2.md`。

> **2026-08-28 — 召回优化(用户「很多镜头没识别到」)**：量化 12 段/HIGH3/LOW9、failure_reason=0（非 app 吞段，是**粗段内子镜头没细拆**；总子 span 仅 18）。主因 = 蒙太奇 `min_frames=3`（丢弃 <1.5s 子镜头）+ `cluster_gap_s=30` + `detect_shots z_thresh=2.0/cut_abs=0.30`（段太粗）。**参数向召回侧优化**（不改语义）：`config.py` 接入 `montage_*` + PipelineConfig 驱动 MontageLocalizer + 降 seg 参数 + `MIN_HITS 2→1/SIM_FLOOR 0.45→0.38` + `retrieval_top_k 20→28` + `index_sampling_fps 0.5→1.0`（密度，用户拍板）。**参数扫描平衡点**：基线 12 段/18 子span/GT6-7；**中间(z=1.7,mf=2,gap=15)=14 段/25 子span/GT 6/7（采纳）**；激进(z=1.4,mf=1,gap=8)=32 段/42 子span/GT4-7（碎片化回归，弃）。**1fps 最终**：14 段/25 子span/HIGH3/GT 6/7（s4 覆盖 0.89→1.00 密度收益）。**多模态核验**（recall_s*.jpg）：新子 span 内容相关（士兵/荒漠/铁丝网/平台上人），GT 门过；但**暗色/崎岖内容匹配松散**（cover 0.38-0.66，少数 0.88）= 特征层上限（CLS 难精判别暗色同类内容）。**特征上限未变**，需第二信号/更好特征=独立研究。后端 133 测试全过。重打包免安装 exe。

> **2026-08-27 — 原始 span 宽度兜底（用户实测「1s 镜头→30s 原片」修复）**：定位置片区宽度远超编辑镜头（[8] 1.5s→22s ratio 14.7x、[11] 3.5s→30s、蒙太奇主 span 34-84s）。修复：`finloc_window` 新增 **`tight_span`**（cs 覆盖峰值为中心、宽度≤`max_orig_span_s`(15s)）+ pipeline 单段 `original` 用 tight_span + `app._bound_span` 比例夹紧 + **蒙太奇子 span 不夹紧**（保 GT recall 6/7，夹紧会退化到 s2/s3/s4 全 MISS）。**重打包免安装 exe + 打包 backend.exe E2E 实测**：12 段全部原片区 ≤15s（蒙太奇主 span 均 15s、[8] 4s、[11] 9.5s、[1] 6s、[5]/[10] 不变），ratio 0.5-4.0（原最高 14.7x），蒙太奇段诚实 LOW、干净段 HIGH 守住。后端 133 测试全过（+`test_tight_span_width_clamped`）。详 DECISIONS「原始 span 宽度兜底」。

> **2026-08-27 — 打包免安装 exe + 实测 + 安装器移除**：应需求重新打包产品 exe 并实测。**① 安装器（NSIS Setup）已从项目删除**（用户拍板）：`electron-builder.json` win.target→`dir` 去掉 nsis 块、`build-release.ps1` →`--win dir`、删除 `release/Video Locator Setup 0.1.0.exe(+.blockmap)`、`docs/DEPLOYMENT.md`/`RELEASE_TEST.md` 改为免安装版。**② 全流程重打包**：`npm run build`→`compile:electron`→`build_backend.py`(PyInstaller, backend.exe 22:07, 815MiB)→`electron-builder --win dir`，产 `mvp/ui/release/win-unpacked/Video Locator.exe`(22:08, 190MB, 含 resources/app+backend+models)。**③ 打包引擎 E2E 实测**：启动打包 `backend.exe`→`/api/health` 200→`POST /api/index`(2.mkv, directml/amd, 3834 帧, 复用缓存)→`POST /api/tasks/analyze`(1.mp4)→completed progress 100→**12 段/MEDIUM1/LOW6/HIGH5/unresolved=0**，每段区间/置信/montage 与诊断复现完全一致（忠实实测出「对不上」的蒙太奇丢子镜头行为）。

> **2026-08-27 — 蒙太奇多段定位已接入产品 engine + 打包 exe 实测**（用户拍板「接入」）：真实案例定位「编辑片有、定位结果没有」= 算法限制（蒙太奇段 finloc 单 run 丢子镜头 + 相似外观误配 + 置信占位未标定）。新方向「蒙太奇多段定位」（query-axis 聚类+门控+每簇 merged span 子 span）研究 GT recall 2/7→86%、子 span 精度≈94%、硬化成可测模块。**已产品化接入**：`domain.Result` + `original_segments`（向后兼容，`original` 主 span 不变）+ 运行时模块 `engine/localization/montage_localize.py` + `app._apply_montage`（蒙太奇段 → 多子 span + `montage_flag` + 诚实降 LOW）+ 前端 `InspectorPanel` 显示多段。**打包免安装 exe 重打 + E2E 实测**：12 段 LOW9/HIGH3（蒙太奇段全诚实 LOW，仅 [5][10][11] 干净段 HIGH），蒙太奇段带 `original_segments`（[0]救回持枪子镜头/[2]5段/[7]3段）——「对不上」「自信答错」双修复生效。后端测试 132（+`test_montage_multi_span`）+ 前端 vitest 67 全过。详 DECISIONS.md「蒙太奇多段定位产品化接入」+ FINDINGS.md。**产品落地（安装器已删，免安装 exe）**。

1. **H4 Windows NVIDIA / CUDA POC（PENDING）——H4-0 环境检查已完成 = `H4_GPU_RUNNER_UNAVAILABLE`**：GitHub Actions 无可用 Windows GPU hosted runner（个人公共仓库 + 官方 GPU runner 对开源项目不开放且无 Windows 变体；本机 GPU = AMD RX 6750 GRE、torch 为 CPU build，无 NVIDIA）。**POC 无法推进**，需用户提供具备 NVIDIA GPU 的环境（自托管 Linux runner / 云或本地 NVIDIA 机器）+ 对应计费权限。届时按：能力探测 → `CUDABackend` 一致性验证（与 CPU 对比，cos>0.999 量级）→ 性能基准；不支持则如实标记 `H4_CUDA_BLOCKED` 继续 CPU。CUDA 不进当前实施阶段。
2. **UI 已转向（2026-08-26 用户重定向 + 方案批准）→ Vue3 + TypeScript + Electron 桌面工作台**，取代原「第 8 项 PySide6 UI」。**Stage 1 初始代码已交付 = `mvp/ui/`（Vue3+TS+Vite+Pinia+router+Electron 壳）**：自定义 Fluent 组件库（自研；无 winitonweb/GPL，商业闭源安全）+ Codex 三栏布局 + `ServiceAPI` 接口（Index/Analysis/Result 三组，一对一映射 `SourceLocatorService`）+ `MockServiceAdapter`（零后端跑通）+ `HttpServiceAdapter`（预留 `/api` 端点 + WS 进度）+ `services/types.ts` 严格镜像后端 JSON（`edited_segment{start,end}`/`original{candidate_start,candidate_end}` 不对称键名、`confidence` 顶层拍平、`manual_override` 门控 `auto_result`）+ 6 页面 + 设计文档（01-arch/02-design/03-components/README/DESKTOP）+ `mvp/api/README.md` 端点契约。**诚实验证**：typecheck（渲染+桌面）、vite build、mock 端到端（Run Analysis→进度→自动跳 Results→8 结果含 Montage/Manual/Unresolved + 对比播放器 + Inspector 详情）、mock 契约测试（7 阶段全序 + 字段形状 + 取消语义）、主题 token 实测（confidence 三色徽章 **不显示百分比**）。
3. **前端接真实后端**：`HttpServiceAdapter` 已从 Mock 切真实 FastAPI（2026-08-26，见 Current Implementation/Current Decision）——`mvp/ui/` 完成 Mock/HTTP 双模式（`VITE_BACKEND_MODE`）、Backend Connection 三态检测（`GET /api/health`）、统一错误处理（`BackendUnavailableError`/500 detail）、vitest 14 项；浏览器实测 Home 侧栏 Connection=Connected。**已完成**：进度 WS + 取消 + 预览 `extracted_path`（2026-08-27）。**下一阶段**：Electron 打包产物 + 真机验收 → 性能基准（10/60/120min）+ Confidence 标定。

## Completed

- **2026-08-27 — 结果页剪辑黑屏/进度条不能滑/预览手动 修复（+导出清理预览）**：根因——① 剪辑黑屏：`GET /api/preview/edited` 原始编辑视频原样返回**无转码**（原片预览走 `extract_clip` 重编码 libx264 MP4 能播；编辑视频 MKV/HEVC，Chromium `<video>` 黑屏）；② 进度条纯摆设：`VideoPlayer.vp__bar` 无 seek 事件（样式 cursor:pointer 却不可拖）且 `VideoComparisonPlayer` 剪辑播放器只绑 `@tick` 未绑 `@seek`；③ 预览手动：`generatePreview` 只在 InspectorPanel 按钮调，`setBatch`/`select` 不自动，原片预览初始 null 占位。**修复**：编辑预览改「抽 `edited_segment` 区间转码 H.264 MP4」（复用 `POST /api/preview`/`PreviewService.extract_segment`，弃用 `/api/preview/edited`）+ `results.ts` 加 `generateEditedPreview`；`setBatch`/`select`/`markManualOverride` **自动生成编辑+原片预览**；`VideoPlayer.vp__bar` 加 seek + `VideoComparisonPlayer` 两侧绑 `@seek` + 时间轴统一区间 local 0..width 1:1 同步；**`PreviewService.cleanup()` + `/api/export` 导出成功后清空 `<app-data>/previews/`**（导出即弃预览，不占磁盘）。**验证**（打包 exe）：`POST /api/preview {HEVC/MKV 编辑视频,区间}`→`200`、`GET media`→`200 video/mp4`、`ffprobe=h264`（黑屏修复）；`/api/export` 后 `previews` 清空（Before:[3文件] After:[]）、导出 results.json 200；前端 `test` 67 + `test:mock`、后端 `unittest` 51 全绿。交付 `mvp/ui/release/win-unpacked/Video Locator.exe`（20:35）。更新 `docs/DEPLOYMENT_HANDOFF.md`。
- **2026-08-27 — 构建索引报「cannot reach backend: fetch failed」修复（health 冷启动容错）**：根因——后端 PyInstaller 冷启动慢（加载 torch/onnx，实测 ~20s，慢机器 >30s），而 Electron `waitForHealth` 只等 30s；后端未 ready 时点构建索引 → 连不上 → `fetch failed`。间接依据：后端本身能正常建索引（300s 源片 150 帧 14.8s 完成、directml），非构建崩溃。修复：`electron/backend/config.ts` `HEALTH_TIMEOUT_MS` 30_000→**90_000**（`waitForHealth` 成功后才 `createWindow`）。验证：启动打包 exe 后端 health 200；经 renderer→后端桥 `POST /api/index` → `200 completed/150帧/directml·amd`（fetch 通路正常）；前端 `test` 67（config.test 更新 90_000）+ 后端 `unittest` 51 全绿。更新 `docs/DEPLOYMENT_HANDOFF.md`（§3.7）。
- **2026-08-27 — 特征提取「转圈」= 慢+无进度显示 修复（含快速诊断）**：诊断确认非真卡——特征提取（编辑片 embed）阶段 progress 固定 30%（阶段级粗进度）+ 无「已处理 N/M 帧」，编辑片越长 embed 越久、进度条停在 30 不动、看着像卡死（100s 剪辑 embed 实测 14s 就 completed）。修复：后端 `analyze_edited_video` 编辑片 embed **分批+每批上报** `EDITED_FEATURE_EXTRACTION current=N total=M message="特征提取 N/M 帧"`；worker 透传 `ev.message` 至 task；`Task.to_dict()` 加 `message`；前端 `TaskJson` 加 `message` + 轮询显示；worker 对 embedding 阶段用真实帧数把 progress **从 10% 平滑到 30%**。**踩坑修复**：build_backend 必须 cwd=benchmark 根`backend.spec` 相对 pathex（mvp/…）——cwd 在 `mvp/ui` 时打包的 `backend.exe` 缺 `mvp` 包（`run_backend: ModuleNotFoundError: No module named 'mvp'`→exe 启动 health 超时），今回退到旧 backend 才复现；已重打包 + 文档记录。**验证**：打包 exe 后端启动正常（health 200、`backend selected=directml`），特征提取阶段 progress `13→16→18→21→24→28`、msg「特征提取 32/200→…→176/200 帧」→ completed；后端 `unittest` 51、前端 `test` 67 + `test:mock` 全绿。更新 `docs/DEPLOYMENT_HANDOFF.md`（§3.6 + §4 重新打包 cwd 注意）。
- **2026-08-27 — 分析「一直转圈」+ 取消无效 + 异步导出 no_results 修复**：根因——① **转圈**：打包 exe Electron 主进程无 `globalThis.WebSocket`（Electron 34 / Node 20）→ 进度 WS 代理禁用（日志 `global WebSocket unavailable`），前端 `subscribeProgress`（WS）收不到终态帧 → `runLocate` Promise 永挂起；② **取消无效**：同一 WS 原因前端无反馈，且 `analyze_edited_video` 的编辑片 `embed_frames` 一次性不查取消；③ **导出 no_results**：`/api/export` 用 `ctx.current_batch`（仅同步 `/api/results` 写），异步 `/api/tasks/analyze` worker 不写。修复：前端 `analysis.runLocate` 改 **HTTP 轮询 `GET /api/tasks/{id}` 至终态**（绕开 WS，任何环境可靠）+ 取消走 `cancelTask`；后端 `analyze_edited_video` embed 改**分批+每批 `_check_cancel`**；新增 `SourceLocatorService.last_result_batch()`，`/api/export` 用 `ctx.current_batch or ctx.service.last_result_batch()`。**验证**：源码 TestClient + 打包 exe HTTP 全链路——`/api/index` completed/directml/15帧 → `/api/index/status` VALID → `/api/tasks/analyze` 轮询 completed（结果 [HIGH, LOW]）→ `/api/export` 200（落盘 results.json）→ `cancel` cancelled/requested；前端 `test` 67 + `test:mock`、后端 `unittest` 51 全绿。交付 `mvp/ui/release/win-unpacked/Video Locator.exe` 更新。更新 `docs/DEPLOYMENT_HANDOFF.md`。
- **2026-08-27 — 构建索引后「缺失」修复（源片绝对路径 + 真实状态端点 + 明确错误）**：根因——项目页源片存**裸文件名**（如 `Dune.mkv`，旧 exe 拖拽未生效且无「打开文件」入口）→ 后端按工作目录解析不到 → `ffprobe` 找不到 → `/api/index` 失败 → 前端**无真实索引状态查询端点**一直显示「缺失」。修复：① 前端源片/剪辑区加「选择源片文件/选择文件」（Electron `dialog.showOpenDialog`，`window.desktop.openFile`）+ 裸名警告（`ProjectDetailPage.vue`）；② 后端 `build_original_index` 前置校验（路径非绝对/不存在 → 提前抛明确中文错误「源片路径无效或不存在…」）+ 异常逃逸加固（except 扩含 `DeviceError`/`OSError`/兜底 `Exception`，日志有 `index failed` 前缀）；③ 后端新增 `GET /api/index/status`（透传 `FeatureStore.validate_index` — VALID/INVALID/MISSING + reason）+ 前端 `getIndexStatus` 改真实查询（不再用 `lastIndexStatus ?? unknownIndexStatus()` 恒 MISSING）；④ 索引徽章「构建中」态 + 失败显示后端具体原因。**验证**：后端 `unittest` 51（+`test_index_status` 4）+ 前端 `test` 67（+index status 2）+ `test:mock` + `typecheck`/`compile:electron` 全绿；后端冒烟：裸名 status→`INVALID/source video missing`、裸名构建→`500+明确中文错误`（不再 ffprobe 崩，`index_status` 前置校验避免无谓 ffprobe）；打包 exe CDP 端到端：`openFile:function` + status/构建裸名明确错误。交付 `mvp/ui/release/win-unpacked/Video Locator.exe`（含新 backend+dist+preload）。更新 `docs/DEPLOYMENT_HANDOFF.md`（源片须绝对路径 + 状态端点 + `test_index_status`）。**范围**：未改算法/置信/GT/Result 契约；`FeatureStore.validate_index` 未改。
- **2026-08-27 — GPU/CPU 真切换 + 拖拽修复（桌面部署交接）**：① **GPU/CPU 切换真正生效**——后端新增 `infrastructure/settings_repo.py`（持久化 `device.preferred` 到 `<data_dir>/settings.json`）+ `SourceLocatorService.device_preference/set_device_preference/device_settings` + 新路由 `routes/settings.py` GET|POST `/api/settings/device`（POST 校验 auto/cpu/directml，非法 400）；前端 `DeviceName` 补 `'directml'`、`HttpServiceAdapter.DEVICE_NAMES` 加 `'directml'`（修 `normDeviceName` 把 directml clamp 成 cpu 的 bug——即使 GPU 真工作 UI 也显示 CPU）+ `getDeviceSettings/setDeviceSettings`；Settings 页由只读写改**真三档下拉**（智能/AMD GPU/CPU）+ 保存生效 + 「期望 vs 实际/回退」提示。② **拖拽修复**——根因 `window.desktop/backend` 均 undefined（Electron sandboxed preload 不能 require 相对模块 `./backend/bridge` → preload 加载失败 → contextBridge 不执行）；修复 `scripts/build-preload.cjs` 用 esbuild 把 preload+bridge bundle 成单文件（保留 `require('electron')`），`compile:electron` 追加。③ **CPU 打包修正**——CPU 走 torch 原生需 `.pth`，bundle 只带 ONNX → 打包 exe 切 CPU 崩；把 `dinov2_vits14_pretrain.pth` 内置 `resources/models/dinov2_vits14` + main.ts 设 `SVL_DINOV2_WEIGHTS`。④ 打包用 **`asar:false`**（`--dir` 下 `addWinAsarIntegrity` 无条件写 exe 撞 Defender 锁；`signAndEditExecutable:false` 在 `--dir` 不生效）。**验证**：后端 `unittest` 47 + 前端 `test` 65 + `test:mock` + `typecheck`/`typecheck:desktop` + `compile:electron` 全绿；打包 exe CDP 端到端：auto→directml/amd、POST cpu→cpu、POST directml→amd、非法 gpu→400、`window.desktop.getPathForFile:function`、Settings 真下拉显示 AMD GPU、后端日志 `selected=directml type=amd`。交付 `mvp/ui/release/win-unpacked/Video Locator.exe`（免安装，asar:false，含新 backend+ONNX+pth）。**安装器暂停（用户拍板）**，只交付免安装 exe；更新 `docs/DEPLOYMENT_HANDOFF.md`。**范围**：未改 `mvp/src` 算法/置信语义/Result 契约；device 层新增选择端点（符合 `DeviceBackend` 可扩展）。
- **2026-08-27 — Desktop 交付 + GPU/拖拽修复（安装器开发已暂停）**：**可直接运行 exe 已交付并实测**：`mvp/ui/release/win-unpacked/Video Locator.exe`（免安装，含 `resources/backend/backend.exe`+`resources/models/dinov2_cls_384`(85M ONNX)+ffmpeg/ffprobe+`app.asar`）。实测：双击自启后端（日志 `backend starting→Started server process→api server started→GET /api/health 200→health connected: backend running`）+ 真实 `/api/index` `device_type: amd`（**GPU 用上了**）+ 关闭后 8765 释放、0 残留。**① GPU→CPU 根因修复**：Phase 4 重定向 `SVL_DATA_DIR` 后模型仍在旧目录（`%LOCALAPPDATA%/SourceVideoLocator/models`）→ 后端在新目录找不到 DirectML ONNX → `DirectML ONNX model asset missing→CPU fallback`。修复：模型内置 `resources/models`（extraResources）+ main.ts 设 `SVL_DML_MODEL=<resourcesPath>/models/.../dinov2_cls_384.onnx`；打包后端+SVL_DML_MODEL+空数据目录实测 `directml`。**Settings「设备」改只读显示实际后端**（原「首选后端」下拉是假控件，选了不生效=“选GPU却回CPU”混淆来源）。**② 拖拽导入修复**：项目页源片/剪辑 drop 区实现 HTML5 拖拽，preload 用 `webUtils.getPathForFile(file)` 取绝对路径入列。**安装器**：`Video Locator Setup 0.1.0.exe`(402MB,含模型,未签名,`signAndEditExecutable:false` 绕 Defender 锁) 仍可生成；**按用户要求安装器进一步开发已暂停**。**交接文档** `docs/DEPLOYMENT_HANDOFF.md`。**验证**：后端 `unittest` 43 + 前端 `test` 63 + Electron `compile:electron`/`typecheck:desktop`/35 测试 + `test:mock` 全绿；打包 exe 端到端自启后端+GPU+干净退出实测过。**范围**：未改 `mvp/src`/算法/API/Result；未做 license/device binding/DRM。
- **2026-08-27 — Desktop Deployment（Electron Builder + NSIS 安装器 + 首次启动初始化）完成**：**安装器已生成** `mvp/ui/release/Video Locator Setup 0.1.0.exe`（321MB，含 845MB 后端 bundle 压缩）+ `win-unpacked/`（`resources/backend/backend.exe`+`_internal`+`ffmpeg/ffprobe` 齐全，`resources/app.asar`）。**关键**：electron-builder 首次在 `addWinAsarIntegrity`（对刚写出 exe 写回）报 Windows `UNKNOWN` 锁——经 `win.signAndEditExecutable: false`（跳过 exe 改写，未签名 MVP 可接受）解决；electron/nsis 二进制用 npmmirror 镜像绕开 GitHub 443。**配置**：`electron-builder.json`（appId `com.videolocator.desktop`、productName `Video Locator`、NSIS `oneClick:false`+\`perMachine\`+\`allowToChangeInstallationDirectory\`+\`createDesktopShortcut\`+\`createStartMenuShortcut\`、`extraResources: resources/backend→backend`、文件 `dist/**/*`+`out/electron/**/*`）。**Phase 4 用户目录隔离**：main.ts 设 `SVL_DATA_DIR/SVL_LOG_DIR`=`<userData>/data`+`<userData>/logs`（后端 `paths.app_data_dir`/`logging` 读取，不改 mvp/src）——运行时数据全在 `%APPDATA%\Video Locator\` 不写 Program Files；`app:paths`/`app:openPath` IPC + preload `getPaths/openPath`。**Phase 5 首次启动初始化**：session store `initApp()`（INITIALIZING→CHECKING_MODEL→READY/FAILED + 进度%）+ App.vue 初始化遮罩（「正在初始化 AI 引擎 N%」「请不要关闭程序」+ 失败重试）。**Phase 6 后端错误处理**：main.ts 原生对话框「AI 服务启动失败」+ 原因/日志位置 + 按钮「打开日志目录/重新启动/复制错误信息」。**Phase 7 发布脚本** `mvp/ui/scripts/build-release.ps1`（build→compile:electron→build backend→electron-builder，用镜像）。**Phase 8** `docs/DEPLOYMENT.md`（安装/首次启动/用户目录/故障排查/卸载）。**验证**：安装器构建成功 + win-unpacked 结构正确；前端 `test` 63 + `typecheck` + `test:mock`、Electron `compile:electron` + `typecheck:desktop` + 35 测试、后端 `unittest` 43 全绿；清理了 smoke 残留 `backend.exe`（8765 释放）与 `resources/backend/logs`。**范围**：未改 `mvp/src`/算法/API 契约/Result；未做 license/device binding/DRM/激活/鉴权。**已知**：安装器未签名（SmartScreen 提示「未知发布者」）；`win-unpacked` 用默认 electron 图标；GUI 安装/卸载/真机运行是手动步（文档记录，未自动化）。
- **2026-08-27 — Release Validation 收尾 + UI 中文化**：**① UI 全部中文化**（`mvp/ui/src` 所有页面/组件/导航/按钮/徽章/后台状态/Inspector/进度管线/命令面板/置信度高⇄中⇄低 + Electron 对话框「后端启动失败/后端离线」；保留技术专名 CPU/AMD GPU/DINOv2/HIGH 数据字段；`test` 63 项+`build`+`typecheck`+`typecheck:desktop`+`compile:electron`+`test:mock` 全绿）。**② Bundle 瘦身**：PyInstaller `excludes` 对钩子收集的 scipy/pandas/PIL/onnx **不生效**→改在 `mvp/scripts/build_backend.py` 装配后 `_prune_unused()` 删除；`backend.exe` 实测 `_internal` 717M→617M、资源目录 **946M→845M（省约 101M）**；剪枝后用打包后端跑**真实 DINOv2 索引**（`frames=15` directml）+ `/api/health` 200 + `/api/preview` 200 **验证无算法影响**。**③ 发布基准**（`mvp/benchmark/release/`）：合成源 directml，**索引 10min 23.96s / 60min 135.22s / 120min 270.68s**（~13fps 近线性），分析 total≈3.35s（embedding≈3.22s ONNX 冷启动），预览 ffmpeg≈0.4s→100KB，→ `result/benchmark_result.json`+`release_smoke_result.json`（index/analysis/preview/export 全 success）。**④ 文档**：`bundle_report.md`（per-package 组成）、`bundle_optimization_report.md`（Before 946M/After 845M/Removed/Risk 实测）、`docs/RELEASE_TEST.md`（安装/启动/后端生命周期/管线/性能/体积/限制）、`mvp/ui/tests/e2e/desktop_smoke_test.md`（安装器步骤待授权，后端层实测证据）、`tools/bundle_report.py`、benchmark `README.md`。**验证**：后端 `unittest` 43 项 + 前端 63 项全绿。**范围**：未改 `mvp/src`/算法/API 契约/Vue 页面流程/Electron 架构；未做 installer/license/device binding/DRM。**已知**：bundle 主体是推理依赖（torch/opencv/onnxruntime/numpy≈560M）固有成本；内存/CPU 未插桩（无 psutil）；源为合成片；进度阶段级。
- **2026-08-27 — Electron 生产打包 + Python 后端 Bundle 完成（PyInstaller onedir）**：**后端打包**：新增 `mvp/scripts/run_backend.py`（PyInstaller 入口：`uvicorn.run("mvp.api.main:app", 127.0.0.1:8765)`，宿主/端口可 `SVL_BACKEND_HOST/PORT` 覆盖）+ `mvp/scripts/backend.spec`（onedir、`console=True`、pathex=`mvp`+`mvp/src`、collect_submodules 收 mvp.api + app/domain/engine/device/infrastructure/media、隐式 uvicorn/anyio/cffi 子模块、excludes tkinter/matplotlib/IPython/torchvision/PyQt/PySide）+ `mvp/scripts/build_backend.py`（编排：PyInstaller → 装配 `mvp/ui/resources/backend`，复制 `tools/ffmpeg.exe` + venv static_ffmpeg `ffprobe.exe`）。产物 `resources/backend` = **923 MiB**（`backend.exe` 52MB + `_internal/` + `ffmpeg.exe`/`ffprobe.exe`）。**Electron 接线**：`electron/backend/config.ts` `BackendConfig` 加 `spawnEnv`，`prodBackendConfig` 改为 `pythonExecutable=<resources>/backend/backend.exe` + `uvicornArgs=[]`（exe 自启 uvicorn）+ `spawnEnv={MEDIA_FFMPEG,MEDIA_FFPROBE}` 指向打包内 ffmpeg/ffprobe；`process.ts` `buildSpawnCommand` 返回合并 `env`、`spawnBackendProcess` 传 `env`+`windowsHide:true`；`config.test.ts` 更新 prod 断言。**验证全绿**：① 打包后端 `backend.exe` 启动 → `/api/health` 200 `{status:ok}`（无缺失导入，session 日志正常）；② **打包后端跑真实 Preview API**：POST `/api/preview` 抽 `originals/2.mkv`[100,110] → `2__100-110.mp4` 3.5MB 落 `app-data/previews`，GET media `Range bytes=0-99` → 206 `content-range: bytes 0-99/3519519` `video/mp4`（证明 `MEDIA_FFMPEG/FFPROBE` env → 内置二进制生效，无需改 mvp/src）；③ 前端 `npm run test` **63 项**（渲染 28 + Electron 35）+ `compile:electron`/`typecheck:desktop` 全过。**范围护栏**：未改 `mvp/src`；未制作安装器；未做 license/device binding/DRM。**文档**：`resources/backend/README.md` 重写（onedir 布局 + MEDIA env 机制 + 构建命令）、`DESKTOP.md` prod spawn 描述更新、`resources/backend/.gitignore` + `mvp/scripts/.gitignore`（排除构建产物）。**已知/待办**：bundle 偏大（torch 拖入 scipy/pandas/PIL，923MB）；模型权重仍运行时解析 app-data（首次可能下载）；真机验收（打包后 Electron 冒烟）待 `resources/backend` 入包 + 安装器授权后。**下一阶段**：Install + License + Device Binding（用户要求时）。
- **2026-08-27 — Video Preview（`extracted_path` 抽取 + 前端接真实视频）完成**：**后端** 新增 `mvp/api/services/preview_service.py`（`PreviewService.extract_segment(video_path, start, end)` -> `PreviewResult{path,duration}`，复用 `ctx.service.ffmpeg.extract_clip` 精确重编码；产物落 `<app-data>/previews`，可 `preview_dir` 覆盖；失败统一 `PreviewError`(LocatorError 子类) -> 500 + `logger.exception`）+ `mvp/api/routes/preview.py`（**POST** `/api/preview {original_path,start,end}` -> `{path,duration}`；**GET** `/api/preview/media/{filename}` 用 `FileResponse` 服务抽取片段（Starlette 原生 HTTP Range 206，HTML5 video 可拖动）；**GET** `/api/preview/edited` 服务会话编辑视频。**安全**：media 只取 `path.name` 且限 `preview_dir` 内防穿越；edited 只服务会话记录一份，不收客户端路径）。`schemas.PreviewRequest` + `AppContext.preview_service`(惰性) + app.py 挂 preview router。**前端** `ServiceAPI` 加 `previewResult(originalPath,start,end):Promise<string>`(Http=POST /api/preview 取 path 拼 `/api/preview/media/<name>` URL / Mock=示例视频地址) + `getEditedVideoUrl()`(Http=`/api/preview/edited`/Mock=示例)；`results` store 加 `previewEditedSrc/previewOriginalSrc/previewLoading/previewError` + `generatePreview()`(用选中 result 的 original span 调 previewResult) + setBatch 里自动设 editedSrc、select/markManualOverride 清旧 originalSrc；`InspectorPanel` 加「Generate Preview」按钮+状态；`ResultsPage` 把 `editedSrc/originalSrc` 传给 `VideoComparisonPlayer`；`VideoComparisonPlayer` 改 `ogCurrent` 为抽取片段本地 0..width 时间轴（Edited 仍播全片绝对 seek，比例同步）+ 切换 result 时 `watch` 重置 playhead。**验证全绿**：后端 api unittest **43 项**(原26+preview 路由 9+preview_service 8) 通过；真实 ffmpeg 端到端冒烟（`2.mkv` 抽取 15s -> `2__100-115.mp4` 4.65MB -> GET media 200 video/mp4 -> Range `bytes=0-99` 206 content-range 正确）；前端 `typecheck`/`build`/`test` **63 项**(原61+preview 2)/`test:mock` 契约 + `typecheck:desktop`/`compile:electron` 全过。**范围护栏**：未改 `mvp/src`/SourceLocatorService/domain/engine/FeatureStore/DeviceBackend/算法；未改 Result schema（`extracted_path` 仍 null 不伪造）；`/api/preview` 独立能力不改 locate/置信度；无 db/cloud/auth；未制作安装器。**下一阶段**：Installer + License + Device Binding（后置）。
- **2026-08-27 — WebSocket 实时进度 + UI Pipeline Binding 完成**：**后端** `mvp/api/tasks/{models,manager}.py` 加进度广播——`Task` 加 `_subscribers`(queue.Queue 列表)+ `subscribe()`(返回队列+初始快照)/`unsubscribe()`/`broadcast_current()`/`_frame()`(progress/completed/failed/cancelled 帧)，状态变更(`mark_running/update_progress/mark_completed/mark_failed/mark_cancelled`)锁内更新后**自动广播**；`TaskManager` 加 `subscribe/unsubscribe/publish` 转发。**新路由** `mvp/api/routes/progress.py` `/ws/progress/{task_id}`：订阅队列 + **后台 reader task** 读客户端 cancel(`{type:"cancel"}`→`TaskManager.cancel`→CancellationToken，不杀线程) + 主协程 `asyncio.to_thread` 轮询队列推帧，终态帧 break（reader+cancel 分离避免并发 receive 取消不稳定）；任务不存在→`{type:"error"}`。`app.py` 挂 progress router（`routes/tasks.py` 占位 ws 已移除）。**`test_progress.py`** 4 项（connect 快照+progress→completed / failed 帧 / cancel→cancelled / unknown task），用 `run_in_background=no-op` + 手动驱动 Task 状态（确定性，不依赖 worker 时序）。**前端** `types.ts` TaskJson/TaskEvent/TaskStatus/TaskStage；`ServiceAPI` 加 `startAnalyzeTask/getTask/cancelTask/subscribeProgress`；`HttpServiceAdapter` 实现（任务 API：POST /api/tasks/analyze|GET /api/tasks/{id}|POST cancel；`subscribeProgress` 开 WS + **disconnect 自动重连一次**）；`MockServiceAdapter` 实现（定时器阶段帧→completed+result）；新 `services/progressSocket.ts`（Electron `window.backend.websocket` override 原生 WebSocket）+ `electron.d.ts` 加 `window.backend.websocket` 类型；**analysis store 改任务式**（startAnalyzeTask→subscribeProgress→completed→getTask 取 result；+cancelTask）；`AnalysisPage` cancel=analysis.cancelTask + elapsed 计时；`ProgressPipeline.vue` 增强显示 current stage/progress%/elapsed + cancel 按钮。**Electron** `bridge.ts` `buildBackendBridge` 加 `websocket`（`BackendIpc` on/send/removeListener）+ `preload.ts` cast 传 ipcRenderer；`main.ts` 加 `backend:ws:open|client-close` IPC handler（**Node global WebSocket** 连 FastAPI `/ws/progress/{task_id}` 转发帧到 renderer webContents）。**验证全绿**：前端 `npm run test` 61 项(原54+新 tasks 6+bridge ws 1)/`typecheck`/`build`/`test:mock`；后端 api unittest 26 项(test_api12+test_tasks10+test_progress4)；真机 uvicorn + `websockets` client 冒烟（create→task_id→WS 收 `failed` 终态帧，证明真实 WS 握手+推送）。**范围护栏**：未改 `mvp/src`/SourceLocatorService/domain/engine/FeatureStore/DeviceBackend/retrieval/localization/confidence；未新增算法进度（阶段级 progress 保留，真实 DINO 逐帧需后续 service hook）；未加 db/cloud/auth；未打包。**已知**：进度为阶段级 coarse；前端 task `progress` 用 0-100（阶段百分比）。
- **2026-08-27 — FastAPI 异步任务基础设施完成（只加 adapter/infrastructure 层）**：`mvp/api/tasks/`（`models.py` Task 模型 + `TaskStatus`(pending/running/completed/failed/cancelled) + `TaskStage`(idle/indexing/segmenting/embedding/retrieval/exporting/finished) + 线程安全更新，`Task.to_dict()` 含 task_id/status/stage/progress/created_at/finished_at/result/error/cancel_requested；`worker.py` `run_worker(task, service)` 后台跑 `SourceLocatorService.locate(edited, original, on_progress, cancel_token)` + `map_progress_stage`(ProgressStage→TaskStage+阶段百分比)，异常→failed、取消(ApplicationError+is_cancelled)→cancelled；`manager.py` `TaskManager(service, run_in_background=可注入)`：submit_analyze/get/cancel，默认 `threading.Thread(daemon)`，测试注入同步 executor 获确定性时序）。**路由** `routes/tasks.py`：`POST /api/tasks/analyze`→`{task_id}`、`GET /api/tasks/{task_id}`→TaskJson、`POST /api/tasks/{task_id}/cancel`→`{status:"cancel_requested"}`。**接线**：`dependencies.AppContext` 加 `task_manager` 字段 + `get_context()` 构造 `TaskManager(service)`（复用同 service 实例）；`schemas.AnalyzeTaskRequest`（edited/original 默认空串→缺字段也走路由 400 bad_request）。**验证**：`test_tasks.py` 10 项 + `test_api.py` 12 项未破坏 + 真机 uvicorn 冒烟（health ok、create→task_id、query→failed 且 `error=IndexError…`）。**范围护栏**：未改 `mvp/src`/SourceLocatorService/domain/engine/FeatureStore/DeviceBackend/算法，未改 Vue/Electron，未动 `mvp/src/app/models` 的 ProgressStage/ProgressEvent/CancellationToken（复用）；现有同步路由 `/api/health|index|analyze|results|export` 保留不动。
- **2026-08-26 — Electron renderer→后端请求桥完成（修复打包 file:// 的 CORS）**：preload 暴露 `window.backend{request,health}`（经 `ipcRenderer.invoke('backend:request')`）→ main `ipcMain.handle('backend:request', createBackendRequestHandler(config))`（**Node fetch** 打到 `http://127.0.0.1:8765{path}`）→ 标准化 `BridgeResponse` 回 renderer；**renderer 从不直接访问 localhost**，故生产 `file://` 页面绕开 FastAPI 桥 CORS。**新增** `electron/backend/bridge.ts`（纯 Node 可测：`buildBackendBridge(ipc)` = preload 可测核心 / `createBackendRequestHandler(config, fetchImpl)` = main handler 可测核心，含安全：**只接受本地绝对路径**禁 scheme/协议相对、方法白名单 GET/POST/PUT/DELETE/PATCH、网络失败→`{network:true}`、5xx→`{detail}`）。**renderer 侧** `src/services/transport.ts`（`createTransport(base)`：检测到 `window.backend`(Electron)→bridgeTransport，否则→fetchTransport；`HttpServiceAdapter` 用 transport 替直接 fetch，错误语义保持 network→`BackendUnavailableError`/非2xx→`Error(detail)`）**+ `src/types/electron.d.ts`**（全局 `Window.backend/desktop` 类型）。**验证全绿**：`npm run test` 54 项（renderer 20 + electron 34；新增 bridge 9 + transport 6）、`typecheck`/`build`/`typecheck:desktop`/`compile:electron` 全过。**范围**：未改 `mvp/src`/SourceLocatorService/FastAPI 业务接口/domain/算法/UI 页面/`HttpServiceAdapter` 契约（只换 transport 实现）；'`window.desktop`' 保留未破坏。**FastAPI CORS 评估=不改**：bridge 下生产 renderer 不触发 CORS；纯浏览器 dev 仍需 `localhost:5173/5174` 放行，桥已配置保持现状。
- **2026-08-26 — Electron Runtime Integration（Python 后端生命周期管理）完成**：`mvp/ui/electron/`（CommonJS 主进程）→ *未打包=dev*（不 spawn，健康探测已有 `uvicorn`）/ *打包=prod*（spawn `resources/backend/python.exe` + 30s 健康轮询）。**新增** `electron/backend/`：`config.ts`（host/port/health path + `prodBackendConfig(resourcesDir)` 打包 python 路径 + `buildUvicornArgs`，不散落 magic string）/ `health.ts`（`createHealthChecker` 注入 fetch/sleep，轮询 `/api/health`，超时抛 `BackendStartError`）/ `process.ts`（`spawnBackendProcess` 注入 spawnFn，`stdio:pipe` 转发 stdout/stderr，`buildSpawnCommand` 纯函数）/ `manager.ts`（`BackendManager`：start/stop/restart/waitForHealth/checkDevBackend + `BackendState` 状态机 + DI 注入 spawner/health/log，测试不起真实 Python）/ `index.ts` 统一导出。**集成** `electron/main.ts`：`resolveMode`=`app.isPackaged`, `bootstrap`=prod→`startBackend()+waitForHealth()`(失败弹 dialog)/dev→`checkDevBackend()`(offline 弹「Backend offline」), dev 默认加载 `localhost:5173`, `before-quit→stopBackend()`(同步 kill 无残留), 保留 contextIsolation+sandbox。**构建修正**：`package.json` `type` module→commonjs + `main`→`out/electron/main.js`（main.ts 用 `__dirname` 去 `import.meta`，匹配 CommonJS emit；electron-builder `files` 已含 `out/electron/**`）；`tsconfig.desktop.build.json` 加 `exclude: electron/**/tests/**`（防编译测试污染 vitest）；`vite.config.ts` 用 `vitest/config` 加 `test.include`(渲染+electron)/`exclude`(out/dist)；`electron-builder.json` 加 `extraResources: resources/backend → backend`；`resources/backend/README.md` 记录打包 python 预期布局；scripts 加 `compile:electron`/`electron:dev`/`build:electron`。**验证全绿**：`npm run typecheck`/`build`/`test`(39 项:渲染 14+electron 25)/`typecheck:desktop`/`compile:electron`/`test:mock` 全过。**范围护栏**：未改 `mvp/src`/SourceLocatorService/FastAPI API/domain/算法/UI 页面逻辑/HttpServiceAdapter 契约。**已知限制**：打包后 renderer 从 `file://` 调后端受桥 CORS 限制（dev vite 5173 无此问题）——属后续 renderer→bridge 阶段，主进程 health check(Node fetch 无 CORS)不受影响。
- **2026-08-26 — 后端统一日志基础设施完成（FastAPI 桥之前）**：`mvp/src/infrastructure/logging.py` 重写为 stderr + **文件双输出**，文件用 **`RotatingFileHandler`（10MB × 5 备份，写 `mvp/logs/video_locator.log`，路径可由 `log_dir`/env `SVL_LOG_DIR` 覆盖）**；**结构化格式** `时间 级别 module=<name> session=<sid> <message>`；**轻量 session_id**（`contextvars` + uuid4 hex8，`new_session_id/set_session_id/get_session_id`，`set_session_id("")` 清空，无数据库）。**接入**：`device/__init__.py` resolve_backend 记录 `backend requested/selected/fallback_reason/unavailable_reason/device`；`app/locator_service.py` 记录 `index started/finished(video,frames,backend,elapsed)`、`index failed`(exception)、`analysis started/segments/embed(frames,fps)`、`analysis failed`(exception)、`locate started/finished(segments,candidates,high/medium/low/unresolved,elapsed)`；`_log.exception` 只加日志、不改异常逻辑。**不动**算法/DINOv2/retrieval/localization/confidence/FeatureStore schema；无 DB/云端上传/UI 日志窗。**`mvp/logs/.gitignore`**(`*.log`) + **`tests/test_logging.py`**（8 项：get_logger/三档写入/RotatingFileHandler 存在(maxBytes,backupCount)/结构化文件含 session/会话 id 语义；hermetic 快照恢复 root handlers 不污染其它模块）。**全套 80 项回归通过**（`get_logger` 用 `__name__`，`configure_logging()` 为 launcher(FastAPI/CLI) 启动钩子，幂等、可切 log_dir）。**未做**：FastAPI 桥、Electron 连接（按要求停在日志基础设施）。
- **2026-08-26 — UI（第 8 项）转向并交付 = Vue3 + TS + Electron 桌面工作台 Stage 1 初始代码**：`mvp/ui/`（Vue3+TS+Vite+Pinia+router+Electron 壳）+ `mvp/api/README.md`（FastAPI 桥端点契约，未实现）。**自研 Fluent 组件库**（`src/components/ui/` Base* + icons.ts + 业务组件 ConfidenceBadge/ResultTable/VideoComparisonPlayer/Timeline/ProgressPipeline/CommandPalette/BackendStatusIndicator/InspectorPanel/ProjectCard）+ **Codex 三栏布局**（sidebar 240 / main 1fr / inspector 320）+ **`ServiceAPI` 抽象**（`MockServiceAdapter` 默认零后端跑通 / `HttpServiceAdapter` 预留 `/api` + WS 进度 + `services/types.ts` 严格镜像 `domain/models.py` JSON：`edited_segment{start,end}`/`original{candidate_start,candidate_end}` 不对称键名、`confidence`/`confidence_score`/`reasons` 顶层拍平、`manual_override` 门控 `manual_timestamp`/`auto_result`）+ **6 页面**（Home/Projects/MediaLibrary/Analysis/Results/Settings）+ 设计文档（`ui/docs/01-architecture|02-design|03-components|README|DESKTOP`）。**验证**：typecheck（渲染 `tsconfig.app` + 桌面 `tsconfig.desktop`）零错、`vite build` 通过、Mock 端到端（AnalysisPage 点 Run → 6 步 pipeline 进度 → 自动跳 Results → 8 结果含 Montage/Manual/Unresolved + 视频对比 + Inspector 详情）、`npm run test:mock` 契约测试（7 阶段全序 `INDEX_BUILD→…→EXPORT` + 字段形状 + 取消语义）、主题 token 实测（confidence HIGH=`rgb(108,203,95)` 等三色徽章，**不显示百分比**）。**口径**：Vue3 推翻文档所锁 PySide6 属用户拍板的有意转向；持久化以已实现代码为准（JSON 单文件批 `*.results.json` + `schema_version`，不用 SQLite）；`confidence_score` 为工程分**非概率**；`device_name` 仅展示不作 UI 分支；研究护栏全守（不改算法/相似度/检索/定位/置信语义、无 GT、不 sweep）。**延后**：`mvp/api/` 真实实现（`VITE_API_BASE` 切换）、Electron 二进制/打包、PySide6 清理、过时研究文档重写。**注**：npm 11 `allow-scripts` 安全门拦 electron/esbuild/vue-demi postinstall——渲染层不受影响（esbuild 平台二进制走 npm 包、已证 `npm run build` 通过），electron 需 `npm approve-scripts electron` + `ELECTRON_MIRROR` 才启用二进制（见 `ui/docs/DESKTOP.md`）。
- **2026-08-25 — `media/ffmpeg`（FFmpegIO）完成**：`mvp/src/media/ffmpeg/`（FFmpegIO + ffprobe + subprocess 封装）。禁用 cv2.CAP_PROP_POS_MSEC，改走 ffmpeg/ffprobe subprocess；`metadata`(format.duration 处理 MKV N/A)/`iter_frames`(流式 rawvideo pipe, 时间基采样, 不缩放)/`grab_frame`/`extract_clip`(精确重编码)/`hash_file`。冒烟 23 项 + unittest 6 项全过，真实 2h08m HEVC MKV 随机定位与帧精度验证通过。
- **2026-08-25 — `domain` + `infrastructure` 完成**：`mvp/src/domain/`（纯数据：TimeSpan/Candidate/Confidence/Alternative/Result + IndexMeta/ExtractorConfig/IndexValidation + 枚举，`to_dict()` 输出产品契约 JSON）+ `mvp/src/infrastructure/`（errors 模型 `LocatorError`/`ConfigError`、logging、paths 数据/index/export 目录、config `AppConfig`/`MediaConfig`/`PipelineConfig`/`ConfidenceConfig` + JSON 覆盖）。`MediaError` 已改为继承 `LocatorError` 统一错误模型。unittest 12 项 + media 复测全过。
- **2026-08-25 — `device/CPUBackend` + `engine/feature_store/FeatureStore` 完成**：`mvp/src/device/`（`DeviceBackend` Protocol + 冻结 DINOv2 backbone 提取 `dinov2_model.py` + `CPUBackend`(无 CUDA, 线程可配, ctypes 内存) + `pick_best_available`(H1 恒 CPU, 预留 H2/H3)）+ `mvp/src/engine/feature_store/`（`FeatureStore` create/load/validate/invalidate/get_metadata/delete, 按 INDEX_SPEC 落盘 `index.json`+`features.npy`+`times.npy`, 失效判定 size→duration→model/dim/fps/version→hash 硬检, `.stale` 隔离; `IndexBundle`）。device smoke 25 项 + FeatureStore/CPUBackend unittest 4 项全过; DINOv2 CPU 真推理 L2 norm=1.0。**`engine/segment` 当时未实现（属 roadmap 第 6 项，已于 2026-08-25 完成，见下）**。
- **2026-08-25 — `engine/retrieval` + `engine/clustering` + `engine/ranking` 完成**：`mvp/src/engine/common/similarity.py`(cosine REUSE)+`retrieval/`(retrieve_hits, 批量 Top-K K=20, orig_times[top_idx])+`clustering/`(_cluster_continuous 连续性感知 10/15/60s + _merged_ok + _bridges, build_candidates→domain.Candidate)+`ranking/`(v2_score = mean*sqrt(hit)*(0.5+0.5*cons)*n_reps^alpha alpha=0.5, rank_candidates)+`engine/candidates.py`(produce_candidates 编排)。**删除了 GT/benchmark/gid/实验路径/work 依赖**；`_dp_path`/`temporal_align`/`localize`/`edited_window_feats`/`_recall`(GT 依赖) 均 RESEARCH_ONLY 未进 runtime；`retrieval_top_k` config 占位 100 改为冻结 20；domain.Candidate 增加 `scene_div`(montage 指示)。retrieval smoke 6 项 + unittest 7 项全过。**注意：合成图案场景 CLS 易混淆，v2_score 会把非 GT 宽窗排前——冻结公式在真实影片数据(研究已验证)表现正常，属已知数据/模型属性**。
- **2026-08-25 — `engine/localization`(finloc longest_run + montage 检测分支) + `engine/confidence/ConfidenceEngine` 完成**：`mvp/src/engine/localization/`(finloc_window=per-orig max-over-query coverage + 3帧平滑 + mask(thresh=0.4 冻结) + longest_run→精确 span + multi_island 结构信号; pipeline.localize_segment=候选→finloc 最佳→回填 best_cover→confidence→RefinedSegment; montage/无 span/run 过短→original 退化候选窗范围)+`engine/confidence/confidence.py`(ConfidenceEngine.assess: rank/margin/n_reps/qcov/consistency/sim_std/scene_div + best_cover/span_stability/coverage_quality/multi_island→score+三档+reasons+hard_flags+montage_flag+alternatives)。**置信度权重/阈值全部为未标定占位**(CONFIDENCE_DESIGN §6)，已纳入 `ConfidenceConfig`(infrastructure/config.py, JSON 可覆盖)供 H1 标定；`engine/localization/__init__` 不 import 编排避免与 confidence 循环导入。禁 per-query argmax/voting/cut-aware/temporal align/改 similarity·ranking。researchnotes：synthetic v2_score 混淆使 smoke 的 top 候选非真值(已知属性, 见上文)；真值定位由 deterministic unittest 覆盖。localization/confidence unittest 10 项 + smoke 9 项全过；全套 46+ 项回归通过。
- **2026-08-25 — `engine/segment`（无 GT 编辑侧查询单元切分）完成**：`mvp/src/engine/segment/`（`ShotSegment` 模型 [span + feats + times 切片, 直接喂 produce_candidates/localize_segment] + `detect_shots` + `adjacent_distances` + `_merge_short`）。**算法**：相邻帧余弦距离 `d=1-cos`（复用冻结 cosine 语义, O(N) 点积）→ **局部 z-score 判别**（`z=(s-局部均值)/max(局部σ,floor)`，边界需 `z>=2` 且 `s>=cut_abs=0.30`——修正了"相对基线在 max() 下是死代码"缺陷，也正对应研究"绝对阈值 d 不可靠"证伪结论）+ **默认平滑窗=1**（不平滑保留单帧 cut 锐利尖峰，分析发现平滑窗 3 会拉平块边界 z≈1.15 漏检）+ NMS(min_gap=max(1,round(min_shot_s*fps))) + merge(过短段并入更弱边界一侧) + 无边界/单帧/空→回退整段 1 查询单元。**整体偏向下切分**（产品约束：过长可接受、过短/碎片化更危险）。`PipelineConfig` 平铺加 4 个占位字段 `seg_cut_abs/seg_z_thresh/seg_min_shot_s/seg_smooth`（3 个接线点 + JSON 覆盖验证通过）。segment unittest 11 项 + smoke(真 ffmpeg+DINOv2 3色多镜头) 结构不变式全过、输出诊断统计；全套 50 项回归通过。研究护栏：未改任何冻结算法、无 GT、未做 sweep、未重引入 Phase19 原片 cut-aware；多模态未触发（冒烟结构合法）。冒烟 3 纯色片段 CLS 混淆未切分=已知合成图案属性，非 segment 缺陷。
- **2026-08-25 — `app/SourceLocatorService`（application service：用例编排 + 失败隔离 + progress + cancellation + 错误翻译 + Result 汇总）完成**：`mvp/src/app/`（`models.py`：`ProgressStage`(7 阶段) + `ProgressEvent{stage,current,total,message}` + `CancellationToken`(threading.Event)；`locator_service.py`：`SourceLocatorService`(build_original_index / analyze_edited_video / locate / _locate_features / export_results / load_results；ffmpeg+backend+FeatureStore 惰性构建)）+ `infrastructure/results_repo.py`（`save_results/load_results` 单文件 `*.results.json` 批信封）+ `infrastructure/errors.py` +4（`IndexError/FeatureExtractionError/LocalizationError/ApplicationError` 均 `LocatorError` 子类）+ `domain/models.py` 增 `Result.failure_reason` + `Confidence/Alternative/Result.from_dict()`（对称 to_dict）+ `ResultBatch`(schema_version/original_video/edited_video/results[])。**编排**：locate(edited, original) = 原片 validate→(重)建→load + 编辑侧 iter_frames@2.0fps→embed_frames→detect_shots + 逐 segment produce_candidates→localize_segment→拼 `domain.Result`(edited=shot.span)。**失败隔离**：单 segment 抛错→unresolved(LOW + failure_reason + 日志 exception)+继续后续段；cancellation 抛 `ApplicationError` 不落入段级隔离。**健壮性**：确定性 unittest 12 项 + 全套回归 62 项通过；synthetic 端到端冒烟通过（a1 命中 HIGH、export/reload 往返、cancellation、失败隔离）；**修复 app 级 bug**：`_result_from_refined` 曾传 `shot` 而非 `shot.span` 致 `Result.edited` 被塞进 ShotSegment（已修）。**真素材首次完整管线跑通（§十，仅观察不调算法）**：2.mkv(1GB)索引 3834 帧/3793.6s + 1.mp4@2fps 254 帧→12 段/158.0s + 12 Results(HIGH5/MEDIUM1/LOW6, unresolved 0, candidate 485) + export/reload 往返 + cancellation + 失败隔离；total 3951.9s；peak_ram N/A（venv 无 psutil）。研究护栏：只编排不改冻结算法语义、无 GT、未 sweep。

## Current Problem

- None identified.

## Current Implementation

- `mvp/src/` 已构建模块树：`media/ffmpeg`（FFmpegIO）、`domain`、`infrastructure`、`device/`（CPUBackend）、`engine/feature_store`（FeatureStore）、`engine/common|retrieval|clustering|ranking`、`engine/candidates`、`engine/localization`（finloc + montage + pipeline）、`engine/confidence`（ConfidenceEngine）、`engine/segment`（ShotSegment + detect_shots）、`app/`（SourceLocatorService + ProgressStage/ProgressEvent/CancellationToken）。
- 测试：`mvp/tests/` 7 模块 61 项 unittest（media 6 / domain+infra 12 / device+feature_store 4 / retrieval+ranking 7 / localization+confidence 10 / segment 11 / locator_service 12）；`mvp/scripts/smoke_*.py` 冒烟（media 23 / device+feature_store 25 / retrieval+ranking 6 / localization+confidence 9 / segment 6 / locator_service 真素材+synthetic）全过。
- 冻结栈已贯通：FFmpegIO 抽帧 → CPUBackend(DINOv2 CPU) 特征 → FeatureStore 落盘索引 → retrieval/clustering/ranking 产出候选窗 → localization 精定位(最长连续 run + montage 检测) → confidence 三档置信 → **`app/SourceLocatorService` 逐个查询单元（`engine/segment`）串联整链并汇总 `domain.Result`，JSON 单文件批持久化**。**UI = Vue3+TS+Electron（`mvp/ui/`），经 `ServiceAPI`（Mock 适配器独立跑通，`HttpServiceAdapter` 预留 `/api`；接真实后端=改 `VITE_API_BASE`）消费同一 `SourceLocatorService`**。
- **2026-08-26 追加**：`infrastructure/logging.py` 统一日志（stderr + `RotatingFileHandler` 10MB×5 写 `mvp/logs/video_locator.log`，结构化 `时间 级别 module session 消息`，`contextvars` session_id）；`device/__init__.py` 记 backend requested/selected/fallback/device；`app/locator_service.py` 记 index/analysis/locate 生命周期 + 异常(exception)。
- **2026-08-26 — `mvp/api/` FastAPI 桥实现**：`mvp/api/`（`main.py`/`app.py`/`schemas.py`/`dependencies.py` + `routes/` health/index/analysis/results + `tests/test_api.py` + 重写 `README.md` + `requirements.txt`）。端点 `/api/health`·`/api/index`·`/api/analyze`·`/api/results`·`/api/export`；`WS /ws/progress` 本阶段仅文档。DI = `AppContext(service,current_original,current_batch)` + `get_context()`(lru_cache 惰性，构造不加载模型/不建索引) + `get_locator_service()`。session 日志 = 请求中间件 `set_session_id(None)` + `get_logger("api").info("METHOD path")`（输出 `module=api session=<sid> POST /api/index`）。错误：`LocatorError`→500 `{error,detail}`+`logger.exception`；缺原片/结果批→400。**验证**：unittest 12 项全过（health/index/analyze/results 拍平/export/500/CORS/session 日志）；真机 `uvicorn mvp.api.main:app --port 8765` 启动，`curl /api/health`=`{"status":"ok","version":"0.1"}`，访问日志 `module=api session=08f2c341 GET /api/health`。**读数口径**：confidence 用冻结 `ResultBatch.to_dict()` 拍平（非嵌套）；`backend.device_type` 纯展示层静态映射、不参与运行逻辑。**下一阶段**：`HttpServiceAdapter` 从 Mock 切真实 API（设 `VITE_API_BASE`），不做 WS/不打桌面/不改 mvp/src。
- **2026-08-26 — `HttpServiceAdapter` 从 Mock 切真实 FastAPI（只改 `mvp/ui/`）**：`mvp/ui/src/services/HttpServiceAdapter.ts` 重写为真实端点（`POST /api/index {video_path}`、`/api/analyze {edited_path}`、`/api/results {edited_path,original_path}`、`/api/export {output_dir}`、`GET /api/health`）；统一错误（网络→`BackendUnavailableError`，HTTP 500→surface detail）；`onProgress` 在 Http 模式发粗粒度阶段提示（无 WS）；`getIndexStatus/getIndexMeta` 反映上次 build 否则 MISSING 占位（后端无 status/meta GET）；`loadResults` 抛 `BackendUnavailableError`（无端点）；`cancel()` 占位 noop。`resolveService.ts` 改按 `VITE_BACKEND_MODE`（默认 mock）/`VITE_API_BASE` 切换（`resolveFor(mode,base)` 纯函数可测）。`ServiceAPI` 增 `checkHealth(): Promise<ConnectionStatus>`；`MockServiceAdapter` 实现始终 CONNECTED。`stores/session.ts` 增 `connection` + `checkHealth()`；`App.vue` 启动调用；`AppSidebar`/`BackendStatusIndicator`（Connection 三态 Connected/Connecting/Offline）+ `HomePage` Backend stat 显示连接态。`types.ts` 增 `ConnectionStatus`。`.env`（默认 mock，注释 http 两行）+ `package.json` 增 `test`/`test:watch`（vitest）。**验证**：typecheck 零错、`vite build` 通过、`npm run test` 14/14（health/index POST 正确/locates 拍平 confidence/500 detail/network→BackendUnavailableError/resolveService 切换）；浏览器实测（FastAPI 8765 + Vite http 模式）Home 侧栏 Connection=Connected、Home Backend stat=CONNECTED。**关键落差/口径**：真实后端无 index-status/meta/load/WS 端点 → 前端 Http 模式对 `getIndexStatus/getIndexMeta/loadResults/onProgress/cancel` 用诚实占位或粗阶段提示，不伪造；`/api/export` 只收 `output_dir`（后端用会话 current_batch），Http adapter 忽略前端 batch/filename、发 `{output_dir: opts?.outDir ?? ""}`。**下一阶段**：WebSocket Progress + Real Pipeline Progress（接真实索引/分析进度：`WS /ws/progress` + 取消 + 预览 `extracted_path`）→ Electron 打包/真机验收 → 性能基准 + Confidence 标定。

## Current Decision

- 第 1~4 项关键决策：FFmpegIO 走 ffmpeg/ffprobe subprocess（禁 cv2 CAP_PROP_POS_MSEC；mkv 用 format.duration）；clip 提取 = `-ss before -i` 重编码 libx264（精确，非 stream copy）；采样时间基 `t=i/fps`（不复刻研究帧索引基）；FeatureStore 落盘 `<stem>__<hash8>.idx/{index.json,features.npy,times.npy}`、失效判定 size→duration→model/dim/fps/version→hash 硬检、旧索引转 `.stale` 隔离；`retrieval_top_k`=20(冻结)；`v2_score` alpha=0.5(冻结)；domain.Candidate 增 `scene_div`（montage 指示）、`best_cover` 留占位由 finloc 回填。
- 第 6 项 `engine/segment` 关键决策：`ShotSegment` 放 **engine 层**（domain 禁 numpy；先例 `Hits`/`LocalizationResult`）；切分**判别用局部 z-score**（非裸绝对阈值——研究 Proof 绝对 d 不可靠；相对基线 `max(cut_abs, cut_rel*base)` 是死代码已弃），`cut_abs` 仅作下限安全垫；**默认平滑窗=1**（不平滑保留单帧 cut，平滑会把单帧边界拉平致漏检）；**整体偏向下切分**（产品约束：过长可接受、过短/碎片化更危险），NMS+merge+min_shot_s 保底；切分参数是**产品化占位**（非冻结 finloc 阈值），已入 `PipelineConfig.seg_*` 供 H1 标定，**不做 sweep**；segment 属产品胶水，**不触碰任何冻结算法**。
- **第 7 项 application service 关键决策**：shot 切分 + per-segment 循环放 **app service 层**（按 STATE.md Next Actions 复用 `detect_shots`/`produce_candidates`/`localize_segment`，不新建 engine 层 `LocalizationEngine` 类）；**失败隔离**——单 segment 异常 → unresolved(LOW + `failure_reason` + 日志 exception) + 后续段继续，cancellation 抛 `ApplicationError` 不落入段级隔离；**ffmpeg/backend/FeatureStore 惰性构建**（纯编排路径不强制解析二进制）；**Progress** 7 阶段 `ProgressStage`+`ProgressEvent` 逐事件回调；**Cancellation** `CancellationToken`(threading.Event) 在 index build/extract/segment loop/export 检查；**错误模型** 补 `IndexError/FeatureExtractionError/LocalizationError/ApplicationError`（均 `LocatorError` 子类，GUI `except LocatorError` 即可）；**持久化（用户拍板）** JSON 单文件批 `schema_version/original_video/edited_video/results[]`（`*.results.json` 落 `paths.export_root()`），复用 `Result.to_dict()/from_dict()` 保留 auto/manual 双轨，`Result.failure_reason` 收窄失败段；**不用 SQLite**。修复 app 级 bug：`_result_from_refined` 曾传 `shot` 而非 `shot.span` 致 `Result.edited` 塞进 ShotSegment。
- **第 8 项后续决策（2026-08-26）**：`mvp/api/` FastAPI 桥 = 本地 Adapter Layer，只转发到 `SourceLocatorService`，不改 mvp/src；端点 health/index/analyze/results/export，`WS /ws/progress` 本阶段仅文档；session 日志复用 `infrastructure.logging`；DI 用 `get_context()`（AppContext 持有 service + 请求状态，构造惰性不加载模型/不建索引）；`backend.device_type` 纯展示静态映射不参与运行逻辑；结果 confidence 拍平用冻结 `to_dict()`。详见 `.agent/DECISIONS.md` 本阶段条目。
- **注意（交接必读）**：检索单元测试用合成图案数据，`v2_score` 会把覆盖更宽的非 GT 窗口排在真 GT 区之前——这是**冻结公式 + 合成图案 CLS 语义混淆**的已知属性；研究在**真实影片**数据上 v2_score 正确分离（研究已验证）。真实用户场景是 film 内容，非合成图案；若真实数据同样混淆，应属 Confidence 显式降险情形（第 5 项），**不是 retrieval 改动**，也不回退冻结公式。

## Next Actions

- **H4 Windows NVIDIA / CUDA POC（= PENDING；H4-0 已检查 = `H4_GPU_RUNNER_UNAVAILABLE`）**：GitHub Actions 无可用 Windows GPU runner（个人公共 repo + 官方 GPU runner 不对开源项目开放、无 Windows 变体；本机无 NVIDIA、torch 为 CPU build）。**当前阻塞**，需用户提供 NVIDIA 环境（自托管 Linux runner / 云或本地 NVIDIA 机器）+ 计费权限后，按：能力探测 → `CUDABackend` 一致性验证（与 CPU 特征/检索/定位/置信对比，cos>0.999 量级）→ 性能基准（10/60/120min）；不支持则如实标记 `H4_CUDA_BLOCKED` 继续 CPU。
- **H3**：已完成（`H3_MPS_GO`，POC = GO，2026-08-26，不再重复运行）。正式 `MPSBackend` 未实现（后续阶段；届时 `embed_frames` batch ≤4）。
- **UI（第 8 项）= Vue3+TS+Electron，Stage 1 初始代码已交付（2026-08-26）**；`mvp/api/` FastAPI 桥**已实现**（2026-08-26）；`HttpServiceAdapter` 已切真实 API（2026-08-26）；**Electron 主进程后端生命周期管理已实现**（2026-08-26）；**Electron renderer→后端请求桥已实现**（2026-08-26，生产 `file://` 绕开 CORS）；**FastAPI 异步任务基础设施已实现**（2026-08-27）；**WebSocket 实时进度 + UI Pipeline Binding 已实现**（2026-08-27）；**Video Preview / extracted_path 已实现**（2026-08-27）；**Electron 生产打包 + Python 后端 Bundle 已实现**（2026-08-27：PyInstaller onedir → `resources/backend/backend.exe` 923MiB，`backend.exe` 启动 + `/api/health` 200 + 真实 Preview 抽取/Range 206 验证通过；Electron `prodBackendConfig` 直启 `backend.exe` + `MEDIA_FFMPEG/FFPROBE` env）。**下一步 = 真机验收（打包后 Electron 冒烟，需 `resources/backend` 入包 + 安装器授权）→ 性能基准 + Confidence 标定**。（Installer + License + Device Binding 属后置授权项，用户要求时再做。）
- 之后（UI 恢复时）：第 8 项 PySide6 UI（Home→Index→Results→Export + 预览 + 手动修正 + 批量导出，消费 `SourceLocatorService`（`build_original_index` / `locate` / `export_results` / `load_results` + `ProgressStage` 进度条 + `CancellationToken` 取消），结果卡片用 `domain.Result`）+ 第 9 项 打包验收 + 性能基准 + Confidence 标定。
- **护栏（贯穿）→ 2026-09-01 用户逐条拍板**：解除=冻结 DINOv2 backbone / ONNX 只出 CLS / 相似度·检索·聚类·v2_score·coverage·finloc 语义 / 不新增 backbone/VLM / 已证伪变体与 patch/multi-scale 不进 runtime / 不 sweep / 不重跑研究 benchmark / 不改 GT；保留=索引/编辑采样、Python 3.11+、Confidence 公式冻结+不标定占位+不用 GT 字段、禁 per-query argmax/voting/cut-aware/temporal align、研究代码隔离、GT v3 基准+新增逐帧确认、FFmpeg seek、L2 归一、DeviceBackend 抽象、不伪造结果、打包需授权、历史结果不覆盖、研究立项触发条件。见 `.agent/DECISIONS.md` 2026-09-01。

---

## Important Constraints

- None recorded.

## Known Issues

- None known.

## Last Updated

2026-09-04 18:10

## TODO Snapshot

## → 2026-09-05:C 项 —— 两级分层切分（用户架构修正）验证完成 = 最佳方案，白闪守卫生效，待拍板进 runtime

- **执行过程（2026-09-05，全部 GPU/DirectML 加速，BACKEND_SELECTED=directml/amd 已固化）**:
  - ① p36 单段 8fps 取证 = 假设成立（8fps 整段即命中 GT 2042-2043.4）;
  - ② 全局采样 4/6/8fps 四片全量 = 严格全部净负（104→100/99/103）+ t2r02b 假阳性兑现 + 2mkv 负例 +1;
  - ③ **用户两级分层切分（粗采样 5-8 帧找候选切点 + 局部 ±20 帧密帧精修边界 + 最短镜头保护 0.5s + fps 换算）
    四片全量 = 严格 112/139 (+8)、场景级 136/139 (+18)、负例 4/9 持平** —— 唯一严格净正收益方案;
  - ④ **白闪守卫（四层：前置过滤/切点后处理/相似度衰减/业务兜底）验证 = 删除 1.mp4 唯一白闪区
    (ed 18.35-18.46) 处的粗网格虚假切点 18.345（段数 70→69），三指标零回退**;
  - ⑤ 诚实边界: n03(66.1-67) 非白闪（帧正常，误配 HIGH 是 CLS 内容相似机制）→ 白闪守卫救不了，
    属 t2r02b 同类「内容相似误配」需另立方案; 负例 3/4 维持。
- **待拍板**: 两级切分 + 白闪守卫是否进 runtime（替换 analyze_edited_video 切分；编辑侧特征不落索引
  无需 bump feature_version；需全套单测 + 四片回归 + UI 结果数/导出验收）。
- **产物**: mvp/scripts/research_twopass_prototype(_v2).py + rerun_twopass.py + flash_guard.py +
  rerun_twopass_flash.py + work/rerun_*_twopass(_flash).results.json +
  FINDINGS_C_ITEM_8FPS.md（六、七章）。GPU 约定: DECISIONS.md 2026-09-05 + AGENTS.md。

## → 2026-09-04(下个对话):C 项 —— 编辑侧采样 2fps→8fps + 细切分（用户拍板记录, 本轮不实施）

- **背景(已定)**: p36 细切分取证通过(数据+VLM+人工三方闭合); test 域扫描完成——方案 A(单帧强证据保留)
  **不实施**(收益仅 +1 且有 t2r02b 假阳性风险), B 顺带关闭。p36 单条 +1 留待 C 项一并处理。
- **C 项内容**: 编辑侧采样率 2fps→8fps 提升 + 更细镜头切分, 涉及编辑侧特征重建/推理成本翻 4 倍,
  且 M4 已证索引侧密帧零增益(查询侧需另证)。**下个对话做**, 本轮不实施。
- 执行时: 先对 p36(及同类「查询单元不纯」段)验证 8fps 查询侧细切分收益, 再决定是否改采样率。

## → 2026-09-04:研究侧收尾归档(M6 v4 + M4/M8 v4 + ⑩ p08b) + 三指标基线固化(measure_baseline.py) = 完成

- **M6 v4 重算**(2026-09-01): RESCUE 0/39、CLS 池内 39/39 → patch 召回方向彻底关闭(有据)。
- **M4 v4 重跑**(仅 p26, DML 664s): v4 真值下 1fps 即 best_rank=1、margin=+0.28 —— 旧「12→43 恶化」是错误 GT 假象。
- **M8 v4 重跑**(纯 numpy 秒级): p26 互换后仍 AMBIGUOUS(0.027)、p08/t3r12 维持; p38/t4r01 剔除。
- **⑩ p08b 复核 = 作废(污染残留)**: 真值窗=p38 旧错误 GT 区, v4 正确=1108-1110, CLS 池内 6/patch 6 零增量。
- **FAILURE_TAXONOMY 收尾**: 失败族 = p08 + t3r12; A 类在 2.mkv 为空(CLS 39/39); 研究侧全维度闭环, 不再立项。
- **基线固化**: measure_shot_recall.py 抽 evaluate()(CLI 不变) + measure_baseline.py(doc/rerun 双模式) →
  work/baseline_v4.json; 验证与 GT_BASELINE 文档数字完全一致(2.mkv 32/39 场景 36/39; test1 32/43 场景 38/43;
  test2 9/20 场景 10/20; test3 31/37 场景 34/37)。⑦ 仍冻结。
- 零 runtime 改动; 三指标 v4 基线不受影响。产物 FINDINGS_M4_V4/M8_V4/P08B_REVIEW_V4 + measure_baseline.py。

## → 2026-09-02:②③ 单调弱先验进候选生成已编码 + 实测零触发（时间轴先验方向有据收窄）

- **实现**: `_apply_timeline_prior`（Ambiguous 型段用前序锚点弱倾向, 逃生门全路径）+ 配置 +
  单测 8 项, 全套 222 全绿。
- **重跑验证**: 新代码含先验重跑 2.mkv/test1-3, 新旧三指标完全一致 → **零触发零影响**。
- **原因**: p08 型兄弟机位 primary 在带内(先验结构性无效, 呼应 M8); 真实跳切全带外(逃生门生效)。
- **结论**: 候选生成级弱先验零增量; 时间轴先验价值已由事后 temporal_repair + 时间轴→Ambiguity
  兑现; 实现保留为护栏, 不调参投入。产物 FINDINGS_TIMELINE_PRIOR.md。

## → 2026-09-02:⑤' test1-3 GT 全部闭环 + 正式化（100 正例 + 5 负例），test3 HIGH 精度重估 23/23

- **⑤' 完成**: 用户逐段人工复核 test1-3 全部段（分:秒标注），已正式化到 `datasets/real/ground_truth_test1/2/3.json`。
  test1 43 正例/1 负例、test2 20 正例/1 负例、test3 37 正例/3 负例（含 r14/r15 加长版负例）。
- **三指标**: test1 严格 32/43 场景 38/43; test2 严格 9/20 场景 10/20（拆条窄窗低估）;
  test3 严格 31/37 场景 34/37、负例误报 2/3（r14/r15 加长版被定位）。
- **重大修正**: test3 r15 原标 HIGH 实为加长版负例（GT 修正）; r10=7:22-7:24 与 conflict_rerank 一致（已修）;
  test1 r14a 缺失段=算法对 GT 漏标; test2 r05 同段 -2726s 真实跳切; test3 r13 倒叙。
- **test3 HIGH 精度重估**: 24→23 段（r15 剔除），r10 已修 → **23/23 = 100%**（原 23/24 双向修正）。
- 产物: `gt_review/GT_BASELINE_test1-3.md` + `FINDINGS_TEST1-3_GT_BUILD.md` + 三份正式 GT。

## → 2026-09-02:test1-3 GT 草案 + 时间轴→Ambiguity 编码 + 时序重排 v4 量化（⑤' 材料交付，等待用户逐段人工复核）

- **⑤' 数据层材料已交付**: 三份 GT 草案 `gt_review/ground_truth_test1/2/3_draft.json`（全部段，tier=pending，定位占位待人工毫秒复核，负例=not_in_source 正确拒绝）+ 复核工作表 `GT_REVIEW_WORKSHEET_test1-3.md`（76 行，含复核结论/最终窗口列）+ 时间轴复核清单 `TIMELINE_CROSSCHECK_test1-3.md`（🔴离群/⚠️倒退/🔵大跳优先级标记）。
- **对照图全部重生成**（`cases/test1|test2|test3/rNN.jpg`，76 张，基于最新定位；此前 test1/test3 基于旧定位、test2 仅 3 张 HIGH）——用户将逐段看图人工复核。
- **② 时间轴→Ambiguity 已编码**（NEXT_STEPS ⑥）: `_apply_temporal_ambiguity` 复用 `find_temporal_outliers` 作为外部 Ambiguity 信号——修复后仍离群的高置信段降档（HIGH→MEDIUM，reason `temporal_outlier_ambiguous`），不改定位；配置 `pipeline.temporal_ambiguity_enabled`（默认 True）+ `ta_max_downgrade`（默认 MEDIUM）。预研确认 current 结果批离群零触发（s7 已修）→ 2.mkv 零回归。单测 8 项新增，**全套 214 项全绿**。
- **④ 时序重排 v4 量化已完成**（NEXT_STEPS ①）: 多版本对比（baseline_p21 29/39 → pre22a 31/39 → pre24 33/39 → current 32/39）+ 精确回滚实验 → **temporal_outlier_repair 净纠正 +1（p16 part→HIT）**；p05 pre24→current 回退=「修正后未跟上」非算法退化；P3 悲观结论重估（p26 修正后完全符合时间轴）。产物 `semantic_signal/FINDINGS_TIMELINE_V4_QUANT.md`。
- **③ test3 r10 单点已核实**: current=442-444（temporal_repair+conflict_rerank 两道修复），真值≈444.5，差 1.5s 场景级命中——留待 ⑤' 毫秒精修。

# TODO

## → 2026-09-01:Ambiguity Detection 原型实验 = 现有信号无法分离正确/错配 HIGH, AMBIGUOUS 无内部信号

- **实验**: 重跑 29 段 EvidenceLocalizer 提取内部 multi-evidence 信号(work/amb_prototype_signals.json)。
- **发现**: HIGH 档精度仅 5/13(38%); 正确/错配在 mode/n_clusters/qcov/dispersion/best_sim/margin 全同构(primary best_sim 0.49-0.69 重叠)。
- **机制**: 错配 HIGH 多为 clean 单证据簇 → secondary=None → margin 饱和 1.0; low_candidate_margin/multiple_similar_candidates 均要求 n_strong_clusters>=2 → clean 段结构上不触发降险 flag。
- **结论**: AMBIGUOUS 检测无内部信号可用(与 M1-M8 兄弟机位混叠 + 08-27 置信标定研究闭合); 校准转向「保守化 HIGH 门槛标定」, 不立项 AMBIGUOUS。
- 产物 `semantic_signal/FINDINGS_AMBIGUITY_PROTOTYPE.md`。

## → 2026-09-01(深夜):用户拍板推进顺序 —— 数据层 test1-3 全量毫秒级重标 + 时间轴→Ambiguity

- **执行顺序(已拍板)**: ① **test1-3 全部 GT 毫秒级人工重标**(数据层主线, 用户逐帧, 吸收 B 段; test4 数据错误剔除) → ② 时间轴→Ambiguity(低成本, 落地校准产品) → ③ test3 r10 单点验证 → ④ 时序重排 v4 量化 → 之后: 单调弱先验进候选生成 → Confidence 保守化标定 → M1-M8 低成本重跑(M4/M8) → p08b 复核。
- **⑤' 与 ⑨ B 段合并**: ⑤'(test1-3 全部毫秒级)是 ⑨(test2+test3 LOW/MEDIUM)的超集+升级, ⑨ 并入 ⑤' 不单列。
- 完整说明: `gt_review/NEXT_STEPS.md`(顶部「✅ 已拍板执行顺序」段)。

## → 2026-09-01:M6 v4 重算完成 = patch 召回 RESCUE 1/41→0/39, 方向彻底关闭(有据)

- **重算结果**(v4 GT, 39 条): **RESCUE 0/39、CLS 池外 0、CLS 池内 39/39**。
- **关键**: v3 的 10 条「CLS 池外」修正后全部 best_rank 1-8 直命中(p05/p10/p20/p23/p24/p26/p32/p41); p13「唯一救回」= GT 标错假象(v4 CLS 1)。
- **结论**: patch 召回 runtime 化零增量(0/39), 方向关闭(有据); M6 原「RESCUE 1/41」作废; 失败族重新定性为「定位精度/兄弟混淆」非「召回层进不了池」。
- 产物 `semantic_signal/FINDINGS_M6_REVISED.md` + `work/patch_recall_gt_results_v4.json`。

## → 2026-09-01:M1-M8 结论重审完成(v4 GT)= p26/p38 从失败族移除, 失败族收窄为 p08 + test 域

- **核心反转**: p26=GT 标错实证(旧 2809→正确 1766-1770, v4 runtime 直接 HIT HIGH)——M1-M8 全部 p26「特征上限/不可辨识」结论作废;
  M1b「VLM 反向选错」实为判对; M2「p26 字幕正面信号」反转(3/3 命中错误窗 2808)。
- **p38**: 与 p08 重复已删, 旧真值 1048 本身错(正确=1108-1110=p08); M5 p08b 救回存疑、M6/M7/M8 p38 半边作废。
- **失败族收窄**: p08(兄弟机位, 2.mkv 唯一) + t3r12/t4r01/test4(test 域未受影响)。
- **待办**: M6 全量「RESCUE 1/41」统计需 v4 重算(方向收益低预判不变); p26/p38 相关 FINDINGS 标注作废。
- 产物 `semantic_signal/FINDINGS_REVIEW_M1M8.md` + FAILURE_TAXONOMY 重审段; 三指标基线=v4。

## → 2026-09-01:GT v4 重建完成 = 41 条全定案写入 ground_truth_v4.json, 重测三指标 + 待重审 M1-M8

- **产物**: `datasets/real/ground_truth_v4.json`(v3 保留 + 31 条 corrections 留痕: 29 relocate / 2 delete) + `gt_review/GT_REBUILD_PROPOSAL.md`(41 条全判定提案+附录 v4 重测)。
- **用户逐条裁决**: p37(与p10 ED重合)/p38(与p08 ED重复) 删除; WRONG_GT 按线索改; PARTIAL 按用户画面收窄; WRONG/NOT_REVIEWED 精确窗口(±几ms容差)。
- **VLM 三批辅助**: 补审 14 条 / PARTIAL 逐帧 13 条 / top-6 候选窗 9 条(Volcengine Ark); 用户看图拍板。
- **v4 重测**: 严格 31/39 / 场景级 36/39 / 负例 2/4 / 支撑 80/137(v3 对照 39/41/39/41/2/4/91/137——下降=修正 GT 真实口径)。
- **关键实证**: p26 MISS→HIT(GT 错非特征上限) → **M1-M8 依赖错误 GT 的结论需重审**; 剩余 MISS p08(特征上限)/p28/p36(runtime 未命中)。

## → 2026-09-01:GT v3 人工审查发现大量标错 = 三指标基线作废, 需重建 GT(专会话执行)

- **审查结论**: 35/41 条已审, 仅 6 OK, 29 条有误(PARTIAL 13/WRONG 8/WRONG_GT 7/p26 确认错), 6 未审(p36-p41)。
- **影响**: 三指标 39/41 作废; M1-M8 结论需重审(p26=GT错; p38 兄弟机位结论存疑)。
- **资产**: `gt_review/GT_REVIEW_RECORD.md`(逐条判定+正确线索)+ 41 张对照图。
- **执行清单(下个专门会话)**: ①按判定修正 GT(有线索的直接改, PARTIAL 对齐窗口, WRONG 需用户补正确位置); ②补审 p36-p41; ③修正后重测三指标; ④重审 M1-M8 中依赖错误 GT 的结论。
- **正确线索速查**: p26→1766-1770; p05→≈978-984; p09→≈1050-1055; p13→≈1343; p20→≈1551-1564; p23→≈1585; p24→≈1599。

## → 2026-09-01:校准阶段(用户拍板 A+B)= Confidence Calibration + Ambiguity Detection + 补真实 GT

- **目标转变**:不是「让所有 Ambiguous 变正确」, 而是「提高可识别样本的召回/精度 + 正确识别 Ambiguous」——成熟产品行为。
- **产品层**:三档置信 → 四行为: HIGH 自动通过 / MEDIUM 自动通过或提示 / LOW 提示人工 / **AMBIGUOUS 明确转人工**(新增, 用 margin/similar_band/multiple_similar_candidates 等现有 ConfidenceConfig 信号判定)。
- **数据层(最重要, 前提)**: **补真实 GT, 范围 A+B**——A=2.mkv 失败族周边+Ambiguous 候选段(进行中: p08/p05/p20 已精修, p28 待终点); **B=test2+test3** LOW/MEDIUM 完整 GT(2026-09-01 test4 数据错误剔除: ed/om 两部不同电影)。GT 新增须逐帧画面确认。
- **置信标定**: 用真实 GT 统计 Easy/Hard/Ambiguous 分布 → 标定 ConfidenceConfig weights/thresholds/hard flags(现全占位)→ HIGH 高 precision、LOW/AMBIGUOUS 高 recall of hard。
- **研究层(Future, 非阻塞)**: 额外来源信号(provenance: 剪辑顺序/字幕台词/音频时间锚点/原片镜头图)——M8 已证当前邻接不可分, 需新信号源。
- **待办顺序**: ①补 GT(A 段优先, 量小) → ②Ambiguity Detection 原型(用现有信号) → ③Confidence Calibration → ④三指标回归。决策见 DECISIONS.md 2026-09-01。

## → 2026-09-01:原片邻接唯一性探针 M8 = 来源身份信息方向证伪, 失败族正式定性「不可辨识样本」

- **起因**:用户正确划分「来源身份信息」= 原片侧 Shot Graph 邻接唯一性(非重包已证伪的 P3 编辑上下文), 唯一未被否定维度。
- **M8 结果**(纯 numpy 秒级):5 个失败案例全 AMBIGUOUS——p38/p08 兄弟机位真-干扰邻接余弦 0.613(同场对话戏邻接同样貌);p26 唯一性差 0.027 不可靠;t3r12 干扰反更唯一;t4r01 相等。
- **归因**:兄弟机位邻接本身相似(场景集中同一时间窗, P3 根因);P2 过度归并担忧证实。
- **净结论**:来源身份信息清单全落空(前后镜头P3/剪辑点/镜头图邻接M8/字幕对白M1-3/音频不同源/OCR字牌单例)→ 失败族正式定性「不可辨识样本」;研究侧全维度闭环(外观/结构/语义/密度)。三指标 39/41 未动。
- **待拍板**:研究侧冻结, 资源回 runtime/产品侧(推荐)。FINDINGS `semantic_signal/FINDINGS_M8.md`。

## → 2026-09-01:局部特征探针 M7(ALIKED n32)= 核心难例 p38/p26 无解, 外观三层(CLS/patch/局部)全部关闭

- **起因**:用户拍板「先1再2」——1=FAILURE_TAXONOMY.md 固化 A/B/C/D 失败分类法; 2=现代局部特征验证(承接用户「Source-specific discrimination」特征层重构主张)。
- **环境**:HF/GDrive/hf-mirror 均不可达 → SuperPoint/DISK 权重拿不到; 用户手动装 kornia 0.8.3 + 下 aliked-n32.pth(本地加载验证通过)。
- **M7 结果**(633s):p38(兄弟机位士兵特写) rank 61 **干扰反超(margin −24)**; p26(夜读) rank 125 **干扰反超(margin −19)**——CLS/patch 双失败的案例 ALIKED 也失败; p08 +34 / t3r12 +48 弱正向但方向不一致; p01/t4r01 无增量。
- **归因**:兄弟机位局部 patch 本身相似(单应成立), 局部证据天然同貌 → 与 Phase 24-1 几何证伪呼应。
- **净结论**:局部特征方向关闭(有据); 失败族在外观三层(CLS/patch/局部描述子)全部无解 = 身份级区分局限; 三指标 39/41 未动。
- **待拍板**:失败族正式定为「身份级区分」局限(维持特征上限表述) or 未来更强语义/身份特征(多模态已测无解)。FINDINGS `semantic_signal/FINDINGS_M7.md`。

## → 2026-09-01:patch 召回探针 M6(GT 全量 41 条)= 仅 +1/41(p13), patch 召回方向关闭(有据)

- **起因**:用户拍板「跑完 41 条 GT 再决定 patch 召回 runtime 化值不值」——M5 只测 6 个探针点, M6 扩到全量 GT。
- **M6 结果**(6633s):41 条 GT 正例——31 条 CLS 已在池内(易例, patch 无增量);10 条 CLS 池外中 **patch 只救回 1 条 p13**(CLS 188→patch 1);9 条未救回(p05/p10/p20/p23/p24/p26/p32/p38/p41)。
- **关键矛盾**:M5 的 p08b 32→2 在 M6 p38(同一目标区 1048-1050, CLS 340→patch 225)不复现——查询帧选取不同(M5 13.9s vs M6 中点 13.25s, 快剪段子镜头交替), patch「救回」对查询帧高度敏感。
- **净结论**:patch 召回 runtime 化 = 仅 +1/41 且信号不稳, 低于「≥2-3 条才值得」门槛 → **方向关闭(有据)**; p26/p24/p41/p38 双通道无解, 失败族维持特征上限。零 runtime, 三指标 39/41 未动。
- **待拍板**:无(方向已关闭, 数据留档)。FINDINGS `semantic_signal/FINDINGS_M6.md`, 脚本 `mvp/scripts/research_patch_recall_gt.py`, 数据 `work/patch_recall_gt_results.json`。

## → 2026-09-01:patch 级召回探针 M5 = patch 对兄弟机位选对实例有效(p08b 32→2), p26 仍特征上限

- **起因**:用户拍板立项「patch 级召回」——E21/patch_rerank 都只在「CLS 已捞进池」的候选里重排, patch 从未参与候选池构建(召回层)。M5 首次实测。
- **前置工程情报**:GPU 资产(DirectML ONNX)原本只导 CLS;新导出 CLS+patch 双输出 ONNX(研究侧临时资产, cos=1.0, DML 18fps vs CPU 1.4fps ≈13×), 探针切 DML 提速。
- **M5 结果**(918s):p08b 32→2(兄弟机位选对实例, E21 一致复现);p08 6→5; **p26 22→24 无解**(干扰 0.951 反超正确 0.900);p01/t3r12/t4r01 保持 top-1 零回退。
- **产品相关缺口**:runtime 检索 top-20, p08b/p26 均超池;patch 可救 p08b 型、救不了 p26。
- **待拍板**:patch 召回是否 runtime 化(预期救兄弟机位族一部分, 接入须 bump feature_version + 三指标回归)。FINDINGS `semantic_signal/FINDINGS_M5.md`, 脚本 `mvp/scripts/research_patch_recall.py`, 数据 `work/patch_recall_results.json`。

## → 2026-09-01:索引密度探针 M4 = 8fps 密帧对判别零增益,索引密度方向关闭(证据链 M1-M4 全貌闭环)

- **起因**:用户质疑「帧数不能再提升吗?GPU 加速还能往上吗?」(对标同类软件 30fps 全片解析)。澄清:Phase 14C「2→4→8fps 零增益」是查询侧结论,**索引侧从未测过** → M4 首次实测(DML batch=1,717s)。
- **M4 结果**:p08 best_rank 都 1(top5 2→5 略改善)/margin 0.393→0.365;**p26 best_rank 12→43(密帧恶化)** margin 仍负(−0.28→−0.25);t3r12/t4r01 持平。**8fps 未把任何负 margin 变正**。
- **归因**:索引密度解决「正确帧数量」不解决「正确 vs 干扰可分性」;p26 干扰 sim 0.876 反超正确 0.596 = CLS 混叠,任何帧率救不了。30fps 全片成本 ×30 换不来判别提升 → **方向关闭**。
- **吞吐实测**:DML batch=1 **20.5fps 最佳**,大 batch 并行无收益(plateau ~15fps),达不到 30fps;工程情报:未来 DML 批量推理用 batch=1。
- **难例多模态辅助**(用户指示):p26 是 M1/M2/M3 靶心——VLM 反向选错、字幕唯一弱正(不可索引)、CLIP 无判别力。三形态已测无可靠分离。
- **证据链 M1-M4**:判定(M1)/召回(M2)/索引(M3)/密度(M4) 四路全无解 → 失败族维持特征上限。零 runtime,三指标未动。脚本 `mvp/scripts/research_semantic_signal_M4_density.py`,数据 `work/semantic_signal_M4_results.json`,FINDINGS `mvp/benchmark/user_case/semantic_signal/FINDINGS_M4.md`。

## → 2026-09-01:方向 C 完整验证探针 M3 = 可规模 CLIP 索引证伪,方向 C 证据链完整(建议关闭)

- **起因**:用户拍板立项方向 C 完整验证(A)。前置:装 sentence-transformers 6.0.1 + clip-ViT-B-32-multilingual-v1(hf-mirror 可达, 512 维, 支持法语; 不碰 onnxruntime/rapidocr)。
- **M3 结果**:CLIP 跨模态字幕检索**无判别力**——相似度全域平台(Δ<0.003),真值 rank p26 2/8、p08 3/7、test4 2/8;**易例 p01 被字幕推到 5/7(比随机差)**。
- **机制**:解说字幕=故事级叙事文案,与画面松散/解耦(test4 事件无对应、p01 抽象带偏);CLIP 对「外观近同」候选无区分力。
- **方向 C 全貌**:M1 判定证伪 + M2 VLM 召回单例弱(3/3, 昂贵) + M3 可规模索引证伪 → 无可靠可规模多模态通道。
- **待拍板**:关闭方向 C(推荐) / 转测纯视觉局部 patch 级召回。零 runtime,三指标未动。FINDINGS `mvp/benchmark/user_case/semantic_signal/FINDINGS_M3.md`。

## → 2026-09-01:字幕语义召回探针 M2 = 方向 C 首次正面信号(p26 3/3 vs 1/3),有清晰边界

- **起因**:用户拍板测「字幕语义召回索引」(方向 C 的正确用法,此前只列远期从未实测)。探针 `mvp/scripts/research_semantic_signal_M2_subtitle_recall.py`,数据 `work/semantic_signal_M2_results.json`,FINDINGS `mvp/benchmark/user_case/semantic_signal/FINDINGS_M2.md`。
- **p26 = ✅ 首次正面信号**:字幕「她用望远镜看 Levi 在做什么」真值 [2808-2811] 3/3 EVENT YES(圆形暗角=望远镜视角),干扰夜阳台 1/3——字幕能分视觉 CLS 分不清的夜读/夜阳台。
- **t3r12 = ⚠️ 弱正向**:真值 2/3 vs 干扰 1/3(重复镜头语义同貌)。
- **test4 = ❌ 0/3 全 NO**:字幕「二十多人被困滑梯管道」与原片画面无对应——解说文案与画面解耦。
- **净结论**:方向 C 非全盘证伪,适用边界=字幕-画面对应性(p26 有效/test4 无效);无文本嵌入模型只能 VLM 匹配。
- **待拍板**:A. 立项字幕语义召回完整验证(引入文本嵌入+建索引+更多样本) / B. 记录为弱正向待更多样本 / C. 与局部 patch 召回合并「两阶段多通道检索」。

## → 2026-09-01:多模态判定探针 M1 = 事件级语义证伪/字幕召回弱信号,特征上限获语义层独立确认

- **起因**:用户拍板分层检索「结合多模态进行判定」,A(召回层+判定层两层都测)。探针 `mvp/scripts/research_semantic_signal_M1_multimodal.py`,数据 `work/semantic_signal_M1_results.json`,FINDINGS `mvp/benchmark/user_case/semantic_signal/FINDINGS_M1.md`。
- **M1a 字幕语义召回 = 弱方向性**:p08 正确 1/3 vs 兄弟 0/3、t3r12 正确 2/3 vs 干扰 1/3(偏好正确), p26 两窗都 2-3/3 → 不能精确分离, 可留作两阶段检索召回侧弱加权候选。
- **M1b VLM 事件级判定 = 证伪(甚至反向)**:p08 兄弟 3/3 SAME; **p26 编辑段被判与干扰夜阳台 3/3 SAME、与真值夜读 0/3 SAME(反向选错, VLM 视觉混淆比 CLS 更严重)**; t3r12 正确/干扰都 SAME。事件级语义与外观共享天花板。
- **证据链闭环**:外观/结构(五路)+事件聚类(方向A)+序列上下文(P3)+语义/多模态(M1) 五层全无解 → 特征上限多维确认。零 runtime,三指标未动。
- **未证伪的剩余方向**:局部(patch)级召回 + 两阶段架构(纯视觉, 非多模态)——探针=验证 patch 级能否把失败族正确实例拉进候选池。

## → 2026-09-01:第六路候选信号·镜头序列上下文探针 P3 = 证伪,特征上限正式封顶

- **起因**:用户指出五路证伪+方向 A 归档后,仍有一路未明确证伪 = 镜头序列上下文(目标镜头前后各 1~2 个镜头内容联合匹配,独立证据源)。要求离线最小验证,成本极低(现有 CLS+镜头边界)。
- **P3a 编辑序列≠原片时间轴(1:1 前提不成立)**:p07→原片[1027,1041]、p08→[1160,1166]、p09→[1131,1147]、p26→[1766,1770](真值 2809 偏 1000s+)、p40/p41→[1894,1900]/[346,355]。
- **P3b p08 序列匹配 = 证伪(不升反降)**:单镜头真值134 rank 44 → 序列匹配 61,**兄弟128 被推 top-1**;top3 全为同一对话戏场景。根因:编辑段后相邻镜头 p09 内容恰来自兄弟区域 1048-1082,编辑上下文自身就混着兄弟内容;同一场对话戏镜头在原片集中在 1010-1154 同一时间窗,无唯一上下文。
- **P3c p26 对照 = 证伪**:真值 rank 17→35,答案被拉向编辑上下文区域(1772-1907)。
- **回答用户三问**:①p08 查询不含完整镜头边界(2/15s),但失败不在此;②编辑侧无细镜头边界,GT 编辑段可当查询镜头;③p26 上下文序列差异更极端。
- **净结论**:外观层/事件聚类/序列上下文全证伪 → 剩余失败族正式封顶为特征上限=已知局限。唯一未探索=方向 C 多模态语义(远期)。零 runtime,三指标未动。脚本 `mvp/scripts/research_semantic_signal_P3_seq_context.py`,数据 `work/semantic_signal_P3_results.json`,FINDINGS `mvp/benchmark/user_case/semantic_signal/FINDINGS_P3.md`。

## → 2026-09-01:剪映通道验收闭环(用户确认)——草稿不再列入待用户动作

- 用户已确认 `D:\JianyingPro Drafts\` 下 test1-ed.loc.v6 / test3-ed.loc.v7 草稿完成,剪映通道最终验收通过。历史条目中「待用户双击/目检剪映草稿」类待办一律视为已闭环,不再要求用户动作。导出自动验收主力维持 PR CEP 通道。

## → 2026-09-01:Phase 24-2 方向 A 探针 P2 完成(用户拍板 A1 继续)= 兄弟机位事件级 top-1/重复镜头保持/同质场景证伪,待拍板归档或进 runtime

- **P2 设计**:事件归并规则(时序近邻+指纹联合聚类,边=场景中心时间差≤T_gap AND 指纹余弦≥S_sim 连通分量;网格 30/60/120s×0.55/0.60/0.65/0.70)+ 编辑段→事件单元端到端排序(对照帧级/场景级/事件级三档)。脚本 `mvp/scripts/research_semantic_signal_P2.py`,数据 `work/semantic_signal_P2_results.json`,FINDINGS `mvp/benchmark/user_case/semantic_signal/FINDINGS_P2.md`。
- **P2a 归并规则**:兄弟机位 134↔128 **12/12 全同单元** ✅(归并稳健捕获同事件多机位,单元含 946-1166s 连续对话戏 19 场景);重复镜头正确[43,44,45] 同单元 ✅ 但干扰 42/46 被时序并入 ⚠️;同质场景 test4 正确实例多数参数不同单元 ❌。
- **P2b 端到端**:p08+p38 兄弟机位 场景级 8/699 → **事件级 top-1**(T_gap=120 三档稳健)——正面回答 P1a「rank 6/11 不唯一」(归并规则=时序+指纹联合而非纯指纹);t3-r12 事件级 top-1(11/12)保持;test4 无效;p01 全档 top-1 零回退;p04/p17 场景级差为「场景指纹均值稀释」代理伪影(帧级 148/10 正常);p26 事件级意外 2/129(谨慎解读)。
- **净结论**:与用户预期(重复镜头族+部分兄弟机位族)一致,兄弟机位族超预期。零 runtime 改动,三指标 39/41 未动。
- **已拍板(2026-09-01):方向 A 归档**。P1+P2 证据链完整,按立项约定「P2 结束无论成败都归档」→ 同质场景维持已知局限。未来如需重开,证据在 `semantic_signal/FINDINGS_P1.md` + `FINDINGS_P2.md`。

## → 2026-09-01:Phase 24-2 语义级第二信号·方向 A 探针 P1 完成 = 部分成立/部分证伪,待拍板 P2 或关闭

- **立项**:五路证伪后用户拍板写「语义级第二信号」立项材料 → `semantic_signal/RESEARCH_PROPOSAL.md`(方向 A 场景实例身份[推荐]/B 序列对齐/C 多模态[远期]);用户选 **A**,探针 `research_semantic_signal_P1.py` 跑完。
- **P1a 归并能力 = 信号存在但不唯一**:兄弟机位场景134↔128(z=2.47 显著近)但 rank=6/11 非唯一 → 聚合信号有,精确归并需更强事件身份特征。
- **P1b t3-r12 = ✅ 可分离**:正确实例场景 avg rank 2.0/7,sep=+0.188 → 方向 A 对**重复镜头族**有效。
- **P1b test4 = ❌ 不可分**:sep=−0.035,同质场景族确认特征上限,方向 A 不覆盖。
- **P2 未跑**(身份嵌入端到端排序);FINDINGS 见 `semantic_signal/FINDINGS_P1.md`。
- **待拍板**:A1 继续 P2(先设计事件归并规则再端到端验证,预期仅重复镜头族+部分兄弟机位族) / A2 关闭方向 A(保留证据,失败族正式关闭为已知局限)。零 runtime;三指标 39/41 未动。

## → 2026-08-31(晚):Phase 24-1 非外观第二信号三探针预研 = 全部证伪,剩余失败族确认为特征上限(闭环)

- **探针①视觉几何一致性 = 证伪(核心)**:patch 互近邻 + 仿射/单应 RANSAC 内点率,p08/p08b(同场戏兄弟机位)真值帧内点率**反被兄弟机位压制**(p08b 兄弟 1109 HOMOG 0.75/n_match 329 vs 真值 1048 0.49/78)。归因:①刚性场景不同机位仍满足单应约束(平面近似),"不同机位→违反仿射"前提在多视图几何上不成立;②1fps 候选粒度下真值窗内 0.33→0.85 跳变,帧对齐极敏感;p26 的 rank 2 为假阳性(干扰 0.591 仍压真值 0.576,无 margin)。**不进 runtime**。
- **探针②时序运动签名 = 证伪(方法学不成立)**:编辑片快剪(压缩比>1),窗口帧差签名时间轴与原片非 1:1,未先对齐前提下连易例 p01 都 -0.87。**不重试**。
- **探针③难例投影头微调 = 数据不缺,无可分信号**:硬负例盘点每索引 2.6万~9.1万对(2.mkv 49,572/test1 51,289/test2 26,018/test3 91,708/test4 31,737,sim≥0.60 跨场景 Δt≥2s),「数据瓶颈」不成立;但 CLS 特征级混叠 + ①② 证伪 → 无可注入的第二信号。**不立项**。
- **回归**:研究侧零 runtime 改动,三指标与 Phase 24 基线一致 = 严格 39/41、FP 2/4、支撑 91/137,零回退。剩余 2 MISS(p08/p26)即本预研确认的特征上限两族。
- **结论**:维持「接受为已知局限」既有拍板;未来唯一有价值方向 = 语义级第二信号(场景身份/多模态字幕),当前无证据不立项。不改 runtime/不打包/不 bump feature_version。
- 脚本:`mvp/scripts/research_phase24_1.py` + `research_phase24_1_data.py`;FINDINGS 见 `mvp/benchmark/user_case/phase24_1/FINDINGS.md`。

## → 2026-08-31:NLE 自动验证通道定案(PR CEP 建成 PASS / Resolve 免费版死路)——①②已完成闭环

- **Resolve 21.0.4 免费版 = 外部脚本不可用**(Studio 独占,UI 无选项/连接全败/网络确认)。保留作 XML 手动目检;排障遗产无害。
- **PR 2026 CEP 面板建成**:`%APPDATA%\Roaming\Adobe\CEP\extensions\com.svl.timelineexport\`(manifest 必须在 `CSXS\` 子目录;PlayerDebugMode 已设 CSXS.9-13 HKCU)。按钮 → 导入 SVL XML → 导时间轴 JSON(`work/svl_pr_timeline.json`)。**E2E:119/119 匹配、0.0000s 偏差**。人工成本=PR 重启+点按钮 ~30s。
- **① 比对脚本已固化并复验**:`mvp/scripts/verify_pr_timeline.py`(读 FCP7 XML 声明 vs PR JSON 逐项四元组 start/end/in/out,`--xml/--json/--tol`;退出码 0=全匹配)。本次复验:119/119、最大偏差 0.0000s、EXIT=0。
- **② host.jsx XML 路径已参数化(2026-08-31)**:`svlRunAll(xmlPath, outPath)` 由面板 evalScript 传参,空值回退默认;`index.html` 加两个输入框(FCP7 XML 路径/输出 JSON 路径);`q()` 双写反斜杠防 ExtendScript 误转义(`\b`/`\t`/`\n` Node 往返测试通过)。旧调用 `svlRunAll()` 兼容。**扩展源码已归档**至 `mvp/scripts/pr/cep_extensions/com.svl.timelineexport/`(仓库为源,安装=复制到 %APPDATA%);用法文档 `mvp/scripts/pr/README.md`。
- **③ FCP7 多轨 XML 导出已支持(现有通道),无需改**。
- 剪映通道维持现状(v6/v7 草稿待用户目检);PR 通道=自动验收主力。
- **待用户动作**:重启 PR → 开面板确认参数化输入框 → (可选)填任意导出批 XML 验证多批复用。

## → 2026-08-30(深夜 III):Phase 24 冲突扩池重排已实施并验收 = 闭环

- **用户拍板 A 立项后已实施**:`engine/localization/conflict_rerank.py`(几何触发+无主张子区间落位+证据门槛)+ `_apply_conflict_rerank` 接线(时序修复之后)+ 5 配置旋钮。实现细节与验收见 `mvp/benchmark/user_case/second_signal/FINDINGS.md`。
- **验收**:后端 206(+19)+api 58 全绿;2.mkv GT v3 三指标零回退(39/41、FP 2/4、支撑 91/137,零触发);test1/2/4 零触发零漂移;**test3 r10 自动修复 447-449→442-444**(人工真值≈444.5),其余段零扰动,对照图 `second_signal/r10_fix_verify.jpg`。**r10 不再需要结果页手动替换。**
- 零打包(长期规则)。剩余失败(p08/p08b/p26/r12)= 特征上限,维持"接受为已知局限"拍板。

## → 2026-08-30(深夜):Phase 24-0 非外观第二信号预研完成 = 仅「冲突触发扩池+时序融合重排」值得立项,待拍板

- **预研结论(证据见 `mvp/benchmark/user_case/second_signal/FINDINGS.md` + `work/second_signal_probe_results.json`)**:①音频判别力否——同场景兄弟实例声学同貌(p08 margin +0.04/p08b −0.03),叠加 BGM/解说污染,不解决主失败族;②时序非重叠先验单独否——r10(错)与 r6(对)重叠几何同构(0.75 vs 0.76),任何阈值修一伤一,量化确认此前撤销结论;③**新发现:t3r10 真值区 441-443 有被压制的独立证据峰(0.73 vs 错位区 0.87)** → 可行方向 = 冲突段触发邻域扩池 + 时序一致联合重排(改动面小,仅冲突段)。
- **待用户拍板**:A. 立项 Part C 小阶段(0.5~1 天,验收=r10 修复+r6 零扰动+三指标不回退)/ B. 彻底关闭接受现状 / C. 维持手动替换不立项。
- 零 runtime 改动(仅新增研究脚本 `mvp/scripts/research_second_signal.py`),测试基线 187+58+67 不变。

## → 2026-08-30(晚)：Phase 23-0 特征升级预研完成 = 不建议 bump feature_version,结论待拍板

- **预研结论(证据见 `mvp/benchmark/user_case/feature_upgrade/FINDINGS.md`)**:ViT-B/14 在 7 探针 × 2 通路(CLS/patch-max)上零增益、p08b 明确恶化;patch ViT-S p08b 险胜与 Phase 21 V3 一致(已在 runtime)。**升规模成本收益不成立,建议不重建索引**。剩余失败族(同场景重复实例/同质场景互混)需非外观第二信号 = 新研究阶段。
- **待用户拍板**:A. 接受剩余失败为已知局限,Phase 23 关闭(推荐)/ B. 立非外观第二信号研究 / C. 坚持全量重建(不建议)。
- **交接基线复验**:185+56+67 全绿;DML 资产在位;8765 旧实例在。api 测试须 PYTHONPATH=`mvp/src;mvp`。

## → 2026-08-30(晚)：Phase 22 全六项完成 + 交接拍板,转入 Phase 23 特征升级(用户拍板立项)

- **交接拍板(2026-08-30 晚,AskUserQuestion 留痕)**:A **特征升级立项** ✅ / B **暂不打包** ✅(长期规则维持) / C **test2-4 LOW/MEDIUM 完整 GT 暂不补** ✅。
- **Phase 23 立项 = 特征升级**:方向 = 更强 backbone(ViT-B/14)或 patch 级检索;需 bump feature_version 全量重建索引(5 部片 GPU 约 1-2h)+ 回归验收,工作量一个大阶段。预研脚本 `mvp/scripts/research_feature_upgrade.py` + `research_feature_upgrade2.py`(失败探针 p26/p08/p08b/p27 + test3-r10 相邻镜头 + test4 观察段 + 易例回归 p01/p04/p17;ViT-S=现成索引缓存 vs ViT-B=局部窗口嵌入)。
- **仍待用户动作(与 Phase 23 并行)**:①重启剪映双击「test1-e...y draft」(v6 草稿已在 `D:\JianyingPro Drafts\`)做剪映通道最终验收;②test3 r10 结果页手动替换(真值 ≈7:24.5,当前错位 7:27-7:29)。

## → 2026-08-30：Phase 21 场景指纹召回扩展层已完成，转入 Phase 22 导出工程文件 + 精度收尾（用户拍板路线）

- **Phase 21（场景指纹召回扩展层）已完成并验收**（详见 STATE.md Current Task + `mvp/benchmark/user_case/scene_recall/FINDINGS.md`）：场景表进索引 + 检索后扩池默认开；同环境对照 38/41→39/41（+1 = p33 真救回），主定位零漂移；测试 144+51 全绿；未打包。
- **环境事件已修复**：`onnxruntime-directml` 1.24.4 恢复（曾被 rapidocr 安装顶掉致 DML 静默失效）；`measure_shot_recall.py` mid_in 规则修复。**跨日基线对比必须核对 backend selected=directml + 评测器同口径**。

## P0 — Phase 22：导出视频工程文件（剪映/PR）+ 精度收尾（2026-08-30 用户拍板顺序）

> 目标：定位结果可直接导出为剪映/Premiere 可导入的工程文件。先打通导出闭环（它定义精度的真实含义：in/out 帧准、段拆几块、置信门槛），再按 ROI 做精度修复。

1. ~~**导出 MVP**~~ — **DONE(2026-08-30)**：`mvp/src/app/exporters.py`（纯函数：ExportClip 计划/门槛策略/切点吸附/三格式渲染）+ `SourceLocatorService.export_project(fmt=edl|fcp7_xml|jianying)`。clip = `original` 主 span + `original_segments` 子 span（与主 span ±0.5s 近重合去重），按编辑时间排序；timecode 一律 NDF、源片 timecode 原点=首帧（与 iter_frames 媒体时间约定一致，MKV start_time 无需修正），fps 由 ffprobe 实测（失败回退 25 留痕）。**EDL**=单视频轨只放 main/low 主 clip（子 span 记录槽会重叠，多轨候选拒绝进 EDL）；**FCP7 XML**（xmeml v4）=V1 main / V2 low 备用 / V3+ 第 k 个子 span 一条轨（记录槽封顶父段编辑时长，跨结果不重叠），每个 clipitem 完整 `<file>` 定义（Premiere 惯例）；**剪映 draft 文件夹**（beta 锁 app_version 5.9.0）=draft_content.json + draft_meta_info.json，speed 素材承载源/记录时长比（快剪压缩>1 的真实节奏）。**API** `POST /api/export` 增 `format/min_confidence/low_policy/snap_scenes`。真机 E2E：test1 结果批 31 事件/119 clipitem/5 轨 14 段全落盘。
2. ~~**场景表切点吸附**~~ — **DONE(2026-08-30)**：`snap_clips_to_scenes` 把 clip 源片侧 in/out 吸附到最近 `IndexBundle.scenes` 边界（±1s 内才吸附，`ExportConfig.snap_tolerance_s`）；只动源片侧、记录侧不动；吸附致塌缩（out≤in+0.05s）回退该侧，整体仍坏则不吸附；超出片尾夹紧。索引不可用（MISSING/INVALID）静默降级 + 警告日志。真机 E2E：新建 120s 真索引（scenes 表 699→局部）→ 7/14 clipitem 实际吸附（EDL/XML 注释留痕 snap=in/out）。
3. ~~**导出策略**~~ — **DONE(2026-08-30, 门槛值待第 5 项标定定稿)**：`ExportConfig`（config JSON `export.*` 可覆盖）：`min_confidence=MEDIUM`（HIGH/MEDIUM 直进）、`low_policy=exclude`（LOW 不导）|`backup`（LOW 主 span 进独立备用轨；EDL 无轨概念照常出事件注释标注 conf=LOW）、`snap_scenes=True`。not_in_source / 段级失败(failure_reason) / 零宽 original|edited 不导；门槛之下结果的子 span 也不导。
4. ~~**精度修复两件（廉价）**~~ — **DONE(2026-08-30 深夜,全部真机验证)**：① card_guard 第三刀=`card_black_ratio` 0.60→**0.85**（test1 r10 实证:夜间剧情画面 black 0.73-0.78,其解说字幕白字行=宽扁大连通域+窗光低扩散骗过 spread/连通域两判据;全片扫描真卡 black≥0.93(n04 0.934-0.969/test1 片尾卡 0.96-0.99)、救回帧≤0.78,0.85 完成分离。真机:test1 32/33 段零漂移,r10 LOW/NIS→HIGH@2982,主案例 GT v3 三指标逐行零漂移=39/41/FP2/4/支撑90/136,n04 守住）。② 超短切=**no_evidence 密帧重试**（诊断发现 38.5-40.5 段实为 2s 压 3 子镜头,2fps 每簇 1 命中被 min_frames=2 门掉;8fps 下 3191 簇 7 命中/0.66 稳过门。实现:首查空证据且密帧未用→密帧为查询重试一次,只影响本来不导出的段,零回归风险;另含 <2 帧段主动用密帧查询。真机:test4 未定位 1→0,38.5 段成诚实 LOW 蒙太奇 5 子 span,其余零漂移）。单测 176 全绿(+5)。
5. **test1-4 镜头级 GT（4 部全部完成）+ Confidence 多案例标定** — **DONE(2026-08-30 傍晚)**：58 个 HIGH 段逐段对照图人工(多模态)裁决，汇总 `cases/multi_case_gt_summary.json`——**总精确率 56/58=96.6%**（test1 29/29、test2 3/3、test3 23/24、test4 0/2）。错配模式=**同质外观场景内部互混**（暗夜战斗/彩色滑梯/同场景重复镜头），案例 test4 全片为极端形态（检索全索引最强 sim 仅 0.53-0.60）。**导出门槛维持 MEDIUM**（HIGH 96.6% 支持直出，LOW 继续排除）；**Confidence 标定结论**：HIGH 档跨 4 视觉域可靠，特征升级触发条件=全片同质场景/同场景重复镜头（需 bump feature_version）。test3 r10 错配（正确位置≈7:24.5）已记录待结果页手动替换——重叠几何不可自动区分（7 段重叠 6 段正确，自动修复已实验并撤销）。
6. **真实导出产物验收**：**部分完成(2026-08-30)**：① 剪映通道——草稿已导入真机（`test1-ed.loc.jy_draft` 拷入 `D:\JianyingPro Drafts\`），**剪映 11.3.0 草稿列表成功识别并解析**（卡片显示 165.3K | 02:11 = draft_content.json 时长被正确读出，≈时间轴 129.5s ✓）；**待用户双击卡片确认时间轴渲染**（5 轨/素材路径/变速）——GUI 自动化被桌面环境阻断（ZCode 前台焦点保护 + 动态壁纸致帧失效），非格式问题。② EDL/XML 通道——**自动帧比对验收 PASS**：新脚本 `mvp/scripts/verify_export_frametruth.py`（解析 EDL 事件→ffprobe 实测 fps 反解时间码→抽「编辑片 record in」vs「源片 source in」帧算 DINOv2 相似度，+10s 错位对照）。test1（28 事件，23.976/30fps 混合）：roundtrip 28/28 ≤半帧、对齐子集 13/28、margin +0.130 PASS；120s E2E（6 事件）：roundtrip 6/6、对齐 4/6、margin +0.091 PASS；XML clipitem 帧号反解与 EDL 一致。判定三项分立（roundtrip=确定性时间码检查 / aligned 子集=系统性换算错误判据 / margin），单段低 sim（窗口内子镜头偏移/暗帧）不判 FAIL。失败样本决定是否立项特征升级。**剪映草稿为 beta（自研锁 5.9.0，11.3 实测列表解析通过），以双击打开为准**。
7. ~~UI 导出页~~ — **DONE(2026-08-30 深夜,含用户 15 条 UI 反馈轮)**:结果页导出对话框(剪映草稿/FCP7 XML/EDL/JSON,固定不含低置信,导出路径来自设置);另落地用户反馈 15 条(删媒体库/检查器/已知限制/无效保存/构建索引步骤;一键分析融合+粗粒度四步管线;分析页固定项目+切换器;项目持久化+删除重命名;候选预览/设为主定位;手动替换落后端批(override 端点);对比播放器解除强制同步;竖屏高度保护;导出路径设置;日志复制/压缩包)。详见 STATE.md UI 轮条目。
8. 打包：**需用户明确允许**（长期规则）。

## （已完成存档）P0 — Phase 21 实施:场景指纹召回扩展层(2026-08-30 交接,执行单见 STATE.md Current Task)

1. ~~**场景表进索引**~~ — DONE(scenes.npy+指纹,+scn1 bump,validate 纳入;5 索引 backfill)。
2. ~~**检索候选扩展**~~ — DONE(场景单元扩池,默认开;两条铁律:不混池重聚类、不进重排器)。
3. ~~**回归门槛**~~ — DONE 达标(同环境对照 38/41→39/41,+1=p33;零漂移;默认开)。
4. ~~单测+全套回归~~ — DONE(144+51 全绿,+10 场景/table/text-anchor 护栏)。

## P0 — MVP Stage 1：Windows CPU（PHASE H1）开发顺序

1. ~~`media/ffmpeg`（FFmpegIO：metadata / seek / 抽帧 / clip extract）~~ — **DONE 2026-08-25**。生产级随机定位基础（禁 cv2.CAP_PROP_POS_MSEC）。`mvp/src/media/ffmpeg/`（FFmpegIO + ffprobe + _runner）。冒烟 23 项 + unittest 6 项过；真实 2h08m HEVC MKV 随机定位/帧精度验证通过。
2. ~~`domain`（Result/Candidate/Confidence/IndexMeta/Enums）+ `infrastructure`（配置/日志/错误模型/路径）~~ — **DONE 2026-08-25**。`mvp/src/domain/`（纯数据 + 枚举 + `to_dict()` 契约 JSON）+ `mvp/src/infrastructure/`（errors/logging/paths/config，JSON 覆盖）。`MediaError` 改继承 `LocatorError` 统一错误模型。test 12 项 + media 复测全过，未提前实现 engine。
3. ~~`device/CPUBackend` + `engine/feature_store/FeatureStore`（index create/load/validate，INDEX_SPEC 落盘）~~ — **DONE 2026-08-25**。`mvp/src/device/`（DeviceBackend Protocol + 冻结 DINOv2 backbone 提取 + CPUBackend 无CUDA + pick_best_available 预留 H2/H3）+ `mvp/src/engine/feature_store/`（FeatureStore create/load/validate/invalidate/get_metadata/delete，落盘 index.json+features.npy+times.npy，size→duration→model/dim/fps/version→hash 硬检失效，`.stale` 隔离）。device smoke 25 项 + unittest 4 项过；DINOv2 CPU 真推理 L2 norm=1.0。
4. ~~`engine/ranking`(v2_score) + `engine/retrieval` + `engine/clustering`（从研究代码 REUSE/REFACTOR 提取）~~ — **DONE 2026-08-25**。`mvp/src/engine/`：common/similarity(cosine REUSE)、retrieval/retrieve_hits(批量 TopK K=20)、clustering/continuity 10/15/60s + _merged_ok + _bridges、ranking/v2_score(mean*sqrt(hit)*(0.5+0.5*cons)*n_reps^0.5)、candidates.py(produce_candidates 编排)。删 GT/gid/benchmark/实验路径/work 依赖；`_dp_path`/`temporal_align`/`localize`/`_recall`=RESEARCH_ONLY 未进 runtime；config retrieval_top_k 100→冻结 20。retrieval smoke 6 项 + unittest 7 项过。（注：合成图案场景 CLS 易混淆，冻结公式在真实影片数据正常。）
5. ~~`engine/localization`（longest_run + montage 检测分支）+ `engine/confidence/ConfidenceEngine`~~ — **DONE 2026-08-25**。`mvp/src/engine/localization/`（finloc_window = per-orig max-over-query coverage + 3帧平滑 + mask(0.4 冻结) + longest_run → 精确 span；multi_island 结构检测；pipeline.localize_segment = 候选→finloc 最佳→回填 best_cover→confidence→RefinedSegment；montage/无 span/run 过短 → original 退化候选窗范围）+ `engine/confidence/confidence.py`（ConfidenceEngine.assess → score + HIGH/MEDIUM/LOW + reasons + hard_flags + montage_flag + alternatives）。置信度权重/阈值全部为未标定占位，纳入 ConfidenceConfig（infrastructure/config.py，JSON 可覆盖）供 H1 标定；`engine/localization/__init__` 不 import 编排避免循环导入。禁 per-query argmax/voting/cut-aware/temporal align/改 similarity·ranking。unittest 10 项 + smoke 9 项全过（synthetic v2_score 混淆使 smoke top 候选非真值，属已知数据/模型属性，真值定位由 deterministic unittest 覆盖）。
6. ~~`engine/segment`（edited shot 切分，定义无 GT 查询单元）~~ — **DONE 2026-08-25**。`mvp/src/engine/segment/`（`ShotSegment` 模型 + `detect_shots` + `adjacent_distances` + `_merge_short`）。相邻帧余弦距离 `d=1-cos`（复用冻结 cosine）→ **局部 z-score 判别**（`z>=2` 且 `s>=cut_abs=0.30`；弃"相对基线死代码"，正对研究"绝对阈值 d 不可靠"证伪）+ 默认平滑窗=1（保留单帧 cut 锐利尖峰）+ NMS(min_gap=max(1,round(min_shot_s*fps))) + merge(过短段并入更弱边界一侧) + 无边界/单帧/空→回退整段（现状行为）。整体偏向下切分（产品约束）。`PipelineConfig` 加 4 占位字段 `seg_cut_abs/seg_z_thresh/seg_min_shot_s/seg_smooth`（3 接线点 + JSON 覆盖验证过）。segment unittest 11 项 + smoke(真 ffmpeg+DINOv2 3色多镜头) 结构不变式全过、输出诊断统计；全套 50 项回归通过。研究护栏：未改冻结算法、无 GT、未 sweep、未重引入 Phase19 原片 cut-aware。冒烟 3 纯色 CLS 混淆未切分=已知合成图案属性。
7. ~~application service（用例编排 + Result 持久化）~~ — **DONE 2026-08-25**。`mvp/src/app/`（`models.py`：`ProgressStage`(7 阶段)+`ProgressEvent`+`CancellationToken`；`locator_service.py`：`SourceLocatorService`(build_original_index/analyze_edited_video/locate/_locate_features/export_results/load_results；ffmpeg+backend+FeatureStore 惰性构建)）+ `infrastructure/results_repo.py`（`save_results/load_results` JSON 单文件批 `*.results.json`）+ `infrastructure/errors.py` +4（`IndexError/FeatureExtractionError/LocalizationError/ApplicationError` 均 `LocatorError` 子类）+ `domain/models.py` 增 `Result.failure_reason`+`Confidence/Alternative/Result.from_dict()`+`ResultBatch`。编排：locate(edited,original)=原片 validate→(重)建→load + 编辑侧 iter_frames@2.0fps→embed_frames→detect_shots + 逐 segment produce_candidates→localize_segment→拼 `domain.Result`(edited=shot.span)。失败隔离：单 segment 抛错→unresolved(LOW+failure_reason+日志 exception)+继续后续段，cancellation 抛 `ApplicationError` 不落入段级隔离。unittest 12 项 + 全套回归 62 项通过；synthetic 端到端冒烟通过（a1 命中 HIGH、export/reload 往返、cancellation、失败隔离）；修复 app 级 bug（`_result_from_refined` 曾传 `shot` 而非 `shot.span`）。**真素材首次完整管线跑通（§十，仅观察不调算法）**：2.mkv 索引 3834 帧/3793.6s、1.mp4 12 段/254 帧、12 Results(HIGH5/MEDIUM1/LOW6, candidate 485)、export/reload 往返、cancellation、失败隔离、total 3951.9s；peak_ram N/A（venv 无 psutil）。研究护栏：只编排不改冻结算法语义、无 GT、未 sweep。
8. **PySide6 UI → 转向 = Vue3+TS+Electron 桌面工作台（2026-08-26 交付 Stage 1）**。`mvp/ui/`（Vue3+TS+Vite+Pinia+router+Electron 壳）+ 自研 Fluent 组件 + `ServiceAPI`(Mock/Http 双适配器) + `services/types.ts` 镜像后端 JSON + 6 页面 + 设计文档。**`mvp/api/` FastAPI 桥已实现（2026-08-26）**；**`HttpServiceAdapter` 已切真实 FastAPI（2026-08-26）**（Mock/HTTP 双模式、Backend Connection 三态、统一错误处理）；**Electron 主进程后端生命周期管理已实现（2026-08-26）**；**Electron renderer→后端请求桥已实现（2026-08-26）**（生产 `file://` 绕开 CORS）；**FastAPI 异步任务基础设施已实现（2026-08-27）**（TaskManager + 后台 worker 调 `locate` + `/api/tasks/analyze|/{id}|/{id}/cancel`）；**WebSocket 实时进度 + UI Pipeline Binding 已实现（2026-08-27）**（`/ws/progress/{task_id}` 阶段级进度帧 + `{cancel}`、`HttpServiceAdapter.subscribeProgress`(WS 一次重连)、`analysis` store 任务式、`ProgressPipeline` 显示 stage/%-elapsed/cancel、Electron `window.backend.websocket` 代理；前端 `test` 61 项 + 后端 `unittest` 26 项全绿）；**Video Preview / extracted_path 已实现（2026-08-27）**（`POST /api/preview` + `GET /api/preview/media|edited` Range 服务 + `previewResult`/`getEditedVideoUrl` + `VideoComparisonPlayer` 接真实 src + Inspector「Generate Preview」；后端 `unittest` 43 项 + 前端 `test` 63 项 + 真实 ffmpeg 冒烟全绿）。**剩余**：Electron 打包产物 + 真机验收（`resources/backend` python.exe + api 就位后 spawn/health 冒烟；`ui/docs/DESKTOP.md`）→ 性能基准 + Confidence 标定。Installer + License + Device Binding 属后置授权项。
9. **PyInstaller 打包 + 真机验收**：PyInstaller **打包 done** + **bundle 瘦身 done**（946M→845M）+ **可运行 exe 已交付并实测**（`release/win-unpacked/Video Locator.exe`：自启后端 + `/api/health` 200 + `/api/index` `device_type: amd`(GPU) + 干净退出；模型内置 `resources/models` + `SVL_DML_MODEL`）；**安装器（NSIS Setup）已按用户拍板删除（2026-08-27，只交付免安装 exe）**。**2026-08-27 追加**：GPU/CPU 真切换（`/api/settings/device` + Settings 真下拉 + 持久化）+ 拖拽修复（preload esbuild bundle）+ CPU pth 内置（`SVL_DINOV2_WEIGHTS`）+ 打包改 `asar:false`；**同日再追加**：索引缺失修复（`GET /api/index/status` + `build_original_index` 前置校验 +徽章「构建中」）；**再追加**：分析「一直转圈/取消无效/异步导出 no_results」修复（HTTP 轮询 `GET /api/tasks/{id}` + embed 分批取消检查 + `last_result_batch()`）；**再追加**：特征提取「转圈」=慢+无进度显示修复（embed 分批上报「N/M 帧」+ progress 10→30 平滑）；**再追加**：结果页预览修复（剪辑预览改抽 edited_segment 转码 H.264 + 自动生成预览 + VideoPlayer 可拖 + 导出清空 previews）；**再追加（2026-08-27 晚）**：全流程重打包免安装 exe + `backend.exe` 打包引擎 E2E 实测（health 200 / index 复用 3834 帧 directml / analyze completed 12 段 / 与诊断复现一致）。**待授权/后续**：代码签名、Custom Icon、Commercial Protection（license/device binding/DRM）；安装器已删不恢复。
10. **性能基准（10/60/120min）**：**done（2026-08-27，`mvp/benchmark/release/`，directml）**——索引 10min 23.96s / 60min 135.22s / 120min 270.68s（~13fps 近线性）、分析 total≈3.35s（embedding 冷启动）、预览 ffmpeg≈0.4s→100KB，→ `result/benchmark_result.json` + `release_smoke_result.json`。**未插桩**：RAM/CPU（venv 无 psutil），如需可补。
11. Confidence 标定（`CONFIDENCE_DESIGN.md` 权重/阈值用真实数据校准：HIGH 高 precision、LOW 高 recall-of-hard）。**2026-08-27 研究证：纯置信度标定只能降档「自信错答」，修不了「定位内容对不上」**（置信信号全部由 CLS-sim 单调派生、无法区分正确 vs 相似外观）。作为「对不上」的修复优先级下调；保守化降档仍可作为产品侧短期缓解。

## P0.5 — 蒙太奇定位研究（2026-08-27 新立；研究闭环 + 产品化接入完成）

- **背景**：真实用户案例定位「编辑片有、定位结果没有」= 算法限制（蒙太奇→finloc 单 run 丢子镜头 + 相似外观误配），非 bug。多段定位是修复方向，与已证伪的 17A/18/19 不同（先聚类+门控+每簇 finloc，而非 per-query argmax 并集/voting/原片侧 cut-aware）。
- **研究闭环**：信号证实（干净=1 簇/蒙太奇=多簇并对齐 GT）+ 原型（query-axis 聚类+门控+每簇 merged span）+ GT 校检 **基线 2/7→86%**、子 span 精度≈94%、弱簇过滤、硬化可测模块。产物 `mvp/benchmark/user_case/montage_research/`（FINDINGS.md / montage_localize.py / verify_s*.jpg / query_axis.json / montage_localize.json），脚本 `mvp/scripts/research_*.py`。
- **产品化接入（2026-08-27，用户拍板「接入」）**：`Result.original_segments`（+`OriginalSegment`，向后兼容 `original` 主 span）、运行时模块 `engine/localization/montage_localize.py`、`app._apply_montage`（蒙太奇→多子 span+诚实降 LOW）、前端 `InspectorPanel` 多段显示。**打包免安装 exe 重打 + E2E 实测**：12 段 LOW9/HIGH3，蒙太奇段带 `original_segments`。「对不上」「自信答错」双修复生效。后端测试 132 + 前端 vitest 67 全过。
- **剩余（文档化，非高频）**：s5 跨段边界伪影 + 低频相似外观单例（需第二信号，实测误配低频、边际价值低，另立研究时机=真实数据证明高频）。

## P1 — MVP 增强项（不阻塞 H1，属于 MVP 外）

- （需授权/条件触发）若产品域蒙太奇占比高 → s4 单类定向验证：DINOv2 稠密 + VCSL/VTA 序列对齐，判断能否分清"多-shot 同源"；成功才接入二级定位器。
- 场景身份模型（解 s0/s1 深 rank）——非外观第二信号。
- VDF 音频指纹辅助（AGPL，需法律确认，仅"音频不变"场景）。

## P2 — 硬件后续阶段（严格顺序，不并行）

> **硬件路线（2026-08-25 锁定）**：H1 CPU → H2 AMD GPU(DirectML) → H3 macOS MPS → H4 CUDA。**不支持 macOS Intel**。
> **H2 = `DirectMLBackend` 已接入并验证（`H2_AMD_DIRECTML = IMPLEMENTED`）**：resolver + CPU fallback + CPU/DML 正确性 + FeatureStore 兼容 + 真实 2.mkv 生产冒烟（381.3s/3834帧/~10 fps）全过。POC 证据见 `mvp/poc/amdgpu_onnx/`（保留不删）。H3 = GO（POC 完成）；H4 = PENDING（下一阶段 POC 准备）；**UI（第 8 项）已转向 Vue3+TS+Electron，Stage 1 已交付**；**后端统一日志基础设施已交付（2026-08-26，FastAPI 桥之前）**。

- **PHASE H2 — Windows AMD GPU：✅ DONE（2026-08-25）**。`DirectMLBackend`（`mvp/src/device/directml_backend.py`）+ `resolve_backend` 自动选择/CPU fallback + 模型资产 resolver + `DeviceConfig` + `export_dml_model.py`。AMD 实机（RX 6750 GRE）：一致、10× 加速、内存稳定、FeatureStore 兼容。若未来某设备 DML 不稳则标 `AMD_GPU_BACKEND_BLOCKED` 继续 CPU（当前已自动 fallback，不影响 CPU MVP）。
- **PHASE H3 — macOS Apple Silicon：✅ `H3_MPS_GO`（2026-08-26，POC 完成，不再重复运行）**。真实 Apple Silicon（macOS 15.7.7 / arm64 / torch 2.13.0）CI 云端验证：`device_mps_actual=mps`（非 CPU fallback）、正确性 cos_mean=1.0 / max_abs_diff=6.3e-7 / mps norm deviation 1.19e-7、7.875× 加速（0.87→6.84 fps，batch=4 最佳 6.67）、500 帧 all_finite 无异常无 fallback、峰值内存 ~1.07GB。**关键约束：MPS batch_size 必须 ≤4（batch≥8 触发 `Invalid buffer size 5.37GiB` + 性能暴跌 2.83fps；推荐 batch=4）**。**不重新运行 H3 POC**。正式 `MPSBackend` 未实现（H3 后续阶段），**届时 embed_frames batch 必须 ≤4**。CI workflow `.github/workflows/h3-macos-mps.yml` = **workflow_dispatch-only（手动；不 push 自动触发）**。POC 见 `mvp/poc/macos_mps/`；`mvp/src/device` 零改动。**不支持 macOS Intel**。
- **PHASE H4 — Windows NVIDIA GPU (CUDA)**：未来 `CUDABackend`，无需改 Engine/FeatureStore/UI/AppService。不进当前实施阶段。**H4-0（环境与 runner 可用性检查，2026-08-26）= `H4_GPU_RUNNER_UNAVAILABLE`**——GitHub Actions 无可用 Windows GPU runner（个人公共仓库 + 官方 GPU runner 对开源项目不开放、无 Windows 变体；本机 GPU=AMD RX 6750 GRE、torch CPU build，无 NVIDIA）。需用户提供 NVIDIA 环境 + 计费权限方可推进 POC。

## Research Archive（研究阶段 TODO，已冻结，仅追溯）

- 历史研究任务（Phase 12~19）全部完成；见 `.agent/STATE.md → Research Archive`、`PROJECT_HANDOFF.md`、各 `benchmark_report_phase*.md`。
- 已关闭的待办：`--engine all` 重建 `benchmark_results.json`（需授权覆盖历史）；DINOv2+TA 成段（dinov2_ta total_pred=0，序列对齐 blocked，已归入"序列级需定向验证"）。

## 护栏（贯穿 MVP）→ 2026-09-01 用户逐条拍板（权威见 DECISIONS.md）

- **解除**：冻结 DINOv2 backbone / ONNX 只出 CLS / 相似度·检索·聚类·v2_score·coverage·finloc 语义 / 不新增 backbone/VLM / 已证伪变体与 patch/multi-scale 不进 runtime / 不做大规模 sweep / 不重跑研究 benchmark / 不改 GT。
- **保留**：研究代码（experiments/diagnostics/work/phase reports）保留，产品以"提取函数/公式"复用，不 import 研究脚本；不为好看伪造结果；未测/不可用如实标 NOT TESTED / BLOCKED。
