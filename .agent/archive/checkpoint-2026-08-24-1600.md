# Checkpoint

## Project

benchmark

## Created

2026-08-24 16:00

## Git

Repository detected: False

Git repository not detected. Checkpoint will continue without Git information.

### Modified Files

```text
No modified files detected.
```

### Recent Commits

```text
No recent commits available.
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark（D:\claudework\benchmark）

## Current Phase

Phase 12：解决真实电影解说素材的视觉特征匹配问题（双基线实验）

## Current Task

STEP 4-6（in progress）：Experiment A（DINOv2+TA）已跑出 real 全量结果（**recall=0**，特征诊断中）；Experiment B（官方 ISC+TransVCL）代码已就绪，待跑；随后失败分类 + 生成 `benchmark_report_phase12.md`。

## Completed

- Phase 1~11 全部完成（见 PROJECT_HANDOFF.md）：三引擎（VDF/TransVCL/VCSL）synthetic + real 实测、报告、License 矩阵、Phase 11 选型（TECHNOLOGY_SELECTION.md）
- Phase 12 基础设施：DINOv2 vits14 权重已下载（88MB，`work/dinov2_weights/`，校验通过）；ISC21 isc_ft_v107 权重已下载（401MB，`work/isc21_weights/`，输出 256 维 L2 特征验证通过）；timm 已装
- Phase 12 代码：`src/experiments/dinov2_features.py`（ViT-S/14 手写前向，load_state_dict 精确匹配）、`src/experiments/ta.py`（单调 DP 时间对齐，自包含测试通过）、`src/adapters/dinov2_ta.py`（0.5fps）、`src/experiments/isc_features.py`（ISC21 1fps）、`src/adapters/transvcl_official.py`（复用 run.py subprocess）、`src/experiments/failure_analysis.py`；已注册进 `src/benchmark.py` 的 ENGINES
- **Experiment A real 全量已跑**（`results/dinov2_ta/`）：原片 3830 帧特征提取 3026s（50min）+ 编辑片 64 帧 46s。结果 recall=0，total_pred=0（DP 未产出任何片段）

## Current Problem

**Experiment A 的 DINOv2 特征在 real GT 位置相似度极低，且 DINOv2 最佳匹配与 GT 系统性不符：**

- GT 7 段（edited 8/19/27/28/30/36/46s → orig 1302/6350/6351/6598/6599/6600/6601s），但 DINOv2 对每个编辑帧的 top 匹配集中在 **1130~1580s**（随编辑时间单调递增，sim 0.5~0.86），GT 声称的 6350/6598s 处 sim 仅 0.04~0.24
- 决定性测试：edited 20s 帧 vs orig 6599.1s 帧（ffmpeg/cv2 双验证定位正确）DINOv2 余弦 ≈ -0.002，ORB 仅 1 个匹配——**两帧不是同一画面**
- 待确认：**GT 本身可能不准确**（原 GT 用 ORB+人工核对建立），或编辑片画面与原片对应关系与 GT 不符。不能仅凭 DINOv2 断定 GT 错，需 ORB 独立交叉验证（edited 20s → 到底 1296s 还是 6599s）后再决定如何处理

## Current Implementation

- 特征链路已验证：DINOv2 384 维 CLS（L2 归一化）+ 单调 DP TA；ISC21 256 维（L2 归一化）+ TransVCL run.py subprocess
- 缓存：`work/dinov2_feats/{alias}@0.5fps.npy`、`work/isc_feats/{alias}.npy`（isc 缓存尚未生成，B 未跑）
- 待跑：`benchmark.py --engine transvcl_official --dataset real`

## Current Decision

- Phase 12 路线：A（DINOv2+TA，0.5fps）+ B（官方 ISC21+TransVCL，1fps）两个干净基线，不融合（用户 Phase 12 任务书）
- 不淘汰 TransVCL；不用 ORB-BOW 喂 TransVCL；不改 GT

## Next Actions

1. **解决 GT 疑点**：用 ORB 交叉验证 edited 20s 帧 → 原片 1296s vs 6599s，判断 GT 是否准确（这是 A 是否可信的前提）
2. 跑 Experiment B：`benchmark.py --engine transvcl_official --dataset real`（ISC 特征提取 + TransVCL CPU 推理）
3. 失败案例分类：`src/experiments/failure_analysis.py`
4. 生成 `benchmark_report_phase12.md`（对比表 + 失败案例 + 回答三问）

## Important Constraints

- **`datasets/real/ground_truth.json` 不可修改**（任务书铁律）
- 不为好看伪造/修改结果；失败如实标 BLOCKED
- 不做 GUI、不做最终产品、不研究音频指纹、不淘汰 TransVCL
- Python 用绝对路径 `D:\claudework\video-dedup-tool\.venv\Scripts\python.exe`
- 无 GPU（CPU only）；网络限制见 AGENTS.md

## Known Issues

- DINOv2 特征在 GT 位置相似度低 + top 匹配与 GT 不符（见 Current Problem，处理中）
- Experiment A 特征提取极慢（原片 3830 帧 50min，CPU）；缓存已存在，重跑复用
- `benchmark_results.json` 当前被 `--engine dinov2_ta` 单引擎运行覆盖为 dinov2_ta/real 单行；最终需 `--engine all` 重建三引擎汇总
- `src/experiments/*.py` 通过 sys.path.insert 导入，非包结构，直接 `python src/experiments/xxx.py` 可能 import 失败（用 `-c` + sys.path 方式）

## Last Updated

2026-08-24 16:00

## TODO Snapshot

# TODO

## P0 — Current

- [ ] 验证 GT 疑点：ORB 交叉验证 edited 20s 帧 → 原片 1296s vs 6599s，判断 real ground_truth.json 是否准确（当前 Experiment A recall=0 的关键阻塞）
- [ ] 跑 Experiment B：`benchmark.py --engine transvcl_official --dataset real`（首次会提取 ISC 特征 + TransVCL CPU 推理）

## P1 — Next

- [ ] 失败案例分类：`src/experiments/failure_analysis.py`（按任务书 A~J 十类）
- [ ] 生成 `benchmark_report_phase12.md`（A/B 对比表 + 失败案例 + 回答三问）
- [ ] 若 GT 确认有误：向用户报告 GT 证据（不擅自改 GT，需用户决策）

## P2 — Later

- [ ] 若 Experiment A/B 在正确特征下 recall>0：更新 ARCHITECTURE_ANALYSIS.md + README（Phase 12 收尾）
- [ ] `--engine all` 重建三引擎汇总 `benchmark_results.json`（当前被单引擎覆盖）

## Blocked

- Experiment B：依赖 ISC 特征提取首次运行（401MB 权重已下载，模型加载已验证通过；CPU 特征提取耗时待实测）
- GT 疑点未解决前，Experiment A 的结论不可信（需先做 ORB 交叉验证）
