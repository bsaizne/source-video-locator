# Checkpoint

## Project

benchmark

## Created

2026-08-25 04:20

## Git

Repository detected: False

Git repository not detected. Checkpoint will continue without Git information.

### Modified Files

```text
No modified files detected.
```

### Recent Commits

```text
No recent commits available.
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark（D:\claudework\benchmark）

## Current Phase

Phase 19 完成（Cut-Aware Coverage Localization，**只改 finloc 层**，DINOv2/retrieval/ExpA/GT/cosine/指标全不变，首轮固定方案无 sweep）。**结论：** `cut_aware_coverage_localize`（per-orig coverage 主信号 `cover=sim.max(axis=0)` + 3 帧平滑 + 阈值 mask + 收紧 source-cut `cut_thresh 0.65 / min_gap 2 帧` + 每个 run 在内部 cut 处切分 + 有序 chain 合并 `gap≤4s 且 gap 无 cut`）修好了 17A/18 的 s0/s1 稳定(0.571/0.647 保持)、0 None、s5 不再反向跨段([1396,1406] cross=F)；但**主目标 s4 未达成**(仍 0.333，[1383,1389] 的 8s coverage 洞 gap_merge=4 桥接不了，右岛 [1390,1396] 还跨 1394 边界)、且 **s2 0.5→0.25 / s3 0.75→0.25 / s6 0.75→0.5 明显退化**（cut 在段内误检：s6 cut@1584、s2 cut@1340/1344、s3 cut@1340，"无-cut-merge 门控"把它们截断）。s5 的 cross=F 是 best-chain-by-coverage 分数巧合 + coverage 信号之功，非 cut 稳健切分（blocked=[] 为空）。按安全阀：主目标失败+多处退化 → **不 sweep cut_thresh/gap、不调参，记录失败原因**。见 `benchmark_report_phase19_cut_coverage.md`。

## Current Task

- ✅ Phase 19：Cut-Aware Coverage Localization（2026-08-25）。`src/experiments/phase19_cut_coverage.py`——`cut_aware_coverage_localize` 取代 finloc `longest_run`（per-orig coverage 主信号 + 3 帧平滑 + 收紧 source-cut + 每个 run 在内部 cut 处切分 + 有序 chain 合并 `gap≤4s 且 gap 无 cut`），其余复用 query_density_14c。产物 `work/phase19_cut_coverage/phase19_results.json`、`benchmark_report_phase19_cut_coverage.md`。**recall_B 7/7、0 None、0 cross；但 s4 仍 0.333（[1383,1389] 的 8s coverage 洞 gap_merge=4 桥接不了，右岛 [1390,1396] 跨 1394）、s2 0.5→0.25 / s3 0.75→0.25 / s6 0.75→0.5（cut 段内误检：s6@1584、s2@1340/1344、s3@1340，无-cut-merge 门控截断）。s5 cross=F 是 coverage 信号+分数巧合非 cut 功。裁决：主目标失败+多处退化，不 sweep/不调参，记录失败原因，未进 Phase 20。**
- ✅ Phase 18：Temporal Neighborhood Voting Prototype（2026-08-25）。`src/experiments/phase18_voting.py`——`temporal_neighborhood_voting_localize` 取代 finloc `longest_run`（per-query 邻域 Top-K 加权 histogram argmax→consensus + cummax 单调 + source cut 门 + gap_merge 并集），其余复用 query_density_14c。产物 `work/phase18/phase18_results.json`、`benchmark_report_phase18.md`。**s4 0.333→0.889(end_err 0)、s2 0.5→0.667、0 None；但 s0 0.571→0.0、s3 0.75→0.0、s6 0.75→0.167（静态塌缩成点）、s1 0.647→0.333、s5 w3 仍反向跨段(-10)。n_sub 恒 1 → multi-shot 并集+cut 门未触发；cut 过度检测(每窗 5~10)。裁决：部分改善+多处退化，不 sweep/不调参，记录失败原因，未进 Phase 19。**
- ✅ Phase 17A：Multi-shot Temporal Localization（2026-08-25）。`src/experiments/phase17a_multiseg.py`——仅把 finloc 的 `longest_run` 换成 `multi_segment_localize`（edited 轴证据子段 + monotonic/speed/teleport 校验 + gap 合并 + 并集），其余全部复用 query_density_14c。产物 `work/phase17a/phase17a_results.json`、`benchmark_report_phase17a.md`。**s4 0.333→1.0（gt_query，目标达成）；但 s0/s2→None、s1/s3/s6→过窄、s5→反向跨段(≈10s)。gap 2/4/6 全同（无 gap 可调），裁决：不继续调 gap，记录失败原因，未进 Phase 18。**
- ✅ Phase 16B.1：Agent Multimodal Post-Review（2026-08-25）。逐帧读 `diagnostics/{s2,s4,s0,s1}/sX_comparison.jpg`、`s4_gt_audit/{edited_strip,original_1s,original_0_5s_boundary}.jpg` + `case.json`/`s4_gt_audit.json`，解释 16B 结果。产物 `diagnostics/phase16b_multimodal_review.md` + `.json`。仅新增两文件，未动任何代码/GT/特征。
- ✅ Phase 16B：A. `src/experiments/phase16b_rerank.py`——候选打分乘 `n_reps^α`（distinct edit 帧=查询时序覆盖度），α=0.5 宽稳定平台，s2 2→1、其余不变、recall_B 7/7、finloc 不变；纯 numpy 无特征重算。B. `phase16b_patch.py`——`dinov2_features.py:118` 的 patch pooling（`x[:,1:]` mean→[B,384]）+ `patch_features.py` 建 `2@0.5fps_patch.npy`（[3830,384]），换特征源走同管线：s2 仍 rank2（错窗 sim 0.758>正确区 0.658）、s4 iou 0.333→0.429 但 traj 越界 end+24、s3 iou 0.75→0.0、s0 cover 1.0→0.667。C. `phase16b_multiscale.py`——`sim=w*CLS+(1-w)*patch`，w∈{0.3,0.5,0.7}：s2 恒 rank2、s4 iou 0.444(w.5)/0.529(w.3) 但越界、s3 0.75→0.0、s5 0.455→0.353。**裁决：采纳 A；不采纳 B/C。** 产物：`work/dinov2_feats/2@0.5fps_patch.npy`、`work/phase16b_{rerank,patch,multiscale}/results.json`、`work/phase16b_report_data.json`、`benchmark_report_phase16b.md`。**完成后停止，未进 Phase 16B.1 之后。**

## Completed

- Phase 1~11 全部完成（见 PROJECT_HANDOFF.md）
- Phase 12：双基线 real 首跑 recall=0；三重证据确认原 GT 6/7 段错位；重建 corrected Segment GT；B recall 0.143 / A recall 0。
- Phase 13A/B/C：Fine Segment Localization。13A 连续轨迹达成（s6 0.75/s1 0.632）；13B 证原片 0.5fps 非瓶颈；13C 证 gap tolerance 近无效、expanded query 有害。
- Phase 14A：全局 Candidate Retrieval。s0 被召回（补回 TransVCL 漏掉）、868s 巨窗消除、7/7 正确区浮现；strict cover 3/7。
- Phase 14A.1：Candidate Clustering 连续性修正。gap10/15/20 → recall_B 恒 3/7、无巨窗。产物 `work/phase14a1_clustering/phase14a1_results.json`。
- Phase 14C：Edited Query Density。pad=±4s 固定、只查 {2,4,8}fps。**n_recall_B 恒 6/7（s0/s1/s2/s4/s5/s6=Y, s3=N）在每档 fps 完全相同**；s4 0.818/s6 1.0/s2 0.538（对比 14A.1 pad0，提升全来自 pad）。更高 fps 反略伤 fine localization。产物 `work/phase14c_density/phase14c_results.json`。
- **Phase 16A**：corrected GT 边界复核并收紧 s2/s3/s4（0.5s 边界探针 + contact sheet）；14C 重跑 **n_recall_B=7/7**（s3 由 miss 变命中）。产物 `diagnostics/{s2,s3,s4}_gt_audit*`、`work/phase16a_gt_correction/comparison.json`。
- **Phase 16B（本次）**：见 Current Task。结论 ExpA 有效（s2 2→1 零附带损伤）；ExpB/C 无净收益、不建议继续。`benchmark_report_phase16b.md`。
- **Phase 16B.1（本次）**：Agent Multimodal Post-Review，纯视觉复盘（读帧解释 16B），产物 `diagnostics/phase16b_multimodal_review.md` + `.json`。
- **Phase 17A（本次）**：Multi-shot Temporal Localization（只改 finloc）。s4 0.333→1.0 验证假设；但 multi-seg 退化 5/6 段 + s5 反向跨段，gap 2/4/6 全同 → 不采纳，不继续调 gap。`benchmark_report_phase17a.md`。
- **Phase 18（本次）**：Temporal Neighborhood Voting Prototype（只改 finloc）。s4 0.333→0.889 / s2 0.5→0.667 / 0 None；但 s0/s3/s6 塌缩成点、s1 过窄、s5 w3 反向跨段；multi-shot 并集+cut 门未触发、cut 过度检测。属部分改善+多处退化 → 不采纳，不继续调参。`benchmark_report_phase18.md`。
- **Phase 19（本次）**：Cut-Aware Coverage Localization（只改 finloc）。coverage 主信号保持 s0/s1 稳定、0 None、s5 不再反向跨段；但 s4 仍 0.333（[1383,1389] 的 8s coverage 洞 gap_merge=4 桥接不了、右岛 [1390,1396] 跨 1394）、s2 0.5→0.25 / s3 0.75→0.25 / s6 0.75→0.5（cut 段内误检 + 无-cut-merge 门控截断）；s5 cross=F 是 coverage 信号+分数巧合非 cut 功。主目标失败+多处退化 → 不采纳，不调参。`benchmark_report_phase19_cut_coverage.md`。

## Current Problem

- **已解决（16A）**：s3 GT 边界问题，收紧后 recall 7/7。
- **已修复（16B-ExpA）**：s2 候选排序——`n_reps`（查询时序覆盖度）把正确区 [1296,1346](10 edit 帧) 抬到 rank1，错窗暗室内男 [1212,1234](5 edit 帧) 降 rank2；零附带损伤，recall_B 7/7。
- **已解决（17A）s4 finloc 根因**：不是检索/特征，而是 `longest_run` 单连续 run 对 montage 过严。query 轴并集（multi_segment_localize，gt_query）把 s4 0.333→1.0，**完美补回 [1374,1392]**。**但实现的 per-query-argmax 信号太脆弱**——在 s0/s2 无单调 run→None、s1/s3/s6 过窄、s5 反向跨 s4/s5 越界。证明"query 轴并集"方向对、但需**更稳的 query 证据**（时间邻域一致性投票，非单帧 argmax）+ 经 shot 切分校验的并集融合。
- **Phase 18 验证了"更稳 query 证据"的邻域投票版**：修好 s4(0.889)/s2(0.667)、消除 None，但**静态场景塌缩成点**（s0/s3/s6）且 **s5 反向跨段未可靠消除**（w3 仍在）。**已确认根因不是"argmax 抖动"，而是更根本的**：(a) per-query 单 bin 众数丢弃区间（per-orig 覆盖才能保区间）；(b) 单调路径永不断段 → multi-shot 并集+cut 门成了死路径；(c) cut 过度检测 + 门控只防 merge 不防单 run 内跨越。
- **仍未解**：s0/s1 正确区深 rank6/5（相似场景混淆，需非外观第二信号）；s5 的 finloc 边界（18 暴露 s5 单 run 跨 s4/s5 cut 反向吸收）；s2 正确候选 rank2（ExpA 已修排序列，finloc iou 0.5 仍非满）；s4 start 仍差 2s。

## Current Implementation

- 已验证：ExpA 得分 = `mean*sqrt(hit)*(0.5+0.5*cons) * n_reps^0.5`，仅改候选排序（`best_cover` 与 rank 无关 → 不影响 recall/cover/finloc），alpha∈[0.4,0.8] 稳定；4fps 下 s0 6→4、s1 5→4。
- 特征：DINOv2 384-d CLS + 新增 mean-pooled patch 384-d（`dinov2_features.py:118` 取 `x[:,1:]` + `patch_features.py`）。patch ORG 缓存 [3830,384] 与 CLS 逐行对齐。
- 检索/finloc 主流程未改（`best_cover`/`localize`/`_cluster_continuous` 与特征源无关）。

## Current Decision

- **采纳 ExpA（rerank `n_reps`）**：一条结构信号（候选覆盖编辑剪辑的时序广度）干净修好 s2，无需特征重算/不改 GT/不改口径。
- **不采纳 ExpB/C**：证据充分——修不好 s2，s4 仅指标微升+越界，且重伤 s3/s0/s6/s5/s2；固着于"特征判别"方向不可行。
- **仅 finloc 层换 multi_segment = 已验证但不可采纳**：s4 0.333→1.0 证明假设成立，但 multi-seg 退化 5/6 段、s5 反向跨段、gap 2/4/6 全同（无 gap 可调）→ 按安全阀不继续调 gap。若要继续，方向 = **更稳的 query 证据（时间邻域一致性投票）+ 经 shot 切分校验的跨 shot 并集融合**（并集须防 s5 式反向 merge），或接受 s4 已可由 query 轴并集修复、其余段维持 longest_run。**Phase 17A 后停止，未进 Phase 18。**
- **仅 finloc 换 temporal neighborhood voting = 已验证但不可采纳**：s4 0.889/s2 0.667/0 None 优于 17A，但 s0/s3/s6 塌缩成点、s1 过窄、s5 反向跨段未可靠消除——**"邻域投票"保留了 multi-shot 能力却丢了 longest_run 稳定性，未同时满足两要求**。关键失败机制已定位（per-query 单 bin 丢区间、单调路径不断段使并集+cut 门成死路径、cut 过度检测）。**Phase 18 后停止，未进 Phase 19。**
- **仅 finloc 换 cut-aware coverage = 已验证但不可采纳**：coverage 主信号保持 s0/s1 稳定、0 None、s5 不再反向跨段（对比 17A/18）；但 s4 仍 0.333（[1383,1389] 的 8s coverage 洞 gap_merge=4 桥接不了）、s2/s3/s6 明显退化（cut 在段内误检 + "无-cut-merge 门控"截断）。**"cut-aware run 级切分" 反例成立**：它修不好 s4、且因 0.5fps 下内容不连续性几乎处处高（d 0.6~0.94）无法区分"段内 montage 洞"与"跨段真边界"而重伤 s2/s3/s6；s5 的 cross=F 是 coverage 信号+best-chain-by-coverage 分数巧合，非 cut 稳健切分。**Phase 19 后停止，未进 Phase 20。**
- ExpB/C 之外的 finloc 次优补丁：s5 需避免把 s4 尾段 [1384,1393] 并进来（17A 暴露）；s0/s1 相似场景混淆需非外观第二信号。

## Next Actions

0. **（17A/18/19 均已测且不可采纳）** 17A `multi_segment`（per-query argmax）：仅修好 s4(1.0) 整体退化；18 `temporal_neighborhood_voting_localize`（per-query 邻域投票）：s4(0.889)/s2(0.667)/0 None 但仍退化 s0/s3/s6/s1、s5 反向跨段；19 `cut_aware_coverage_localize`（per-orig coverage 主信号 + run 级 source-cut 切分）：s0/s1 稳定、0 None、s5 不再反向跨段，但 **s4 仍 0.333、s2/s3/s6 退化**。**三轮一致证明：①per-orig coverage 是对的主信号（稳定性来源）；②"cut 段内切分"修不好 s4 且因 0.5fps 下内容不连续性近乎处处高而无法区分段内洞与段外边界、重伤 s2/s3/s6，反例成立。** finloc 已到边际收益递减，剩难点是 **query 视角时序一致性/更高层场景模型**（s4 的跨 shot coverage 洞 + s5 反向 + s0/s1 相似混淆），或**接受 s4 0.333 / 其余 baseline**。未授权不启动。
1. （可选，需授权）校正 Segment GT 下 `--engine all` 重建 `benchmark_results.json`（当前空/单引擎态；会覆盖历史）。
2. （可选）s0/s1 相似场景混淆：ExpA 的 `n_reps` 在 4fps 令 s0 6→4、s1 5→4，可评估是否整体加入；或更高层场景模型。
3. （明确不建议）s2/s4 换 pooled-patch / multi-scale 特征（16B 已证无净收益）；不继续调 multi-seg gap（17A 已证 gap 2/4/6 全同）；不继续调 window/gap/boundary_thresh/cut_thresh（18/19 已证非杠杆，失败机制在聚合/门控设计与信号粒度）。
4. 若无后续：Phase 12/13/14A/14A.1/14C/15/16A/16B/16B.1/17A/18/19 以 report §6 + corrected Segment GT + 各阶段结果 + Phase15/16B/17A/18/19 报告收尾。

## Last Updated

2026-08-25 04:20
## Important Constraints

- **`datasets/real/ground_truth.json` 不可修改**（已遵守）。
- 不为好看伪造/修改结果；失败如实标注。
- **正式 GT 以 Segment 为单位**；不把 original_end 固定为 start+1；boundary_uncertain 时如实标记。
- 不做 GUI、不做最终产品、不研究音频指纹、不淘汰 TransVCL。
- Python 绝对路径 `D:\claudework\video-dedup-tool\.venv\Scripts\python.exe`。
- `benchmark.py` 每次运行覆盖 `benchmark_results.json`（本阶段未跑）。

## Known Issues

- **s4 finloc iou 0.333**（CLS 对暗色军事镜切分不清；pooled-patch/multi-scale 不能干净修复）。
- **s0/s1 正确区深 rank6/5**（相似场景混淆；ExpA `n_reps` 在 4fps 有助 s0/s1）。
- **ExpB/C 附带损伤**：patch 使 s0 cover 1.0→0.667、s3 finloc 0.75→0.0、s2 iou 0.5→0.226、s6 0.75→0.5；multi-scale 使 s3 0.75→0.0、s5 0.455→0.353。
- `benchmark_results.json` 空/单引擎态（每次运行覆盖）。`--engine all` 重建暂缓。
- DINOv2 整帧 CLS 与 pooled-patch 对"暗色+军人/人物"军旅素材判别力有限（语义混淆，Phase 15 C）。
- s0 校正值 weak；s0–s5 为 boundary_uncertain。

## Last Updated

2026-08-25 04:20

## TODO Snapshot

# TODO

## P0 — Current（Phase 12 收尾）

- [x] 三重证据确认原 real ground_truth.json 6/7 段错位（真实对应 1261–1742s）
- [x] 用户拍板：正式 GT 以 **Segment** 为单位（保留 edited/original 起止，不把 original_end 固定为 start+1；只能确认场景→标 boundary_uncertain；锚点仅辅助）
- [x] 逐段连续对应终核（`vision_qa.cli case` 生成编辑片段帧 vs 候选原片区间帧 contact sheet），确认 s6=1578–1602 连续、s0 最弱
- [x] 重建 `datasets/real/ground_truth_corrected.json`（Segment + boundary_uncertain；原 GT md5 不变）
- [x] runner 加 `--ground-truth`/`--gt` 开关（benchmark.py + report.py），正控通过
- [x] 校正 Segment GT 下重测：**B recall=0.143（匹配 s5）/ A recall=0**；更新报告 §5/§6、STATE、终核产物、记忆

## P1 — Next（Phase 13/14 + 可选，需用户授权/决策）

- [x] **Phase 14A：Candidate Retrieval 全局粗定位**（2026-08-24）。per-GT edited 2fps 查询 × 全片 3830 帧 Top-K=20，按原片时间近邻聚成候选窗（强制 ≤60s）。**成果：s0 被召回 [1130,1140]（曾被 TransVCL 窗口 [1261,2130] 完全漏掉）；869s 巨窗消除（max 44s/avg 4.6s）；7/7 GT 正确区域都出现在候选窗。** strict cover≥0.5 召回 3/7（s0/s1/s5）。敏感性 gap 10/15/20/30：s4=聚类伪影（gap≥15 翻 0.27→0.82），s2 恒 0.46、s6 恒 0.17、s3 0.12→0.38。见 `work/phase14a_candidate_retrieval/phase14a_results.json`。**完成后停止，未进下一阶段。**
  - [x] **Phase 14A.1：Candidate Clustering 连续性修正**（2026-08-24）。仅改聚类/排序：gap 10/15/20 + >10s 桥接需过 单调+mean_sim+局部速度 判据 + 60s 硬上限。**recall_A=7/7、recall_B 恒 3/7、无巨窗（≤44s）**。关键：s4 正确区在 10/15/20 都未桥接（两碎片，连续性拒 12s 桥，因 4 帧查询→20s 区 edit↔orig 非单调）；Phase 14A pure-time gap15 能让 s4 翻 0.82 但无时序验证。**结论：聚类参数非杠杆，剩 miss 是查询密度(s2尾/s4/s6)+特征难例(s3)；gap→20 引入 s4 假桥[6614,6644]。** 修复 `_hits` sims reshape bug。见 `work/phase14a1_clustering/phase14a1_results.json`。
  - [x] **Phase 14C：Edited Query Density**（2026-08-24）。pad=±4s 固定，只改查询采样率 {2,4,8}fps，其余全不动。**n_recall_B 恒 6/7 所有 fps 相同——fps 非杠杆；提升来自窗口 span（pad 把 recall 3/7→6/7）**：s4 0.273→0.818（两碎片融合[1374,1404] bridged=[]）、s6 0.167→1.0、s2 0.462→0.538（正确区 rank2 被错误场景 rank1[1212,1234] sim0.726 压过）。更高 fps 反略伤 finloc。产物 `work/phase14c_density/phase14c_results.json`；脚本 `src/experiments/query_density_14c.py`。（Phase 15 视觉修正：s3 "特征难例"实为 **GT 边界/不确定**，见下。）
  - [x] **Phase 15：Agent-Assisted Visual Diagnosis**（2026-08-25）。新建 `src/diagnostics/` 管线；每 GT 生成 contact sheet(sX_comparison.jpg + sX_edited.jpg) + case.json + `diagnostics/AGENT_REVIEW_PROMPT.md`；Agent 逐段视觉审查写 `diagnosis.json`；装配 `benchmark_report_phase15.md`。**诊断**：s2=C 判别失败(暗室内男子0.726反超正确区0.558)、s4=C(暗色军事画面不可分, finloc iou0.273)、s3=E GT 边界/不确定(蒙太奇拉向 s2/s4 两区+GT 尾段异构)、s0/s1=B 相似场景混淆(正确区 rank6/5)、s6=F 证据不足(已解决)、s5=B 干净命中。**贯穿主因：整帧 CLS 混同'暗色+军人/人物+纹理'外观。**
  - [x] **Phase 16A：corrected GT 边界复核/收紧**（2026-08-25）。0.5s 边界探针 + contact sheet 复核 s2/s3/s4：s2[1332,1358]→[1332,1348]、s3[1340,1356]→[1340,1348]、s4[1370,1392]→[1374,1392]；14C 重跑 **n_recall_B=7/7**（s3 由 miss 变命中 cover0.375→0.75）。产物 `diagnostics/{s2,s3,s4}_gt_audit*`、`work/phase16a_gt_correction/comparison.json`。
  - [x] **Phase 16B：Candidate Ranking + Fine Localization 精度提升**（2026-08-25）。只测 A rerank / B patch / C multi-scale（不换 backbone/不改 GT/不改口径）。**结果：ExpA（`n_reps` 查询时序覆盖度 rerank，α=0.5）把 s2 best_rank 2→1，其余段不变、recall_B 恒 7/7 ——有效，采纳。ExpB（patch pooling）/ ExpC（multi-scale）修不好 s2、s4 仅指标微升但 traj 越界、重伤 s3/s0/s6/s5/s2 ——无净收益，明确不采纳。** 产物 `work/dinov2_feats/2@0.5fps_patch.npy`、`work/phase16b_{rerank,patch,multiscale}/results.json`、`work/phase16b_report_data.json`、`benchmark_report_phase16b.md`。
  - [x] **Phase 16B.1：Agent Multimodal Post-Review**（2026-08-25）。纯视觉复盘（逐帧读 `diagnostics/{s2,s4,s0,s1}/sX_comparison.jpg`、`s4_gt_audit/*` + `case.json`/`s4_gt_audit.json`），仅产出 `diagnostics/phase16b_multimodal_review.md` + `.json`，不改任何代码/GT/特征。结论：ExpA=时间覆盖度判断（非更准视觉相似度）；s4 瓶颈=shot-based temporal localization（longest_run 单连续段对蒙太奇过严，非查不到）；s0/s1 深 rank 说明仅靠 n_reps 有上限（需非外观第二信号）。
  - [x] **Phase 17A：Multi-shot Temporal Localization**（2026-08-25）。只改 finloc：`src/experiments/phase17a_multiseg.py` 的 `multi_segment_localize()` 取代 `longest_run`（edited 轴证据子段 + monotonic/speed/teleport + gap 合并 + 并集），其余复用 query_density_14c。**s4 0.333→1.0（gt_query）验证假设成立；但 s0/s2→None、s1/s3/s6→过窄、s5→反向跨 s4/s5（start_err -10）；gap 2/4/6 逐段全同（无 gap 可调）→ 按安全阀不继续调 gap，记录失败原因（per-query-argmax 信号脆弱）。** 产物 `work/phase17a/phase17a_results.json`、`benchmark_report_phase17a.md`。未进 Phase 18。
  - [x] **Phase 18：Temporal Neighborhood Voting Prototype**（2026-08-25）。只改 finloc：`src/experiments/phase18_voting.py` 的 `temporal_neighborhood_voting_localize()`（per-query 邻域 Top-K 加权 histogram argmax→consensus + cummax 单调 + source cut 门 + gap_merge 并集）取代 `longest_run`，其余复用 query_density_14c，window 只测 {3,5}。**s4 0.333→0.889(end_err 0)/s2 0.5→0.667/0 None；但 s0 0.571→0.0、s3 0.75→0.0、s6 0.75→0.167（静态塌缩成点）、s1 0.647→0.333、s5 w3 仍反向跨段(-10)。n_sub 恒 1 → multi-shot 并集+cut 门未触发；cut 过度检测(每窗 5~10)。属部分改善+多处退化 → 不采纳，不 sweep window 7/9/11/不调参，记录失败原因。** 产物 `work/phase18/phase18_results.json`、`benchmark_report_phase18.md`。未进 Phase 19。
  - [x] **Phase 19：Cut-Aware Coverage Localization**（2026-08-25）。只改 finloc：`src/experiments/phase19_cut_coverage.py` 的 `cut_aware_coverage_localize()` 取代 `longest_run`（per-orig coverage 主信号 `cover=sim.max(axis=0)` + 3 帧平滑 + mask≥0.4 + 收紧 source-cut `cut_thresh 0.65/min_gap 2 帧` + 每个 run 在内部 cut 处切分 + 有序 chain 合并 `gap≤4s 且 gap 无 cut`），其余复用 query_density_14c；main 先跑 pre-flight 只读诊断（s4 内部 1382-1384 / s4/s5 边界 1392-1394 → 判 A）。**recall_B 7/7、0 None、0 cross；但 s4 仍 0.333（[1383,1389] 的 8s coverage 洞 gap_merge=4 桥接不了、右岛 [1390,1396] 跨 1394 边界）、s2 0.5→0.25 / s3 0.75→0.25 / s6 0.75→0.5（cut 段内误检：s6@1584、s2@1340/1344、s3@1340，"无-cut-merge 门控"截断 union）。s5 cross=F 是 coverage 信号+best-chain-by-coverage 分数巧合（blocked=[]），非 cut 稳健切分。主目标失败+多处退化 → 不采纳，不 sweep cut_thresh/gap、不调参，记录失败原因。** 产物 `work/phase19_cut_coverage/phase19_results.json`、`benchmark_report_phase19_cut_coverage.md`。未进 Phase 20。
- [x] **（19 已测：per-orig 覆盖主信号 + run 级 source-cut 切分 = 不可采纳，方向收窄）** 该方向即 Phase 19 实现。coverage 主信号部分验证了对（s0/s1 稳定、0 None、s5 不反向跨段），但 **run 级 cut 切分修不好 s4（8s coverage 洞>gap_merge）且因 0.5fps 下内容不连续性近乎处处高（d 0.6~0.94）无法区分段内洞与段外边界、重伤 s2/s3/s6（cut 段内误检+无-cut-merge 门控截断）**，反例成立。17A/18/19 三轮一致：per-query 单证据不稳、per-orig coverage + content-cut 无法识别"同段内 montage 洞 vs 跨段边界"。若再推进 finloc（需授权）= query 视角时序一致性/更高层场景模型，或接受 s4 0.333 / 其余 baseline。
- [ ] （可选，需授权）**s0/s1 相似场景混淆**：ExpA 的 `n_reps` 在 4fps 令 s0 6→4、s1 5→4，可评估整体接入；或更高层场景模型。
- [ ] **（明确不建议，16B 已证）s2/s4 换 pooled-patch / multi-scale 特征**：16B 显示错窗暗室内男在 CLS 与 pooled-patch 空间都更近，修不好 s2；s4 仅指标微升+越界。若坚持，方向=局部 patch↔patch 对应（shot 边界匹配）或场景身份模型。
- [ ] （可选，需授权）校正 Segment GT 下 `--engine all` 重建 `benchmark_results.json`（当前空/单引擎态；会重跑 vdf/vcsl/transvcl 并覆盖历史）。
- [ ] （可选）DINOv2+TA 调优：放宽 DP 单调对齐阈值以成段（当前 total_pred=0）。

## P2 — Later

- [ ] 若 Phase 13 跑通：用校正 Segment GT 更新 ARCHITECTURE_ANALYSIS.md + README 选型结论
- [ ] 评估 s0 弱置信度项（可选：更精细视觉定位）

## Blocked

- **`--engine all` 重建汇总**：被"不覆盖 results/vdf|vcsl|transvcl 历史结果"约束阻断，需用户授权允许覆盖。
- **DINOv2+TA 成段**：DP 单调对齐阈值过严，需调优特征或阈值（在正确 GT 下仍 total_pred=0）。
- **boundary_uncertain 的 6/7 段**：s0–s5 仅确认场景/连续区域，完整边界待 Phase 13。
