# Checkpoint

## Project

benchmark

## Created

2026-08-27 20:42

## Git

Repository detected: True

Git information collected.

### Modified Files

```text
M .agent/CHANGELOG.md
 M .agent/DECISIONS.md
 M .agent/INDEX.md
 M .agent/STATE.md
 M .agent/TODO.md
 M .gitignore
 M PROJECT_HANDOFF.md
 M mvp/docs/THIRD_PARTY_NOTICES.md
 M mvp/src/app/locator_service.py
 M mvp/src/device/__init__.py
 M mvp/src/infrastructure/__init__.py
 M mvp/src/infrastructure/logging.py
?? .agent/archive/checkpoint-2026-08-26-0146.md
?? .agent/archive/checkpoint-2026-08-26-0150.md
?? .agent/archive/checkpoint-2026-08-26-0159.md
?? .agent/archive/checkpoint-2026-08-26-1945.md
?? .agent/archive/checkpoint-2026-08-26-2054.md
?? .agent/archive/checkpoint-2026-08-26-2227.md
?? .agent/archive/checkpoint-2026-08-27-0127.md
?? .agent/archive/checkpoint-2026-08-27-0336.md
?? .claude/
?? bundle_optimization_report.md
?? bundle_report.md
?? docs/
?? mvp/api/
?? mvp/benchmark/
?? mvp/logs/
?? mvp/scripts/.gitignore
?? mvp/scripts/backend.spec
?? mvp/scripts/build_backend.py
?? mvp/scripts/run_backend.py
?? mvp/src/infrastructure/settings_repo.py
?? mvp/tests/test_logging.py
?? mvp/ui/
```

### Recent Commits

```text
3b7a031 H3: finalize H3_MPS_GO; revert CI to workflow_dispatch-only
a9d7ce4 H3: lower MPS batch (4) to sidestep 5.37GiB buffer limit; publish results to ci-results
d2e528e H3 POC: capture any MPS runtime/op error into results.json instead of crashing
09be13b H3: add push trigger on master so a git push auto-runs the POC
00fa0e7 Fix .gitignore: anchor src/ to repo root (was ignoring mvp/src)
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark → **Source Video Locator MVP**（D:\claudework\benchmark）

> **项目状态：ALGORITHM_RESEARCH_FROZEN / MVP_DEVELOPMENT_ACTIVE（2026-08-26）**
> 算法研究已冻结收尾；进入 MVP 产品化（Source Video Locator）。研究结论见 `ARCHITECTURE_DECISION_PHASE20.md`；MVP 设计文档见 `mvp/docs/`。
>
> **硬件/开发状态（2026-08-26 统一）**：H1 Windows CPU = **IMPLEMENTED**；H2 Windows AMD / DirectML = **IMPLEMENTED**；H3 macOS Apple Silicon / MPS = **GO / POC completed**；H4 Windows NVIDIA / CUDA = **PENDING**（H4-0 环境检查已完成 = **`H4_GPU_RUNNER_UNAVAILABLE`**，无可用 Windows GPU runner，POC 无法推进）；UI（第 8 项 PySide6）= **PAUSED**。

> **硬件路线（2026-08-25 用户拍板锁定）**：H1 Windows CPU → H2 **Windows AMD GPU(DirectML)** → H3 macOS Apple Silicon(MPS) → H4 Windows NVIDIA(CUDA)。**不支持 macOS Intel**。`DeviceBackend` 必须保持可扩展——未来加 `CUDABackend` 不改上层 FeatureStore/Retrieval/Ranking/Localization/Confidence/UI。CUDA 不进当前实施阶段。
>
> **AMD GPU POC（`mvp/poc/amdgpu_onnx/`，独立、未接入 MVP）= `AMD_BACKEND_GO`**：RX 6750 GRE + ONNX Runtime DirectML + 冻结 DINOv2 ViT-S/14 CLS-384。15.15 fps，10.39× 加速，Top-1 邻居一致、embedding 数值稳定、500 帧无 NaN/norm 异常。
>
> **H2-Preflight（`h2_preflight.py`，2026-08-25）= `MEMORY_STABLE`**：2.mkv@0.5fps 建索引 3834/3834 帧全跑完，内存 278.5→336.8MB（+58.3MB，全部为首批~250帧一次性 warmup；之后 328–337MB 窄带波动，非线性增长），all_finite / norm=1.0，无 DML allocator 错误，全片 323.9s、11.84 fps。
>
> **H2 = `DirectMLBackend` 已正式接入并验证（2026-08-25）→ `H2_AMD_DIRECTML = IMPLEMENTED`**：
> `mvp/src/device/directml_backend.py`（`DeviceBackend` 实现，复用冻结 `_imagenet_preprocess`，ONNX+DML provider，numpy L2）+ `device/resolve_backend(preferred="auto")` 统一 resolver（能力探测 + 自动 CPU fallback + 明确日志 fallback_reason）+ `device/resolve_dml_model`/`asset_meta`（模型资产 resolver）+ `infrastructure.DeviceConfig`（preferred/onnx_model/dml_device_id/dml_batch_size）+ `mvp/scripts/export_dml_model.py`（从冻结模型重导出产品 ONNX 资产到 `<app_data>/models/dinov2_cls_384/` 含 `asset.json`）。
> 验证：全套 72 项测试过（含新增 `test_directml_backend.py` 10 项：A 类无 GPU→fallback/probe 结构/B 类 AMD 实机推理+CPU 正确性 cos>0.999+FeatureStore 完整兼容 create/load/validate/invalidate）；真实 2.mkv 生产索引构建 `smoke_directml_backend.py`：381.3s / 3834 帧 / 10.06 fps / `[3834,384] float32` / 内存 +53.4MB 稳定 / `IndexMeta.backend="directml" feature_version=...@0.5_l2`（与 CPU 同 schema，不复制 store）/ reload 0.002s。相比 H1 CPU ~3793.6s ≈ **9.95× 墙钟加速**（vs POC 324s 略高 +18%，因生产路径含文件哈希/持久化/探测开销）。

---

## Current Phase

**MVP 产品化 — Stage 1（编码）进行中**：已完成第 1~7 项 + **H1/H2 已接入并验证（H1_CPU / H2_AMD_DIRECTML = IMPLEMENTED）** + **H3（macOS MPS）POC = GO / 已完成（2026-08-26，不重复运行）**。**UI（第 8 项）已转向 = Vue3+TS+Electron 桌面工作台，Stage 1 初始代码已交付（2026-08-26）**（见 Current Task §2）。算法研究 Phase 1~19 已冻结收尾。

> **2026-08-26 — H3（macOS Apple Silicon / MPS）POC 完成 → `H3_MPS_GO`**（真实 Apple Silicon 验证通过，GitHub Actions 云端跑成功）。**repo**：`bsaizne/source-video-locator`（private→public；push 走 SSH——本机网络 HTTPS 443 被阻断、SSH 22/443 可达；DNS=小米路由 192.168.31.1 解析到 GitHub 20.205.243.x 段但 443 握手超时）。已 `git init` + 完整 `.gitignore`（排视频/权重/特征/第三方引擎/工具二进制/研究代码 `src/`/phase 报告/生成物/凭据/IDE）+ 入仓 `mvp/`、`.github/workflows/h3-macos-mps.yml`、`.agent/`、设计/研究结论文档；**含修复：`.gitignore` 无锚 `src/` 曾误忽略整个 `mvp/src`（致 CI checkout 缺 `device/dinov2_model.py`），已改 `/src/`**。**H3 POC = GO**：macOS 15.7.7 / arm64 / torch 2.13.0，`device_mps_actual=mps`（确认非 CPU fallback）、权重下载+sha256 通过、正确性 cos_mean=1.0 / max_abs_diff=6.3e-7 / mps norm deviation 1.19e-7、7.875× 加速（0.87→6.84 fps；batch=4 最佳 6.67；batch≥8 触发 `Invalid buffer size 5.37GiB` + 暴跌 2.83fps）、500 帧 all_finite 无异常无 fallback、峰值内存 ~1.07GB。**关键约束：MPS batch_size ≤4（推荐 4）**——未来正式 `MPSBackend` 必须遵守。CI workflow 已改 **workflow_dispatch-only**（手动；不 push 自动触发，避免每次 push 烧 macOS runner）。`mvp/src/device` 零改动；无 MPSBackend / UI / H4。POC 见 `mvp/poc/macos_mps/`。

> **2026-08-26 — H4-0（Windows NVIDIA / CUDA 环境与 runner 可用性检查）= `H4_GPU_RUNNER_UNAVAILABLE`**：目标环境为 GitHub Actions → Windows GPU larger runner → NVIDIA → CUDA。核实结论：① `bsaizne/source-video-locator` 为**个人（User）账户的公共仓库**（api 确认 `owner.type="User"`、`private:false`）；② 官方 GPU hosted runners **不对开源/公共仓库开放**，且需 **organization/enterprise 级计费配置**；③ 官方 GPU hosted runner **无 Windows 变体**（H3 所用 `macos-15` 为标准 runner，非 GPU）；④ 该 repo Actions 历史仅 4 次 `H3 macOS MPS POC`（macos-15），从未使用 Windows/GPU 较大 runner；⑤ 本机无 GitHub 认证（无 gh CLI / token / credential.helper，`actions/runners` endpoint 返 401）；⑥ 本机 GPU = **AMD Radeon RX 6750 GRE 10GB**（非 NVIDIA），`torch 2.13.0+cpu`、`torch.cuda.is_available()=False`、`device_count=0` → 本机也无法做 CUDA POC。**未创建 `mvp/poc/nvidia_cuda/` / workflow，未改 `mvp/src`，未实现 NVIDIA 支持，未进 UI。** 结论（不伪造）：当前无可用 Windows GPU runner，H4 POC 无法推进；需用户提供 NVIDIA 环境（自托管 Linux runner / 云或本地 NVIDIA 机器）+ 计费权限。

---

## Current Task

1. **H4 Windows NVIDIA / CUDA POC（PENDING）——H4-0 环境检查已完成 = `H4_GPU_RUNNER_UNAVAILABLE`**：GitHub Actions 无可用 Windows GPU hosted runner（个人公共仓库 + 官方 GPU runner 对开源项目不开放且无 Windows 变体；本机 GPU = AMD RX 6750 GRE、torch 为 CPU build，无 NVIDIA）。**POC 无法推进**，需用户提供具备 NVIDIA GPU 的环境（自托管 Linux runner / 云或本地 NVIDIA 机器）+ 对应计费权限。届时按：能力探测 → `CUDABackend` 一致性验证（与 CPU 对比，cos>0.999 量级）→ 性能基准；不支持则如实标记 `H4_CUDA_BLOCKED` 继续 CPU。CUDA 不进当前实施阶段。
2. **UI 已转向（2026-08-26 用户重定向 + 方案批准）→ Vue3 + TypeScript + Electron 桌面工作台**，取代原「第 8 项 PySide6 UI」。**Stage 1 初始代码已交付 = `mvp/ui/`（Vue3+TS+Vite+Pinia+router+Electron 壳）**：自定义 Fluent 组件库（自研；无 winitonweb/GPL，商业闭源安全）+ Codex 三栏布局 + `ServiceAPI` 接口（Index/Analysis/Result 三组，一对一映射 `SourceLocatorService`）+ `MockServiceAdapter`（零后端跑通）+ `HttpServiceAdapter`（预留 `/api` 端点 + WS 进度）+ `services/types.ts` 严格镜像后端 JSON（`edited_segment{start,end}`/`original{candidate_start,candidate_end}` 不对称键名、`confidence` 顶层拍平、`manual_override` 门控 `auto_result`）+ 6 页面 + 设计文档（01-arch/02-design/03-components/README/DESKTOP）+ `mvp/api/README.md` 端点契约。**诚实验证**：typecheck（渲染+桌面）、vite build、mock 端到端（Run Analysis→进度→自动跳 Results→8 结果含 Montage/Manual/Unresolved + 对比播放器 + Inspector 详情）、mock 契约测试（7 阶段全序 + 字段形状 + 取消语义）、主题 token 实测（confidence 三色徽章 **不显示百分比**）。
3. **前端接真实后端**：`HttpServiceAdapter` 已从 Mock 切真实 FastAPI（2026-08-26，见 Current Implementation/Current Decision）——`mvp/ui/` 完成 Mock/HTTP 双模式（`VITE_BACKEND_MODE`）、Backend Connection 三态检测（`GET /api/health`）、统一错误处理（`BackendUnavailableError`/500 detail）、vitest 14 项；浏览器实测 Home 侧栏 Connection=Connected。**已完成**：进度 WS + 取消 + 预览 `extracted_path`（2026-08-27）。**下一阶段**：Electron 打包产物 + 真机验收 → 性能基准（10/60/120min）+ Confidence 标定。

## Completed

- **2026-08-27 — 结果页剪辑黑屏/进度条不能滑/预览手动 修复（+导出清理预览）**：根因——① 剪辑黑屏：`GET /api/preview/edited` 原始编辑视频原样返回**无转码**（原片预览走 `extract_clip` 重编码 libx264 MP4 能播；编辑视频 MKV/HEVC，Chromium `<video>` 黑屏）；② 进度条纯摆设：`VideoPlayer.vp__bar` 无 seek 事件（样式 cursor:pointer 却不可拖）且 `VideoComparisonPlayer` 剪辑播放器只绑 `@tick` 未绑 `@seek`；③ 预览手动：`generatePreview` 只在 InspectorPanel 按钮调，`setBatch`/`select` 不自动，原片预览初始 null 占位。**修复**：编辑预览改「抽 `edited_segment` 区间转码 H.264 MP4」（复用 `POST /api/preview`/`PreviewService.extract_segment`，弃用 `/api/preview/edited`）+ `results.ts` 加 `generateEditedPreview`；`setBatch`/`select`/`markManualOverride` **自动生成编辑+原片预览**；`VideoPlayer.vp__bar` 加 seek + `VideoComparisonPlayer` 两侧绑 `@seek` + 时间轴统一区间 local 0..width 1:1 同步；**`PreviewService.cleanup()` + `/api/export` 导出成功后清空 `<app-data>/previews/`**（导出即弃预览，不占磁盘）。**验证**（打包 exe）：`POST /api/preview {HEVC/MKV 编辑视频,区间}`→`200`、`GET media`→`200 video/mp4`、`ffprobe=h264`（黑屏修复）；`/api/export` 后 `previews` 清空（Before:[3文件] After:[]）、导出 results.json 200；前端 `test` 67 + `test:mock`、后端 `unittest` 51 全绿。交付 `mvp/ui/release/win-unpacked/Video Locator.exe`（20:35）。更新 `docs/DEPLOYMENT_HANDOFF.md`。
- **2026-08-27 — 构建索引报「cannot reach backend: fetch failed」修复（health 冷启动容错）**：根因——后端 PyInstaller 冷启动慢（加载 torch/onnx，实测 ~20s，慢机器 >30s），而 Electron `waitForHealth` 只等 30s；后端未 ready 时点构建索引 → 连不上 → `fetch failed`。间接依据：后端本身能正常建索引（300s 源片 150 帧 14.8s 完成、directml），非构建崩溃。修复：`electron/backend/config.ts` `HEALTH_TIMEOUT_MS` 30_000→**90_000**（`waitForHealth` 成功后才 `createWindow`）。验证：启动打包 exe 后端 health 200；经 renderer→后端桥 `POST /api/index` → `200 completed/150帧/directml·amd`（fetch 通路正常）；前端 `test` 67（config.test 更新 90_000）+ 后端 `unittest` 51 全绿。更新 `docs/DEPLOYMENT_HANDOFF.md`（§3.7）。
- **2026-08-27 — 特征提取「转圈」= 慢+无进度显示 修复（含快速诊断）**：诊断确认非真卡——特征提取（编辑片 embed）阶段 progress 固定 30%（阶段级粗进度）+ 无「已处理 N/M 帧」，编辑片越长 embed 越久、进度条停在 30 不动、看着像卡死（100s 剪辑 embed 实测 14s 就 completed）。修复：后端 `analyze_edited_video` 编辑片 embed **分批+每批上报** `EDITED_FEATURE_EXTRACTION current=N total=M message="特征提取 N/M 帧"`；worker 透传 `ev.message` 至 task；`Task.to_dict()` 加 `message`；前端 `TaskJson` 加 `message` + 轮询显示；worker 对 embedding 阶段用真实帧数把 progress **从 10% 平滑到 30%**。**踩坑修复**：build_backend 必须 cwd=benchmark 根`backend.spec` 相对 pathex（mvp/…）——cwd 在 `mvp/ui` 时打包的 `backend.exe` 缺 `mvp` 包（`run_backend: ModuleNotFoundError: No module named 'mvp'`→exe 启动 health 超时），今回退到旧 backend 才复现；已重打包 + 文档记录。**验证**：打包 exe 后端启动正常（health 200、`backend selected=directml`），特征提取阶段 progress `13→16→18→21→24→28`、msg「特征提取 32/200→…→176/200 帧」→ completed；后端 `unittest` 51、前端 `test` 67 + `test:mock` 全绿。更新 `docs/DEPLOYMENT_HANDOFF.md`（§3.6 + §4 重新打包 cwd 注意）。
- **2026-08-27 — 分析「一直转圈」+ 取消无效 + 异步导出 no_results 修复**：根因——① **转圈**：打包 exe Electron 主进程无 `globalThis.WebSocket`（Electron 34 / Node 20）→ 进度 WS 代理禁用（日志 `global WebSocket unavailable`），前端 `subscribeProgress`（WS）收不到终态帧 → `runLocate` Promise 永挂起；② **取消无效**：同一 WS 原因前端无反馈，且 `analyze_edited_video` 的编辑片 `embed_frames` 一次性不查取消；③ **导出 no_results**：`/api/export` 用 `ctx.current_batch`（仅同步 `/api/results` 写），异步 `/api/tasks/analyze` worker 不写。修复：前端 `analysis.runLocate` 改 **HTTP 轮询 `GET /api/tasks/{id}` 至终态**（绕开 WS，任何环境可靠）+ 取消走 `cancelTask`；后端 `analyze_edited_video` embed 改**分批+每批 `_check_cancel`**；新增 `SourceLocatorService.last_result_batch()`，`/api/export` 用 `ctx.current_batch or ctx.service.last_result_batch()`。**验证**：源码 TestClient + 打包 exe HTTP 全链路——`/api/index` completed/directml/15帧 → `/api/index/status` VALID → `/api/tasks/analyze` 轮询 completed（结果 [HIGH, LOW]）→ `/api/export` 200（落盘 results.json）→ `cancel` cancelled/requested；前端 `test` 67 + `test:mock`、后端 `unittest` 51 全绿。交付 `mvp/ui/release/win-unpacked/Video Locator.exe` 更新。更新 `docs/DEPLOYMENT_HANDOFF.md`。
- **2026-08-27 — 构建索引后「缺失」修复（源片绝对路径 + 真实状态端点 + 明确错误）**：根因——项目页源片存**裸文件名**（如 `Dune.mkv`，旧 exe 拖拽未生效且无「打开文件」入口）→ 后端按工作目录解析不到 → `ffprobe` 找不到 → `/api/index` 失败 → 前端**无真实索引状态查询端点**一直显示「缺失」。修复：① 前端源片/剪辑区加「选择源片文件/选择文件」（Electron `dialog.showOpenDialog`，`window.desktop.openFile`）+ 裸名警告（`ProjectDetailPage.vue`）；② 后端 `build_original_index` 前置校验（路径非绝对/不存在 → 提前抛明确中文错误「源片路径无效或不存在…」）+ 异常逃逸加固（except 扩含 `DeviceError`/`OSError`/兜底 `Exception`，日志有 `index failed` 前缀）；③ 后端新增 `GET /api/index/status`（透传 `FeatureStore.validate_index` — VALID/INVALID/MISSING + reason）+ 前端 `getIndexStatus` 改真实查询（不再用 `lastIndexStatus ?? unknownIndexStatus()` 恒 MISSING）；④ 索引徽章「构建中」态 + 失败显示后端具体原因。**验证**：后端 `unittest` 51（+`test_index_status` 4）+ 前端 `test` 67（+index status 2）+ `test:mock` + `typecheck`/`compile:electron` 全绿；后端冒烟：裸名 status→`INVALID/source video missing`、裸名构建→`500+明确中文错误`（不再 ffprobe 崩，`index_status` 前置校验避免无谓 ffprobe）；打包 exe CDP 端到端：`openFile:function` + status/构建裸名明确错误。交付 `mvp/ui/release/win-unpacked/Video Locator.exe`（含新 backend+dist+preload）。更新 `docs/DEPLOYMENT_HANDOFF.md`（源片须绝对路径 + 状态端点 + `test_index_status`）。**范围**：未改算法/置信/GT/Result 契约；`FeatureStore.validate_index` 未改。
- **2026-08-27 — GPU/CPU 真切换 + 拖拽修复（桌面部署交接）**：① **GPU/CPU 切换真正生效**——后端新增 `infrastructure/settings_repo.py`（持久化 `device.preferred` 到 `<data_dir>/settings.json`）+ `SourceLocatorService.device_preference/set_device_preference/device_settings` + 新路由 `routes/settings.py` GET|POST `/api/settings/device`（POST 校验 auto/cpu/directml，非法 400）；前端 `DeviceName` 补 `'directml'`、`HttpServiceAdapter.DEVICE_NAMES` 加 `'directml'`（修 `normDeviceName` 把 directml clamp 成 cpu 的 bug——即使 GPU 真工作 UI 也显示 CPU）+ `getDeviceSettings/setDeviceSettings`；Settings 页由只读写改**真三档下拉**（智能/AMD GPU/CPU）+ 保存生效 + 「期望 vs 实际/回退」提示。② **拖拽修复**——根因 `window.desktop/backend` 均 undefined（Electron sandboxed preload 不能 require 相对模块 `./backend/bridge` → preload 加载失败 → contextBridge 不执行）；修复 `scripts/build-preload.cjs` 用 esbuild 把 preload+bridge bundle 成单文件（保留 `require('electron')`），`compile:electron` 追加。③ **CPU 打包修正**——CPU 走 torch 原生需 `.pth`，bundle 只带 ONNX → 打包 exe 切 CPU 崩；把 `dinov2_vits14_pretrain.pth` 内置 `resources/models/dinov2_vits14` + main.ts 设 `SVL_DINOV2_WEIGHTS`。④ 打包用 **`asar:false`**（`--dir` 下 `addWinAsarIntegrity` 无条件写 exe 撞 Defender 锁；`signAndEditExecutable:false` 在 `--dir` 不生效）。**验证**：后端 `unittest` 47 + 前端 `test` 65 + `test:mock` + `typecheck`/`typecheck:desktop` + `compile:electron` 全绿；打包 exe CDP 端到端：auto→directml/amd、POST cpu→cpu、POST directml→amd、非法 gpu→400、`window.desktop.getPathForFile:function`、Settings 真下拉显示 AMD GPU、后端日志 `selected=directml type=amd`。交付 `mvp/ui/release/win-unpacked/Video Locator.exe`（免安装，asar:false，含新 backend+ONNX+pth）。**安装器暂停（用户拍板）**，只交付免安装 exe；更新 `docs/DEPLOYMENT_HANDOFF.md`。**范围**：未改 `mvp/src` 算法/置信语义/Result 契约；device 层新增选择端点（符合 `DeviceBackend` 可扩展）。
- **2026-08-27 — Desktop 交付 + GPU/拖拽修复（安装器开发已暂停）**：**可直接运行 exe 已交付并实测**：`mvp/ui/release/win-unpacked/Video Locator.exe`（免安装，含 `resources/backend/backend.exe`+`resources/models/dinov2_cls_384`(85M ONNX)+ffmpeg/ffprobe+`app.asar`）。实测：双击自启后端（日志 `backend starting→Started server process→api server started→GET /api/health 200→health connected: backend running`）+ 真实 `/api/index` `device_type: amd`（**GPU 用上了**）+ 关闭后 8765 释放、0 残留。**① GPU→CPU 根因修复**：Phase 4 重定向 `SVL_DATA_DIR` 后模型仍在旧目录（`%LOCALAPPDATA%/SourceVideoLocator/models`）→ 后端在新目录找不到 DirectML ONNX → `DirectML ONNX model asset missing→CPU fallback`。修复：模型内置 `resources/models`（extraResources）+ main.ts 设 `SVL_DML_MODEL=<resourcesPath>/models/.../dinov2_cls_384.onnx`；打包后端+SVL_DML_MODEL+空数据目录实测 `directml`。**Settings「设备」改只读显示实际后端**（原「首选后端」下拉是假控件，选了不生效=“选GPU却回CPU”混淆来源）。**② 拖拽导入修复**：项目页源片/剪辑 drop 区实现 HTML5 拖拽，preload 用 `webUtils.getPathForFile(file)` 取绝对路径入列。**安装器**：`Video Locator Setup 0.1.0.exe`(402MB,含模型,未签名,`signAndEditExecutable:false` 绕 Defender 锁) 仍可生成；**按用户要求安装器进一步开发已暂停**。**交接文档** `docs/DEPLOYMENT_HANDOFF.md`。**验证**：后端 `unittest` 43 + 前端 `test` 63 + Electron `compile:electron`/`typecheck:desktop`/35 测试 + `test:mock` 全绿；打包 exe 端到端自启后端+GPU+干净退出实测过。**范围**：未改 `mvp/src`/算法/API/Result；未做 license/device binding/DRM。
- **2026-08-27 — Desktop Deployment（Electron Builder + NSIS 安装器 + 首次启动初始化）完成**：**安装器已生成** `mvp/ui/release/Video Locator Setup 0.1.0.exe`（321MB，含 845MB 后端 bundle 压缩）+ `win-unpacked/`（`resources/backend/backend.exe`+`_internal`+`ffmpeg/ffprobe` 齐全，`resources/app.asar`）。**关键**：electron-builder 首次在 `addWinAsarIntegrity`（对刚写出 exe 写回）报 Windows `UNKNOWN` 锁——经 `win.signAndEditExecutable: false`（跳过 exe 改写，未签名 MVP 可接受）解决；electron/nsis 二进制用 npmmirror 镜像绕开 GitHub 443。**配置**：`electron-builder.json`（appId `com.videolocator.desktop`、productName `Video Locator`、NSIS `oneClick:false`+\`perMachine\`+\`allowToChangeInstallationDirectory\`+\`createDesktopShortcut\`+\`createStartMenuShortcut\`、`extraResources: resources/backend→backend`、文件 `dist/**/*`+`out/electron/**/*`）。**Phase 4 用户目录隔离**：main.ts 设 `SVL_DATA_DIR/SVL_LOG_DIR`=`<userData>/data`+`<userData>/logs`（后端 `paths.app_data_dir`/`logging` 读取，不改 mvp/src）——运行时数据全在 `%APPDATA%\Video Locator\` 不写 Program Files；`app:paths`/`app:openPath` IPC + preload `getPaths/openPath`。**Phase 5 首次启动初始化**：session store `initApp()`（INITIALIZING→CHECKING_MODEL→READY/FAILED + 进度%）+ App.vue 初始化遮罩（「正在初始化 AI 引擎 N%」「请不要关闭程序」+ 失败重试）。**Phase 6 后端错误处理**：main.ts 原生对话框「AI 服务启动失败」+ 原因/日志位置 + 按钮「打开日志目录/重新启动/复制错误信息」。**Phase 7 发布脚本** `mvp/ui/scripts/build-release.ps1`（build→compile:electron→build backend→electron-builder，用镜像）。**Phase 8** `docs/DEPLOYMENT.md`（安装/首次启动/用户目录/故障排查/卸载）。**验证**：安装器构建成功 + win-unpacked 结构正确；前端 `test` 63 + `typecheck` + `test:mock`、Electron `compile:electron` + `typecheck:desktop` + 35 测试、后端 `unittest` 43 全绿；清理了 smoke 残留 `backend.exe`（8765 释放）与 `resources/backend/logs`。**范围**：未改 `mvp/src`/算法/API 契约/Result；未做 license/device binding/DRM/激活/鉴权。**已知**：安装器未签名（SmartScreen 提示「未知发布者」）；`win-unpacked` 用默认 electron 图标；GUI 安装/卸载/真机运行是手动步（文档记录，未自动化）。
- **2026-08-27 — Release Validation 收尾 + UI 中文化**：**① UI 全部中文化**（`mvp/ui/src` 所有页面/组件/导航/按钮/徽章/后台状态/Inspector/进度管线/命令面板/置信度高⇄中⇄低 + Electron 对话框「后端启动失败/后端离线」；保留技术专名 CPU/AMD GPU/DINOv2/HIGH 数据字段；`test` 63 项+`build`+`typecheck`+`typecheck:desktop`+`compile:electron`+`test:mock` 全绿）。**② Bundle 瘦身**：PyInstaller `excludes` 对钩子收集的 scipy/pandas/PIL/onnx **不生效**→改在 `mvp/scripts/build_backend.py` 装配后 `_prune_unused()` 删除；`backend.exe` 实测 `_internal` 717M→617M、资源目录 **946M→845M（省约 101M）**；剪枝后用打包后端跑**真实 DINOv2 索引**（`frames=15` directml）+ `/api/health` 200 + `/api/preview` 200 **验证无算法影响**。**③ 发布基准**（`mvp/benchmark/release/`）：合成源 directml，**索引 10min 23.96s / 60min 135.22s / 120min 270.68s**（~13fps 近线性），分析 total≈3.35s（embedding≈3.22s ONNX 冷启动），预览 ffmpeg≈0.4s→100KB，→ `result/benchmark_result.json`+`release_smoke_result.json`（index/analysis/preview/export 全 success）。**④ 文档**：`bundle_report.md`（per-package 组成）、`bundle_optimization_report.md`（Before 946M/After 845M/Removed/Risk 实测）、`docs/RELEASE_TEST.md`（安装/启动/后端生命周期/管线/性能/体积/限制）、`mvp/ui/tests/e2e/desktop_smoke_test.md`（安装器步骤待授权，后端层实测证据）、`tools/bundle_report.py`、benchmark `README.md`。**验证**：后端 `unittest` 43 项 + 前端 63 项全绿。**范围**：未改 `mvp/src`/算法/API 契约/Vue 页面流程/Electron 架构；未做 installer/license/device binding/DRM。**已知**：bundle 主体是推理依赖（torch/opencv/onnxruntime/numpy≈560M）固有成本；内存/CPU 未插桩（无 psutil）；源为合成片；进度阶段级。
- **2026-08-27 — Electron 生产打包 + Python 后端 Bundle 完成（PyInstaller onedir）**：**后端打包**：新增 `mvp/scripts/run_backend.py`（PyInstaller 入口：`uvicorn.run("mvp.api.main:app", 127.0.0.1:8765)`，宿主/端口可 `SVL_BACKEND_HOST/PORT` 覆盖）+ `mvp/scripts/backend.spec`（onedir、`console=True`、pathex=`mvp`+`mvp/src`、collect_submodules 收 mvp.api + app/domain/engine/device/infrastructure/media、隐式 uvicorn/anyio/cffi 子模块、excludes tkinter/matplotlib/IPython/torchvision/PyQt/PySide）+ `mvp/scripts/build_backend.py`（编排：PyInstaller → 装配 `mvp/ui/resources/backend`，复制 `tools/ffmpeg.exe` + venv static_ffmpeg `ffprobe.exe`）。产物 `resources/backend` = **923 MiB**（`backend.exe` 52MB + `_internal/` + `ffmpeg.exe`/`ffprobe.exe`）。**Electron 接线**：`electron/backend/config.ts` `BackendConfig` 加 `spawnEnv`，`prodBackendConfig` 改为 `pythonExecutable=<resources>/backend/backend.exe` + `uvicornArgs=[]`（exe 自启 uvicorn）+ `spawnEnv={MEDIA_FFMPEG,MEDIA_FFPROBE}` 指向打包内 ffmpeg/ffprobe；`process.ts` `buildSpawnCommand` 返回合并 `env`、`spawnBackendProcess` 传 `env`+`windowsHide:true`；`config.test.ts` 更新 prod 断言。**验证全绿**：① 打包后端 `backend.exe` 启动 → `/api/health` 200 `{status:ok}`（无缺失导入，session 日志正常）；② **打包后端跑真实 Preview API**：POST `/api/preview` 抽 `originals/2.mkv`[100,110] → `2__100-110.mp4` 3.5MB 落 `app-data/previews`，GET media `Range bytes=0-99` → 206 `content-range: bytes 0-99/3519519` `video/mp4`（证明 `MEDIA_FFMPEG/FFPROBE` env → 内置二进制生效，无需改 mvp/src）；③ 前端 `npm run test` **63 项**（渲染 28 + Electron 35）+ `compile:electron`/`typecheck:desktop` 全过。**范围护栏**：未改 `mvp/src`；未制作安装器；未做 license/device binding/DRM。**文档**：`resources/backend/README.md` 重写（onedir 布局 + MEDIA env 机制 + 构建命令）、`DESKTOP.md` prod spawn 描述更新、`resources/backend/.gitignore` + `mvp/scripts/.gitignore`（排除构建产物）。**已知/待办**：bundle 偏大（torch 拖入 scipy/pandas/PIL，923MB）；模型权重仍运行时解析 app-data（首次可能下载）；真机验收（打包后 Electron 冒烟）待 `resources/backend` 入包 + 安装器授权后。**下一阶段**：Install + License + Device Binding（用户要求时）。
- **2026-08-27 — Video Preview（`extracted_path` 抽取 + 前端接真实视频）完成**：**后端** 新增 `mvp/api/services/preview_service.py`（`PreviewService.extract_segment(video_path, start, end)` -> `PreviewResult{path,duration}`，复用 `ctx.service.ffmpeg.extract_clip` 精确重编码；产物落 `<app-data>/previews`，可 `preview_dir` 覆盖；失败统一 `PreviewError`(LocatorError 子类) -> 500 + `logger.exception`）+ `mvp/api/routes/preview.py`（**POST** `/api/preview {original_path,start,end}` -> `{path,duration}`；**GET** `/api/preview/media/{filename}` 用 `FileResponse` 服务抽取片段（Starlette 原生 HTTP Range 206，HTML5 video 可拖动）；**GET** `/api/preview/edited` 服务会话编辑视频。**安全**：media 只取 `path.name` 且限 `preview_dir` 内防穿越；edited 只服务会话记录一份，不收客户端路径）。`schemas.PreviewRequest` + `AppContext.preview_service`(惰性) + app.py 挂 preview router。**前端** `ServiceAPI` 加 `previewResult(originalPath,start,end):Promise<string>`(Http=POST /api/preview 取 path 拼 `/api/preview/media/<name>` URL / Mock=示例视频地址) + `getEditedVideoUrl()`(Http=`/api/preview/edited`/Mock=示例)；`results` store 加 `previewEditedSrc/previewOriginalSrc/previewLoading/previewError` + `generatePreview()`(用选中 result 的 original span 调 previewResult) + setBatch 里自动设 editedSrc、select/markManualOverride 清旧 originalSrc；`InspectorPanel` 加「Generate Preview」按钮+状态；`ResultsPage` 把 `editedSrc/originalSrc` 传给 `VideoComparisonPlayer`；`VideoComparisonPlayer` 改 `ogCurrent` 为抽取片段本地 0..width 时间轴（Edited 仍播全片绝对 seek，比例同步）+ 切换 result 时 `watch` 重置 playhead。**验证全绿**：后端 api unittest **43 项**(原26+preview 路由 9+preview_service 8) 通过；真实 ffmpeg 端到端冒烟（`2.mkv` 抽取 15s -> `2__100-115.mp4` 4.65MB -> GET media 200 video/mp4 -> Range `bytes=0-99` 206 content-range 正确）；前端 `typecheck`/`build`/`test` **63 项**(原61+preview 2)/`test:mock` 契约 + `typecheck:desktop`/`compile:electron` 全过。**范围护栏**：未改 `mvp/src`/SourceLocatorService/domain/engine/FeatureStore/DeviceBackend/算法；未改 Result schema（`extracted_path` 仍 null 不伪造）；`/api/preview` 独立能力不改 locate/置信度；无 db/cloud/auth；未制作安装器。**下一阶段**：Installer + License + Device Binding（后置）。
- **2026-08-27 — WebSocket 实时进度 + UI Pipeline Binding 完成**：**后端** `mvp/api/tasks/{models,manager}.py` 加进度广播——`Task` 加 `_subscribers`(queue.Queue 列表)+ `subscribe()`(返回队列+初始快照)/`unsubscribe()`/`broadcast_current()`/`_frame()`(progress/completed/failed/cancelled 帧)，状态变更(`mark_running/update_progress/mark_completed/mark_failed/mark_cancelled`)锁内更新后**自动广播**；`TaskManager` 加 `subscribe/unsubscribe/publish` 转发。**新路由** `mvp/api/routes/progress.py` `/ws/progress/{task_id}`：订阅队列 + **后台 reader task** 读客户端 cancel(`{type:"cancel"}`→`TaskManager.cancel`→CancellationToken，不杀线程) + 主协程 `asyncio.to_thread` 轮询队列推帧，终态帧 break（reader+cancel 分离避免并发 receive 取消不稳定）；任务不存在→`{type:"error"}`。`app.py` 挂 progress router（`routes/tasks.py` 占位 ws 已移除）。**`test_progress.py`** 4 项（connect 快照+progress→completed / failed 帧 / cancel→cancelled / unknown task），用 `run_in_background=no-op` + 手动驱动 Task 状态（确定性，不依赖 worker 时序）。**前端** `types.ts` TaskJson/TaskEvent/TaskStatus/TaskStage；`ServiceAPI` 加 `startAnalyzeTask/getTask/cancelTask/subscribeProgress`；`HttpServiceAdapter` 实现（任务 API：POST /api/tasks/analyze|GET /api/tasks/{id}|POST cancel；`subscribeProgress` 开 WS + **disconnect 自动重连一次**）；`MockServiceAdapter` 实现（定时器阶段帧→completed+result）；新 `services/progressSocket.ts`（Electron `window.backend.websocket` override 原生 WebSocket）+ `electron.d.ts` 加 `window.backend.websocket` 类型；**analysis store 改任务式**（startAnalyzeTask→subscribeProgress→completed→getTask 取 result；+cancelTask）；`AnalysisPage` cancel=analysis.cancelTask + elapsed 计时；`ProgressPipeline.vue` 增强显示 current stage/progress%/elapsed + cancel 按钮。**Electron** `bridge.ts` `buildBackendBridge` 加 `websocket`（`BackendIpc` on/send/removeListener）+ `preload.ts` cast 传 ipcRenderer；`main.ts` 加 `backend:ws:open|client-close` IPC handler（**Node global WebSocket** 连 FastAPI `/ws/progress/{task_id}` 转发帧到 renderer webContents）。**验证全绿**：前端 `npm run test` 61 项(原54+新 tasks 6+bridge ws 1)/`typecheck`/`build`/`test:mock`；后端 api unittest 26 项(test_api12+test_tasks10+test_progress4)；真机 uvicorn + `websockets` client 冒烟（create→task_id→WS 收 `failed` 终态帧，证明真实 WS 握手+推送）。**范围护栏**：未改 `mvp/src`/SourceLocatorService/domain/engine/FeatureStore/DeviceBackend/retrieval/localization/confidence；未新增算法进度（阶段级 progress 保留，真实 DINO 逐帧需后续 service hook）；未加 db/cloud/auth；未打包。**已知**：进度为阶段级 coarse；前端 task `progress` 用 0-100（阶段百分比）。
- **2026-08-27 — FastAPI 异步任务基础设施完成（只加 adapter/infrastructure 层）**：`mvp/api/tasks/`（`models.py` Task 模型 + `TaskStatus`(pending/running/completed/failed/cancelled) + `TaskStage`(idle/indexing/segmenting/embedding/retrieval/exporting/finished) + 线程安全更新，`Task.to_dict()` 含 task_id/status/stage/progress/created_at/finished_at/result/error/cancel_requested；`worker.py` `run_worker(task, service)` 后台跑 `SourceLocatorService.locate(edited, original, on_progress, cancel_token)` + `map_progress_stage`(ProgressStage→TaskStage+阶段百分比)，异常→failed、取消(ApplicationError+is_cancelled)→cancelled；`manager.py` `TaskManager(service, run_in_background=可注入)`：submit_analyze/get/cancel，默认 `threading.Thread(daemon)`，测试注入同步 executor 获确定性时序）。**路由** `routes/tasks.py`：`POST /api/tasks/analyze`→`{task_id}`、`GET /api/tasks/{task_id}`→TaskJson、`POST /api/tasks/{task_id}/cancel`→`{status:"cancel_requested"}`。**接线**：`dependencies.AppContext` 加 `task_manager` 字段 + `get_context()` 构造 `TaskManager(service)`（复用同 service 实例）；`schemas.AnalyzeTaskRequest`（edited/original 默认空串→缺字段也走路由 400 bad_request）。**验证**：`test_tasks.py` 10 项 + `test_api.py` 12 项未破坏 + 真机 uvicorn 冒烟（health ok、create→task_id、query→failed 且 `error=IndexError…`）。**范围护栏**：未改 `mvp/src`/SourceLocatorService/domain/engine/FeatureStore/DeviceBackend/算法，未改 Vue/Electron，未动 `mvp/src/app/models` 的 ProgressStage/ProgressEvent/CancellationToken（复用）；现有同步路由 `/api/health|index|analyze|results|export` 保留不动。
- **2026-08-26 — Electron renderer→后端请求桥完成（修复打包 file:// 的 CORS）**：preload 暴露 `window.backend{request,health}`（经 `ipcRenderer.invoke('backend:request')`）→ main `ipcMain.handle('backend:request', createBackendRequestHandler(config))`（**Node fetch** 打到 `http://127.0.0.1:8765{path}`）→ 标准化 `BridgeResponse` 回 renderer；**renderer 从不直接访问 localhost**，故生产 `file://` 页面绕开 FastAPI 桥 CORS。**新增** `electron/backend/bridge.ts`（纯 Node 可测：`buildBackendBridge(ipc)` = preload 可测核心 / `createBackendRequestHandler(config, fetchImpl)` = main handler 可测核心，含安全：**只接受本地绝对路径**禁 scheme/协议相对、方法白名单 GET/POST/PUT/DELETE/PATCH、网络失败→`{network:true}`、5xx→`{detail}`）。**renderer 侧** `src/services/transport.ts`（`createTransport(base)`：检测到 `window.backend`(Electron)→bridgeTransport，否则→fetchTransport；`HttpServiceAdapter` 用 transport 替直接 fetch，错误语义保持 network→`BackendUnavailableError`/非2xx→`Error(detail)`）**+ `src/types/electron.d.ts`**（全局 `Window.backend/desktop` 类型）。**验证全绿**：`npm run test` 54 项（renderer 20 + electron 34；新增 bridge 9 + transport 6）、`typecheck`/`build`/`typecheck:desktop`/`compile:electron` 全过。**范围**：未改 `mvp/src`/SourceLocatorService/FastAPI 业务接口/domain/算法/UI 页面/`HttpServiceAdapter` 契约（只换 transport 实现）；'`window.desktop`' 保留未破坏。**FastAPI CORS 评估=不改**：bridge 下生产 renderer 不触发 CORS；纯浏览器 dev 仍需 `localhost:5173/5174` 放行，桥已配置保持现状。
- **2026-08-26 — Electron Runtime Integration（Python 后端生命周期管理）完成**：`mvp/ui/electron/`（CommonJS 主进程）→ *未打包=dev*（不 spawn，健康探测已有 `uvicorn`）/ *打包=prod*（spawn `resources/backend/python.exe` + 30s 健康轮询）。**新增** `electron/backend/`：`config.ts`（host/port/health path + `prodBackendConfig(resourcesDir)` 打包 python 路径 + `buildUvicornArgs`，不散落 magic string）/ `health.ts`（`createHealthChecker` 注入 fetch/sleep，轮询 `/api/health`，超时抛 `BackendStartError`）/ `process.ts`（`spawnBackendProcess` 注入 spawnFn，`stdio:pipe` 转发 stdout/stderr，`buildSpawnCommand` 纯函数）/ `manager.ts`（`BackendManager`：start/stop/restart/waitForHealth/checkDevBackend + `BackendState` 状态机 + DI 注入 spawner/health/log，测试不起真实 Python）/ `index.ts` 统一导出。**集成** `electron/main.ts`：`resolveMode`=`app.isPackaged`, `bootstrap`=prod→`startBackend()+waitForHealth()`(失败弹 dialog)/dev→`checkDevBackend()`(offline 弹「Backend offline」), dev 默认加载 `localhost:5173`, `before-quit→stopBackend()`(同步 kill 无残留), 保留 contextIsolation+sandbox。**构建修正**：`package.json` `type` module→commonjs + `main`→`out/electron/main.js`（main.ts 用 `__dirname` 去 `import.meta`，匹配 CommonJS emit；electron-builder `files` 已含 `out/electron/**`）；`tsconfig.desktop.build.json` 加 `exclude: electron/**/tests/**`（防编译测试污染 vitest）；`vite.config.ts` 用 `vitest/config` 加 `test.include`(渲染+electron)/`exclude`(out/dist)；`electron-builder.json` 加 `extraResources: resources/backend → backend`；`resources/backend/README.md` 记录打包 python 预期布局；scripts 加 `compile:electron`/`electron:dev`/`build:electron`。**验证全绿**：`npm run typecheck`/`build`/`test`(39 项:渲染 14+electron 25)/`typecheck:desktop`/`compile:electron`/`test:mock` 全过。**范围护栏**：未改 `mvp/src`/SourceLocatorService/FastAPI API/domain/算法/UI 页面逻辑/HttpServiceAdapter 契约。**已知限制**：打包后 renderer 从 `file://` 调后端受桥 CORS 限制（dev vite 5173 无此问题）——属后续 renderer→bridge 阶段，主进程 health check(Node fetch 无 CORS)不受影响。
- **2026-08-26 — 后端统一日志基础设施完成（FastAPI 桥之前）**：`mvp/src/infrastructure/logging.py` 重写为 stderr + **文件双输出**，文件用 **`RotatingFileHandler`（10MB × 5 备份，写 `mvp/logs/video_locator.log`，路径可由 `log_dir`/env `SVL_LOG_DIR` 覆盖）**；**结构化格式** `时间 级别 module=<name> session=<sid> <message>`；**轻量 session_id**（`contextvars` + uuid4 hex8，`new_session_id/set_session_id/get_session_id`，`set_session_id("")` 清空，无数据库）。**接入**：`device/__init__.py` resolve_backend 记录 `backend requested/selected/fallback_reason/unavailable_reason/device`；`app/locator_service.py` 记录 `index started/finished(video,frames,backend,elapsed)`、`index failed`(exception)、`analysis started/segments/embed(frames,fps)`、`analysis failed`(exception)、`locate started/finished(segments,candidates,high/medium/low/unresolved,elapsed)`；`_log.exception` 只加日志、不改异常逻辑。**不动**算法/DINOv2/retrieval/localization/confidence/FeatureStore schema；无 DB/云端上传/UI 日志窗。**`mvp/logs/.gitignore`**(`*.log`) + **`tests/test_logging.py`**（8 项：get_logger/三档写入/RotatingFileHandler 存在(maxBytes,backupCount)/结构化文件含 session/会话 id 语义；hermetic 快照恢复 root handlers 不污染其它模块）。**全套 80 项回归通过**（`get_logger` 用 `__name__`，`configure_logging()` 为 launcher(FastAPI/CLI) 启动钩子，幂等、可切 log_dir）。**未做**：FastAPI 桥、Electron 连接（按要求停在日志基础设施）。
- **2026-08-26 — UI（第 8 项）转向并交付 = Vue3 + TS + Electron 桌面工作台 Stage 1 初始代码**：`mvp/ui/`（Vue3+TS+Vite+Pinia+router+Electron 壳）+ `mvp/api/README.md`（FastAPI 桥端点契约，未实现）。**自研 Fluent 组件库**（`src/components/ui/` Base* + icons.ts + 业务组件 ConfidenceBadge/ResultTable/VideoComparisonPlayer/Timeline/ProgressPipeline/CommandPalette/BackendStatusIndicator/InspectorPanel/ProjectCard）+ **Codex 三栏布局**（sidebar 240 / main 1fr / inspector 320）+ **`ServiceAPI` 抽象**（`MockServiceAdapter` 默认零后端跑通 / `HttpServiceAdapter` 预留 `/api` + WS 进度 + `services/types.ts` 严格镜像 `domain/models.py` JSON：`edited_segment{start,end}`/`original{candidate_start,candidate_end}` 不对称键名、`confidence`/`confidence_score`/`reasons` 顶层拍平、`manual_override` 门控 `manual_timestamp`/`auto_result`）+ **6 页面**（Home/Projects/MediaLibrary/Analysis/Results/Settings）+ 设计文档（`ui/docs/01-architecture|02-design|03-components|README|DESKTOP`）。**验证**：typecheck（渲染 `tsconfig.app` + 桌面 `tsconfig.desktop`）零错、`vite build` 通过、Mock 端到端（AnalysisPage 点 Run → 6 步 pipeline 进度 → 自动跳 Results → 8 结果含 Montage/Manual/Unresolved + 视频对比 + Inspector 详情）、`npm run test:mock` 契约测试（7 阶段全序 `INDEX_BUILD→…→EXPORT` + 字段形状 + 取消语义）、主题 token 实测（confidence HIGH=`rgb(108,203,95)` 等三色徽章，**不显示百分比**）。**口径**：Vue3 推翻文档所锁 PySide6 属用户拍板的有意转向；持久化以已实现代码为准（JSON 单文件批 `*.results.json` + `schema_version`，不用 SQLite）；`confidence_score` 为工程分**非概率**；`device_name` 仅展示不作 UI 分支；研究护栏全守（不改算法/相似度/检索/定位/置信语义、无 GT、不 sweep）。**延后**：`mvp/api/` 真实实现（`VITE_API_BASE` 切换）、Electron 二进制/打包、PySide6 清理、过时研究文档重写。**注**：npm 11 `allow-scripts` 安全门拦 electron/esbuild/vue-demi postinstall——渲染层不受影响（esbuild 平台二进制走 npm 包、已证 `npm run build` 通过），electron 需 `npm approve-scripts electron` + `ELECTRON_MIRROR` 才启用二进制（见 `ui/docs/DESKTOP.md`）。
- **2026-08-25 — `media/ffmpeg`（FFmpegIO）完成**：`mvp/src/media/ffmpeg/`（FFmpegIO + ffprobe + subprocess 封装）。禁用 cv2.CAP_PROP_POS_MSEC，改走 ffmpeg/ffprobe subprocess；`metadata`(format.duration 处理 MKV N/A)/`iter_frames`(流式 rawvideo pipe, 时间基采样, 不缩放)/`grab_frame`/`extract_clip`(精确重编码)/`hash_file`。冒烟 23 项 + unittest 6 项全过，真实 2h08m HEVC MKV 随机定位与帧精度验证通过。
- **2026-08-25 — `domain` + `infrastructure` 完成**：`mvp/src/domain/`（纯数据：TimeSpan/Candidate/Confidence/Alternative/Result + IndexMeta/ExtractorConfig/IndexValidation + 枚举，`to_dict()` 输出产品契约 JSON）+ `mvp/src/infrastructure/`（errors 模型 `LocatorError`/`ConfigError`、logging、paths 数据/index/export 目录、config `AppConfig`/`MediaConfig`/`PipelineConfig`/`ConfidenceConfig` + JSON 覆盖）。`MediaError` 已改为继承 `LocatorError` 统一错误模型。unittest 12 项 + media 复测全过。
- **2026-08-25 — `device/CPUBackend` + `engine/feature_store/FeatureStore` 完成**：`mvp/src/device/`（`DeviceBackend` Protocol + 冻结 DINOv2 backbone 提取 `dinov2_model.py` + `CPUBackend`(无 CUDA, 线程可配, ctypes 内存) + `pick_best_available`(H1 恒 CPU, 预留 H2/H3)）+ `mvp/src/engine/feature_store/`（`FeatureStore` create/load/validate/invalidate/get_metadata/delete, 按 INDEX_SPEC 落盘 `index.json`+`features.npy`+`times.npy`, 失效判定 size→duration→model/dim/fps/version→hash 硬检, `.stale` 隔离; `IndexBundle`）。device smoke 25 项 + FeatureStore/CPUBackend unittest 4 项全过; DINOv2 CPU 真推理 L2 norm=1.0。**`engine/segment` 当时未实现（属 roadmap 第 6 项，已于 2026-08-25 完成，见下）**。
- **2026-08-25 — `engine/retrieval` + `engine/clustering` + `engine/ranking` 完成**：`mvp/src/engine/common/similarity.py`(cosine REUSE)+`retrieval/`(retrieve_hits, 批量 Top-K K=20, orig_times[top_idx])+`clustering/`(_cluster_continuous 连续性感知 10/15/60s + _merged_ok + _bridges, build_candidates→domain.Candidate)+`ranking/`(v2_score = mean*sqrt(hit)*(0.5+0.5*cons)*n_reps^alpha alpha=0.5, rank_candidates)+`engine/candidates.py`(produce_candidates 编排)。**删除了 GT/benchmark/gid/实验路径/work 依赖**；`_dp_path`/`temporal_align`/`localize`/`edited_window_feats`/`_recall`(GT 依赖) 均 RESEARCH_ONLY 未进 runtime；`retrieval_top_k` config 占位 100 改为冻结 20；domain.Candidate 增加 `scene_div`(montage 指示)。retrieval smoke 6 项 + unittest 7 项全过。**注意：合成图案场景 CLS 易混淆，v2_score 会把非 GT 宽窗排前——冻结公式在真实影片数据(研究已验证)表现正常，属已知数据/模型属性**。
- **2026-08-25 — `engine/localization`(finloc longest_run + montage 检测分支) + `engine/confidence/ConfidenceEngine` 完成**：`mvp/src/engine/localization/`(finloc_window=per-orig max-over-query coverage + 3帧平滑 + mask(thresh=0.4 冻结) + longest_run→精确 span + multi_island 结构信号; pipeline.localize_segment=候选→finloc 最佳→回填 best_cover→confidence→RefinedSegment; montage/无 span/run 过短→original 退化候选窗范围)+`engine/confidence/confidence.py`(ConfidenceEngine.assess: rank/margin/n_reps/qcov/consistency/sim_std/scene_div + best_cover/span_stability/coverage_quality/multi_island→score+三档+reasons+hard_flags+montage_flag+alternatives)。**置信度权重/阈值全部为未标定占位**(CONFIDENCE_DESIGN §6)，已纳入 `ConfidenceConfig`(infrastructure/config.py, JSON 可覆盖)供 H1 标定；`engine/localization/__init__` 不 import 编排避免与 confidence 循环导入。禁 per-query argmax/voting/cut-aware/temporal align/改 similarity·ranking。researchnotes：synthetic v2_score 混淆使 smoke 的 top 候选非真值(已知属性, 见上文)；真值定位由 deterministic unittest 覆盖。localization/confidence unittest 10 项 + smoke 9 项全过；全套 46+ 项回归通过。
- **2026-08-25 — `engine/segment`（无 GT 编辑侧查询单元切分）完成**：`mvp/src/engine/segment/`（`ShotSegment` 模型 [span + feats + times 切片, 直接喂 produce_candidates/localize_segment] + `detect_shots` + `adjacent_distances` + `_merge_short`）。**算法**：相邻帧余弦距离 `d=1-cos`（复用冻结 cosine 语义, O(N) 点积）→ **局部 z-score 判别**（`z=(s-局部均值)/max(局部σ,floor)`，边界需 `z>=2` 且 `s>=cut_abs=0.30`——修正了"相对基线在 max() 下是死代码"缺陷，也正对应研究"绝对阈值 d 不可靠"证伪结论）+ **默认平滑窗=1**（不平滑保留单帧 cut 锐利尖峰，分析发现平滑窗 3 会拉平块边界 z≈1.15 漏检）+ NMS(min_gap=max(1,round(min_shot_s*fps))) + merge(过短段并入更弱边界一侧) + 无边界/单帧/空→回退整段 1 查询单元。**整体偏向下切分**（产品约束：过长可接受、过短/碎片化更危险）。`PipelineConfig` 平铺加 4 个占位字段 `seg_cut_abs/seg_z_thresh/seg_min_shot_s/seg_smooth`（3 个接线点 + JSON 覆盖验证通过）。segment unittest 11 项 + smoke(真 ffmpeg+DINOv2 3色多镜头) 结构不变式全过、输出诊断统计；全套 50 项回归通过。研究护栏：未改任何冻结算法、无 GT、未做 sweep、未重引入 Phase19 原片 cut-aware；多模态未触发（冒烟结构合法）。冒烟 3 纯色片段 CLS 混淆未切分=已知合成图案属性，非 segment 缺陷。
- **2026-08-25 — `app/SourceLocatorService`（application service：用例编排 + 失败隔离 + progress + cancellation + 错误翻译 + Result 汇总）完成**：`mvp/src/app/`（`models.py`：`ProgressStage`(7 阶段) + `ProgressEvent{stage,current,total,message}` + `CancellationToken`(threading.Event)；`locator_service.py`：`SourceLocatorService`(build_original_index / analyze_edited_video / locate / _locate_features / export_results / load_results；ffmpeg+backend+FeatureStore 惰性构建)）+ `infrastructure/results_repo.py`（`save_results/load_results` 单文件 `*.results.json` 批信封）+ `infrastructure/errors.py` +4（`IndexError/FeatureExtractionError/LocalizationError/ApplicationError` 均 `LocatorError` 子类）+ `domain/models.py` 增 `Result.failure_reason` + `Confidence/Alternative/Result.from_dict()`（对称 to_dict）+ `ResultBatch`(schema_version/original_video/edited_video/results[])。**编排**：locate(edited, original) = 原片 validate→(重)建→load + 编辑侧 iter_frames@2.0fps→embed_frames→detect_shots + 逐 segment produce_candidates→localize_segment→拼 `domain.Result`(edited=shot.span)。**失败隔离**：单 segment 抛错→unresolved(LOW + failure_reason + 日志 exception)+继续后续段；cancellation 抛 `ApplicationError` 不落入段级隔离。**健壮性**：确定性 unittest 12 项 + 全套回归 62 项通过；synthetic 端到端冒烟通过（a1 命中 HIGH、export/reload 往返、cancellation、失败隔离）；**修复 app 级 bug**：`_result_from_refined` 曾传 `shot` 而非 `shot.span` 致 `Result.edited` 被塞进 ShotSegment（已修）。**真素材首次完整管线跑通（§十，仅观察不调算法）**：2.mkv(1GB)索引 3834 帧/3793.6s + 1.mp4@2fps 254 帧→12 段/158.0s + 12 Results(HIGH5/MEDIUM1/LOW6, unresolved 0, candidate 485) + export/reload 往返 + cancellation + 失败隔离；total 3951.9s；peak_ram N/A（venv 无 psutil）。研究护栏：只编排不改冻结算法语义、无 GT、未 sweep。

## Current Problem

- None identified.

## Current Implementation

- `mvp/src/` 已构建模块树：`media/ffmpeg`（FFmpegIO）、`domain`、`infrastructure`、`device/`（CPUBackend）、`engine/feature_store`（FeatureStore）、`engine/common|retrieval|clustering|ranking`、`engine/candidates`、`engine/localization`（finloc + montage + pipeline）、`engine/confidence`（ConfidenceEngine）、`engine/segment`（ShotSegment + detect_shots）、`app/`（SourceLocatorService + ProgressStage/ProgressEvent/CancellationToken）。
- 测试：`mvp/tests/` 7 模块 61 项 unittest（media 6 / domain+infra 12 / device+feature_store 4 / retrieval+ranking 7 / localization+confidence 10 / segment 11 / locator_service 12）；`mvp/scripts/smoke_*.py` 冒烟（media 23 / device+feature_store 25 / retrieval+ranking 6 / localization+confidence 9 / segment 6 / locator_service 真素材+synthetic）全过。
- 冻结栈已贯通：FFmpegIO 抽帧 → CPUBackend(DINOv2 CPU) 特征 → FeatureStore 落盘索引 → retrieval/clustering/ranking 产出候选窗 → localization 精定位(最长连续 run + montage 检测) → confidence 三档置信 → **`app/SourceLocatorService` 逐个查询单元（`engine/segment`）串联整链并汇总 `domain.Result`，JSON 单文件批持久化**。**UI = Vue3+TS+Electron（`mvp/ui/`），经 `ServiceAPI`（Mock 适配器独立跑通，`HttpServiceAdapter` 预留 `/api`；接真实后端=改 `VITE_API_BASE`）消费同一 `SourceLocatorService`**。
- **2026-08-26 追加**：`infrastructure/logging.py` 统一日志（stderr + `RotatingFileHandler` 10MB×5 写 `mvp/logs/video_locator.log`，结构化 `时间 级别 module session 消息`，`contextvars` session_id）；`device/__init__.py` 记 backend requested/selected/fallback/device；`app/locator_service.py` 记 index/analysis/locate 生命周期 + 异常(exception)。
- **2026-08-26 — `mvp/api/` FastAPI 桥实现**：`mvp/api/`（`main.py`/`app.py`/`schemas.py`/`dependencies.py` + `routes/` health/index/analysis/results + `tests/test_api.py` + 重写 `README.md` + `requirements.txt`）。端点 `/api/health`·`/api/index`·`/api/analyze`·`/api/results`·`/api/export`；`WS /ws/progress` 本阶段仅文档。DI = `AppContext(service,current_original,current_batch)` + `get_context()`(lru_cache 惰性，构造不加载模型/不建索引) + `get_locator_service()`。session 日志 = 请求中间件 `set_session_id(None)` + `get_logger("api").info("METHOD path")`（输出 `module=api session=<sid> POST /api/index`）。错误：`LocatorError`→500 `{error,detail}`+`logger.exception`；缺原片/结果批→400。**验证**：unittest 12 项全过（health/index/analyze/results 拍平/export/500/CORS/session 日志）；真机 `uvicorn mvp.api.main:app --port 8765` 启动，`curl /api/health`=`{"status":"ok","version":"0.1"}`，访问日志 `module=api session=08f2c341 GET /api/health`。**读数口径**：confidence 用冻结 `ResultBatch.to_dict()` 拍平（非嵌套）；`backend.device_type` 纯展示层静态映射、不参与运行逻辑。**下一阶段**：`HttpServiceAdapter` 从 Mock 切真实 API（设 `VITE_API_BASE`），不做 WS/不打桌面/不改 mvp/src。
- **2026-08-26 — `HttpServiceAdapter` 从 Mock 切真实 FastAPI（只改 `mvp/ui/`）**：`mvp/ui/src/services/HttpServiceAdapter.ts` 重写为真实端点（`POST /api/index {video_path}`、`/api/analyze {edited_path}`、`/api/results {edited_path,original_path}`、`/api/export {output_dir}`、`GET /api/health`）；统一错误（网络→`BackendUnavailableError`，HTTP 500→surface detail）；`onProgress` 在 Http 模式发粗粒度阶段提示（无 WS）；`getIndexStatus/getIndexMeta` 反映上次 build 否则 MISSING 占位（后端无 status/meta GET）；`loadResults` 抛 `BackendUnavailableError`（无端点）；`cancel()` 占位 noop。`resolveService.ts` 改按 `VITE_BACKEND_MODE`（默认 mock）/`VITE_API_BASE` 切换（`resolveFor(mode,base)` 纯函数可测）。`ServiceAPI` 增 `checkHealth(): Promise<ConnectionStatus>`；`MockServiceAdapter` 实现始终 CONNECTED。`stores/session.ts` 增 `connection` + `checkHealth()`；`App.vue` 启动调用；`AppSidebar`/`BackendStatusIndicator`（Connection 三态 Connected/Connecting/Offline）+ `HomePage` Backend stat 显示连接态。`types.ts` 增 `ConnectionStatus`。`.env`（默认 mock，注释 http 两行）+ `package.json` 增 `test`/`test:watch`（vitest）。**验证**：typecheck 零错、`vite build` 通过、`npm run test` 14/14（health/index POST 正确/locates 拍平 confidence/500 detail/network→BackendUnavailableError/resolveService 切换）；浏览器实测（FastAPI 8765 + Vite http 模式）Home 侧栏 Connection=Connected、Home Backend stat=CONNECTED。**关键落差/口径**：真实后端无 index-status/meta/load/WS 端点 → 前端 Http 模式对 `getIndexStatus/getIndexMeta/loadResults/onProgress/cancel` 用诚实占位或粗阶段提示，不伪造；`/api/export` 只收 `output_dir`（后端用会话 current_batch），Http adapter 忽略前端 batch/filename、发 `{output_dir: opts?.outDir ?? ""}`。**下一阶段**：WebSocket Progress + Real Pipeline Progress（接真实索引/分析进度：`WS /ws/progress` + 取消 + 预览 `extracted_path`）→ Electron 打包/真机验收 → 性能基准 + Confidence 标定。

## Current Decision

- 第 1~4 项关键决策：FFmpegIO 走 ffmpeg/ffprobe subprocess（禁 cv2 CAP_PROP_POS_MSEC；mkv 用 format.duration）；clip 提取 = `-ss before -i` 重编码 libx264（精确，非 stream copy）；采样时间基 `t=i/fps`（不复刻研究帧索引基）；FeatureStore 落盘 `<stem>__<hash8>.idx/{index.json,features.npy,times.npy}`、失效判定 size→duration→model/dim/fps/version→hash 硬检、旧索引转 `.stale` 隔离；`retrieval_top_k`=20(冻结)；`v2_score` alpha=0.5(冻结)；domain.Candidate 增 `scene_div`（montage 指示）、`best_cover` 留占位由 finloc 回填。
- 第 6 项 `engine/segment` 关键决策：`ShotSegment` 放 **engine 层**（domain 禁 numpy；先例 `Hits`/`LocalizationResult`）；切分**判别用局部 z-score**（非裸绝对阈值——研究 Proof 绝对 d 不可靠；相对基线 `max(cut_abs, cut_rel*base)` 是死代码已弃），`cut_abs` 仅作下限安全垫；**默认平滑窗=1**（不平滑保留单帧 cut，平滑会把单帧边界拉平致漏检）；**整体偏向下切分**（产品约束：过长可接受、过短/碎片化更危险），NMS+merge+min_shot_s 保底；切分参数是**产品化占位**（非冻结 finloc 阈值），已入 `PipelineConfig.seg_*` 供 H1 标定，**不做 sweep**；segment 属产品胶水，**不触碰任何冻结算法**。
- **第 7 项 application service 关键决策**：shot 切分 + per-segment 循环放 **app service 层**（按 STATE.md Next Actions 复用 `detect_shots`/`produce_candidates`/`localize_segment`，不新建 engine 层 `LocalizationEngine` 类）；**失败隔离**——单 segment 异常 → unresolved(LOW + `failure_reason` + 日志 exception) + 后续段继续，cancellation 抛 `ApplicationError` 不落入段级隔离；**ffmpeg/backend/FeatureStore 惰性构建**（纯编排路径不强制解析二进制）；**Progress** 7 阶段 `ProgressStage`+`ProgressEvent` 逐事件回调；**Cancellation** `CancellationToken`(threading.Event) 在 index build/extract/segment loop/export 检查；**错误模型** 补 `IndexError/FeatureExtractionError/LocalizationError/ApplicationError`（均 `LocatorError` 子类，GUI `except LocatorError` 即可）；**持久化（用户拍板）** JSON 单文件批 `schema_version/original_video/edited_video/results[]`（`*.results.json` 落 `paths.export_root()`），复用 `Result.to_dict()/from_dict()` 保留 auto/manual 双轨，`Result.failure_reason` 收窄失败段；**不用 SQLite**。修复 app 级 bug：`_result_from_refined` 曾传 `shot` 而非 `shot.span` 致 `Result.edited` 塞进 ShotSegment。
- **第 8 项后续决策（2026-08-26）**：`mvp/api/` FastAPI 桥 = 本地 Adapter Layer，只转发到 `SourceLocatorService`，不改 mvp/src；端点 health/index/analyze/results/export，`WS /ws/progress` 本阶段仅文档；session 日志复用 `infrastructure.logging`；DI 用 `get_context()`（AppContext 持有 service + 请求状态，构造惰性不加载模型/不建索引）；`backend.device_type` 纯展示静态映射不参与运行逻辑；结果 confidence 拍平用冻结 `to_dict()`。详见 `.agent/DECISIONS.md` 本阶段条目。
- **注意（交接必读）**：检索单元测试用合成图案数据，`v2_score` 会把覆盖更宽的非 GT 窗口排在真 GT 区之前——这是**冻结公式 + 合成图案 CLS 语义混淆**的已知属性；研究在**真实影片**数据上 v2_score 正确分离（研究已验证）。真实用户场景是 film 内容，非合成图案；若真实数据同样混淆，应属 Confidence 显式降险情形（第 5 项），**不是 retrieval 改动**，也不回退冻结公式。

## Next Actions

- **H4 Windows NVIDIA / CUDA POC（= PENDING；H4-0 已检查 = `H4_GPU_RUNNER_UNAVAILABLE`）**：GitHub Actions 无可用 Windows GPU runner（个人公共 repo + 官方 GPU runner 不对开源项目开放、无 Windows 变体；本机无 NVIDIA、torch 为 CPU build）。**当前阻塞**，需用户提供 NVIDIA 环境（自托管 Linux runner / 云或本地 NVIDIA 机器）+ 计费权限后，按：能力探测 → `CUDABackend` 一致性验证（与 CPU 特征/检索/定位/置信对比，cos>0.999 量级）→ 性能基准（10/60/120min）；不支持则如实标记 `H4_CUDA_BLOCKED` 继续 CPU。
- **H3**：已完成（`H3_MPS_GO`，POC = GO，2026-08-26，不再重复运行）。正式 `MPSBackend` 未实现（后续阶段；届时 `embed_frames` batch ≤4）。
- **UI（第 8 项）= Vue3+TS+Electron，Stage 1 初始代码已交付（2026-08-26）**；`mvp/api/` FastAPI 桥**已实现**（2026-08-26）；`HttpServiceAdapter` 已切真实 API（2026-08-26）；**Electron 主进程后端生命周期管理已实现**（2026-08-26）；**Electron renderer→后端请求桥已实现**（2026-08-26，生产 `file://` 绕开 CORS）；**FastAPI 异步任务基础设施已实现**（2026-08-27）；**WebSocket 实时进度 + UI Pipeline Binding 已实现**（2026-08-27）；**Video Preview / extracted_path 已实现**（2026-08-27）；**Electron 生产打包 + Python 后端 Bundle 已实现**（2026-08-27：PyInstaller onedir → `resources/backend/backend.exe` 923MiB，`backend.exe` 启动 + `/api/health` 200 + 真实 Preview 抽取/Range 206 验证通过；Electron `prodBackendConfig` 直启 `backend.exe` + `MEDIA_FFMPEG/FFPROBE` env）。**下一步 = 真机验收（打包后 Electron 冒烟，需 `resources/backend` 入包 + 安装器授权）→ 性能基准 + Confidence 标定**。（Installer + License + Device Binding 属后置授权项，用户要求时再做。）
- 之后（UI 恢复时）：第 8 项 PySide6 UI（Home→Index→Results→Export + 预览 + 手动修正 + 批量导出，消费 `SourceLocatorService`（`build_original_index` / `locate` / `export_results` / `load_results` + `ProgressStage` 进度条 + `CancellationToken` 取消），结果卡片用 `domain.Result`）+ 第 9 项 打包验收 + 性能基准 + Confidence 标定。
- **研究护栏（贯穿）**：不新增 backbone/VLM、不改相似度/检索/排序/定位/置信语义、不改 GT、不重跑研究 benchmark、不把已证伪变体与 patch/multi-scale 引入 runtime、不做大规模 sweep。仅当真实用户数据证明某类失败显著影响产品价值，才另立新的 Research Phase。

---

## Important Constraints

- None recorded.

## Known Issues

- None known.

## Last Updated

2026-08-27 20:42

## TODO Snapshot

# TODO

## → 2026-08-25：算法研究冻结，进入 MVP 产品化（Source Video Locator）

- **研究阶段（Phase 1~19）已冻结收尾**。当前 = **ALGORITHM_RESEARCH_FROZEN / MVP_DEVELOPMENT_ACTIVE**。研究结论见 `ARCHITECTURE_DECISION_PHASE20.md`。
- **MVP Stage 0（设计文档）已完成**：`mvp/docs/` 8 份（MVP_PRODUCT_SPEC / MVP_ARCHITECTURE / CONFIDENCE_DESIGN / INDEX_SPEC / DEVICE_BACKEND_SPEC / TECH_STACK_DECISION / THIRD_PARTY_NOTICES / MVP_ROADMAP）。
- **MVP Stage 1（编码）待开始**：见下 P0。

## P0 — MVP Stage 1：Windows CPU（PHASE H1）开发顺序

1. ~~`media/ffmpeg`（FFmpegIO：metadata / seek / 抽帧 / clip extract）~~ — **DONE 2026-08-25**。生产级随机定位基础（禁 cv2.CAP_PROP_POS_MSEC）。`mvp/src/media/ffmpeg/`（FFmpegIO + ffprobe + _runner）。冒烟 23 项 + unittest 6 项过；真实 2h08m HEVC MKV 随机定位/帧精度验证通过。
2. ~~`domain`（Result/Candidate/Confidence/IndexMeta/Enums）+ `infrastructure`（配置/日志/错误模型/路径）~~ — **DONE 2026-08-25**。`mvp/src/domain/`（纯数据 + 枚举 + `to_dict()` 契约 JSON）+ `mvp/src/infrastructure/`（errors/logging/paths/config，JSON 覆盖）。`MediaError` 改继承 `LocatorError` 统一错误模型。test 12 项 + media 复测全过，未提前实现 engine。
3. ~~`device/CPUBackend` + `engine/feature_store/FeatureStore`（index create/load/validate，INDEX_SPEC 落盘）~~ — **DONE 2026-08-25**。`mvp/src/device/`（DeviceBackend Protocol + 冻结 DINOv2 backbone 提取 + CPUBackend 无CUDA + pick_best_available 预留 H2/H3）+ `mvp/src/engine/feature_store/`（FeatureStore create/load/validate/invalidate/get_metadata/delete，落盘 index.json+features.npy+times.npy，size→duration→model/dim/fps/version→hash 硬检失效，`.stale` 隔离）。device smoke 25 项 + unittest 4 项过；DINOv2 CPU 真推理 L2 norm=1.0。
4. ~~`engine/ranking`(v2_score) + `engine/retrieval` + `engine/clustering`（从研究代码 REUSE/REFACTOR 提取）~~ — **DONE 2026-08-25**。`mvp/src/engine/`：common/similarity(cosine REUSE)、retrieval/retrieve_hits(批量 TopK K=20)、clustering/continuity 10/15/60s + _merged_ok + _bridges、ranking/v2_score(mean*sqrt(hit)*(0.5+0.5*cons)*n_reps^0.5)、candidates.py(produce_candidates 编排)。删 GT/gid/benchmark/实验路径/work 依赖；`_dp_path`/`temporal_align`/`localize`/`_recall`=RESEARCH_ONLY 未进 runtime；config retrieval_top_k 100→冻结 20。retrieval smoke 6 项 + unittest 7 项过。（注：合成图案场景 CLS 易混淆，冻结公式在真实影片数据正常。）
5. ~~`engine/localization`（longest_run + montage 检测分支）+ `engine/confidence/ConfidenceEngine`~~ — **DONE 2026-08-25**。`mvp/src/engine/localization/`（finloc_window = per-orig max-over-query coverage + 3帧平滑 + mask(0.4 冻结) + longest_run → 精确 span；multi_island 结构检测；pipeline.localize_segment = 候选→finloc 最佳→回填 best_cover→confidence→RefinedSegment；montage/无 span/run 过短 → original 退化候选窗范围）+ `engine/confidence/confidence.py`（ConfidenceEngine.assess → score + HIGH/MEDIUM/LOW + reasons + hard_flags + montage_flag + alternatives）。置信度权重/阈值全部为未标定占位，纳入 ConfidenceConfig（infrastructure/config.py，JSON 可覆盖）供 H1 标定；`engine/localization/__init__` 不 import 编排避免循环导入。禁 per-query argmax/voting/cut-aware/temporal align/改 similarity·ranking。unittest 10 项 + smoke 9 项全过（synthetic v2_score 混淆使 smoke top 候选非真值，属已知数据/模型属性，真值定位由 deterministic unittest 覆盖）。
6. ~~`engine/segment`（edited shot 切分，定义无 GT 查询单元）~~ — **DONE 2026-08-25**。`mvp/src/engine/segment/`（`ShotSegment` 模型 + `detect_shots` + `adjacent_distances` + `_merge_short`）。相邻帧余弦距离 `d=1-cos`（复用冻结 cosine）→ **局部 z-score 判别**（`z>=2` 且 `s>=cut_abs=0.30`；弃"相对基线死代码"，正对研究"绝对阈值 d 不可靠"证伪）+ 默认平滑窗=1（保留单帧 cut 锐利尖峰）+ NMS(min_gap=max(1,round(min_shot_s*fps))) + merge(过短段并入更弱边界一侧) + 无边界/单帧/空→回退整段（现状行为）。整体偏向下切分（产品约束）。`PipelineConfig` 加 4 占位字段 `seg_cut_abs/seg_z_thresh/seg_min_shot_s/seg_smooth`（3 接线点 + JSON 覆盖验证过）。segment unittest 11 项 + smoke(真 ffmpeg+DINOv2 3色多镜头) 结构不变式全过、输出诊断统计；全套 50 项回归通过。研究护栏：未改冻结算法、无 GT、未 sweep、未重引入 Phase19 原片 cut-aware。冒烟 3 纯色 CLS 混淆未切分=已知合成图案属性。
7. ~~application service（用例编排 + Result 持久化）~~ — **DONE 2026-08-25**。`mvp/src/app/`（`models.py`：`ProgressStage`(7 阶段)+`ProgressEvent`+`CancellationToken`；`locator_service.py`：`SourceLocatorService`(build_original_index/analyze_edited_video/locate/_locate_features/export_results/load_results；ffmpeg+backend+FeatureStore 惰性构建)）+ `infrastructure/results_repo.py`（`save_results/load_results` JSON 单文件批 `*.results.json`）+ `infrastructure/errors.py` +4（`IndexError/FeatureExtractionError/LocalizationError/ApplicationError` 均 `LocatorError` 子类）+ `domain/models.py` 增 `Result.failure_reason`+`Confidence/Alternative/Result.from_dict()`+`ResultBatch`。编排：locate(edited,original)=原片 validate→(重)建→load + 编辑侧 iter_frames@2.0fps→embed_frames→detect_shots + 逐 segment produce_candidates→localize_segment→拼 `domain.Result`(edited=shot.span)。失败隔离：单 segment 抛错→unresolved(LOW+failure_reason+日志 exception)+继续后续段，cancellation 抛 `ApplicationError` 不落入段级隔离。unittest 12 项 + 全套回归 62 项通过；synthetic 端到端冒烟通过（a1 命中 HIGH、export/reload 往返、cancellation、失败隔离）；修复 app 级 bug（`_result_from_refined` 曾传 `shot` 而非 `shot.span`）。**真素材首次完整管线跑通（§十，仅观察不调算法）**：2.mkv 索引 3834 帧/3793.6s、1.mp4 12 段/254 帧、12 Results(HIGH5/MEDIUM1/LOW6, candidate 485)、export/reload 往返、cancellation、失败隔离、total 3951.9s；peak_ram N/A（venv 无 psutil）。研究护栏：只编排不改冻结算法语义、无 GT、未 sweep。
8. **PySide6 UI → 转向 = Vue3+TS+Electron 桌面工作台（2026-08-26 交付 Stage 1）**。`mvp/ui/`（Vue3+TS+Vite+Pinia+router+Electron 壳）+ 自研 Fluent 组件 + `ServiceAPI`(Mock/Http 双适配器) + `services/types.ts` 镜像后端 JSON + 6 页面 + 设计文档。**`mvp/api/` FastAPI 桥已实现（2026-08-26）**；**`HttpServiceAdapter` 已切真实 FastAPI（2026-08-26）**（Mock/HTTP 双模式、Backend Connection 三态、统一错误处理）；**Electron 主进程后端生命周期管理已实现（2026-08-26）**；**Electron renderer→后端请求桥已实现（2026-08-26）**（生产 `file://` 绕开 CORS）；**FastAPI 异步任务基础设施已实现（2026-08-27）**（TaskManager + 后台 worker 调 `locate` + `/api/tasks/analyze|/{id}|/{id}/cancel`）；**WebSocket 实时进度 + UI Pipeline Binding 已实现（2026-08-27）**（`/ws/progress/{task_id}` 阶段级进度帧 + `{cancel}`、`HttpServiceAdapter.subscribeProgress`(WS 一次重连)、`analysis` store 任务式、`ProgressPipeline` 显示 stage/%-elapsed/cancel、Electron `window.backend.websocket` 代理；前端 `test` 61 项 + 后端 `unittest` 26 项全绿）；**Video Preview / extracted_path 已实现（2026-08-27）**（`POST /api/preview` + `GET /api/preview/media|edited` Range 服务 + `previewResult`/`getEditedVideoUrl` + `VideoComparisonPlayer` 接真实 src + Inspector「Generate Preview」；后端 `unittest` 43 项 + 前端 `test` 63 项 + 真实 ffmpeg 冒烟全绿）。**剩余**：Electron 打包产物 + 真机验收（`resources/backend` python.exe + api 就位后 spawn/health 冒烟；`ui/docs/DESKTOP.md`）→ 性能基准 + Confidence 标定。Installer + License + Device Binding 属后置授权项。
9. **PyInstaller 打包 + 真机验收**：PyInstaller **打包 done** + **bundle 瘦身 done**（946M→845M）+ **可运行 exe 已交付并实测**（`release/win-unpacked/Video Locator.exe`：自启后端 + `/api/health` 200 + `/api/index` `device_type: amd`(GPU) + 干净退出；模型内置 `resources/models` + `SVL_DML_MODEL`）；**安装器 NSIS** `Video Locator Setup 0.1.0.exe` 可生成但**进一步开发已暂停**（未签名；按用户要求暂停，恢复需代码证书/自定义图标）。**2026-08-27 追加**：GPU/CPU 真切换（后端 `/api/settings/device` + Settings 真下拉 + 持久化 `settings.json`）+ 拖拽修复（preload esbuild bundle，绕 sandbox require 相对模块）+ CPU pth 内置（`SVL_DINOV2_WEIGHTS`，修打包 exe 切 CPU 崩）+ 打包改 `asar:false`（绕 `addWinAsarIntegrity` 写 exe 锁）；验证后端 unittest 47/前端 65/test:mock/端到端 GPU amd 全过；更新 `docs/DEPLOYMENT_HANDOFF.md`；**同日再追加**：索引缺失修复（源片用「选择源片文件」取绝对路径 + 后端 `GET /api/index/status` 真实状态端点 + `build_original_index` 前置校验抛明确中文错误 + 异常逃逸加固「index failed 日志」 + 徽章「构建中」+ 失败显示后端具体原因；后端 unittest 51/前端 67 全过）；**同日再追加**：分析「一直转圈/取消无效/异步导出 no_results」修复（前端 `runLocate` 改 HTTP 轮询 `GET /api/tasks/{id}` 至终态——绕开打包 exe Electron 无 `globalThis.WebSocket` 导致的 WS 代理禁用 + `cancelTask`；后端 `analyze_edited_video` embed 分批+每批取消检查；`/api/export` 改 `service.last_result_batch()`；打包 exe 全链路 INDEX completed→VALID→TASK completed→EXPORT 200→CANCEL cancelled 实测过）；**再追加**：特征提取「转圈」=慢+无进度显示修复（embed 分批上报「特征提取 N/M 帧」+progress 10→30 平滑+Task.message 透传；后端 `build_backend.py` 须 cwd=benchmark 根否则缺 `mvp` 包→exe 启动 health 超时，已重打包）；**再追加**：结果页预览修复（剪辑预览改抽 edited_segment 区间转码 H.264 MP4 可播 + 默认自动生成编辑/原片预览 + VideoPlayer 进度条可拖 + `/api/export` 导出后清空 `<app-data>/previews/`）。**待授权**：GUI 安装卸载冒烟、代码签名、Custom Icon、Commercial Protection（license/device binding/DRM）。
10. **性能基准（10/60/120min）**：**done（2026-08-27，`mvp/benchmark/release/`，directml）**——索引 10min 23.96s / 60min 135.22s / 120min 270.68s（~13fps 近线性）、分析 total≈3.35s（embedding 冷启动）、预览 ffmpeg≈0.4s→100KB，→ `result/benchmark_result.json` + `release_smoke_result.json`。**未插桩**：RAM/CPU（venv 无 psutil），如需可补。
11. Confidence 标定（`CONFIDENCE_DESIGN.md` 权重/阈值用真实数据校准：HIGH 高 precision、LOW 高 recall-of-hard）。

## P1 — MVP 增强项（不阻塞 H1，属于 MVP 外）

- （需授权/条件触发）若产品域蒙太奇占比高 → s4 单类定向验证：DINOv2 稠密 + VCSL/VTA 序列对齐，判断能否分清"多-shot 同源"；成功才接入二级定位器。
- 场景身份模型（解 s0/s1 深 rank）——非外观第二信号。
- VDF 音频指纹辅助（AGPL，需法律确认，仅"音频不变"场景）。

## P2 — 硬件后续阶段（严格顺序，不并行）

> **硬件路线（2026-08-25 锁定）**：H1 CPU → H2 AMD GPU(DirectML) → H3 macOS MPS → H4 CUDA。**不支持 macOS Intel**。
> **H2 = `DirectMLBackend` 已接入并验证（`H2_AMD_DIRECTML = IMPLEMENTED`）**：resolver + CPU fallback + CPU/DML 正确性 + FeatureStore 兼容 + 真实 2.mkv 生产冒烟（381.3s/3834帧/~10 fps）全过。POC 证据见 `mvp/poc/amdgpu_onnx/`（保留不删）。H3 = GO（POC 完成）；H4 = PENDING（下一阶段 POC 准备）；**UI（第 8 项）已转向 Vue3+TS+Electron，Stage 1 已交付**；**后端统一日志基础设施已交付（2026-08-26，FastAPI 桥之前）**。

- **PHASE H2 — Windows AMD GPU：✅ DONE（2026-08-25）**。`DirectMLBackend`（`mvp/src/device/directml_backend.py`）+ `resolve_backend` 自动选择/CPU fallback + 模型资产 resolver + `DeviceConfig` + `export_dml_model.py`。AMD 实机（RX 6750 GRE）：一致、10× 加速、内存稳定、FeatureStore 兼容。若未来某设备 DML 不稳则标 `AMD_GPU_BACKEND_BLOCKED` 继续 CPU（当前已自动 fallback，不影响 CPU MVP）。
- **PHASE H3 — macOS Apple Silicon：✅ `H3_MPS_GO`（2026-08-26，POC 完成，不再重复运行）**。真实 Apple Silicon（macOS 15.7.7 / arm64 / torch 2.13.0）CI 云端验证：`device_mps_actual=mps`（非 CPU fallback）、正确性 cos_mean=1.0 / max_abs_diff=6.3e-7 / mps norm deviation 1.19e-7、7.875× 加速（0.87→6.84 fps，batch=4 最佳 6.67）、500 帧 all_finite 无异常无 fallback、峰值内存 ~1.07GB。**关键约束：MPS batch_size 必须 ≤4（batch≥8 触发 `Invalid buffer size 5.37GiB` + 性能暴跌 2.83fps；推荐 batch=4）**。**不重新运行 H3 POC**。正式 `MPSBackend` 未实现（H3 后续阶段），**届时 embed_frames batch 必须 ≤4**。CI workflow `.github/workflows/h3-macos-mps.yml` = **workflow_dispatch-only（手动；不 push 自动触发）**。POC 见 `mvp/poc/macos_mps/`；`mvp/src/device` 零改动。**不支持 macOS Intel**。
- **PHASE H4 — Windows NVIDIA GPU (CUDA)**：未来 `CUDABackend`，无需改 Engine/FeatureStore/UI/AppService。不进当前实施阶段。**H4-0（环境与 runner 可用性检查，2026-08-26）= `H4_GPU_RUNNER_UNAVAILABLE`**——GitHub Actions 无可用 Windows GPU runner（个人公共仓库 + 官方 GPU runner 对开源项目不开放、无 Windows 变体；本机 GPU=AMD RX 6750 GRE、torch CPU build，无 NVIDIA）。需用户提供 NVIDIA 环境 + 计费权限方可推进 POC。

## Research Archive（研究阶段 TODO，已冻结，仅追溯）

- 历史研究任务（Phase 12~19）全部完成；见 `.agent/STATE.md → Research Archive`、`PROJECT_HANDOFF.md`、各 `benchmark_report_phase*.md`。
- 已关闭的待办：`--engine all` 重建 `benchmark_results.json`（需授权覆盖历史）；DINOv2+TA 成段（dinov2_ta total_pred=0，序列对齐 blocked，已归入"序列级需定向验证"）。

## 护栏（贯穿 MVP）

- 不新增 backbone/VLM、不改相似度/检索/排序/定位/置信语义、不改 GT、不重跑研究 benchmark、不把已证伪变体与 patch/multi-scale 引入 runtime、不做大规模 sweep。
- 研究代码（experiments/diagnostics/work/phase reports）保留；产品以"提取函数/公式"复用，不 import 研究脚本。
- 不为好看伪造结果；未测/不可用如实标 NOT TESTED / BLOCKED。
