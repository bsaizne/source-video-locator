# Checkpoint

## Project

benchmark

## Created

2026-08-25 02:46

## Git

Repository detected: False

Git repository not detected. Checkpoint will continue without Git information.

### Modified Files

```text
No modified files detected.
```

### Recent Commits

```text
No recent commits available.
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark（D:\claudework\benchmark）

## Current Phase

Phase 16B 完成（Candidate Ranking + Fine Localization 精度提升）。**只测三类：A rerank / B patch / C multi-scale；不换 backbone / 不改 GT / 不改 benchmark 定义。**结论：**ExpA（`n_reps` 查询时序覆盖度 rerank）有效——s2 best_rank 2→1，零附带损伤，recall_B 恒 7/7；ExpB（patch pooling）/ ExpC（multi-scale）无净收益、不建议继续**（修不好 s2，错窗暗室内男在 CLS 与 pooled-patch 空间都更近；s4 IoU 仅数值微升但 traj 过度延伸、精度更差，并重伤 s3 0.75→0.0、s0 cover 1.0→0.667、s6/s5/s2）。已写 `benchmark_report_phase16b.md`。**完成后停止，未进 Phase 17。**

## Current Task

- ✅ Phase 16B：A. `src/experiments/phase16b_rerank.py`——候选打分乘 `n_reps^α`（distinct edit 帧=查询时序覆盖度），α=0.5 宽稳定平台，s2 2→1、其余不变、recall_B 7/7、finloc 不变；纯 numpy 无特征重算。B. `phase16b_patch.py`——`dinov2_features.py:118` 的 patch pooling（`x[:,1:]` mean→[B,384]）+ `patch_features.py` 建 `2@0.5fps_patch.npy`（[3830,384]），换特征源走同管线：s2 仍 rank2（错窗 sim 0.758>正确区 0.658）、s4 iou 0.333→0.429 但 traj 越界 end+24、s3 iou 0.75→0.0、s0 cover 1.0→0.667。C. `phase16b_multiscale.py`——`sim=w*CLS+(1-w)*patch`，w∈{0.3,0.5,0.7}：s2 恒 rank2、s4 iou 0.444(w.5)/0.529(w.3) 但越界、s3 0.75→0.0、s5 0.455→0.353。**裁决：采纳 A；不采纳 B/C。** 产物：`work/dinov2_feats/2@0.5fps_patch.npy`、`work/phase16b_{rerank,patch,multiscale}/results.json`、`work/phase16b_report_data.json`、`benchmark_report_phase16b.md`。**完成后停止，未进 Phase 17。**

## Completed

- Phase 1~11 全部完成（见 PROJECT_HANDOFF.md）
- Phase 12：双基线 real 首跑 recall=0；三重证据确认原 GT 6/7 段错位；重建 corrected Segment GT；B recall 0.143 / A recall 0。
- Phase 13A/B/C：Fine Segment Localization。13A 连续轨迹达成（s6 0.75/s1 0.632）；13B 证原片 0.5fps 非瓶颈；13C 证 gap tolerance 近无效、expanded query 有害。
- Phase 14A：全局 Candidate Retrieval。s0 被召回（补回 TransVCL 漏掉）、868s 巨窗消除、7/7 正确区浮现；strict cover 3/7。
- Phase 14A.1：Candidate Clustering 连续性修正。gap10/15/20 → recall_B 恒 3/7、无巨窗。产物 `work/phase14a1_clustering/phase14a1_results.json`。
- Phase 14C：Edited Query Density。pad=±4s 固定、只查 {2,4,8}fps。**n_recall_B 恒 6/7（s0/s1/s2/s4/s5/s6=Y, s3=N）在每档 fps 完全相同**；s4 0.818/s6 1.0/s2 0.538（对比 14A.1 pad0，提升全来自 pad）。更高 fps 反略伤 fine localization。产物 `work/phase14c_density/phase14c_results.json`。
- **Phase 16A**：corrected GT 边界复核并收紧 s2/s3/s4（0.5s 边界探针 + contact sheet）；14C 重跑 **n_recall_B=7/7**（s3 由 miss 变命中）。产物 `diagnostics/{s2,s3,s4}_gt_audit*`、`work/phase16a_gt_correction/comparison.json`。
- **Phase 16B（本次）**：见 Current Task。结论 ExpA 有效（s2 2→1 零附带损伤）；ExpB/C 无净收益、不建议继续。`benchmark_report_phase16b.md`。

## Current Problem

- **已解决（16A）**：s3 GT 边界问题，收紧后 recall 7/7。
- **已修复（16B-ExpA）**：s2 候选排序——`n_reps`（查询时序覆盖度）把正确区 [1296,1346](10 edit 帧) 抬到 rank1，错窗暗室内男 [1212,1234](5 edit 帧) 降 rank2；零附带损伤，recall_B 7/7。
- **未解（16B 证非排序/特征可修）**：s4 finloc iou 仍 0.333（暗色军事镜 shot-boundary 区分；patch/multi-scale 只给指标微升+越界失真+伤它段）；s0/s1 正确区深 rank6/5（相似场景混淆）；patch/multi-scale 均修不好 s2（错场景在嵌入空间更近）——印证 Phase 15 C=特征判别失败：pooled-patch 仍属全局外观描述子。

## Current Implementation

- 已验证：ExpA 得分 = `mean*sqrt(hit)*(0.5+0.5*cons) * n_reps^0.5`，仅改候选排序（`best_cover` 与 rank 无关 → 不影响 recall/cover/finloc），alpha∈[0.4,0.8] 稳定；4fps 下 s0 6→4、s1 5→4。
- 特征：DINOv2 384-d CLS + 新增 mean-pooled patch 384-d（`dinov2_features.py:118` 取 `x[:,1:]` + `patch_features.py`）。patch ORG 缓存 [3830,384] 与 CLS 逐行对齐。
- 检索/finloc 主流程未改（`best_cover`/`localize`/`_cluster_continuous` 与特征源无关）。

## Current Decision

- **采纳 ExpA（rerank `n_reps`）**：一条结构信号（候选覆盖编辑剪辑的时序广度）干净修好 s2，无需特征重算/不改 GT/不改口径。
- **不采纳 ExpB/C**：证据充分——修不好 s2，s4 仅指标微升+越界，且重伤 s3/s0/s6/s5/s2；固着于"特征判别"方向不可行。
- 若非要做 s4 shot-boundary，方向应为**局部 patch↔patch 对应**或**场景身份模型**（超出 16B 范围），或接受 s4 finloc 0.333 为已知极限。
- Phase 16B 结束即停，不进 Phase 17。

## Next Actions

1. （可选，需授权）校正 Segment GT 下 `--engine all` 重建 `benchmark_results.json`（当前空/单引擎态；会覆盖历史）。
2. （可选）s0/s1 相似场景混淆：ExpA 的 `n_reps` 在 4fps 令 s0 6→4、s1 5→4，可评估是否整体加入；或更高层场景模型。
3. （明确不建议）s2/s4 换 pooled-patch / multi-scale 特征（16B 已证无净收益）。
4. 若无后续：Phase 12/13/14A/14A.1/14C/15/16A/16B 以 report §6 + corrected Segment GT + 各阶段结果 + Phase15/16B 报告收尾。

## Last Updated

2026-08-25 02:46
## Important Constraints

- **`datasets/real/ground_truth.json` 不可修改**（已遵守）。
- 不为好看伪造/修改结果；失败如实标注。
- **正式 GT 以 Segment 为单位**；不把 original_end 固定为 start+1；boundary_uncertain 时如实标记。
- 不做 GUI、不做最终产品、不研究音频指纹、不淘汰 TransVCL。
- Python 绝对路径 `D:\claudework\video-dedup-tool\.venv\Scripts\python.exe`。
- `benchmark.py` 每次运行覆盖 `benchmark_results.json`（本阶段未跑）。

## Known Issues

- **s4 finloc iou 0.333**（CLS 对暗色军事镜切分不清；pooled-patch/multi-scale 不能干净修复）。
- **s0/s1 正确区深 rank6/5**（相似场景混淆；ExpA `n_reps` 在 4fps 有助 s0/s1）。
- **ExpB/C 附带损伤**：patch 使 s0 cover 1.0→0.667、s3 finloc 0.75→0.0、s2 iou 0.5→0.226、s6 0.75→0.5；multi-scale 使 s3 0.75→0.0、s5 0.455→0.353。
- `benchmark_results.json` 空/单引擎态（每次运行覆盖）。`--engine all` 重建暂缓。
- DINOv2 整帧 CLS 与 pooled-patch 对"暗色+军人/人物"军旅素材判别力有限（语义混淆，Phase 15 C）。
- s0 校正值 weak；s0–s5 为 boundary_uncertain。

## Last Updated

2026-08-25 02:46

## TODO Snapshot

# TODO

## P0 — Current（Phase 12 收尾）

- [x] 三重证据确认原 real ground_truth.json 6/7 段错位（真实对应 1261–1742s）
- [x] 用户拍板：正式 GT 以 **Segment** 为单位（保留 edited/original 起止，不把 original_end 固定为 start+1；只能确认场景→标 boundary_uncertain；锚点仅辅助）
- [x] 逐段连续对应终核（`vision_qa.cli case` 生成编辑片段帧 vs 候选原片区间帧 contact sheet），确认 s6=1578–1602 连续、s0 最弱
- [x] 重建 `datasets/real/ground_truth_corrected.json`（Segment + boundary_uncertain；原 GT md5 不变）
- [x] runner 加 `--ground-truth`/`--gt` 开关（benchmark.py + report.py），正控通过
- [x] 校正 Segment GT 下重测：**B recall=0.143（匹配 s5）/ A recall=0**；更新报告 §5/§6、STATE、终核产物、记忆

## P1 — Next（Phase 13/14 + 可选，需用户授权/决策）

- [x] **Phase 14A：Candidate Retrieval 全局粗定位**（2026-08-24）。per-GT edited 2fps 查询 × 全片 3830 帧 Top-K=20，按原片时间近邻聚成候选窗（强制 ≤60s）。**成果：s0 被召回 [1130,1140]（曾被 TransVCL 窗口 [1261,2130] 完全漏掉）；869s 巨窗消除（max 44s/avg 4.6s）；7/7 GT 正确区域都出现在候选窗。** strict cover≥0.5 召回 3/7（s0/s1/s5）。敏感性 gap 10/15/20/30：s4=聚类伪影（gap≥15 翻 0.27→0.82），s2 恒 0.46、s6 恒 0.17、s3 0.12→0.38。见 `work/phase14a_candidate_retrieval/phase14a_results.json`。**完成后停止，未进下一阶段。**
  - [x] **Phase 14A.1：Candidate Clustering 连续性修正**（2026-08-24）。仅改聚类/排序：gap 10/15/20 + >10s 桥接需过 单调+mean_sim+局部速度 判据 + 60s 硬上限。**recall_A=7/7、recall_B 恒 3/7、无巨窗（≤44s）**。关键：s4 正确区在 10/15/20 都未桥接（两碎片，连续性拒 12s 桥，因 4 帧查询→20s 区 edit↔orig 非单调）；Phase 14A pure-time gap15 能让 s4 翻 0.82 但无时序验证。**结论：聚类参数非杠杆，剩 miss 是查询密度(s2尾/s4/s6)+特征难例(s3)；gap→20 引入 s4 假桥[6614,6644]。** 修复 `_hits` sims reshape bug。见 `work/phase14a1_clustering/phase14a1_results.json`。
  - [x] **Phase 14C：Edited Query Density**（2026-08-24）。pad=±4s 固定，只改查询采样率 {2,4,8}fps，其余全不动。**n_recall_B 恒 6/7 所有 fps 相同——fps 非杠杆；提升来自窗口 span（pad 把 recall 3/7→6/7）**：s4 0.273→0.818（两碎片融合[1374,1404] bridged=[]）、s6 0.167→1.0、s2 0.462→0.538（正确区 rank2 被错误场景 rank1[1212,1234] sim0.726 压过）。更高 fps 反略伤 finloc。产物 `work/phase14c_density/phase14c_results.json`；脚本 `src/experiments/query_density_14c.py`。（Phase 15 视觉修正：s3 "特征难例"实为 **GT 边界/不确定**，见下。）
  - [x] **Phase 15：Agent-Assisted Visual Diagnosis**（2026-08-25）。新建 `src/diagnostics/` 管线；每 GT 生成 contact sheet(sX_comparison.jpg + sX_edited.jpg) + case.json + `diagnostics/AGENT_REVIEW_PROMPT.md`；Agent 逐段视觉审查写 `diagnosis.json`；装配 `benchmark_report_phase15.md`。**诊断**：s2=C 判别失败(暗室内男子0.726反超正确区0.558)、s4=C(暗色军事画面不可分, finloc iou0.273)、s3=E GT 边界/不确定(蒙太奇拉向 s2/s4 两区+GT 尾段异构)、s0/s1=B 相似场景混淆(正确区 rank6/5)、s6=F 证据不足(已解决)、s5=B 干净命中。**贯穿主因：整帧 CLS 混同'暗色+军人/人物+纹理'外观。**
  - [x] **Phase 16A：corrected GT 边界复核/收紧**（2026-08-25）。0.5s 边界探针 + contact sheet 复核 s2/s3/s4：s2[1332,1358]→[1332,1348]、s3[1340,1356]→[1340,1348]、s4[1370,1392]→[1374,1392]；14C 重跑 **n_recall_B=7/7**（s3 由 miss 变命中 cover0.375→0.75）。产物 `diagnostics/{s2,s3,s4}_gt_audit*`、`work/phase16a_gt_correction/comparison.json`。
  - [x] **Phase 16B：Candidate Ranking + Fine Localization 精度提升**（2026-08-25）。只测 A rerank / B patch / C multi-scale（不换 backbone/不改 GT/不改口径）。**结果：ExpA（`n_reps` 查询时序覆盖度 rerank，α=0.5）把 s2 best_rank 2→1，其余段不变、recall_B 恒 7/7 ——有效，采纳。ExpB（patch pooling）/ ExpC（multi-scale）修不好 s2、s4 仅指标微升但 traj 越界、重伤 s3/s0/s6/s5/s2 ——无净收益，明确不采纳。** 产物 `work/dinov2_feats/2@0.5fps_patch.npy`、`work/phase16b_{rerank,patch,multiscale}/results.json`、`work/phase16b_report_data.json`、`benchmark_report_phase16b.md`。
- [ ] **Phase 14B（需用户决定，已基本被 14C 内嵌 finloc 覆盖）**：把全局检索候选窗交给 Fine Localization。
- [ ] （可选，需授权）**s0/s1 相似场景混淆**：ExpA 的 `n_reps` 在 4fps 令 s0 6→4、s1 5→4，可评估整体接入；或更高层场景模型。
- [ ] **（明确不建议，16B 已证）s2/s4 换 pooled-patch / multi-scale 特征**：16B 显示错窗暗室内男在 CLS 与 pooled-patch 空间都更近，修不好 s2；s4 仅指标微升+越界。若坚持，方向=局部 patch↔patch 对应（shot 边界匹配）或场景身份模型。
- [ ] （可选，需授权）校正 Segment GT 下 `--engine all` 重建 `benchmark_results.json`（当前空/单引擎态；会重跑 vdf/vcsl/transvcl 并覆盖历史）。
- [ ] （可选）DINOv2+TA 调优：放宽 DP 单调对齐阈值以成段（当前 total_pred=0）。

## P2 — Later

- [ ] 若 Phase 13 跑通：用校正 Segment GT 更新 ARCHITECTURE_ANALYSIS.md + README 选型结论
- [ ] 评估 s0 弱置信度项（可选：更精细视觉定位）

## Blocked

- **`--engine all` 重建汇总**：被"不覆盖 results/vdf|vcsl|transvcl 历史结果"约束阻断，需用户授权允许覆盖。
- **DINOv2+TA 成段**：DP 单调对齐阈值过严，需调优特征或阈值（在正确 GT 下仍 total_pred=0）。
- **boundary_uncertain 的 6/7 段**：s0–s5 仅确认场景/连续区域，完整边界待 Phase 13。
