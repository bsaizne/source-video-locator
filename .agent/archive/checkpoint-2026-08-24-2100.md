# Checkpoint

## Project

benchmark

## Created

2026-08-24 21:00

## Git

Repository detected: False

Git repository not detected. Checkpoint will continue without Git information.

### Modified Files

```text
No modified files detected.
```

### Recent Commits

```text
No recent commits available.
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark（D:\claudework\benchmark）

## Current Phase

Phase 12：解决真实电影解说素材的视觉特征匹配问题（双基线实验）—— 双基线已跑完并定性，**关键发现：real GT 系统性错位（6/7 段）**，报告已生成，待用户决策 GT 处理方式。

## Current Task

双基线（A. DINOv2+TA / B. ISC+TransVCL）real 已跑完，recall 均为 0。经三重证据（DINOv2 诊断 / TransVCL-ISC 官方特征 / Claude 多模态视觉核对 contact sheet）确认 **GT 中 6 段（6349–6602s）指向电影片尾画面，与编辑片实际内容不符**；真实对应关系在原片 ~1261–1742s。`benchmark_report_phase12.md` 已生成。**GT 未修改**，待用户拍板。

另新增多模态视觉分析辅助模块 `src/vision_qa/`（8 文件 + README，可插拔后端，端到端跑通，不污染 Benchmark）。

## Completed

- Phase 1~11 全部完成（见 PROJECT_HANDOFF.md）
- Phase 12 基础设施：DINOv2 vits14 权重、ISC21 isc_ft_v107 权重、timm 均已装
- **Experiment A（DINOv2+TA）** real 全量已跑：原片 3830 帧特征 50min + 编辑片 64 帧 46s。recall=0，total_pred=0（DP 未产出片段）
- **Experiment B（ISC+TransVCL）** real 全量已跑：官方特征链路跑通（7660 帧提取 84min + 推理 10s）。recall=0，total_pred=4（全部 FP，但视觉核对其中 3 个是真定位，落在 1397–1742s）
- **GT 疑点验证**：ORB 判别力弱（缩放进画面），改用 NCC 全片扫描 + DINOv2 诊断 + TransVCL 定位 + **Claude 多模态视觉逐段核对**，三重证据确认 GT 6/7 段错位
- **失败分类**（A~J）：TransVCL A=6/B=1，DINOv2 A=7
- **vision_qa 模块**：src/vision_qa/（schemas / frame_sampler / contact_sheet / prompt / multimodal_client / verifier / failure_analyzer / cli / README），端到端跑通，verification_cases/case_001~004 已生成

## Current Problem

**real GT 系统性错位（6/7 段）**：GT 标注 6349–6602s（电影片尾诗句卡/航拍），编辑片实际内容对应原片 1261–1742s（士兵峡谷 bunker 等）。DINOv2 top 匹配 1130–1584s（单调递增）、TransVCL 定位 1261–2130s、Claude 视觉核对 3 个候选为真匹配，三者一致。**无法修改 GT（任务书铁律）**，需用户决策。

## Current Implementation

- 特征链路：DINOv2 384-d（L2）+ 单调 DP TA；ISC21 256-d（L2）+ TransVCL run.py subprocess
- 缓存：`work/dinov2_feats/{1,2}@0.5fps.npy`、`work/isc_feats/{1,2}.npy`
- 诊断产物：`work/dinov2_gt_diagnosis.json`、`work/ncc_gt_scan_results.json`、`work/failures/vision_annotated.json`
- vision_qa：`src/vision_qa/`（不参与核心引擎，不修改预测/GT/指标）

## Current Decision

- GT 依铁律**不修改**；report 如实标注 GT 疑点与三路证据。重建 GT 需用户拍板
- vision_qa 为**辅助分析工具**（视觉测试员），非核心引擎；后端 `disabled` 时由 Claude 会话读图分析

## Next Actions

1. 向用户汇报 GT 疑点结论 + vision_qa 交付，请求决策（重建 GT / 另建替代 GT / 人工确认）
2. 用户拍板后：重建 real GT 并重测 A/B 基线
3. （可选）`--engine all` 重建三引擎汇总 `benchmark_results.json`

## Important Constraints

- **`datasets/real/ground_truth.json` 不可修改**（任务书铁律，已遵守）
- 不为好看伪造/修改结果；失败如实标注（本报告如实写 recall=0 并归因 GT）
- 不做 GUI、不做最终产品、不研究音频指纹、不淘汰 TransVCL
- Python 绝对路径 `D:\claudework\video-dedup-tool\.venv\Scripts\python.exe`
- vision_qa 不得影响核心 Benchmark / 指标（隔离已验证）

## Known Issues

- Experiment A 特征提取极慢（CPU 50min）；重跑复用缓存
- `benchmark_results.json` 当前被单引擎运行覆盖为 transvcl_official/real；需 `--engine all` 重建
- `src/experiments/*.py` 及 `src/vision_qa/*.py` 通过 sys.path / 相对导入，视运行方式可能需要 PYTHONPATH=`src`
- DINOv2 整帧 CLS 受竖屏黑边影响，部分真匹配（s1/s4/s6 确认）与部分误匹配（s0/s2/s5）混叠，TA 阈值需调优

## Last Updated

2026-08-24 21:00

## TODO Snapshot

# TODO

## P0 — Current

- [x] 验证 GT 疑点：ORB 交叉验证 edited 20s 帧 → 原片 1296s vs 6599s，判断 real ground_truth.json 是否准确（当前 Experiment A recall=0 的关键阻塞）
  - ORB 判别力弱（缩放进画面），改用 NCC 全片扫描 + DINOv2 诊断 + TransVCL 定位 + Claude 多模态视觉核对，三重证据确认 **GT 6/7 段系统性错位**
- [x] 跑 Experiment B：`benchmark.py --engine transvcl_official --dataset real`（官方 ISC 特征 + TransVCL CPU 推理）
  - 链路跑通，recall=0、total_pred=4（全部 FP），根因是 GT 错位而非特征失效

## P1 — Next

- [x] 失败案例分类：`src/experiments/failure_analysis.py`（A~J 十类）
  - A=6/B=1（TransVCL）、A=7（DINOv2）
- [x] 生成 `benchmark_report_phase12.md`（A/B 对比表 + 失败案例 + 回答三问）
- [x] 新增多模态视觉分析辅助模块 `src/vision_qa/`（8 文件 + README），端到端跑通，未污染 Benchmark
- [x] GT 确认有误：已向用户展示证据（6/7 段错位，真实对应 1261–1742s）。**未擅自改 GT**，待用户决策

## P2 — Later

- [ ] 用户拍板 GT 处理方式后：重建 real GT（或建替代 GT）并重测 A/B 基线
- [ ] 若 A/B 在正确 GT 下 recall>0：更新 ARCHITECTURE_ANALYSIS.md + README（Phase 12 收尾）
- [ ] `--engine all` 重建三引擎汇总 `benchmark_results.json`（当前被单引擎覆盖）

## Blocked

- **GT 重建**：依赖用户决策（是否接受本报告证据、按 1261–1742s 重建 GT 或逐段人工确认）
- A 基线在正确 GT 下能否成段：取决于 TA 阈值放宽 / 特征再评估（待 GT 修正后复测）
