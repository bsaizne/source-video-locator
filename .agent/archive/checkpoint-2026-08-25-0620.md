# Checkpoint

## Project

benchmark

## Created

2026-08-25 06:20

## Git

Repository detected: False

Git repository not detected. Checkpoint will continue without Git information.

### Modified Files

```text
No modified files detected.
```

### Recent Commits

```text
No recent commits available.
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark → **Source Video Locator MVP**（D:\claudework\benchmark）

> **项目状态：ALGORITHM_RESEARCH_FROZEN / MVP_DEVELOPMENT_ACTIVE（2026-08-25）**
> 算法研究已冻结收尾；进入 MVP 产品化（Source Video Locator）。研究结论见 `ARCHITECTURE_DECISION_PHASE20.md`；MVP 设计文档见 `mvp/docs/`。

---

## Current Phase

**MVP 产品化 — Stage 0（设计文档）已完成，下一步进入 MVP Stage 1（编码）。** 算法研究 Phase 1~19 已冻结收尾。

---

## Current Task

1. `media/ffmpeg`（FFmpegIO：metadata / seek / 抽帧 / clip extract）—— 生产级随机定位基础（禁用 cv2.CAP_PROP_POS_MSEC）。
2. `domain`（Result/Candidate/Confidence/IndexMeta/Enums）+ `infrastructure`（配置/日志/错误/路径）。
3. `device/CPUBackend` + `engine/feature_store/FeatureStore`（index create/load/validate，`INDEX_SPEC` 落盘）。
4. `engine/ranking`(v2_score) + `engine/retrieval` + `engine/clustering`（从研究代码 REUSE/REFACTOR 提取）。
5. `engine/localization`（longest_run + montage 检测分支）+ `engine/confidence/ConfidenceEngine`。
6. `engine/segment`（edited shot 切分，定义查询单元）。
7. application service（用例编排 + Result 持久化）。
8. **PySide6 UI**（Home→Index→Results→Export + 预览 + 手动修正 + 批量导出）。
9. 打包（PyInstaller）+ 真机验收 + **性能基准**（10/60/120min）+ **Confidence 标定**（`CONFIDENCE_DESIGN.md` 权重/阈值用真实数据校准）。

## Completed

- None recorded.

## Current Problem

- None identified.

## Current Implementation

- No implementation details recorded.

## Current Decision

- None recorded.

## Next Actions

- 从 media/ffmpeg（FFmpegIO）开始实现
- 按 Stage 1 编号顺序推进：domain → infrastructure → device/CPUBackend → engine 各模块 → PySide6 UI → 打包验收
**研究护栏（贯穿）**：不新增 backbone/VLM、不改相似度/检索/排序/定位/置信语义、不改 GT、不重跑研究 benchmark、不把已证伪变体与 patch/multi-scale 引入 runtime、不做大规模 sweep。仅当真实用户数据证明某类失败显著影响产品价值，才另立新的 Research Phase。

---

## Important Constraints

- None recorded.

## Known Issues

- None known.

## Last Updated

2026-08-25 06:20

## TODO Snapshot

# TODO

## → 2026-08-25：算法研究冻结，进入 MVP 产品化（Source Video Locator）

- **研究阶段（Phase 1~19）已冻结收尾**。当前 = **ALGORITHM_RESEARCH_FROZEN / MVP_DEVELOPMENT_ACTIVE**。研究结论见 `ARCHITECTURE_DECISION_PHASE20.md`。
- **MVP Stage 0（设计文档）已完成**：`mvp/docs/` 8 份（MVP_PRODUCT_SPEC / MVP_ARCHITECTURE / CONFIDENCE_DESIGN / INDEX_SPEC / DEVICE_BACKEND_SPEC / TECH_STACK_DECISION / THIRD_PARTY_NOTICES / MVP_ROADMAP）。
- **MVP Stage 1（编码）待开始**：见下 P0。

## P0 — MVP Stage 1：Windows CPU（PHASE H1）开发顺序

1. `media/ffmpeg`（FFmpegIO：metadata / seek / 抽帧 / clip extract）—— 生产级随机定位基础（禁 cv2.CAP_PROP_POS_MSEC）。
2. `domain`（Result/Candidate/Confidence/IndexMeta/Enums）+ `infrastructure`（配置/日志/错误模型/路径）。
3. `device/CPUBackend` + `engine/feature_store/FeatureStore`（index create/load/validate，INDEX_SPEC 落盘）。
4. `engine/ranking`(v2_score) + `engine/retrieval` + `engine/clustering`（从研究代码 REUSE/REFACTOR 提取）。
5. `engine/localization`（longest_run + montage 检测分支）+ `engine/confidence/ConfidenceEngine`。
6. `engine/segment`（edited shot 切分，定义无 GT 查询单元）。
7. application service（用例编排 + Result 持久化）。
8. **PySide6 UI**（Home→Index→Results→Export + 预览 + 手动修正 + 批量导出）。
9. PyInstaller 打包 + 真机验收。
10. 性能基准（10/60/120min，Index Build / Feature Extraction / Localization / Total / RAM / Disk / CPU util）。
11. Confidence 标定（`CONFIDENCE_DESIGN.md` 权重/阈值用真实数据校准：HIGH 高 precision、LOW 高 recall-of-hard）。

## P1 — MVP 增强项（不阻塞 H1，属于 MVP 外）

- （需授权/条件触发）若产品域蒙太奇占比高 → s4 单类定向验证：DINOv2 稠密 + VCSL/VTA 序列对齐，判断能否分清"多-shot 同源"；成功才接入二级定位器。
- 场景身份模型（解 s0/s1 深 rank）——非外观第二信号。
- VDF 音频指纹辅助（AGPL，需法律确认，仅"音频不变"场景）。

## P2 — 硬件后续阶段（严格 H1→H2→H3，不并行）

- **PHASE H2 — Windows AMD GPU**：`ROCmBackend`（能力检测 + 自动 CPU fallback）→ CPU/GPU 数值一致性验证 → 性能基准 → UI 显示 backend。**6750 GRE 实机验证 ROCm/Windows 可用性**；不稳则标 `AMD_GPU_BACKEND_BLOCKED` 继续 CPU。
- **PHASE H3 — macOS Apple Silicon**：`MPSBackend`（能力检测 + fallback）→ 一致性 + 性能基准 → 打包(.app/签名/公证) → 实机验收。**不支持 macOS Intel**。
- 未来：`CUDABackend`（NVIDIA），无需改 Engine/UI/FeatureStore/AppService。

## Research Archive（研究阶段 TODO，已冻结，仅追溯）

- 历史研究任务（Phase 12~19）全部完成；见 `.agent/STATE.md → Research Archive`、`PROJECT_HANDOFF.md`、各 `benchmark_report_phase*.md`。
- 已关闭的待办：`--engine all` 重建 `benchmark_results.json`（需授权覆盖历史）；DINOv2+TA 成段（dinov2_ta total_pred=0，序列对齐 blocked，已归入"序列级需定向验证"）。

## 护栏（贯穿 MVP）

- 不新增 backbone/VLM、不改相似度/检索/排序/定位/置信语义、不改 GT、不重跑研究 benchmark、不把已证伪变体与 patch/multi-scale 引入 runtime、不做大规模 sweep。
- 研究代码（experiments/diagnostics/work/phase reports）保留；产品以"提取函数/公式"复用，不 import 研究脚本。
- 不为好看伪造结果；未测/不可用如实标 NOT TESTED / BLOCKED。
