# Checkpoint

## Project

benchmark

## Created

2026-08-24 22:45

## Git

Repository detected: False

Git repository not detected. Checkpoint will continue without Git information.

### Modified Files

```text
No modified files detected.
```

### Recent Commits

```text
No recent commits available.
```

## State Snapshot

# PROJECT STATE

## Project

视频片段反向定位引擎 Benchmark（D:\claudework\benchmark）

## Current Phase

Phase 13A 完成（Fine Segment Localization step1）：Edited 0.5→2fps + 覆盖度法把"1–2 个离散 Top-1"变成连续匹配轨迹——**s6 IoU 0→0.75、s1 0.417→0.632**（边界连续确认段成功）；s2/s3/s4/s5 0.36–0.46（end 提前 −10~−12s）；**s0 失败**（TransVCL P0[1261,2130] 不含 s0 真实区间[1126,1138]，粗定位错位，非收窄算法问题）。完成即停，未进 13B。详见 `work/phase13a_results.json`。

## Current Task

- ✅ 验证"Edited 加密 → 更连续匹配轨迹 + 明显收窄"：达成（连续 run、s6/s1 命中 IoU≥0.5）。
- ✅ 完成即停止，未自动进入 Phase 13B（原片加密需用户授权）。
- 结论：Phase 13A 证明方向正确；剩余瓶颈 = end 侧相似度截断（−10~−12s）+ s0 粗定位错位。

## Completed

- Phase 1~11 全部完成（见 PROJECT_HANDOFF.md）
- Phase 12：双基线 real 首跑 recall=0；三重证据确认原 GT 6/7 段错位（§2）
- **Phase 12 续（本次）**：
  - 用户明确：正式 GT 以 Segment 为单位、保留 edited/original 起止、**不把 original_end 固定为 start+1**；只能确认场景→标 `boundary_uncertain=true`；锚点仅作辅助。
  - 逐段连续对应终核（`vision_qa.cli case` 生成「编辑片段帧 vs 候选原片区间帧」），确认 s6 连续对应（1578–1602）、s0 最弱。
  - 重建 `ground_truth_corrected.json`（Segment + boundary_uncertain）；原 GT md5 不变。
  - 重测：**B recall 0.143 / A recall 0**；量化 B 的边界过宽问题。

## Current Problem

**已解决 GT 错位根因**，并暴露 B 的剩余瓶颈：**边界粒度**。TransVCL 输出 33–869s 宽片段 vs 真实 Segment 12–26s，仅 s5 一处 IoU≥0.5 命中（且 end 超出 14s）。这是 **Phase 13 Fine Segment Localization** 的输入，非 Phase 12 阻塞。

## Current Implementation

- 特征链路：DINOv2 384-d（L2）+ 单调 DP TA；ISC21 256-d（L2）+ TransVCL run.py subprocess
- 缓存：`work/dinov2_feats/{1,2}@0.5fps.npy`、`work/isc_feats/{1,2}.npy`
- GT 选择开关：`--ground-truth`（benchmark.py line ~66/84、report.py build() + __main__）
- 校正 GT：`datasets/real/ground_truth_corrected.json`（Segment + `extra.boundary_uncertain` + `extra.anchor_mid`）；原 GT 不动

## Current Decision

- **GT**：Segment 结构，另建 corrected 文件，原 GT 不动（md5 复核一致）。用户拍板方案 1 + 连续区间 + 如实标记。
- **GT 与算法结果分离**：修正仅改评估基准，不改引擎/预测/指标标尺。
- `--engine all` 重建三引擎汇总**暂缓**（会覆盖 vdf/vcsl/transvcl 历史 + 旧三引擎 real 基于错 GT 失效），需用户授权。
- **Phase 13**：对 TransVCL "正确区域但边界过宽" 做 Fine Segment Localization（已量化：命中段 end +14s / s6 起早 23s）。

## Next Actions

1. **Phase 13A 已完成的后续（需用户授权/决策）**：
   - Phase 13B：仅对原片候选窗口做 ffmpeg 加密抽帧，用更细边界（Phase 12 报告 §6 目标 IoU≥0.5 的段 s2/s3/s4/s5 现 0.36–0.46 卡在 end −10~−12s，加密原片或调 end 侧策略可破）。
   - 修 s0 粗定位：TransVCL P0[1261,2130] 不含 s0 真实区间[1126,1138]（P0 从 1261 起），需换候选窗口策略（如 GT±40s）或单独定位。
2. （可选，需授权）校正 Segment GT 下 `--engine all` 重建 `benchmark_results.json`。
3. （可选）DINOv2+TA 调优：放宽 DP 阈值以成段（当前 total_pred=0）。
4. 若无后续：Phase 12/13A 以 report §6 + corrected Segment GT + A/B 重测 + phase13a_results 收尾。

## Important Constraints

- **`datasets/real/ground_truth.json` 不可修改**（已遵守：md5 前后一致）
- 不为好看伪造/修改结果；失败如实标注（本次如实写 B=0.143/A=0 并归因边界粒度 / TA 阈值）
- **正式 GT 以 Segment 为单位**；不把 original_end 固定为 start+1；boundary_uncertain 时如实标记
- 不做 GUI、不做最终产品、不研究音频指纹、不淘汰 TransVCL
- Python 绝对路径 `D:\claudework\video-dedup-tool\.venv\Scripts\python.exe`
- vision_qa 不得影响核心 Benchmark / 指标（隔离已验证）
- `benchmark.py` 每次运行覆盖 `benchmark_results.json`（--engine all 重建；单引擎只剩该引擎）

## Known Issues

- `benchmark_results.json` 空/单引擎态（被每次运行覆盖，正控测试时被清空）。`--engine all` 重建暂缓。
- `src/experiments/*.py` / `src/vision_qa/*.py` 视运行方式需 `PYTHONPATH=src`
- DINOv2 整帧 CLS 连续映射噪声大（8s→1130 但 10s→1580），锚点靠视觉终核确认；DP TA 阈值需调优（total_pred=0）
- s0 校正值 weak（场景确认、边界不明）；s0–s5 为 boundary_uncertain（锚点±场景宽估计）

## Last Updated

2026-08-24 22:45

## TODO Snapshot

# TODO

## P0 — Current（Phase 12 收尾）

- [x] 三重证据确认原 real ground_truth.json 6/7 段错位（真实对应 1261–1742s）
- [x] 用户拍板：正式 GT 以 **Segment** 为单位（保留 edited/original 起止，不把 original_end 固定为 start+1；只能确认场景→标 boundary_uncertain；锚点仅辅助）
- [x] 逐段连续对应终核（`vision_qa.cli case` 生成编辑片段帧 vs 候选原片区间帧 contact sheet），确认 s6=1578–1602 连续、s0 最弱
- [x] 重建 `datasets/real/ground_truth_corrected.json`（Segment + boundary_uncertain；原 GT md5 不变）
- [x] runner 加 `--ground-truth`/`--gt` 开关（benchmark.py + report.py），正控通过
- [x] 校正 Segment GT 下重测：**B recall=0.143（匹配 s5）/ A recall=0**；更新报告 §5/§6、STATE、终核产物、记忆

## P1 — Next（Phase 13 + 可选，需用户授权/决策）

- [ ] **Phase 13：Fine Segment Localization**——把 TransVCL "正确区域但边界过宽"（33–869s 宽预测 vs 真实 Segment 12–26s）收窄到真实区间。已量化：唯一命中段 end 超出 14s、s6 起早 23s。
  - [x] Phase 12 结尾零推理诊断：0.5fps 下每 GT 仅 1–2 个 edited 帧，只能得到离散 Top-1（s6=[1580,1580]），无法连续收窄。
  - [x] **Phase 13A（已完成，2026-08-24）**：Edited 0.5→2fps + 覆盖度法。s6 IoU 0→**0.75**、s1 0.417→**0.632**；s2/s3/s4/s5 0.36–0.46（end 普遍提前 −10~−12s）；**s0 失败**（TransVCL P0=1261-2130 不含 s0 真实区间 1126-1138，粗定位错位）。连续匹配目标达成（离散点→连续 run）。见 `work/phase13a_results.json`。完成即停，未进 13B。
  - [ ] （授权后）Phase 13B：对原片候选窗口做 ffmpeg 加密抽帧（更细亚秒级边界）；并修 s0 的粗定位窗口策略。
- [ ] （可选，需授权）校正 Segment GT 下 `--engine all` 重建 `benchmark_results.json`（当前空/单引擎态；会重跑 vdf/vcsl/transvcl 并覆盖历史，旧三引擎 real 原结果基于错 GT 失效）
- [ ] （可选）DINOv2+TA 调优：放宽 DP 单调对齐阈值以成段（当前 total_pred=0）

## P2 — Later

- [ ] 若 Phase 13 跑通：用校正 Segment GT 更新 ARCHITECTURE_ANALYSIS.md + README 选型结论
- [ ] 评估 s0 弱置信度项（可选：更精细视觉定位）

## Blocked

- **`--engine all` 重建汇总**：被"不覆盖 results/vdf|vcsl|transvcl 历史结果"约束阻断，需用户授权允许覆盖。
- **DINOv2+TA 成段**：DP 单调对齐阈值过严，需调优特征或阈值（在正确 GT 下仍 total_pred=0）。
- **boundary_uncertain 的 6/7 段**：s0–s5 仅确认场景/连续区域，完整边界待 Phase 13。
