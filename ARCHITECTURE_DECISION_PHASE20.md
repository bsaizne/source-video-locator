# 最终算法架构决策分析（Phase 20 决策点）

> 生成日期：2026-08-25
> 依据：Phase 16A / 16B / 16B.1 / 17A / 18 / 19 全部实际实验结果 + Phase 15 多模态视觉诊断 + Phase 11 选型与许可评估。
> 性质：**架构决策文档，非实验阶段**。不写代码、不改现有代码、不改 GT、不重跑 benchmark、不调参、不新增模型、不自动进入 Phase 20。

---

# 1. Executive Summary

本项目 Phase 13–19 实际执行了 Phase 11 结论 C 的"自研视觉定位核心（VCSL 思路升级：ORB→DINOv2 + temporal re-ranking）"，得到一个在**校正 Segment GT** 下 `recall_B = 7/7` 的 DINOv2 pipeline。核心结论：

- **召回层已解决**：候选召回（哪段原片）可靠——7 段正确区全部进入候选窗。
- **排序层部分解决**：ExpA `n_reps`（查询时序覆盖度）干净修好 s2（best_rank 2→1），但 s0/s1 正确区仍深 rank6/5（相似场景混淆，需非外观信号）。
- **精定位层有硬边界**：连续镜头段可靠（s0 0.571 / s1 0.647 / s2 0.5 / s3 0.75 / s6 0.75）；但**真多-shot 蒙太奇段不可靠**（s4 IoU 0.333；s5 0.455 + 反向跨段风险）。
- **s4 是"可接受的困难边界案例"，非"必须攻克的核心"**：主问题已被 recall_B 7/7 回答；s4 是 2–2.5s 编辑跨 3–4 个来源 shot 的**本质欠定**对应；三轮框架内优化（17A/18/19）全部"修好 s4 却伤及 3–5 段"。
- **sequence-level 模型值得引入，但只在指定触发条件后**：VCSL/VTA（MIT）是唯一贴合"multi-shot montage source localization"的现成候选，但 real 上完全未验证、且 dinov2_ta（DINOv2+序列对齐）曾被 blocked（total_pred=0）——**方向对、无正面证据、不可无验证接入**。应"定向单类验证后"才规模化。
- **MVP READY = YES（有条件，版本一）**：能支撑"连续镜头精定位 + 蒙太奇粗定位（标低置信+可手修）"的 MVP；条件是置信度需工程化（防语义混淆虚高）、蒙太奇案例在 UI 显式标注"建议人工确认"、macOS 需实机验证。

---

# 2. 已验证有效组件（矩阵）

| Component | 实测结果 | 是否采纳 | 原因 |
|---|---|---|---|
| DINOv2 CLS（VITS14/384-d，0.5fps） | retrieval 底座；recall_B 7/7 | ✅ 采纳（作特征底座） | 足以支撑全局召回 7/7；但整帧 CLS 对"暗色+人物+军事纹理"判别力有限（Phase15 C，s2/s4），无法区分语义场景身份 |
| Global Candidate Retrieval（per-GT edited 2fps 查询 × 全片 Top-K=20） | 7/7 正确区进候选窗；s0 被召回（补回 TransVCL 漏掉）；869s 巨窗消除 | ✅ 采纳 | 全局粗定位核心，解决"哪段原片"；cover≥0.5 strict 仅 3/7，但正确区都出现在候选窗 |
| Candidate Clustering（gap10/15/20 + 连续性判据 + ≤60s 硬限） | recall_A 7/7 / recall_B 恒 3/7（14A.1）；无巨窗 | ✅ 采纳 | 聚类参数非杠杆；连续性判据防巨窗有效；gap→20 引入 s4 假桥[6614,6644]故不宜调大 |
| ExpA `n_reps` rerank（α=0.5） | s2 best_rank 2→1；其余段不变；recall_B 恒 7/7；零附带损伤 | ✅ 采纳 | 查询时序覆盖度 = 与外观**正交**的结构先验；区分"覆盖整段编辑蒙太奇"（s2 正确区 n_reps=10）vs"只覆盖外观子集"（错窗 n_reps=5）。唯一干净修 s2 的信号，纯 numpy 无需重算特征 |
| per-original max-over-query coverage（`cover=sim.max(axis=0)`） | s0–s3/s6 稳定区间、0 None 的基石 | ✅ 采纳（作主定位信号） | 天然保留区间；单帧噪声不切断 run；是 s0/s1/s2/s3/s6 稳定性的来源 |
| longest_run（finloc baseline） | s0 0.571 / s1 0.647 / s2 0.5 / s3 0.75 / s6 0.75；**s4 0.333 / s5 0.455** | ✅ 保留（作稳定定位器） | 对连续镜头可靠；对真蒙太奇（s4）过严（单条连续 run 假设）——这是 s4 待解点，但其余段靠它保稳 |
| per-query argmax multi-segment（17A） | s4 0.333→**1.0**（gt_query）；但 s0/s2→None、s1/s3/s6 过窄、s5 反向跨段；gap 2/4/6 逐段全同 | ❌ 不采纳 | per-query **单帧 argmax** 太脆：跨 shot 抖动 + monotonic/teleport 校验把整条 run 拒绝→None；"gap 合并"从未真正触发（无 gap 可调） |
| temporal neighborhood voting（18） | s4 0.333→0.889、s2 0.5→0.667、0 None；但 s0 0.571→0.0、s3 0.75→0.0、s6 0.75→0.167（静态塌缩成点）、s1 0.647→0.333、s5 w3 仍反向跨段 | ❌ 不采纳 | per-query **consensus（单 bin 众数）** 丢弃区间→静态/弱场景塌缩成点；cut 门控只防 merge、不防单 run 内部跨 cut；cut 过度检测（每窗 5–10） |
| patch pooling（16B-ExpB） | 修不好 s2（错窗 sim 0.758 仍 > 正确区 0.658）；s4 仅数值微升但 traj 越界 end+24；重伤 s3 0.75→0.0、s0 cover 1.0→0.667、s6 0.75→0.5 | ❌ 不采纳 | mean-pooled patch 仍是**全局外观**描述子（1369 patch 均值），无法判别语义身份；指标提升但精度下降 |
| multi-scale CLS+patch（16B-ExpC） | 修不好 s2（恒 rank2）；s4 0.444/0.529 但越界；重伤 s3 0.75→0.0、s5 0.455→0.353 | ❌ 不采纳 | 同 patch，全局外观混叠；无净收益；w 在 {0.3,0.5,0.7} 均未翻盘 |
| cut-aware localization（19） | s0/s1 稳定、0 None、s5 不再反向跨段；但 **s4 仍 0.333**（[1383,1389] coverage 洞 8s > gap_merge 4s、右岛[1390,1396]跨 1394）、s2 0.5→0.25/s3 0.75→0.25/s6 0.75→0.5（cut 段内误检） | ❌ 不采纳 | 0.5fps 下相邻内容距离 d=1-cos 几乎处处高（实测 0.6~0.94），**无法区分"段内蒙太奇洞"vs"跨段真 cut"** → cut 段内误检 + "无-cut-merge 门控"截断 union。s5 cross=F 是 coverage 信号+分数巧合，非 cut 功 |

**一句话**：有效栈 = `DINOv2 CLS → Global Retrieval → Clustering → ExpA n_reps → 按 per-orig coverage 的 longest_run finloc`。所有"换更强/更智能的 finloc 或特征"方向（17A/18/19/ExpB/ExpC）均已证伪。

---

# 3. 已证伪组件（不值得继续投入）

1. **per-query 单证据定位（argmax / consensus / 邻域投票）**——17A/18 双证：要么过脆（argmax→None），要么丢区间（consensus→静态塌缩成点）。**"保留区间"只能靠 per-orig 覆盖信号**，per-query 位置信号做不了主定位。
2. **run 级 source-cut 切分 + 无-cut-merge 门控**——19 证伪：0.5fps 特征粒度不足以区分 TRUE cut 与段内内容断，只会段内误切+截断 union。
3. **patch pooling / multi-scale 特征**——16B ExpB/C 证伪：全局外观描述子无法解决语义场景混淆；指标微升但越界失真、连环伤 3 段。
4. **finloc 阈值/窗口/gap 调参**——18 证 window 非杠杆、17A 证 gap 2/4/6 全同、19 证 cut_thresh/gap 非杠杆。**失败机制在聚合/门控设计与信号粒度，不在阈值**。
5. **同一查询下更高 fps 采样**——14C 证对召回零增益、反略伤精定位。

---

# 4. 当前系统能力边界（区分五层）

| 层 | 状态 | 证据 | 结论 |
|---|---|---|---|
| A. Candidate Recall（找到正确原区） | **可靠** | recall_B 7/7；7 段正确区全部进候选窗 | 已解决 |
| B. Candidate Ranking（正确区排多前） | **部分可靠** | s2/s4/s5/s6 rank1；**s0 rank6 / s1 rank5**（相似场景混淆）；ExpA 修 s2 但 s0/s1 仅 4fps 下 6→4/5→4 | 不改 recall（Top-K=20 容得下），但深 rank 是"外观混淆"信号，影响可解释性/鲁棒性 |
| C. Fine Localization（连续镜头精定位） | **可靠** | s0 0.571 / s1 0.647 / s2 0.5 / s3 0.75 / s6 0.75 | 连续镜头可用 |
| D. Multi-shot Montage Localization | **弱** | **s4 0.333**（真蒙太奇）；s5 0.455 + 反向跨段风险 | 唯一未攻克处 |
| E. 最终原片提取可靠性（端到端） | **部分可靠** | 连续镜头：候选窗+区间定位可靠 + FFmpeg 提取可用；蒙太奇：粗定位到正确窗但边界不精，且 s5 类有跨段吞并别的段的风险（17A/18 暴露，19 靠 coverage 信号"巧合"规避） | 需产品层兜底（标低置信+手修） |

**关键**：A（召回）已解决是"系统能回答主问题"的根本；B（深 rank）是外观缺陷但不影响召回；C（连续）可靠；**D（蒙太奇）是唯一硬伤**；E 由产品化承担。

---

# 5. s4 是否值得继续攻

## s4 的视觉结构（Phase 15 + 16B.1 复核，非新实验）

s4 当前 GT [1374,1392]。edited 查询（2fps，28–34s）覆盖：直升机飞越（共享 s2/s3 区）→ 士兵碎石山脊走远（灰/阴天）→ 士兵暗色森林军基地（格架塔/迷彩网）→ 暗色操纵杆 → **蓝色 HUD 屏幕**。原片侧 [1374,1392] 剖成 4 个**不同来源 shot**：
- [1374,1379] 掩体旁士兵碎石地走远（✅）
- [1380,1382] 男人朝镜头走近 + 暗森林（✅，shot 切换）
- [1383,1385] 暗色特写：注射器/士兵暗脸/手按压（✅，又一 shot）
- [1386,1392] 蓝色控制台/HUD + 红键盘手（✅ 匹配 blue-HUD）

这三段 edited-内容**同属一条连续叙事，但分属 4 个 shot**，中间有低相似度过渡带 [1382,1384]。16B.1 诊断：s4 真瓶颈 = **shot-based temporal localization**（`longest_run` 单连续 run 对蒙太奇过严），主；CLS 判别为次。**不是"查不到"**（cover 已达 1.0），是"不会跨 shot 边界取多子段并集"。

## 方案对比

| | 方案 A：frame-level DINOv2+coverage 继续优化 | 方案 B：引入 sequence-level / temporal model | 方案 C：第一版接受 s4 只能部分定位 |
|---|---|---|---|
| 技术收益 | **低**。17A/18/19 三轮已证明"修好 s4 必伤 3–5 段"；0.5fps CLS + 内容距离不可分（19）是硬限制 | **中高**（未实测）。sequence-level 对齐从"序列节奏"判多-shot 是否同源，理论上正对 s4 | 无新算法，但回避风险/成本 |
| 开发成本 | 中（需再探索聚合/门控，但已到边际） | 中高（接入 VTA/VCSL + 去 FP 层 + 阈值调优；VCSL 当前 precision 仅 25%） | 低（置信度标注 + 手修 UI） |
| 风险 | **高**（每次修复退化 3–5 段；且已证非杠杆处继续试是浪费） | 中（real 上序列对齐**完全未证**；dinov2_ta 曾 blocked total_pred=0） | 低 |
| 对最终产品价值 | 低（只影响稀有案例的边界精度） | 中（前提是产品域蒙太奇占比高） | 直接可用 |
| 预计成功概率（定性） | **低（<30%）** | **中（~50%）但需先做 s4 单类定向验证** | **高（现成可行）** |

## 判断结论

**s4 = 可接受的困难边界案例，非必须攻克的核心**。理由：
1. 主问题（哪段原片）已被 recall_B 7/7 回答；残余仅"蒙太奇边界精度"。
2. s4 是 2–2.5s 编辑跨 3–4 个来源 shot 的**本质欠定**对应——对应关系先天不唯一，用更长编辑上下文或序列级证据才好解决，帧级单势难为。
3. 三轮框架内优化全部以"伤及 3–5 段干净段"收场，净收益为负。
4. 产品层用"标低置信 + 手修 + 给出候选窗范围"可低成本兜底。

**例外（会翻转到"值得攻"的情形）**：若产品目标域**蒙太奇/多-shot 素材占比高**（如"电影解说套长片"高频出现 s4 类），则 s4 不再是边界案例而是**核心场景**——那时才进入方案 B 的定向验证。

---

# 6. Sequence-level model 是否值得引入

## 既有事实（避免误判）

- **TransVCL + ORB-BOW = 0 recall 不能淘汰 TransVCL**：ORB-BOW（256 维）与官方 ISC/VCSL 预训练分布严重不匹配，输出全低置信度噪声（conf 0.035–0.066），模型把两序列当不相关输入。这是"特征喂养错误"，非引擎能力上限（论文 65%+ 需官方稠密 CNN 特征）。**但也不能因此自动推荐 TransVCL**——它需官方特征（工程重：torch ~2GB、模型分发）、且定位目标是"场景级"，不比 DINOv2+VTA 更适合 montage；列为研究参考。
- **VCSL/VTA（MIT）**：设计目标就是 video copy scene localization + temporal alignment（VTA = HV/DTW + temporal consistency re-ranking），**确实针对场景定位/时间对齐**。但 Phase 11 表示 real 上 recall 0（32x32 灰度 + 竖屏黑边失效），且当前实现是 minimal（precision 25%，46 FP）。
- **dinov2_ta（DINOv2 + TA 序列对齐）**：已实现但 **blocked（total_pred=0）**——DP 单调对齐阈值过严，在正确 GT 下也不成段。**提示"把序列对齐直接搬到 DINOv2 帧特征上"有集成风险**，不应视为已有成果。

## 什么时候才值得引入

不是"复杂问题就换模型"。已有证据触发的条件：

| 触发条件 | 状态 | 说明 |
|---|---|---|
| ① frame-level 工程优化明确到顶 | ✅ 已满足 | 17A/18/19 三轮失败 + ExpB/C 无净收益——"工程优化路线"已耗尽 |
| ② 产品目标域蒙太奇/多-shot 素材占比高 | ⚠️ 需产品决策 | 若是"电影片段/单镜头定位"→s4 是少数；若是"解说套长片"→s4 是高频核心 |
| ③ s4 单类上 sequence-level 有正面证据 | ❌ 未满足（可低成本验证） | 在 s4 单案例上，用 DINOv2 稠密特征 + VTA/DTW 做**定向小验证**，证明能比 frame-level 更准地区分"多-shot 同源序列" |

**结论**：只有当 ② 成立 **且** ③ 通过定向验证成功，才值得规模化引入 sequence-level model。在此之前，遵守"不默认换模型"：保留 frame-level 栈，产品或接受 s4 粗糙定位。

**关于"sequence-level 是否真正针对 multi-shot montage source localization"的重估**：VCSL/VTA 方向对（时间对齐），但 real 未证 + dinov2_ta blocked，故只能以"**二级精定位器（探测到 montage 才激活）+ 定向单类验证**"方式接入，绝不可整体替换现有 pipeline 或默认上马。

---

# 7. 如果继续，推荐架构（仅在 §6 的 ②+③ 都成立时）

```
核心（保留）:
  DINOv2 CLS 0.5fps → Global Candidate Retrieval（edited 2fps 查询 × Top-K）
    → Candidate Clustering（连续性判据, ≤60s）
    → ExpA n_reps rerank（α=0.5）
    → finloc: longest_run（per-orig coverage）作稳定主定位【保持，供 s0–s3/s6】
    → 【新增分支】montage 检测：当候选窗内 per-orig coverage 呈多岛
       （如 s4 [1376,1382]+[1390,1396] 且中间洞 > 阈值）→ 触发二级 sequence-level 定位
       （DINOv2 稠密特征 + VTA/DTW 时间对齐）决定"多-shot 是否同源"，输出并集或"标低置信"
    → 置信度（mean_sim × cons × n_reps × 多岛状态）→ FFmpeg 提取
可选辅助: VDF 音频指纹（子进程 AGPL 隔离），对"音频不变"场景加速/补强
```

**关键设计**：montage 作为**检测到才激活的分支**，而非全局替换 longest_run——这样连续镜头段维持 baseline 稳定，只有 s4 类触发二级。但"检测 montage"在 0.5fps 下本身就难（19 的教训：洞 vs cut 不可分），因此二级定位必须用**真正的序列级证据**（VTA DTW）/更高层场景模型，而非帧级内容距离；且二级失败时**必须回退到"标低置信 + 候选窗"**而不输出伪精确边界。

---

# 8. 如果停止研究，MVP 架构

```
MVP（无序列级模型）:
  DINOv2 CLS 0.5fps → Global Retrieval → Clustering（≤60s）→ ExpA n_reps →
    finloc longest_run（per-orig coverage）→ 置信度 → FFmpeg 提取（tools/ffmpeg.exe）
产品层增强:
  - 对"低置信 / 多岛"候选窗 → 标"定位可能不精确（蒙太奇片段）"，给出候选窗范围而非单边界
  - 允许用户手动修正边界（拖拽 / 输入起止；算法无需改动）
  - 结果表格：每 edited 片段 → 原片候选（带 IoU/cover/置信度）+ [提取] 按钮
许可: 全 MIT/Apache-2.0（DINOv2），规避 VDF AGPL；不引入 VDF 则 0 许可风险
```

这一版诚实定位为"**粗定位 + 连续镜头精定位**"工具，蒙太奇显式降级。

---

# 9. 当前最大技术风险

**最大风险不是"找不到"，而是"在相似内容里错了还自以为对"**：DINOv2 整帧 CLS 对"暗色+人物+军事纹理"语义混淆（Phase 15 C），使错误候选可能拿到高 sim（s2 错窗 0.726 > 正确区 0.558；s0/s1 正确区被暗色场景压到 rank6/5）。**在真实军旅/暗色素材上，系统的"置信度"可能虚高**——用户拿一个看似高置信的结果，实际是错的。这是对产品最危险的一条。

缓解（须产品化）：置信度必须综合**时序覆盖度 n_reps + 多岛状态 + 与外观正交的结构一致/单调性**，而非只看 sim；对"暗色/人物特写"类内容显式降低置信。

次要风险：s5 类反向跨段（把 s4 尾段吞并）；蒙太奇 s4 粗糙定位；VCSL/VTA 未在 real 验证；TransVCL 未在官方特征下估。

---

# 10. MVP Ready 判断

| 维度 | 判断 | 依据 |
|---|---|---|
| 1. Windows/macOS 本地运行 | ✅（Windows 实测）/ ⚠️ macOS 未实测 | DINOv2 torch 全平台；opencv/numpy/ffmpeg 有；mac MPS 需实机验证 |
| 2. 普通单镜头/连续片段定位 | ✅ 可靠 | 召回 7/7；连续段 IoU 0.5–0.75 |
| 3. 多镜头 montage 能力 | ❌ 仅粗定位 | s4 0.333 / s5 0.455 + 跨段风险 |
| 4. 失败时能给置信度 | ⚠️ 部分（需工程化防虚高） | 有 mean_sim/cons/n_reps 可用；但语义混淆可能虚高（§9） |
| 5. 可手动修正 | ✅ 产品层可做 | 不依赖算法 |
| 6. FFmpeg 自动提取 | ✅ | `tools/ffmpeg.exe` 已有 |
| 7. 算法足够支撑 MVP | ⚠️ 支撑"连续精定位 + 蒙太奇粗定位"，但"精确蒙太奇 + 可信置信度"不成熟 | — |

**MVP READY = YES（有条件，版本一）**。版本一能支撑"连续镜头精定位（主）+ 蒙太奇粗定位（标低置信、可手修）"。**条件**：
1. 置信度必须工程化，防"语义混淆虚高"（§9）。
2. 蒙太奇/多岛案例必须在 UI 显式标"建议人工确认"，并给出候选窗范围而非伪精确边界。
3. macOS 需实机验证（当前仅 Windows 实测）。
4. 许可须规避 VDF AGPL（引入则需法律确认）。

**MVP 外的增强项**（不属于版本一）：VTA/sequence-level 二级精定位；VDF 音频指纹辅助；蒙太奇精确边界；场景身份模型（解 s0/s1 深 rank）。

---

# 11. Phase 20 推荐路线

**推荐：不启动新的算法实验阶段**（不自动进 Phase 20）。Phase 13–19 已把"DINOv2 + coverage"的检索/排序/连续定位打磨到可交付，finloc 三方向已到边际。剩余问题（s4 蒙太奇、s0/s1 深 rank、置信度可信度）均属"高投入、不确定、或产品层可兜底"。

**按优先级**：
- **P0（产品或工程化，不研究——最大价值/成本比）**：把当前 `DINOv2→Retrieval→n_reps→longest_run` 封装成 MVP 核心——置信度工程化（防虚高）、蒙太奇显式标低置信、支持手修、FFmpeg 提取、macOS 实机验证。
- **P1（条件触发的研究，需用户授权）**：**仅当**产品目标确认含大量蒙太奇素材，才做**定向 s4 单类验证**：DINOv2 稠密特征 + VCSL/VTA 序列对齐，判断能否比 frame-level 更准地区分"多-shot 同源"。成功 → 接入为二级定位器（§7）；失败 → 确认 s4 为固有难例，维持"标低置信+手修"。
- **P2（暂缓）**：TransVCL 官方特征评估（工程重，价值待定）；VDF 音频辅助（AGPL 需法律确认，仅"音频不变"场景有用）。

**明确不做**：继续 frame-level DINOv2+coverage 工程优化（17A/18/19 已证无效且伤及其他段）；继续调 finloc 阈值/窗口/gap/cut_thresh（已证非杠杆）；ExpB/ExpC 特征方向（已证无净收益）。

---

## 必须指出的数据冲突（不强行单一结论）

1. **s4 根因诊断冲突**：Phase 15 判 s4 为 **C=特征判别失败**（暗色军事画面 CLS 不可分）；16B.1 修订为 **shot-based temporal localization 为主、CLS 判别为次**（cover 已 1.0，是"不会跨 shot 取并集"）。两诊断都支持"帧级难"，但对"换特征 vs 换定位策略"指向不同：16B（ExpB/C）证明换特征无效，故倾向 16B.1 的"定位策略"诊断。**已由 17A/18/19 的实际失败收敛——但三者仍是"换定位策略"在不同实现下的失败，未能直接证明"序列级"可行。**
2. **"s4 需序列级" vs "序列级在此数据已 blocked"**：17A/18/19 指出 s4 需跨 shot 的时序/序列证据（指向 sequence-level）；但 dinov2_ta（序列对齐在 DINOv2 上）**blocked total_pred=0**、Phase 11 三引擎 real 全败——说明"序列对齐"在这类真实素材上**无正面证据**。这是文档中最大的张力：**方向（序列级）与可行性（该数据上未验证/曾阻塞）并存**。
3. **s5 的"改好"是否稳健**：19 的 s5 cross=F 是 coverage 信号 + best-chain-by-coverage 分数巧合（blocked=[] 为空），非 cut 稳健切分之功——即 19 相对 17A/18 在 s5 上的"进步"不可全归因于其宣称的 cutaware 机制。
4. **召回与深 rank 的矛盾**：recall_B 7/7（检索"看起来成功"）与 s0/s1 正确区深 rank6/5（检索"实则是外观混淆侥幸"）并存——**召回通过是因为 Top-K=20 足够宽，掩盖了排名层的外观缺陷**。这在产品上可能表现为"偶尔对、偶尔错"，需要置信度/位置敏感地呈现。
