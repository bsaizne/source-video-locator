# M1-M8 结论重审（GT v4 修正后）——2026-09-01 GT 重建专会话
> ⚠️ **test4 数据错误(2026-09-01):** test4-ed.mp4 与 test4-om.mkv 为两部不同电影(81s竖屏 vs 71min横屏), 本文件中依赖 test4/t4r01 的结论全部作废(逻辑剔除, 详见 datasets/real/test4-INVALID.md)。

> 起因: ground_truth_v4.json 修正后, M1-M8 探针中依赖 p 系列 GT 的结论需重审。
> 原则: 以修正后 GT(ground_truth_v4.json)为准; 区分「直接推导」(现有 JSON 数据 + v4 GT 即可判定) 与「需重算验证」。
> ⚠️ 诚实边界: 重审为**数据级重判**(用探针存档 JSON + 修正 GT 推导), 未重跑昂贵探针(M6 110min / M5 918s / M7 633s);
> 标注「需重算」的项目 = 数字会变但方向性判断可给。

## 一、最重大反转: p26 = GT 标错, 全部「特征上限」结论作废 ⭐

**v4 已证**: p26 旧 GT 2809-2810(站立举牌) 错 → 正确 1766-1770(坐椅看书)。
**直接证据**: v4 重测三指标中 p26 **runtime 直接 HIT**(s16 sub 1762-1769 **HIGH** 命中)——修正 GT 后 CLS 全索引能正确找到它。

| 探针 | 旧结论(基于 2809 真值) | 重审判定(真值=1766-1770) |
|---|---|---|
| **M1b** VLM 事件级 | 正确窗 2809 判 0/3 SAME, 干扰 1766 判 3/3 SAME →「**VLM 反向选错**」 | **反转**: VLM 判 1766 SAME 3/3 恰是**正确真值**——VLM 判对了, 是 GT 错致「反向」误判 |
| **M1a** 字幕召回 | 正确 2808 3/3, 干扰 1766 2/3 →「两窗都命中无法分离」 | 修正后真值窗(1766) 2/3, 错误窗(2808) 3/3 → 字幕略偏错误窗, 无判别力 |
| **M2** 字幕召回 | 真值 2808 **3/3 EVENT YES** vs 干扰 1766 1/3 →「p26 首个正面信号」 | **反转**: 真值=1766(坐椅看书) 字幕仅 1/3; 3/3 命中的是**错误窗**(2808 望远镜视角) → M2 的 p26 正面信号作废 |
| **M3** CLIP 索引 | 真值 rank 2/8 | 数字作废(基于旧真值); CLIP 无判别力(Δ<0.003)结论为结构性的, 与具体 GT 无关 → **主体仍成立, p26 排位作废** |
| **M4** 密度 | p26 best_rank 12→43 恶化, margin −0.28→−0.25 | 旧真值窗 2809; 修正后真值 1766 → 数字需重算; 「密帧零增益」结构性结论(p08/t3r12/t4r01)不依赖错误 GT → **p26 行作废, 结构结论维持** |
| **M5** patch 召回 | p26 patch 24, 干扰 0.951 反超正确 0.900, margin −0.05「无解」 | **需重算**: 真值/干扰互换后 margin 可能反转; v4 runtime 已 HIGH 命中, p26 上「patch 无解」存疑 |
| **M6** 全量 | p26 CLS 22→patch 24「擦线池外/区分度失败」 | **作废**: p26 是 GT 标错, 非特征上限; runtime 已证明 CLS 层可分 |
| **M7** ALIKED | p26 rank 125 + margin −19「无解」 | **需重算**: 真值/干扰互换; p26 不再成立为「特征上限」案例 |
| **M8** 邻接 | s_true=295(2809)/s_dist=208(1766), 唯一性差 0.027 AMBIGUOUS | **需重算**: 真值场景=208(1766)、干扰=295(2809) 方向互换; p26 从 M8「不可辨识」名单移除 |

**结论**: p26 在所有探针里作为「夜读不可辨识/特征上限」的核心案例**全部失效**——它是 GT 标错,
修正后 runtime 秒回(1762-1769 HIGH)。「失败族=不可辨识样本」的定性中 **p26 应删除**。

## 二、p38 = 与 p08 重复已删, 兄弟机位结论半边作废

**v4 已证**: p38 与 p08 编辑段重合(12.5-14.0 vs 12.6-14.3), 已删除; 且 VLM 补审判 p38 正确位置=C1 1108-1110(=p08 位置)。
即 **p38 的旧真值 1048-1050(士兵特写)本身就是错的**, 正确位置实为 1108-1110。

| 探针 | 旧结论(基于 p38 真值 1048-1050) | 重审判定 |
|---|---|---|
| **M5** p08b | 士兵特写 1048-1050 vs 瞭望塔 1108: CLS 32→patch **2**「patch 救回兄弟机位」 | ⚠️ **存疑**: p08b 真值窗=1048-1050 即 p38 错误 GT 区; 若编辑段正确位置是 1108-1110, 则「patch 救回士兵特写」可能是**匹配到错误但相似的士兵特写**——需用修正 GT 重算 p08b |
| **M7** ALIKED | p38 true=1048/dist=1108: rank 61 margin −24「兄弟机位局部无解」 | **半边作废**: p38 删除且其真值错; p08(true=1108/dist=1048) margin +34 半边维持——局部特征对 p08 兄弟机位仍无解 |
| **M8** 邻接 | p38/p08 真-干扰邻接 0.613 AMBIGUOUS | p38 半边作废; **p08(1108-1110) 仍为唯一合法兄弟机位案例**, 其邻接同貌结论维持 |
| **M6** 全量 | p38 CLS 340→patch 225「未救回」 | **作废**: p38 已删, 行无效 |

**结论**: 兄弟机位「特征上限」结论**只剩 p08 半边成立**(p08 v3/v4 均 MISS, GT 未改)。

## 三、M6 全量 patch 召回统计需基于 v4 重算

- M6 依赖全部 41 条 GT, 其中 29 条 relocate + 2 条 delete → **「RESCUE 1/41(p13)」统计作废, 需重算**。
- 已知变化方向: p26(修正后 runtime 已 HIT)、p38(删除)、p24(1588-1598→1599-1610 移窗)、p20(1568-1580→1551-1564 移窗)、p05/p09/p23/p28 均移窗——CLS/patch best_rank 全部会变。
- **方向性预判**: 「patch 召回收益极低、不值得 runtime 化」大概率仍成立(31/41 本就进池, patch 只对兄弟机位型有边际), 但具体数字(p13 唯一救回/p20 差 3 名)需 v4 口径重算验证。

## 四、不受影响/维持的结论

| 探针 | 维持结论 |
|---|---|
| M1-M3 | **t3r12 / test4**(test 域, 不受 2.mkv GT v3 修正影响)——精灵王重复镜头、同质滑梯结论维持 |
| M4 | 「8fps 密帧对判别零增益」结构性结论(p08/t3r12/t4r01 持平)——维持; 仅 p26 行作废 |
| M7 | **p08** 兄弟机位(1108-1110)局部特征无解——维持 |
| M8 | **p08** 邻接同貌 AMBIGUOUS——维持; t3r12/t4r01 维持 |
| FAILURE_TAXONOMY | A/B/C/D 分类法框架维持, 但 **p26 从 A 类(区分度失败)移除、p38 从 A 类移除** |

## 五、净结论

1. **p26 = 从「不可辨识样本/特征上限」名单移除**(实证 GT 错, runtime 秒回);
2. **p38 = 从名单移除**(与 p08 重复, 已删; 且其旧真值 1048 本身错误);
3. **失败族缩窄为**: p08(兄弟机位, 唯一合法 2.mkv 案例) + t3r12/t4r01(test 域, 未受本次修正影响) + test4(同质);
4. **M1-M8 中依赖 p26/p38 的「特征上限」论证链条失去两大核心案例**, 剩 p08 单点支撑兄弟机位族;
5. **M6 全量统计需 v4 重算**(patch 召回 runtime 化值不值), 但方向性(收益低)预判不变;
6. **建议**: ① 重跑 M6(M6 脚本改读 v4 GT, 110min, 出新的 RESCUE 统计); ② 可选重算 M5 p08b/p26 单项(快);
   ③ p26 相关 FINDINGS 标注「GT 修正后作废」。

## 相关产物
- 数据: `work/semantic_signal_M1_results.json` / `M2_results.json` / `M3_results.json` / `M4_results.json` / `patch_recall_results.json` / `patch_recall_gt_results.json` / `local_feature_results.json` / `provenance_neighbor_results.json`
- GT: `datasets/real/ground_truth_v4.json`(corrections 段留痕)
