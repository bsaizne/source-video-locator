# p36 细切分取证实验 + 多模态复审 FINDINGS —— 「查询单元切细 + 单帧强证据保留」可救回 p36(2026-09-04)

> 性质: 研究侧取证(零 runtime 改动)。假设(用户提出): 把镜头切分好、按每个镜头单独对比, 更容易定位。
> 验证对象: p36(2.mkv 剩余两条真漏之一, ed 110-111.5 → GT 2042-2043.4, 当前 runtime 定位 2060.5-2062.5 差 ~18.5s)。
> 三环节: ① 数据对照实验 ② VLM 多模态复审 ③ 待用户人工复审。

## 一、数据对照实验(work/p36_fineseg_experiment.json)

查询 = ed 110-111.5 段 2fps 3 帧(110.0/110.75/111.5); 正确区 GT=2042-2043.4。

| 方案 | 结果 | 判定 |
|---|---|---|
| A. 整段查询(现状 EvidenceLocalizer min_frames=2) | primary=[1869-1884] cover 0.543, 未命中 | ❌ 复现当前失败 |
| B. 单帧查询(细切分模拟, min_frames=1) | 帧110.0→[1855-1858]; **帧110.75→[2040-2044] 命中 GT**; 帧111.5→[2829-2834] | ✅ 1/3 帧命中 |
| C. 单帧 argmax+finloc(绕过 scene 回退) | 帧110.75 argmax=2042 → finloc [2040-2044] cover 0.294 命中 | ✅ 与 B 闭合 |

**机制确认**: 整段查询的 3 帧 argmax 分散(1856/2042/2829) → 2042 成单例簇被 min_frames=2 门掉 →
scene 回退 top-5 不含正确 scene 232 → 定位 2060。若按镜头切细(帧110.75 独立成查询单元) + 保留单帧强证据,
2042 直接 top-1 → 命中 GT。**用户假设对 p36 成立。**

## 二、VLM 多模态复审(work/p36_review/p36_vlm.json, Volcengine Ark)

对照图 work/p36_review/p36_candidates.jpg: 顶行 ED 三帧, 下四行 W0=2040-2044(细切分命中区)/W1=2060-2065(runtime 当前)/
W2=2829-2834/W3=1855-1858。VLM 判定(独立于 CLS 相似度的语义证据):

- **W0_MATCH: YES**
- W1/W2/W3_MATCH: PARTIAL(仅匹配 ED 中「男射手」子镜头, 不匹配 ED 的「头顶水瓶女子」子镜头)
- **BEST_WINDOW: W0**

**多模态结论**: W0(2040-2044)=细切分命中的正确区, VLM 语义判定为与 ED 内容匹配的最佳窗;
W1(runtime 当前主定位 2060-2065)仅部分匹配。→ 与数据实验双向闭合: 细切分救回的是**内容真正匹配**的位置,
不是相似度巧合。

> 附: VLM 还提示 ED 段本身是 montage(含「男射手」与「头顶水瓶女子」两个子镜头)——与整段查询 argmax 分散
> 的机制观察一致, 进一步支持「该段应按子镜头切细」的判断。

## 三、用户人工复审(2026-09-04 已确认)

- 用户人工查看 work/p36_review/p36_candidates.jpg: **ED 与 W0(2040-2044) 匹配(1匹配); ED 与 W1(2060-2065, runtime 当前定位) 不匹配(2不匹配)**。
- → 三方证据(数据实验 + VLM 语义 + 人工)完全闭合: **W0 为正确位置, 细切分救回成立, 非相似度巧合**。

## 五、test 域同类段扫描 + 多模态复审(第 2 步, 2026-09-04) —— 方案 A 收益上限 = +1, 不实施

- **数据扫描**(work/fineseg_scan_test.json): test1-3 严格未命中 24 段中 6 段「单帧查询命中 GT 窗口」候选;
  但交叉基线口径后 4 段(t1r14d/t2r01b/t3r02b/t3r03a)在 measure 里**已是 HIT**(经现有 sub span), 无增量;
- **真新增候选 2 段经 VLM 语义复审全部不成立**:
  - t2r02b: 单帧窗 2921-2934 与 ED 内容**不匹配**(VLM BEST_WINDOW=NONE; 用户人工复审一致) = 假阳性候选;
  - t2r03a: VLM 判 W_WHOLE(现状 3407-3424)比单帧窗 3406-3407 更匹配(用户人工复审一致) = 细切分会回退;
- **最终**: test 域真实新增严格命中 = 0; 方案 A(单帧强证据保留)收益上限 = +1(仅 p36), 且伴随 t2r02b 型
  假阳性风险(单帧簇高置信≠内容匹配, 多模态证实) → **不建议实施 A(ROI 不成立, 有据)**;
- **B(scene 回退扩容)**: A 表现已出(无增量), 按用户拍板逻辑顺带关闭;
- **C(编辑侧 2fps→8fps + 细切分)**: 治本路径, 记录入 TODO, **下个对话做**(本轮不实施)。
- 证据: work/fineseg_review/(t2r02b_candidates.jpg, t2r03a_candidates.jpg, fineseg_vlm.json)。

## 四、结论与立项建议(复审已通过, 待用户拍板是否实施)

1. **假设成立(有数据+多模态双证据)**: 细切分可救回 p36 这条真漏。
2. **可落地方向(若立项)**: 编辑侧「查询单元不纯」检测 → ①提高编辑采样率(2fps→8fps)细切;
   ②单帧强证据保留(argmax 高置信单例簇不再被 min_frames=2 直接门掉, 保留为弱候选走 finloc);
   ③scene 回退扩容(当前 top-5 未含正确 scene 232, 可扩大 scene_top_k 或提高 min_sim)。
3. **收益边界(诚实)**: p36 是 2.mkv 仅剩的两条真漏之一; test 域是否也有同类「查询单元不纯」段需另查;
   按三指标, 救回 p36 = 严格 32/39→33/39。是否值得立项需用户拍板(研究立项触发条件护栏)。
4. **人工复审入口**: 对照图 work/p36_review/p36_candidates.jpg + 上述 VLM 判定, 请用户人工确认:
   ED 内容是否确与 W0(2040-2044)匹配、与 W1(2060-2065)不匹配。

## 相关产物

- 实验: mvp/scripts/research_p36_fineseg_experiment.py + work/p36_fineseg_experiment.json
- 复审: mvp/scripts/build_p36_review_sheet.py + review_p36_vlm.py + work/p36_review/(p36_candidates.jpg, p36_vlm.json)