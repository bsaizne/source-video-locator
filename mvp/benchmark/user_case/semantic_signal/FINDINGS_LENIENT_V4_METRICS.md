# ② p05/p35/p41「修正后未跟上」+ p20/p34 边界偏差 → 评测口径澄清 FINDINGS（2026-09-04）

> 性质: 纯数据评估（零 runtime 改动, 不动 ConfidenceConfig/不标定, 只澄清评测口径）
> 脚本: mvp/scripts/measure_lenient_v4.py | 数据: work/rerun_2mkv_timelineprior.results.json + ground_truth_v4.json
> 目的: 量化「边界偏差(±几秒容差) + 评测真值前移(旧GT=算法产物, 用户修正后前移)」对 2.mkv v4 三指标的影响,
> 区分「算法真实失败」与「严格口径低估」。

## 一、三档口径对照（rerun 批）

| 口径 | 命中 | 说明 |
|---|---|---|
| 严格(覆盖≥40%/含中点/within±2s, 与 measure_shot_recall 一致) | **32/39** | 现行回归基线 |
| 宽松①(span 与 GT±6s 重叠 或 覆盖≥30%) | **35/39** | +p20(覆盖31%边界偏差)+p34(差6s)+p41(sub 1772-1780 vs 1786-1787, 差~8s) |
| 宽松②(span 与 GT±15s 重叠 或 覆盖≥30%, 对齐场景级口径) | **37/39** | 再 +p05(差~17s)+p35(差~11s) |

**±15s 下剩余 2 条 MISS = p08(兄弟机位特征上限) + p36(定位选择问题)** —— 与 ① 判定完全一致, 双向闭合。

## 二、逐条归因（对照 V4_MISS_RECHECK）

| pid | GT_v4 | runtime span | 偏差 | 归因 | 宽松口径处理 |
|---|---|---|---|---|---|
| p20 | 1553-1554.2 | sub 1560-1598 | ~7s | 边界偏差(覆盖31%) | 宽松①命中 |
| p34 | 2058-2060 | main 2064-2066 等 | ~6s | 边界偏差 | 宽松①命中 |
| p41 | 1786-1787 | sub 1772.5-1780.5 | ~8s | 修正后未跟上(旧GT区1772) | 宽松①命中 |
| p05 | 979.2-982 | main 996-998 | ~17s | 修正后未跟上(旧GT区996) | 宽松②命中 |
| p35 | 2101.2-2103 | sub 2112-2114 | ~11s | 修正后未跟上(旧GT区2112) | 宽松②命中 |
| p08 | 1108.15-1109.1 | main 1048-1050 | 60s | 兄弟机位混淆(特征上限, v3/v4 均 MISS) | 不命中(真失败) |
| p36 | 2042-2043.4 | main 2060.5-2062.5 | ~18.5s | 定位选择问题(见①) | 不命中(真失败) |

## 三、结论

1. **严格 32/39 被「边界偏差 + 修正未跟上」低估 5 条**: p20/p34(边界偏差) 与 p41/p05/p35(评测真值前移)
   —— 这 5 条**不是算法退化**, 是 v3 旧 GT 本身就是算法检索产物、用户逐帧核实后前移了真值, 算法检索结果不变。
2. **真实失败仅 2 条**: p08(兄弟机位, 已知特征上限) + p36(定位选择, ① 已复现机制)——与 ① 判定双向闭合。
3. **口径建议(产品/汇报用)**: 汇报「可识别样本」水平用 宽松②(±15s/覆盖≥30%) = **37/39 (94.9%)**;
   严格 32/39 为回归上限口径(防回退用), 但标注「含 5 条 GT 前移/边界偏差低估」。
4. **与 ⑦ 冻结的关系**: 本澄清不触碰 ConfidenceConfig/不标定阈值, 仅建议汇报口径——不违反护栏。
   若未来解除 ⑦, 标定分母应使用宽松②口径的可识别样本(37/39), 而非把 5 条口径低估计入算法失败样本。
5. 零 runtime 改动; measure_baseline.py 默认仍用严格口径(回归基准不变), 宽松口径为汇报参考。

## 相关产物

- 脚本: mvp/scripts/measure_lenient_v4.py | FINDINGS ①: FINDINGS_P28P36_RECALL_VERIFY.md