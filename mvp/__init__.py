"""Source Video Locator MVP root package.

``mvp/src`` is the importable product source tree (added to ``sys.path`` by the
entry point); this ``mvp`` package exists so the test suite can import
``mvp.tests.*``. ``docs/`` holds the frozen Stage 0 design documents.
"""
